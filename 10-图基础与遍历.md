# 第 10 章 图基础与遍历

> 树是「**没有环、且连通**」的图。把这两个限制一拿掉，你能表达的东西就爆炸式增长：社交网络、路网、网页链接、任务依赖、神经网络的计算图、强化学习的**状态转移图**——几乎一切「**关系**」都是图。
>
> 图的算法有几百种，但它们的**地基只有两块**：**怎么存一张图**（表示），和**怎么系统地走遍它**（遍历）。而遍历也只有两种基本走法：**深度优先 DFS**（一路探到底再回头）和**广度优先 BFS**（一层一层往外扩）。妙就妙在——后面所有的「高级」图算法（拓扑排序、最短路、连通分量、割点、强连通分量、最大流），**本质上都是「按某种顺序遍历 + 顺手记录信息」**，而 DFS/BFS 就是在定义那个顺序。
>
> 这一章把地基打牢：**表示（邻接矩阵 / 邻接表）、DFS/BFS、连通性、判环、二分图、拓扑排序**。学完它，你就有了读懂第 11 章一切图算法的钥匙。

---

## 本章目标

读完并跑完本章代码，你应该能够：

1. 说清图的术语（顶点 / 边 / 有向 / 无向 / 度 / 路径 / 环 / 连通 / 分量），并区分「树」与「图」的本质差异（**图有环 -> 遍历必须防死循环**）；
2. 掌握三种表示——**邻接矩阵**（$O(V^2)$ 空间、$O(1)$ 判边）、**邻接表**（$O(V+E)$ 空间、按度遍历）、**边表**（适合 Kruskal / Bellman-Ford），并会按场景取舍；
3. 手写 **DFS**（递归版 + 显式栈迭代版）与 **BFS**（队列），分析都是 $O(V+E)$；
4. 用 **BFS 求无权图最短路**，并解释为什么 BFS 的层数就是最短距离；
5. 用一次遍历求**连通分量个数**，并说清有向图为什么需要「强连通分量」（第 11 章）；
6. 掌握两种**判环**：无向图用「回到非父结点」、有向图用「回到递归栈中的灰结点」；
7. 用 **BFS 二染色**判**二分图**，并讲清「二分图 $\Leftrightarrow$ 无奇环」；
8. 实现 **拓扑排序**（Kahn 入度法 + DFS 后序反转法），并把它用到「关键路径 / 任务调度」；理解**只有 DAG 才有拓扑序**。

---

## 前置知识

- 第 07 章（树与二叉树）—— 树遍历是图遍历的「无环特例」；层序 / 前序的概念直接平移；
- 第 06 章（队列）—— BFS 的核心数据结构；第 05 章（栈）—— DFS 的迭代版用它；
- 第 01 章（算法分析）—— $O(V+E)$ 的来历（每个点、每条边各访问常数次）。

---

## 10.1 图的术语与「树 vs 图」

| 术语 | 含义 |
|---|---|
| 顶点（vertex）$V$ | 图里的「点」 |
| 边（edge）$E$ | 连接两个顶点的「线」 |
| 无向图 | 边没有方向，$u{-}v$ 等价于 $v{-}u$ |
| 有向图 | 边有方向，$u\to v$ 与 $v\to u$ 不同 |
| 度（degree） | 一个顶点连出的边数；有向图分「入度 / 出度」 |
| 路径（path） | 一串首尾相接的边 |
| 环（cycle） | 起点 = 终点的路径 |
| 连通 | 任意两点间都有路径 |
| 连通分量 | 无向图里「极大连通子图」的个数（几个岛） |

**树就是「连通的、无环的无向图」**——这是一个非常精确的刻画。一对比就知道图的遍历为什么多了一道工序：

> **树里不会走回头路，图里会**（因为有环）。所以**图的遍历必须带 `visited[]` 标记**，否则会顺着环无限打转。这是本章和上一章最大的区别，也是所有图算法的第一道防线。

---

## 10.2 图的三种表示：存下来才算数

### 邻接矩阵（adjacency matrix）

一个 $V\times V$ 的表，`mat[u][v] = 1`（或边权）表示有边。

- **优点**：判「$u,v$ 之间有没有边」是 $O(1)$；实现最简单；适合**稠密图**（边数接近 $V^2$）。
- **缺点**：空间 $O(V^2)$；遍历 $u$ 的邻居要扫一整行 $O(V)$，稀疏图上极浪费。

### 邻接表（adjacency list）

每个顶点挂一个「邻居列表」。

- **优点**：空间 $O(V+E)$；遍历 $u$ 的邻居是 $O(\deg(u))$。**稀疏图的标配**（现实里绝大多数图都是稀疏的）。
- **缺点**：判「$u,v$ 有没有边」要扫 $u$ 的邻居列表 $O(\deg(u))$。

### 边表（edge list）

一个 `(u, v, w)` 的数组。

- **优点**：直接遍历所有边，适合 **Kruskal（第 11 章）**、**Bellman-Ford** 这类「按边处理」的算法。
- **缺点**：想找「某点的邻居」要先扫一遍。

| 表示 | 空间 | 判边 $u{-}v$ | 遍历 $u$ 的邻居 | 遍历所有边 | 典型场景 |
|---|---|---|---|---|---|
| 邻接矩阵 | $O(V^2)$ | $O(1)$ | $O(V)$ | $O(V^2)$ | 稠密图、需常数判边 |
| **邻接表** | $O(V+E)$ | $O(\deg u)$ | $O(\deg u)$ | $O(V+E)$ | **稀疏图（默认选择）** |
| 边表 | $O(E)$ | $O(E)$ | $O(E)$ | $O(E)$ | Kruskal / Bellman-Ford |

> **取舍法则**：先问「图有多稀疏」。$E \ll V^2$ 就用**邻接表**（本章代码用的就是它）；需要频繁 $O(1)$ 判边、或图很稠密，才上**邻接矩阵**。

---

## 10.3 深度优先遍历（DFS）：一路探到底

**DFS 的心跳**：从起点出发，**能往前走就往前走**；走到死路（没有未访问的邻居）就**回退**一步，看还有没有别的岔路。整个过程用**栈**管理（递归的调用栈，或显式 `std::stack`）。

```cpp
void dfsRecursive(int start, std::vector<int>& order) const {
    std::vector<char> visited(n_, 0);
    std::function<void(int)> go = [&](int u) {
        visited[u] = 1;
        order.push_back(u);
        for (int w : adj_[u])
            if (!visited[w]) go(w);          // 有未访问邻居就深入
    };
    go(start);
}
```

**迭代版**用一个显式栈。**关键区别**：迭代版必须**入栈时就标记 `visited`**，否则同一个结点会被多条边重复入栈（甚至爆炸）。

```cpp
void dfsIterative(int start, std::vector<int>& order) const {
    std::vector<char> visited(n_, 0);
    std::vector<int> stk{start};
    visited[start] = 1;                      // ← 入栈即标记，别等到弹出
    while (!stk.empty()) {
        const int u = stk.back(); stk.pop_back();
        order.push_back(u);
        for (auto it = adj_[u].rbegin(); it != adj_[u].rend(); ++it)
            if (!visited[*it]) { visited[*it] = 1; stk.push_back(*it); }
    }
}
```

> **注意**：迭代版和递归版的访问顺序**可能不同**（因为迭代版「入栈即标记」），但**都合法**——DFS 的顺序本来就依赖邻居的枚举次序。别为了「顺序一致」纠结。

---

## 10.4 广度优先遍历（BFS）：一层层往外扩

**BFS 的心跳**：从起点出发，先把**距离 1** 的所有点访问完，再访问**距离 2** 的，以此类推。用**队列**实现。

```cpp
void bfs(int start, std::vector<int>& order, std::vector<int>* dist = nullptr) const {
    std::vector<char> visited(n_, 0);
    std::vector<int> d(n_, -1);
    std::queue<int> q;
    visited[start] = 1; d[start] = 0; q.push(start);
    while (!q.empty()) {
        const int u = q.front(); q.pop();
        order.push_back(u);
        for (int w : adj_[u])
            if (!visited[w]) {
                visited[w] = 1;
                d[w] = d[u] + 1;               // 层数 = 无权最短路
                q.push(w);
            }
    }
    if (dist) *dist = d;
}
```

跑 `code/10_graph_basics.cpp` 的遍历部分：

```
=== 1. 邻接表 + DFS / BFS（无向图）===
  顶点数 V = 5，边数 E = 6
  DFS(递归) 从 0： 0 1 2 4 3 
  DFS(迭代) 从 0： 0 1 3 4 2 
  BFS 从 0：      0 1 2 3 4 
  到各点的无权最短距离： d(0)=0 d(1)=1 d(2)=1 d(3)=2 d(4)=2 
```

**DFS 探到底**（$0\to1\to2\to4\to3$），**BFS 逐层扩散**（先 $0$，再 $\{1,2\}$，再 $\{3,4\}$）。BFS 的 `dist[]` 就是**无权图的最短路长度**——这可不是巧合，而是 BFS 的定义性质：**第一次访问到某个点，走过的层数一定是最少的**（否则该点应该更早被访问）。这就是「BFS 求最短路」的原理。

> **重要区分**：
> - **DFS 用栈、BFS 用队列**，这一点决定了「探到底」还是「一层层」；
> - 二者复杂度都是 **$O(V+E)$**（每个点入出一次，每条边被检查常数次）；
> - **BFS 只能求无权（或边权相等）最短路**；带权最短路要 Dijkstra（第 11 章）。

---

## 10.5 连通分量个数：数「几个岛」

无向图里，从每个**未访问**顶点出发做一次 DFS/BFS，扫到的就是**一整个连通分量**。分量个数 = 需要发起多少次遍历。

```cpp
int connectedComponents() const {
    std::vector<char> visited(n_, 0);
    int components = 0;
    for (int s = 0; s < n_; ++s) {
        if (visited[s]) continue;
        ++components;                        // 发现一个新岛
        /* 从 s 做一次 DFS/BFS，把整个分量标 visited */
    }
    return components;
}
```

跑代码：

```
=== 2. 连通分量个数（无向图的「几个岛」）===
  7 个顶点、3 条边 -> 连通分量数 = 4（应为 4：{0,1,2}、{3,4}、{5}、{6}）
```

> **有向图的坑**：有向图里「从 $u$ 能到 $v$」不代表「从 $v$ 能到 $u$」。所以有向图分**强连通（互相可达）**与**弱连通**两种概念，求强连通分量要用 **Tarjan / Kosaraju**（第 11 章）。本节的 `connectedComponents` 只对**无向图**有定义。

---

## 10.6 判环：两种图，两套逻辑

**「图里有没有环」是所有调度类问题的前置检查**（有环就不能拓扑排序，有环就说明循环依赖）。但**无向图和有向图的判环完全不一样**：

### 无向图：遇到「已访问的、且不是父结点」的邻居，就有环

在无向图里，一条边会把「父」也当邻居。走回去找父结点是**正常回退**，不算环；但如果撞上一个**已访问、却不是父**的邻居，说明我们从另一条路绕回来了——**成环**。

```cpp
bool hasCycleUndirected() const {
    std::function<bool(int,int)> go = [&](int u, int parent) -> bool {
        visited[u] = 1;
        for (int w : adj_[u]) {
            if (!visited[w]) { if (go(w, u)) return true; }
            else if (w != parent) return true;      // 回到非父结点 -> 环
        }
        return false;
    };
    // 对每个未访问点 go(s, -1)
}
```

### 有向图：遇到「还在递归栈里（灰）的」结点，才有环

**有向图里绝对不能只看「已访问」**——因为 $u\to v$ 和 $u\gets v$ 的方向不同，一个「已完成的」点（黑）被再次遇到是**完全正常**的（DAG 里到处都是菱形结构）。只有撞上一个**仍在当前递归栈中（灰）**的点，才意味着「转了一圈又回来了」。

用**三色标记**：`0` 白（未访问）、`1` 灰（在栈中）、`2` 黑（已完成）。

```cpp
bool hasCycleDirected() const {
    std::function<bool(int)> go = [&](int u) -> bool {
        state[u] = 1;                                   // 灰：入栈
        for (int w : adj_[u]) {
            if (state[w] == 1) return true;             // 回到灰点 -> 回边 -> 环
            if (state[w] == 0 && go(w)) return true;
        }
        state[u] = 2;                                   // 黑：出栈（完成）
        return false;
    };
    // 对每个未访问点 go(s)
}
```

跑代码：

```
=== 3. 判环：无向用「回边」，有向用「在栈中」===
  无向：4 点 3 边（树）无环 ✓；加 1 条边后成环 ✓
  有向 DAG 0->1->2->3, 0->3：无环 ✓
  有向环 0->1->2->0：检测到环 ✓
```

> **易错点**：有向图判环时，如果偷懒只用一个 `visited` 布尔量，会把 DAG 里大量「**汇合**」（两条路径到达同一点）误判成环。**必须区分「在栈中」和「已完成」**——这正是三色标记的意义。

---

## 10.7 二分图：BFS 二染色

**二分图（bipartite）**：顶点可以分成两个集合 $A, B$，使得**每条边都跨在 $A$、$B$ 之间**（集合内没有边）。

**判定方法**：**BFS 二染色**——给起点染 $0$，它每个邻居染相反的颜色 $1$。如果**遇到一个邻居已经和当前点同色**，说明冲突，**不是二分图**。

```cpp
bool isBipartite() const {
    std::vector<int> color(n_, -1);
    for (int s = 0; s < n_; ++s) {
        if (color[s] != -1) continue;
        std::queue<int> q; color[s] = 0; q.push(s);
        while (!q.empty()) {
            const int u = q.front(); q.pop();
            for (int w : adj_[u]) {
                if (color[w] == -1) { color[w] = color[u] ^ 1; q.push(w); }
                else if (color[w] == color[u]) return false;   // 同色相邻 -> 冲突
            }
        }
    }
    return true;
}
```

跑代码：

```
=== 4. 二分图判定（BFS 二染色）===
  偶环 C6（6 个点）：是二分图 ✓
  奇环 C5（5 个点）：不是二分图 ✓
```

**关键定理**：**图是二分图 $\Leftrightarrow$ 不含奇环**。直观理解：走一个环回到起点，颜色必须「翻偶数次」才能回到原色（$0\to1\to0\to1\cdots$），所以**环长必须是偶数**。奇环走完必然同色冲突。

> **AI 落点**：二分图是**「冲突可化解」**的抽象。推荐系统里的「用户-物品」二部图、任务分配、以及**图神经网络里的二分消息传递**（如推荐 GNN），都以它为基础；而「能不能二染色」等价于「矛盾约束是否可满足」。

---

## 10.8 网格 flood fill：把二维格子当隐式图

很多问题（数岛屿、图像连通区域、迷宫连通、扫雷）表面是「二维网格」，本质是**图**：每个格子是顶点，**上下左右相邻**是边。这时不需要真的建图——**遍历时现算邻居**即可（隐式图）。

```cpp
int countIslands(std::vector<std::string> grid) {
    const int dr[] = {1,-1,0,0}, dc[] = {0,0,1,-1};
    // 对每个未访问的陆地格子，做一次 DFS，把整个岛标记掉
}
```

跑代码：

```
=== 5. 网格 flood fill：数岛屿（隐式图）===
  岛屿个数 = 3（应为 3）
```

> **技巧点**：数岛屿时，**遍历时直接把 `'1'` 改成 `'0'`** 就省掉了额外的 `visited` 数组——「原地标记」。这是 flood fill 的常用手法（图像处理的「油漆桶」工具就是它）。

---

## 10.9 拓扑排序：把 DAG 拉成合法的执行顺序

**问题**：一堆任务有依赖（$u$ 必须先于 $v$）。排一个顺序，使**每条边 $u\to v$ 都满足 $u$ 排在 $v$ 前面**。这顺序叫**拓扑序（topological order）**。

**存在拓扑序 $\Leftrightarrow$ 图是 DAG（有向无环图）**。有环就无解（循环依赖）。

### 算法一：Kahn（BFS 版）——「入度归零法」

1. 统计每个点的**入度**；
2. 把所有**入度为 0** 的点放进队列；
3. 每次出队一个点 $u$ 输出，并把它出边指向的 $v$ 的入度减 1；**减到 0 就入队**；
4. 若最终**输出点数 $<\,$总点数**，说明有环（剩下的点互相依赖，谁都不肯先走）。

```cpp
while (!q.empty()) {
    const int u = q.front(); q.pop();
    order.push_back(u);
    for (int w : adj_[u])
        if (--indeg[w] == 0) q.push(w);      // 入度降到 0 才出队
}
return (int)order.size() == n;               // 排满 n 个 => 无环
```

### 算法二：DFS 版——「后序反转法」

对每个点 DFS，**在递归返回时（后序）**把点压进结果；最后**整体反转**即拓扑序。

**为什么对？** 一条边 $u\to v$：DFS 到 $u$ 时会先深入 $v$，所以 $v$ **先**完成（后序里更早入栈）。反转后，$u$ 就排在了 $v$ 前面。**后序 = 逆拓扑序**，反转得拓扑序。

```cpp
std::function<void(int)> go = [&](int u) {
    state[u] = 1;
    for (int w : adj_[u]) { if (state[w]==1) acyclic=false; if (state[w]==0) go(w); }
    state[u] = 2;
    post.push_back(u);                        // 后序
};
// ...
order.assign(post.rbegin(), post.rend());     // 反转 => 拓扑序
```

跑 `code/10_topological_sort.cpp`：

```
=== 1. 课程依赖（经典例子）===
  Kahn 拓扑序: 高数 -> 线代 -> 概率 -> 数据结构 -> 机器学习 -> 算法
  DFS  拓扑序: 概率 -> 线代 -> 高数 -> 机器学习 -> 数据结构 -> 算法
  两种算法给出的顺序都满足所有依赖 ✓（拓扑序通常不唯一）

=== 2. 有环图：拓扑排序无解 ===
  循环依赖 0->1->2->0：Kahn 返回 0（false = 有环），能排出的点 = 0 < 3 ✓
  DFS 版同样检测到环 ✓
```

注意两种算法的结果**不一样**——这完全正常，**拓扑序一般不唯一**（入度同时为 0 的点谁先谁后都行）。代码用 `isTopoOrder` 校验「每条边的起点都排在终点前面」，二者都通过；而且对 **500 个随机小 DAG** 全部验证通过：

```
=== 3. ★ 随机小 DAG：拓扑序合法性验证 ===
  500 个随机 DAG：Kahn 与 DFS 的拓扑序都通过合法性校验 ✓
```

> **为什么「输出点数 < 总点数」就说明有环？** 若图有环，环上每个点都至少有一条**指向环内**的入边，**环上所有点的入度永远不会降到 0**，于是它们永远进不了队列——被「卡住」的正是环。Kahn 算法顺便就把环检测了。

### 应用：关键路径（DAG 上的最长路）

拓扑排序最实用的副产品，是在 **DAG 上做动态规划**。因为拓扑序保证了「处理一个点时，它的所有前驱都已处理完」，于是可以**按拓扑序松弛**：

$$dist[v] = \max\big(dist[v],\ dist[u] + w(u,v)\big)$$

求**最长路**（关键路径）和**最短路**用的是同一套框架，只差 `max` / `min`。跑代码：

```
=== 4. ★ 应用：项目关键路径（DAG 最长路）===
  从「备菜」出发，各步最早完工时间：
     备菜 = 0 分钟
     炒菜 = 10 分钟
     煮饭 = 5 分钟
     装盘 = 27 分钟
     蒸汤 = 25 分钟
  整个流程最短完工时间（关键路径长度）= 27 分钟
```

**「备菜 $\to$ 煮饭 $\to$ 蒸汤 $\to$ 装盘」= $5+20+2=27$ 分钟**，是决定总工期的**瓶颈路径**（关键路径）。想让项目更快，只能压缩关键路径上的任务。

> **注意**：**普通有向图上求最长路是 NP 难的**（因为有环时可以无限绕），但**在 DAG 上**可以用拓扑序 $O(V+E)$ 搞定。这个「DAG 上都能 DP」的性质，是**神经网络计算图**、**概率图模型（贝叶斯网络）**、**因果推断**的算法基础。

---

## 10.10 复杂度对照

| 算法 | 复杂度 | 数据结构 | 备注 |
|---|---|---|---|
| DFS（递归 / 迭代） | $O(V+E)$ | 栈 | 连通性、判环、拓扑序、后续所有高级算法 |
| BFS | $O(V+E)$ | 队列 | 无权最短路、层次遍历、二染色 |
| 连通分量 | $O(V+E)$ | 遍历 | 无向图专用 |
| 无向图判环 | $O(V+E)$ | DFS | 回到「非父结点」 |
| 有向图判环 | $O(V+E)$ | DFS 三色 | 回到「灰结点」 |
| 二分图判定 | $O(V+E)$ | BFS 二染色 | $\Leftrightarrow$ 无奇环 |
| 拓扑排序 Kahn | $O(V+E)$ | 队列 + 入度 | 顺带判环 |
| 拓扑排序 DFS | $O(V+E)$ | 栈 | 后序反转 |
| DAG 最长 / 最短路 | $O(V+E)$ | 拓扑序 + DP | 关键路径 |

（本章基本只对**邻接表**表示成立；邻接矩阵下遍历邻居是 $O(V)$，总复杂度变 $O(V^2)$。）

---

## 常见错误与陷阱

1. **遍历忘了 `visited`。** 图有环，不加标记会**无限递归 / 死循环**。这是从树过渡到图最容易犯的错。

2. **迭代 DFS「弹出时才标记 `visited`」。** 正确做法是**入栈即标记**。否则同一结点会被多条边重复入栈，最坏情况下栈里塞进 $O(E)$ 个副本，甚至超时。

3. **有向图判环用「已访问」而非「在栈中」。** 会把 DAG 里的「汇合」（菱形结构）误判成环。**必须三色标记**。

4. **无向图判环没排除父结点。** 每个点都会「看到」它的父结点，不排除就会把所有无向图都判成有环。

5. **BFS 求最短路却用在带权图上。** BFS 只对**无权**（或所有边等权）图求最短路。带权最短路要用 **Dijkstra**（无负权）或 **Bellman-Ford**（有负权），第 11 章。

6. **拓扑排序结果不唯一，误以为算法有 bug。** 入度为 0 的点谁先谁后都合法；不同枚举次序得到的拓扑序都正确。**校验要看「边约束」，不要看「输出是否唯一」**。

7. **对非 DAG 调拓扑排序却不检查是否排满。** `order.size() < n` 就是**有环**的信号，一定要检查，否则会拿到一个「残缺的假顺序」。

8. **有向图连通分量用无向图的方法。** 有向图要把「可达」拆成「强连通（互相可达）」「弱连通（忽略方向后连通）」，前者需 Tarjan / Kosaraju。

9. **网格 flood fill 越界 / 泄漏。** 邻接检查 `nr,nc` 必须判边界；把 `'1'` 改成 `'0'` 做「原地标记」时，别忘了**先标记再入栈**，否则同一格会被反复入栈。

10. **邻接表无向图加边忘了加两条。** `addEdge(u,v)` 在无向图里要**同时**把 $v$ 加进 $u$ 的邻居、把 $u$ 加进 $v$ 的邻居；漏一条会得到一个「有向化」的图，边数也数错。代码里 `edgeCount()` 对无向图要 `/2`，就是这个原因。

11. **递归 DFS 在超深图上爆栈。** 图上万条链式依赖时，递归会**栈溢出**（本环境 ASan 会报 `stack-overflow`）。工程上改用**显式栈的迭代版**。

---

## 与 AI / 世界模型的连接

图是「关系」的通用语言，而 DFS/BFS 是**在关系网络上推理**的第一把工具。世界模型、强化学习、GNN、知识图谱——**几乎所有「结构化推理」都从图的遍历起步**。

### 1. ★ 状态转移图与规划

**强化学习 / 世界模型**把环境建模成「状态 + 转移」的图（状态转移图 / MDP）。**BFS 在状态图上找无权最短路径**（最少步数到达目标），**DFS 用来枚举所有可能的轨迹**。**规划（planning）**的很大一部分，就是「在隐式状态图上做搜索」——而搜索的第一步，就是会正确地、系统地**遍历**这棵树 / 图。

### 2. ★ 因果图与拓扑排序（世界模型的核心）

**世界模型要回答「如果做了 A，B 会怎样」——这是因果推断**。因果结构天然是**DAG**（有原因才有结果，不能循环）。于是：

- **拓扑排序给出「因果发生的先后顺序」**（谁先算、谁依赖谁）；
- **DAG 上的 DP 给出「因果效应的传播」**（沿着因果链累积）；
- **判环**告诉我们「因果假设是否自洽」（循环因果 = 模型有 bug）。

**贝叶斯网络**、**结构因果模型（SCM）**、**Do-calculus** 的算法内核，都是「DAG + 拓扑序 + 条件概率」。

### 3. ★ 计算图的前向 / 反向传播

深度学习框架（PyTorch / JAX）把模型表示成**计算图（DAG）**：结点是张量运算，边是数据依赖。

- **前向传播 = 按拓扑序依次计算每个结点**（父算完才能算子）；
- **反向传播 = 反拓扑序**（从损失往回传梯度，正是 DFS 后序的反转）；
- **自动微分**的底层就是「在计算图上做一次 DFS 后序」。

**这解释了为什么反向传播要「从后往前」**——因为它是计算图 DAG 的**逆拓扑序**。

### 4. ★ GNN 与消息传递

**图神经网络（GNN）**的每一层做一次**邻接聚合（message passing）**：每个结点收集邻居的信息更新自己。这本质上就是**「按 BFS 的层」传播信息**——第 $L$ 层 GNN 让信息在图上走 $L$ 步。**感受野 = BFS 的层数**，这和 CNN 的卷积核感受野是同一回事，只不过把网格换成了任意图。

### 5. ★ 知识图谱与检索

**知识图谱**（实体 + 关系 = 有向图）上的**多跳推理**：从问题实体出发，**BFS 扩展几跳**得到候选答案路径；**子图检索**、**路径排序**都基于图遍历。RAG 里「沿关系找证据链」也是遍历。

### 6. 二分图与匹配

**推荐系统**的「用户-物品」二部图、**任务分配**、**图匹配**——二部图是「两类对象只跨类相连」的抽象，判定与后续的**最大匹配 / 匈牙利算法**（第 11 章）都建立在本章的二染色基础上。

**一句话**：图遍历教你「**如何在关系网络中系统地移动**」。从状态图上的规划，到计算图的前反向传播，到因果 DAG 的拓扑序，到 GNN 的消息传递——**世界模型的每一次「推理」，几乎都是在一张图上做一次有组织的遍历**。

---

## 本章小结

- **树 $\subset$ 图**：图有环，所以**遍历必须带 `visited`**——这是本章与上一章的根本区别；
- **三种表示**：邻接矩阵 $O(V^2)$ / $O(1)$ 判边；**邻接表 $O(V+E)$**（稀疏图默认）；边表（Kruskal/Bellman-Ford）；
- **DFS**（栈，一路探到底）与 **BFS**（队列，逐层扩散），都是 $O(V+E)$；
- **BFS 求无权最短路**（层数 = 最短距离）；**DFS/迭代版必须「入栈即标记」**；
- **连通分量**：从每个未访问点起一次遍历；有向图要用强连通分量（第 11 章）；
- **判环**：无向「回到非父结点」、有向「回到栈中的灰结点」（三色标记）；
- **二分图 $\Leftrightarrow$ 无奇环**，用 BFS 二染色判定；
- **拓扑排序**：Kahn（入度归零）+ DFS（后序反转），$O(V+E)$；**只有 DAG 才有**；顺便判环；
- **DAG 上可 $O(V+E)$ 求最短路 / 最长路（关键路径）**——这是计算图 / 因果图 DP 的基础；
- ★ **AI 连接**：状态图规划、因果 DAG + 拓扑序、计算图前反向传播、GNN 消息传递、知识图谱多跳检索、二分图匹配。

---

## 练习

### 基础

1. 给本章的 `Graph` 加上**邻接矩阵**构造方式，并写函数比较「邻接矩阵判边」和「邻接表判边」在稠密 / 稀疏图上的耗时差异。
2. 手画一张 5 个点的有向图，分别写出 Kahn 和 DFS 得到的拓扑序，并解释为什么它们可以不唯一。
3. 用 BFS 改造 `code/10_graph_basics.cpp` 的 `countIslands`，改为统计**最大岛屿的面积**。
4. 说明为什么 **BFS 的 `dist[]` 是无权最短路**，并给出一个反例：如果边有不同权重，BFS 会给出错误答案。

### 进阶

5. **实现 Tarjan 求割点（articulation point）**：用一次 DFS 的 `dfn/low` 值，找出「删掉它图就不连通」的点。这是网络鲁棒性分析的核心。
6. **实现有向图的强连通分量（SCC）**：Kosaraju 两遍 DFS（第二遍在原图的反图上），或 Tarjan 一遍 DFS。并把 SCC 缩点成一个 DAG，再拓扑排序。
7. **实现无向图的欧拉路径判定**：判断一张图能否「一笔画」（每条边恰好走一次）。提示：度数奇偶性 + 连通性。
8. **实现「单词接龙」**：用 BFS 在意为「差一个字母」的单词图上求最短变换序列，分析建图与搜索的复杂度。

### 挑战

9. **实现双色可选的双向 BFS**：从起点和终点**同时**做 BFS，在中间相遇即停。证明它在「分支因子大、目标深」时比单向 BFS 快得多，并用状态空间搜索（如 15-puzzle）实测。
10. **用图遍历实现「依赖注入的求值顺序」**：给一组带依赖的变量（可能有环），输出一个合法求值顺序；若有环则指出**环上的一条具体路径**（提示：DFS 三色标记时记录父指针，遇到灰点时回溯出环）。
11. **实现「计算图的自动微分」**：把标量运算建成 DAG，先拓扑排序做前向求值，再逆拓扑序做反向传播求梯度。用它与数值微分的有限差分对拍，验证梯度正确。

<details>
<summary>参考答案（要点）</summary>

**1.** 邻接矩阵构造 `mat[u][v]`；判边 $O(1)$。稀疏图（$E\ll V^2$）里邻接表判边虽 $O(\deg)$ 但实际步子很少，且省内存；稠密图里矩阵更快且常数小。

**2.** 若某层有 $\ge 2$ 个入度 0 的点，它们互不依赖，先后可交换 -> 至少两个不同的拓扑序。画出该图即可看到分叉。

**3.** BFS/DFS flood fill 时用一个计数器记录本次淹掉的格子数，取所有岛的最大值。

**4.** BFS 按「边数」分层，第一次到某点走的是最少边路径。反例：$0\to1$ 权 1、$0\to2$ 权 100、$2\to1$ 权 1；BFS 会走 $0\to1$（1 条边），但真正最短是 $0\to2\to1$（权 2）——注意这里 BFS 给的是「最少边数」而非「最小权」，若题目要最小权就错了。

**5.** Tarjan：DFS 记录 `dfn`（访问时间）与 `low`（子树能回溯到的最早时间）。对根：$\ge 2$ 棵子树才是割点；对非根 $u$：存在孩子 $v$ 使 `low[v] >= dfn[u]`，则 $u$ 是割点。

**6.** Kosaraju：第一遍 DFS 记录「完成顺序」；按完成顺序的**逆序**在**反图**上再做 DFS，每棵 DFS 树 = 一个 SCC。缩点后按 SCC 拓扑序排列即得 DAG。

**7.** 欧拉路径（一笔画）存在 $\Leftrightarrow$ 图连通（忽略孤立点）且**奇数度顶点数为 0 或 2**。用 DFS 判连通 + 数奇度点。

**8.** 建图：对每对只差一个字母的单词连边（或枚举每个单词的「通配模式」桶）。BFS 求最少步数。建图 $O(N^2 L)$ 或通配法 $O(NL^2)$，搜索 $O(V+E)$。

**9.** 双向 BFS：两个队列交替扩展，用两个 `dist` 表；当某点被两边都访问到即相遇，答案 = `d1+d2`。因为每边只扩展 $b^{d/2}$ 个结点，总代价 $\sim 2b^{d/2}\ll b^d$。

**10.** 拓扑排序给合法顺序。找环：DFS 三色，遇灰点 $v$ 时从当前点沿「父指针」回溯到 $v$，即得环上路径。

**11.** 前向：拓扑序依次算值。反向：逆拓扑序，对每个结点用链式法则把上游梯度累加给输入。与有限差分 $(f(x+\epsilon)-f(x-\epsilon))/2\epsilon$ 比较，误差 $O(\epsilon^2)$。

</details>

---

## 延伸阅读

- **Sahni《数据结构、算法与应用（C++ 版）》第 2 版 第 14 章**「Graphs」：图的 ADT、邻接矩阵 / 邻接表、DFS / BFS、连通分量与生成树；
- **Sahni 第 2 版 第 15 章**（Sorting）中「拓扑排序」与图排序的相关讨论；
- **《算法导论》第 20 章**「基本图算法」——DFS/BFS、拓扑排序、强连通分量（严格证明 DFS 的括号定理与白路径定理）；
- **Cormen et al.** 关于「三色标记判环」与「DFS 分类边（树边 / 后向边 / 前向边 / 交叉边）」的讨论；
- **Tarjan, R. E. (1972)** *Depth-First Search and Linear Graph Algorithms* —— 用 DFS 的 `dfn/low` 一遍求割点、强连通分量的经典论文；
- **文献：** *Kahn, A. B. (1962)* Topological sorting of large networks —— Kahn 算法原始论文；
- 配套代码：`code/10_graph_basics.cpp`、`code/10_topological_sort.cpp`。

---

**下一章**：第 11 章《图算法》—— 在图遍历的地基上，讲**最小生成树、单源 / 全源最短路、并查集、A\*、二分图匹配**，并把它们接到「世界模型的规划与推理」上。
