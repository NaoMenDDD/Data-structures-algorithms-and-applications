# 第 16 章 面向世界模型研究的进阶路线

> 十五个章节走下来，你已经把 Sahni 教材里的零件一件件学完了：数组、链表、栈、队列、树、堆、搜索树、图、排序、哈希，以及分治、贪心、DP、回溯、分支限界、近似与元启发式。可是**零件不是机器**——知道 AVL 怎么旋转，不等于知道世界模型里哪一层在用它；能背出 Master 定理，不等于能在读一篇 World Models 论文时看出"这里的 LSTM 状态更新其实就是个循环队列"。
>
> 这一章是**总装配**。它做四件事：
>
> 1. **跑一遍「微基准套件」**：用 7 个真实测量把整门课串成一条性能故事线——同一个问题，换个数据结构，快 100 倍；同一个算法，换个访存顺序，快约 6 倍。**所有数字都是你机器上跑出来的**，不是抄的。
> 2. **给你一张知识地图**：把 16 章压成一张「零件目录」，用「什么时候用哪个」的方式速查。
> 3. **剖面一个真实系统**：从「一块 GPU 上跑的世界模型」往下拆，逐层告诉你每个数据结构落在哪。
> 4. **给出进阶路线 + 自测清单**：研究向最小必要集、该补的数学、该读的论文、以及 30 个能检验你「是否真会」的问题。
>
> 一句话贯穿全章：**数据结构和算法不是"考试科目"，是你未来系统里的零件目录。这一章教你怎么查目录、怎么按图索骥。**

---

## 本章目标

读完并跑完本章代码，你应该能够：

1. 亲手运行 `code/16_benchmark_suite.cpp`，用**实测数据**说清 7 组「同一问题不同结构 / 同一算法不同写法」的性能差异，并解释**每一组数字背后的机制**（渐进复杂度、缓存、分支预测、数值稳定）；
2. 拥有一张**可检索的知识地图**：给定一个研究问题（"我要做 top-k 采样"、"我要加速注意力"、"我要存 100 亿条经验"），能立刻定位到该用哪个结构/算法；
3. 把「一块 GPU 上跑的世界模型」**逐层剖面**：数据处理 → 张量算子 → 检索 → 规划搜索 → 训练循环，每一层点名对应的数据结构；
4. 按自己的时间与目标，从**三条进阶路线**里选一条，并知道「研究向最小必要集」是哪几章；
5. 用**自测清单**（30 问）检验自己：能答上 25 个以上，说明你确实"掌握"了这门课，而不只是"看过"；
6. 避开**六个最常见的误区**（把"看懂"当"会写"、迷信渐进复杂度忽略常数、忽视数值稳定性、忽视内存层级、只会 STL 不懂原理、把近似当"次品"）。

---

## 前置知识

- 第 00–15 章全部。这一章不再引入新知识，而是**复用并横向连接**前面所有内容；
- 一点点**计算机体系结构直觉**：寄存器 / L1 / L2 / L3 / 主存 的速度阶梯（每级差约 3–10 倍）、缓存行（cache line，64 字节）、顺序访问 vs 随机访问；
- 一点点**深度学习系统常识**：张量、算子、GEMM、注意力、KV cache、beam search（不知道也没关系，本章会点明）。

---

## 16.1 总装配：7 个微基准，一条性能故事线

前面的每一章都各自验证过一个点。这一节把它们**放进同一个程序、同一台机器、同一个计时器**里并排测，让差异直接可见。程序是 `code/16_benchmark_suite.cpp`，编译运行：

```bash
cd "MyGit/Data structures-algorithms-and-applications"
g++ -std=c++17 -O2 -Wall -Wextra code/16_benchmark_suite.cpp -o /tmp/b16 && /tmp/b16
```

下面是我机器上的**完整真实输出**（你的绝对数值会不同，但**相对关系**会惊人地一致）：

```text
########## 面向世界模型研究的微基准套件 ##########

===== 1) Top-k：大小为 k 的堆 vs 全排序 =====
  n=2000000，k=100
    全排序 O(n log n)  106.462 ms
    堆     O(n log k)  1.03371 ms   加速 102.991x
  -> k << n 时堆完胜；流式/内存受限场景只能上堆（无法全排序）。

===== 2) 查找：unordered_map vs 有序数组二分 =====
  n=1000000 键（哈希表内存约 31 MB，有序数组约 3 MB），1000000 次查找
    unordered_map（哈希）  15.1258 ms   命中 1000000   平均 15.1258 ns/次
    有序数组二分            106.589 ms   命中 1000000   平均 106.589 ns/次（≈ log2(1000000)≈20 次比较）
  -> 点查询哈希更快（O(1) vs O(log n)），代价是内存约 10 倍 + 跳指针缓存不友好。
     有序数组的赢面在别处：零额外内存、数据本就有序、需要**范围查询**、
     或顺序扫描 —— 缓存友好度把「每步 20 次比较」的劣势部分抵消回来。

===== 3) kNN：KD 树剪枝 vs 暴力（并验证结果一致） =====
  n=200000 点（2D），5000 次查询，k=10
    KD 树     9.56344 ms   （平均访问结点 52 / 200000）
    暴力       15378.5 ms
    两法前 k 距离和之比 ≈ 1（应为 1，验证结果一致）
  -> KD 树只访问约 0.026% 的点，检索快一个量级；
     维度升高会「维度灾难」，于是有了 LSH / PQ / HNSW（向量库）。

===== 4) 矩阵乘法：内存布局决定性能 =====
  n=512，浮点乘加量 ≈ 268.435 MFLOP
    朴素 ijk     245.352 ms   1.09408 GFLOP/s
    换序 ikj     37.9316 ms   7.07683 GFLOP/s
    分块(BLK=64) 43.0863 ms   6.23018 GFLOP/s
  -> 三种写法**算法完全一样**（都是 O(n^3)），性能却差数倍，全在缓存命中率。
     深度学习框架的矩阵乘（cuBLAS）、FlashAttention 都在做这件事。

===== 5) 数值稳定：softmax 的溢出与平移不变性 =====
  logits = 1000 1001 999 1002 998
    朴素 softmax  : nan nan nan nan nan   <- exp(1000)=inf -> inf/inf=nan
    稳定 softmax  : 0.0861285 0.234122 0.0316849 0.636409 0.0116562
    参考(平移后)  : 0.0861285 0.234122 0.0316849 0.636409 0.0116562
    logsumexp(logits) = 1002.45（= max + log Σexp(z-max)）
  -> softmax 平移不变，但**必须在数值上减去最大值**，否则 exp 溢出成 nan。
     这就是注意力 / 交叉熵实现里处处 logsumexp 的原因。

===== 6) 序列解码：贪心 vs 束搜索 vs 穷举 =====
  K=4 个候选 token，解码长度 T=6（穷举 4^6=4096 条路径）
    贪心（=束宽1）  得分 6.19592
    束搜索 宽2      得分 6.74029
    束搜索 宽4      得分 7.71313
    穷举（精确最优） 得分 7.71313
  -> 贪心每步只看眼前，会错过全局最优；束搜索保留 B 条部分路径，用「B 倍开销」换质量；
     B 大到 K^T 就退化成穷举。这是生成式模型 / 世界模型规划的标准解码策略。

===== 7) 经验回放：环形缓冲区 =====
  容量 = 1048576，写入 3000000 条
    写入        21.8013 ms   （137.606 M 条/s）
    随机采样     38.5051 ms   （100000 批 × 32 条，acc=7.92207e+12）
  -> 环形缓冲区写入 O(1)、内存有界（旧经验被覆盖），是 RL / 世界模型数据管线的标配。
     底层就是第 06 章的「循环队列」。

全部基准完成，所有正确性断言通过。
```

下面逐条拆解——**每一组都对应前面的一章，且每个数字都有一句"为什么"**。

### 基准 1：Top-k —— 堆 vs 全排序（第 08 章）

从 200 万个分数里取最大的 100 个。**全排序**要 $O(n\log n)$，**大小为 $k$ 的小顶堆**只要 $O(n\log k)$。

- 实测：**106 ms 对 1.03 ms，快 103 倍**。
- 机制：$n\log k$ 与 $n\log n$ 的比值是 $\frac{\log k}{\log n}=\frac{\log 100}{\log 2\times10^6}\approx\frac{6.6}{21}\approx0.32$，**光看渐进只有 3 倍**。真正的 100 倍来自两处：(1) 堆**不需要存储/移动全部 $n$ 个元素**（只维护 $k$ 个），(2) 堆的每个元素只与堆顶比较一次就**大概率被丢弃**，cache 与分支预测都极友好。这就是"**渐进复杂度只是下界，常数与访存模式决定一切**"。
- 世界模型落点：**采样**。语言模型每一步要从 $|V|=10^5$ 的词表里取 top-k / top-p，如果做全排序，解码会慢到不可用；堆（或 `nth_element`/部分排序）是标准做法。**beam search 的候选也是在堆上维护的。**

### 基准 2：查找 —— 哈希 vs 有序数组二分（第 09、13 章）

100 万个键，查 100 万次（全命中）。

- 实测：**哈希 15.1 ns/次，二分 106.6 ns/次**（约 7 倍）。
- 机制：哈希是 $O(1)$（平均），二分的 $\log_2 10^6\approx20$ 次比较恰好落在缓存未命中的"危险区"——每次比较都是一次可能跨缓存行的访存。可是**哈希并不总是赢**：它多花约 **10 倍内存**（31 MB vs 3 MB），且**指针跳转随机**。当查询是**范围查询**（"所有分数在 [0.9, 1.0] 的候选"）、或数据本就有序、或内存极度受限时，**有序数组反而是唯一选择**。
- 世界模型落点：**两套检索并存**。embedding 库用哈希/ANN 索引做点查询；但"按时间戳取一段轨迹""按 reward 分位取样"是**范围查询**，靠有序结构。这也解释了为什么数据库里 B+ 树和哈希索引都要有——**它们服务不同的查询模式**。

### 基准 3：kNN —— KD 树剪枝 vs 暴力（第 07 章）

20 万个二维点，5000 次查询，每次找最近的 10 个。

- 实测：**KD 树 9.56 ms，暴力 15379 ms，快约 1600 倍**；KD 树平均只访问 **52/200000 ≈ 0.026%** 的点。
- 机制：KD 树把空间递归二分，查询时**用"当前第 k 近的距离"作为半径去剪枝**——大部分子树整块跳过。暴力则每个点都算距离。**这就是"索引"的全部意义：把 $O(n)$ 降成 $O(\log n)$ 级别。**
- 一个诚实的转折：**这是 2 维**。维数升高会遭遇「**维度灾难**（curse of dimensionality）」——高维空间里"最近邻"几乎和"随机点"一样远，剪枝失效，KD 树退化成暴力。所以现代向量检索（embedding 检索、世界模型的记忆召回）用的是 **LSH / 乘积量化（PQ）/ HNSW**（第 13 章的 LSH 就是其中一族）。
- 世界模型落点：**记忆召回 / 状态检索**。世界模型里"找到与当前观测最像的历史帧"就是 kNN，只是维度是 embedding 维度（几百到几千），所以必须用 ANN 而不是 KD 树。

### 基准 4：矩阵乘法 —— 访存顺序决定性能（第 04 章）

512×512 的矩阵乘法，三种写法，**算法完全相同**（都是三重循环 $O(n^3)$，同样的 $2n^3$ 次乘加）。

- 实测：**朴素 `ijk` 1.09 GFLOP/s，换序 `ikj` 7.08 GFLOP/s，分块 6.23 GFLOP/s**。
- 机制：`ijk` 的内层循环遍历 $B$ 的一整**列**——而 C/C++ 里矩阵按**行**存储，遍历一列是"跳着走"，每步都跨缓存行，**缓存几乎全miss**。`ikj` 把内层改成遍历 $B$ 的一**行**（连续访存），命中率暴涨。**这约 6 倍的差距，与乘法次数毫无关系，纯粹是内存布局。**
- 为什么分块这次没赢：分块（blocking）的威力在**大矩阵**（远超缓存）时才显现——把工作集切到能装进 L1/L2 的小块。这里 $n=512$ 的矩阵约 2 MB，本就能部分装进 L2/L3，所以"简单换序"已经拿到了大部分收益，分块反而多了循环开销。**这是"没有银弹、要看规模"的活教材**——本套件的分块实现是教学用的朴素版，cuBLAS/OpenBLAS 的分块是分层的（L1/L2/L3 三层）且用 SIMD，能再快一个数量级。
- 世界模型落点：**这一条几乎就是深度学习性能的全部**。GEMM（通用矩阵乘）是注意力、卷积、全连接的核心；**FlashAttention** 的整个贡献就是"重新安排计算顺序，避免把 $n\times n$ 注意力矩阵写回主存"——本质上是**分块 + 融合**（第 04 章的加倍）。你在这个基准里看到的约 6 倍，就是 FlashAttention 论文里那些加速数字的微观版本。

### 基准 5：数值稳定 —— softmax 的溢出与平移不变性（第 01 章的"浮点"、跨章）

logits 是 `1000 1001 999 1002 998`。

- 实测：**朴素 softmax 输出 `nan nan nan nan nan`**；减去最大值后得到 `0.086 0.234 0.032 0.636 0.012`（和为 1）。
- 机制：$\exp(1000)$ 在 double 里**溢出成 `inf`**，于是 $\frac{\text{inf}}{\text{inf}}=\text{nan}$。而 softmax 有**平移不变性** $\text{softmax}(z)=\text{softmax}(z-\max)$，减去最大值把指数都压到 $\le0$，$\exp\le1$，永不溢出。**数学上等价，数值上一个能用一个不能用。**
- 实测还打印了 `logsumexp = 1002.45`，即 $\max+\log\sum\exp(z-\max)$——这是**对数域**里更稳定的形式，让你在算 log-probability 时不必先 exp 回去。
- 世界模型落点：**注意力、交叉熵、变分下界（ELBO）、扩散模型的 loss，全部依赖 logsumexp**。你会在每一份高质量的实现里看到"减去 max"这一步——它不是可选的优化，是**正确性的前提**。

### 基准 6：序列解码 —— 贪心 vs 束搜索 vs 穷举（第 08、14、15 章）

$K=4$ 个候选、解码长度 $T=6$，穷举 $4^6=4096$ 条完整路径。

- 实测：**贪心（束宽 1）6.196 < 束搜索宽 2：6.740 < 束搜索宽 4：7.713 = 穷举最优 7.713**。
- 机制：**贪心每步只看眼前**（局部最优），会掉进次优的整条序列；**束搜索**同时保留 $B$ 条最有希望的部分路径，用 $B$ 倍开销换全局质量；当 $B=K^T$ 时它**就是**穷举。这三者是一个统一的**谱系**：$B=1$（贪心）→ $1<B<K^T$（束搜索）→ $B=K^T$（精确）。

  实测里束宽 4 恰好等于最优，是因为 $K=4$ 太便宜——**只要 $B$ 足够大就能覆盖最优**；真实语言模型 $K=5\times10^4$、$T=1000$，$K^T$ 是天文数字，永远只能近似。
- 世界模型落点：**这就是世界模型"想象/规划"的核心**。给定一个学到的动力学模型，要在动作空间里搜出一条最优轨迹，本质就是上面的束搜索 / MCTS 问题；"贪心 vs 束搜索"的质量-开销权衡，直接决定了 agent 是"短视"还是"有远见"。

### 基准 7：经验回放 —— 环形缓冲区（第 06 章）

容量 $2^{20}$ 的 replay buffer，写 300 万条（**溢出**，最旧的被覆盖）。

- 实测：**写入 21.8 ms（1.38 亿条/秒）；随机批量采样 38.5 ms，共 320 万次采样（100000 批 × 32 条），约 12 ns/次**。
- 机制：环形缓冲区用 `(head + 1) % cap` 做**模运算**实现 $O(1)$ 写入 + **有界内存**——写满后自动覆盖最旧数据，**永不需要扩容、永不 OOM**。它同时满足两个看似矛盾的要求：**内存有界** 和 **无限期接收新数据**。
- 世界模型落点：**训练数据管线的主力**。RL 与世界模型训练需要持续从缓冲区**随机采样 mini-batch**（打散时间相关性）；推理时的 KV cache 淘汰、连续批处理的请求队列，也全是环形缓冲 / 双端队列。**这是"训练一个能持续学习的 agent"的工程地基。**

> **把 7 条串起来看**：第 1、2、3、7 条讲**选对数据结构**（同样的任务，换结构差 100–1600 倍）；第 4、5 条讲**写对实现**（同样的算法，换访存/数值处理差约 6 倍甚至从 nan 变可用）；第 6 条讲**选对策略**（贪心/束搜索/穷举是质量与开销的连续权衡）。这三件事——**选结构、写实现、定策略**——就是本课程的全部。

---

## 16.2 知识地图：整本书的零件目录

把 16 章压成一张"什么时候用哪个"的速查表。**左边是问题信号，右边是工具。**

| 你遇到的信号 | 该拿的工具 | 章 | 复杂度 |
|---|---|---|---|
| 要按下标随机访问、要连续内存、要矢量化 | **动态数组** `vector` | 02 | 访问 $O(1)$ / 尾插摊还 $O(1)$ |
| 频繁在中间插入删除、大小未知 | **链表** / 跳表 | 03 / 09 | 插删 $O(1)$（已知位置） |
| 后进先出：DFS、回溯、撤销、调用栈 | **栈** | 05 | $O(1)$ |
| 先进先出：BFS、流水线、缓冲、批处理 | **队列 / deque** | 06 | $O(1)$ |
| 滑动窗口最值、单调性 | **单调栈 / 单调队列** | 05 / 06 | $O(n)$ 总 |
| 数据有序、要范围查询 | **平衡 BST / 跳表 / B+ 树** | 09 | $O(\log n)$ |
| 反复取最值、Top-k、合并有序流 | **堆（优先级队列）** | 08 | 建堆 $O(n)$，取最值 $O(\log n)$ |
| 前缀/字典、自动补全、路由表 | **Trie** | 07 | $O(\text{串长})$ |
| 空间最近邻（低维） | **KD 树** | 07 | 平均 $O(\log n)$ |
| 空间最近邻（高维 embedding） | **LSH / PQ / HNSW** | 13 | 近似 $O(1)$ / $O(\log n)$ |
| 点查询、去重、计数 | **哈希表** | 13 | 平均 $O(1)$ |
| 判断"某元素在不在"、要省内存 | **Bloom filter** | 13 | $O(k)$，有假阳性无假阴性 |
| 集合相似度（去重、检索） | **MinHash + LSH** | 13 | 近似 Jaccard |
| 关系、依赖、传播、状态转移 | **图**（邻接表 / CSR） | 10 | 视算法 |
| 依赖排序、编译调度、关键路径 | **拓扑排序** | 10 | $O(V+E)$ |
| 非负权最短路、规划 | **Dijkstra** | 11 | $O((V+E)\log V)$ |
| 有负权最短路、Bellman 方程 | **Bellman-Ford** | 11 | $O(VE)$ |
| 全局两两最短路、小图 floyd | **Floyd-Warshall** | 11 | $O(V^3)$ |
| 带启发式的单目标最短路 | **A\*** | 11 | 视启发式 |
| 连通性、MST、聚类分层 | **并查集 / Kruskal / Prim** | 11 | 近 $O(E)$ |
| 二分图匹配、任务分配、数据关联 | **匈牙利算法 / 最大流** | 11 | $O(V^3)$ / $O(VE^2)$ |
| 需要稳定 $O(n\log n)$ 排序 | **归并排序** | 12 | $O(n\log n)$ |
| 平均最快、原地 | **快速排序 / introsort** | 12 | 平均 $O(n\log n)$ |
| 整数 / 定长键、要线性时间 | **计数 / 基数排序** | 12 | $O(n+k)$ / $O(d(n+k))$ |
| 只要第 k 大 / top-k | **quickselect / nth_element / 堆** | 12 / 08 | 平均 $O(n)$ / $O(n\log k)$ |
| 数据超内存、要外部排序 | **多路归并 + 置换选择** | 12 | $O(n\log n)$ + I/O |
| 问题能拆成独立子问题 | **分治** | 14 | 看 Master 定理 |
| 局部最优 = 全局最优（有交换论证） | **贪心** | 14 | 常 $O(n\log n)$ |
| 子问题重叠、最优子结构 | **动态规划** | 14 | 状态数 × 转移 |
| 要枚举所有可行解、有剪枝 | **回溯** | 14 | 最坏指数，剪枝后常可行 |
| 要精确最优但可剪枝 | **分支限界** | 14 | 最坏指数 |
| 问题 NP 难、只要求保证 | **近似算法**（2-近似 / PTAS / FPTAS） | 15 | 多项式 |
| NP 难、实践要够好 | **模拟退火 / 遗传 / 蚁群 / 局部搜索** | 15 | 可调 |
| NP 难、能接受指数但实例小 | **分支限界 / 参数化（FPT）** | 14 / 15 | $O(2^k\cdot\text{poly})$ |

> **用法**：拿到一个新问题，先问三句——(1) **数据量和访问模式是什么**？(2) **要精确还是近似**？(3) **内存/时间预算多少**？三句答完，上面表里能定位到只剩两三个候选。

---

## 16.3 剖面：一个真实世界模型系统的逐层对应

抽象的目录不够，我们把"一块 GPU 上跑的世界模型"竖着切开，看每一层用了什么。这是本章最有价值的一张图——**它把整门课映射到一个你未来每天都会碰的系统上**。

```text
┌──────────────────────────────────────────────────────────────────────┐
│  L5  规划与决策层                                                     │
│      · beam search / MCTS 展开想象轨迹         → 堆 + 树 + 回溯 (08/07/14) │
│      · 从动力学模型 rollout，评估价值          → 图搜索 + DP (10/11/14)   │
│      · 候选序列打分、top-p 采样                → 堆 / quickselect (08/12) │
├──────────────────────────────────────────────────────────────────────┤
│  L4  检索与记忆层                                                     │
│      · embedding 检索、状态召回               → LSH/HNSW/PQ (13/07)   │
│      · 词表 / 分词 / 缓存                      → 哈希 / Trie (13/07)    │
│      · KV cache 淘汰（LRU / 滑窗）            → 环形缓冲 / 链表+哈希 (06/03/13) │
│      · 去重、相似度（数据清洗）               → MinHash + LSH (13)      │
├──────────────────────────────────────────────────────────────────────┤
│  L3  算子与计算层                                                     │
│      · 矩阵乘 / 卷积 / 注意力                  → 数组+stride+分块 (02/04)  │
│      · 稀疏注意力 / 稀疏张量                   → CSR (04)              │
│      · softmax / logsumexp（数值稳定）        → 浮点 + 前缀思想 (01/05)  │
│      · 自动微分 / 计算图                       → DAG + 拓扑排序 + 栈 (10/05)│
├──────────────────────────────────────────────────────────────────────┤
│  L2  数据处理与批处理层                                               │
│      · DataLoader 预取、连续批处理             → 队列 / deque (06)       │
│      · 按长度分桶、减少 padding                → 排序 / 哈希分桶 (12/13)   │
│      · 外部排序（超大训练集）                  → 多路归并 (12)          │
├──────────────────────────────────────────────────────────────────────┤
│  L1  训练数据管线层                                                   │
│      · 经验回放池（有界、随机采样）            → 环形缓冲 (06)          │
│      · buffer shuffle / 打散时间相关性         → 洗牌 / 哈希 (12/13)     │
│      · 优先经验回放（按优先级采样）            → 堆 / 求和树 (08/07)     │
├──────────────────────────────────────────────────────────────────────┤
│  L0  系统与内存层                                                     │
│      · 内存分配、对象生命周期                  → RAII / 内存池 / 自由表 (00/03)│
│      · 缓存友好的数据布局                      → 数组优于链表 (02/03/04)  │
│      · 并查集（组件合并，如场景图连通块）       → union-find (11)         │
└──────────────────────────────────────────────────────────────────────┘
```

**怎么用这张图**：当你在读一篇世界模型论文或一份框架源码卡住时，先定位它在哪一层（L0–L5），再查 16.2 的目录——**"这里为什么用这个结构"往往一秒就能想通**。反过来，当你自己设计系统卡住时，也能照着层去问"这一层我该用什么"。

---

## 16.4 三条进阶路线

学完不等于学够。根据你的时间和目标选一条：

| 路线 | 适合 | 重点章节 | 补齐方式 |
|---|---|---|---|
| **研究向（推荐给你）** | 要发论文 / 做世界模型 | 01, 04, 08, 11, 13, 14 | 重 **DP、图搜索、近似/采样、检索索引**；数学补**概率图模型、变分推断、RL 的 Bellman 方程** |
| **系统向** | 要做训练/推理 infra | 02, 03, 04, 06, 08, 12 | 重 **内存布局、缓存、并发数据结构、外部排序**；补 **CUDA、算子融合、分布式通信** |
| **理论向** | 要啃复杂性 / 组合优化 | 01, 09, 11, 14, 15 | 重 **摊还分析、近似比证明、NP 完全归约**；补 **离散数学、线性规划、近似算法专著** |

**关键提醒**：三条路线**都必须先过第 01 章（算法分析）**——它是你判断"这个方案行不行、能不能 scale"的度量衡。没有它，后面全是背结论。

---

## 16.5 研究向最小必要集（只有两周怎么办）

如果你明天就要进组、只有两周，按这个顺序突击（每章只读"与 AI 连接"一节 + 跑代码）：

1. **第 01 章**：会算复杂度和 FLOPs → 能判断一个模型/算法能不能 scale。
2. **第 04 章**：懂 stride 与矩阵乘访存 → 能看懂 `contiguous()`、`einsum`、FlashAttention。
3. **第 08 章**：堆与 Top-k → 能写采样、懂 beam search 的实现。
4. **第 11 章**：Dijkstra / A\* / Bellman-Ford → 看得懂规划与 RL 的 Bellman 方程；**Bellman 方程就是"最短路"**。
5. **第 13 章**：哈希 + LSH → 能实现嵌入检索、KV cache、去重。
6. **第 14 章**：DP 与分支限界 → Viterbi/CTC、记忆化、搜索式规划。
7. **第 15 章**：NP 难与近似 → 理解"为什么研究系统到处是近似与采样"。

**两周计划**：3 天过 01/04（打地基）→ 3 天过 08/13（工程常用）→ 4 天过 11/14（研究核心）→ 2 天过 15 + 本章 16.1（全局观）→ 2 天跑本章基准 + 自测清单。

---

## 16.6 六个常见误区

1. **把"看懂"当"会写"。** 看懂了 AVL 旋转的动画，不代表能不看资料写出旋转。**掌握的检验标准是"白纸手写 + 边界不出错"**，不是"读着觉得对"。
2. **迷信渐进复杂度、忽略常数与访存。** 16.1 的基准 1、2、4 全是反例：$O(n\log k)$ 比 $O(n\log n)$ 快 100 倍；$O(1)$ 的哈希和 $O(\log n)$ 的二分只差 7 倍而不是天壤之别；同复杂度 $O(n^3)$ 的两种写法差约 6 倍。**渐进是地图，常数是路况。**
3. **忽视数值稳定性。** 基准 5 是血泪：数学上正确的 softmax 会因溢出输出 `nan`，训练直接崩。**所有涉及 exp、log、累加、除法的代码都要问一句"极端输入会怎样"。**
4. **忽视内存层级。** 链表在纸面上插删 $O(1)$ 优于数组，但在真实机器上，数组的连续访存常常反杀（第 03 章的 `03_cache_list_vs_array.cpp` 就是实测）。**CPU 不按你的复杂度模型跑，它按缓存行跑。**
5. **只会调 STL，不懂原理。** 会用 `std::priority_queue` 不够，要能在需要时**自己写一个**（改变堆序、支持 decrease-key、做对顶堆求中位数）。读懂源码、能改写、能测，才叫掌握。
6. **把"近似"当"次品"。** 基准 6 与第 15 章反复说明：**近似是主动的策略选择**，不是失败的妥协。世界模型、扩散模型、MCTS、采样解码——现代 AI 的主力全是近似与概率方法。不理解这点，你会以为"没求到最优就是没学好"。

---

## 16.7 自测清单：30 问，你答得上几个

**能答上 25 个以上**，说明你确实"掌握"了这门课。答不上的，回到对应章节。

**基础（第 00–06 章）**

1. 为什么"摊还分析"下 `vector::push_back` 是 $O(1)$？最坏呢？给个例子说明这笔账怎么算。
2. 行主序与列主序下，`A[i][j]` 的地址公式分别是什么？为什么 PyTorch 的 `transpose` 会改变 `contiguous` 状态？
3. 为什么 GPU 上链表远不如数组？请从"内存访问模式"和"合并访存（coalescing）"两个词解释。
4. 栈和递归的对应关系是什么？编译器的调用栈和你手写的栈有什么异同？
5. 循环队列如何用取模实现"无扩容 + 有界内存"？`head`/`tail` 的判定条件怎么写才不歧义（空 vs 满）？

**进阶（第 07–13 章）**

6. 建堆为什么是 $O(n)$ 而不是 $O(n\log n)$？给出求和式的直觉。
7. 对顶堆求数据流中位数的原理？插入和查询各是什么复杂度？
8. 为什么"选 top-k"用大小为 $k$ 的小顶堆而不是大顶堆？k 和 n 的差距如何影响选择？
9. 平衡 BST、跳表、B+ 树各自最适合什么场景？为什么数据库索引偏爱 B+ 树？
10. KD 树在高维为什么会失效（维度灾难）？LSH 是怎么绕开它的？
11. 哈希表链地址法和开放寻址法各自的优缺点？装载因子怎么影响性能？为什么装载因子不能太大？
12. Bloom filter 为什么"有假阳性、无假阴性"？假阳性率与 $m,n,k$ 的关系式是什么？
13. MinHash 为什么能估计 Jaccard 相似度？LSH 分带（banding）怎么把"近似相等"变成"近似相邻"？
14. 一致性哈希解决了什么问题？虚拟节点的作用是什么？
15. 计数排序、基数排序、比较排序的复杂度下界各是什么？为什么比较排序下界是 $\Omega(n\log n)$？

**挑战（第 14–15 章 + 综合）**

16. 用 Master 定理求解 $T(n)=2T(n/2)+O(n)$、$T(n)=T(n-1)+O(1)$、$T(n)=7T(n/2)+O(n^2)$。
17. 动态规划的"最优子结构"和"重叠子问题"分别指什么？为什么二者缺一不可？
18. 记忆化（自顶向下）与递推（自底向上）各有什么优缺点？什么时候必须用其中一个？
19. 编辑距离的 DP 状态和转移是什么？它和序列对齐（alignment）什么关系？
20. 分支限界为什么能比暴力快？下界函数（bound）必须满足什么性质才不丢最优解？
21. 为什么 TSP 没有多项式精确算法（在 P≠NP 假设下）？它的 2-近似可行吗？一般图的 TSP 有常数近似吗？（提示：度量 TSP 有无 1.5-近似？）
22. 顶点覆盖的极大匹配 2-近似为什么是 2？构造一个比值恰好为 2 的实例。
23. FPTAS 和精确 DP 在"伪多项式"这件事上的根本区别是什么？为什么背包的精确 DP 不是多项式算法？
24. 模拟退火的三个要素（邻域、接受准则、降温表）分别起什么作用？起始温度太高/太低各会怎样？
25. 解释"NP 完全"和"NP 难"的区别。为什么"NP 难但可近似"和"NP 难且不可近似"是两回事？
26. 归一化 softmax 为什么必须减去最大值？logsumexp 的好处是什么？（结合基准 5）
27. 注意力为什么要分块（FlashAttention）？这和基准 4 的哪个现象是同一回事？
28. 束搜索的束宽 $B$ 从 1 增大到 $K^T$ 时，成本和结果如何变化？为什么语言模型不能取 $B=K^T$？
29. 经验回放池为什么用环形缓冲而不是可增长的 `vector`？如果无界增长会发生什么？
30. 给你一个"要在 10 亿个 embedding 里做实时最近邻"的任务，你会怎么设计？点出至少三个数据结构并说明理由。

---

## 16.8 延伸阅读与资源

**接续本课程的书**

- 《算法导论》（CLRS）—— 本课程的理论加深版，尤其补第 15 章（DP）、第 34 章（NP 完全）、第 35 章（近似算法）。
- 《计算机程序设计艺术》（TAOCP, Knuth）—— 想深入摊还分析、随机化、排序下界的终极参考。
- *Approximation Algorithms*（Vazirani）—— 第 15 章的进阶；*The Design of Approximation Algorithms*（Williamson & Shmoys）可免费下载。
- 《计算机体系结构：量化研究方法》（Hennessy & Patterson）—— 把 16.1 的"缓存故事"讲透的书，性能工程师必读。

**接到世界模型 / 深度学习系统**

- *World Models*（Ha & Schmidhuber, 2018）—— 本课程名字里"世界模型"的原始论文，从一个 RNN + VAE 的"梦"讲起。
- *Attention Is All You Need*（2017）→ *FlashAttention*（Dao et al.）—— 把第 04 章的矩阵乘/分块、第 05 章的数值稳定连到现代 Transformer。
- *Dreamer* 系列（Hafner et al.）—— 学一个动力学模型 + 在"想象"里用 latent 做规划；L4/L5 的活例子。
- *Monte Carlo Tree Search* 相关（AlphaZero 论文）—— 第 14 章回溯 + 第 08 章堆 + 第 15 章近似的集大成者。
- *PomDPs / RL*（Sutton & Barto, *Reinforcement Learning: An Introduction*）—— 第 11 章 Bellman-Ford 与 DP 就是 RL 的数学核心。
- 向量检索：*Billion-scale similarity search with GPUs*（FAISS）、HNSW 论文 —— 第 13 章 LSH 的工程化。

**工程工具**

- 性能剖析：`perf`、`gprof`、`valgrind --tool=cachegrind`（看本课程基准的缓存 miss 到底发生在哪）。
- 数值：`-fsanitize=address,undefined`（本课程每章都验证过）、`-ffast-math` 的坑（它可能破坏 logsumexp 的"必须减 max"）。
- 对照实验：用 PyTorch 复现基准 4（`torch.matmul` vs 手写三重循环）和基准 5（`F.softmax` vs 手写），你会看到框架替你做了什么。

---

## 常见错误与陷阱

> 本章是总装配，这里的"陷阱"是**贯穿全书的元陷阱**，不是某个具体 bug。

1. **把基准数字当绝对值记。** 你要记的是**相对关系**（堆比全排序快两个数量级、换序比朴素快约 6 倍），不是"106 ms"。换台机器数字会变，规律不变。
2. **为一个"看起来简单"的方案不测就上生产。** 本课程每一章都坚持"写代码 → 跑 → 计时 → 验证"。**不测量就优化是赌博。**
3. **忽略"正确性断言"这一步。** 本套件每个基准都带 `assert`（堆的结果 == 排序结果、KD 树的距离和 == 暴力、束搜索(1) == 贪心……）。**先证明它对，再谈它快。** 一个错的快算法毫无价值。
4. **把"学会数据结构"和"会用数据结构"混为一谈。** 前者是能画出图、说出复杂度；后者是**看到问题能匹配结构、写出正确高效的代码、预判边界崩在哪**。自测清单检验的是后者。
5. **只练"模板题"、不练"系统题"。** 课程里每个结构都配了真实的 AI 场景，是为了让你知道**它为什么存在**。跳过"与 AI 连接"一节，你学的就退化成八股。
6. **学完就停。** 数据结构的"会用"高度依赖**反复手写**。建议：每学完一章，隔一周不看资料重写一次核心结构（堆、AVL、Dijkstra、哈希表），直到能闭卷写出。

---

## 与 AI / 世界模型的连接

这一章本身就是"与 AI 的连接"的总集——16.3 的剖面图是它的核心。这里再点三句最关键的：

- **世界模型 = 学一个动力学 + 在上面搜索/规划。** 学的部分（神经网络）大量依赖第 04 章的矩阵乘访存与第 01 章的 FLOPs 估计；搜的部分（beam/MCTS/rollout）大量依赖第 07 章树、第 08 章堆、第 11 章图搜索、第 14 章 DP/回溯、第 15 章近似。**你学过的每个结构都能在这台机器上找到螺丝孔。**
- **"系统性能"研究的主战场是内存层级，不是复杂度。** 16.1 里约 6 倍的矩阵乘差距、100 倍的 top-k 差距，都来自"数据怎么摆、怎么走"。FlashAttention、vLLM 的 PagedAttention、各种 kernel fusion，本质都是**在缓存/显存受限下重排数据结构**——这正是本课程的底层心法。
- **近似与采样是现代 AI 的默认语言。** 语言模型不是精确解码（beam/sampling），规划不是精确搜索（MCTS/rollout），检索不是精确最近邻（ANN）。第 15 章的"退一步"不是妥协，是**在被证明很难的问题上唯一可行的前进方式**——理解它，你才算读懂了现代 AI 的工程哲学。

---

## 本章小结

- **7 个微基准串起全书**：top-k 堆（第 08 章）快 103 倍、哈希 vs 二分（第 09/13 章）快 7 倍、内存差 10 倍、KD 树（第 07 章）快 1600 倍、矩阵乘换序（第 04 章）快约 6 倍、softmax 稳定版（跨章）从 `nan` 救回、束搜索解码（第 08/14/15 章）质量-开销连续谱、环形缓冲回放（第 06 章）1.38 亿条/秒。**"选结构、写实现、定策略"是全部。**
- **知识地图**把所有结构压成"什么时候用哪个"，**剖面图**把一块 GPU 上的世界模型竖切成 L0–L5 六层，每层点名对应结构。这两张表是本章最值得反复回看的产物。
- **三条进阶路线** + **研究向最小必要集（7 章 / 两周）** 给你可执行的下一步。
- **六个元陷阱**与 **30 问自测清单**帮你区分"看过"和"掌握"。
- 全书心法一句话：**数据结构是零件目录，算法是装配说明；真正的能力是拿到问题就知道该翻哪一页、怎么装、边界在哪。**

---

## 练习

### 基础

1. 跑一遍 `code/16_benchmark_suite.cpp`，记录你机器上的 7 组数字，和本章输出对比。**哪一组的相对关系最出乎你的意料？为什么？**
2. 把基准 1 的 $k$ 从 100 改成 1000000（接近 $n$），重跑。堆还快吗？为什么？（提示：$\log k/\log n$ 逼近 1，且堆不再省内存。）
3. 把基准 4 的分块大小 `BLK` 从 64 改成 8、16、256，观察 GFLOP/s 变化。解释为什么存在"最佳块大小"。

### 进阶

4. 给基准 5 加一个**反向**实验：构造一组很小的 logits（如 `-1000` 附近），证明"朴素 softmax 下溢成 0、减 max 后正常"。写进代码并保存输出。
5. 扩展基准 6：把 $K$ 提到 8、$T$ 提到 10，束宽取 1/2/4/8，画出（用文本表格）"束宽 vs 得分 vs 时间"的关系，验证"收益递减"。
6. 给基准 7 的 replay buffer 加上**按优先级采样**（第 08 章：用一个求和堆或前缀和数组），让它支持"高 TD-error 的经验被采到概率更大"。说明复杂度变化。

### 挑战

7. 实现一个**混合检索**：对基准 3 的二维点，先用哈希把空间粗分格（网格哈希），再在邻近格里做 kNN，和纯 KD 树比精度与速度。讨论高维下网格哈希为什么会退化。
8. 用第 04 章的分块思想，**手写一个"注意力"**（$QK^\top$ → softmax → $\times V$），实现一个"朴素版"（物化完整注意力矩阵）和一个"分块版"（不物化，逐块 softmax 并用 logsumexp 合并），在 $n=2048$、$d=64$ 下比较**显存占用**和**时间**。这就是 FlashAttention 的骨架。
9. 写一个**可复现的小型世界模型实验**：用一个 $K$ 状态、$T$ 步的离散动力学模型（第 14 章 Viterbi 那套），分别用**贪心、束搜索、穷举**做规划，统计"平均回报 vs 计算量"。把第 16.1 基准 6 的结论在带随机性转移动力学上复现一遍。

<details><summary>参考答案（要点）</summary>

**1.** 关键不是数字，是**相对关系**。多数人意外的是基准 2：哈希只比二分快 7 倍（而不是"$O(1)$ 碾压 $O(\log n)$"），因为二分的 20 次比较在现代 CPU 上被缓存和流水线大幅摊薄，而哈希的内存跳转有代价。

**2.** $k\to n$ 时，堆退化成"对全部元素建堆再弹出"，$\log k\to\log n$，且堆要存 $n$ 个元素，`priority_queue` 的常数与内存都不再有利，**全排序可能反超**。这说明"用堆"的前提是 **$k\ll n$**。

**3.** 块太小 → 循环/索引开销占比高、复用不足；块太大 → 装不进 L1/L2、再次缓存失效。存在一个由**缓存容量**决定的最优块大小（常见 32–128，取决于数据类型与缓存层级）。

**4.** logits 全在 $-1000$ 附近时，$\exp(-1000)=0$（下溢），朴素版分子分母全 0 → `nan` 或全 0；减 max 后指数在 $[-\text{range},0]$，正常。**结论：平移不变性同时防上溢和下溢。**

**5.** 束宽增大：得分单调不减（更大 $B$ 包含更小 $B$ 的解集），但**边际收益递减**，而时间近似线性增长（$B$ 倍）。到 $B=K^T$ 时等于穷举。

**6.** 用**求和堆**（每个节点存子树和）可在 $O(\log n)$ 内按前缀和采样一个优先级；或用"按优先级分层 + 每层哈希"。复杂度从 $O(1)$ 采样变成 $O(\log n)$，但换来"高误差样本被更频繁利用"，是 Prioritized Experience Replay 的核心。

**7.** 网格哈希在低维（2–3）非常有效（$O(1)$ 定位候选格）；维度升高时，邻居格的**数量随维度指数增长**（$3^d$），且绝大多数是空格——**这正是维度灾难的另一种表现**，与 KD 树失效同源，都导向 LSH/ANN。

**8.** 朴素版显存 $O(n^2)$（$2048^2\times8$ 字节 ≈ 32 MB，模型一放大就爆）；分块版显存 $O(n\cdot B)$，时间上因为**每块内做 softmax 需要跨块归一化**，要用两遍（先求每块的 max 和 sum，再重算）或**在线 softmax**（running max/sum），这就是 FlashAttention 的 online-softmax 技巧。时间上分块版通常更快（少了显存读写），这就是性能来自"访存"而非"计算"的直接证据。

**9.** 带随机性时，**穷举要枚举 $K^T$ 条完整轨迹并对每条求期望**（可用 DP/前向算法把期望算成 $O(TK^2)$，见第 14 章 Viterbi/HMM）；贪心最省但回报波动大；束搜索居中。预期结论：**束搜索在"接近最优"和"成本可控"之间取得最佳性价比**，且当转移越确定、$K$ 越小，贪心越接近最优。

</details>

---

## 延伸阅读

- Sartaj Sahni, *Data Structures, Algorithms, and Applications in C++*（第 2 版）—— 全书综合，本章对应其第 16–20 章（分治 / 贪心 / DP / 回溯 / 分支限界）及第 20 章（NP 难）的收束；
- Thomas H. Cormen et al., *Introduction to Algorithms*（CLRS）—— 第 15 章 DP、第 34 章 NP 完全、第 35 章近似算法，本课程理论加深版；
- Sutton & Barto, *Reinforcement Learning: An Introduction* —— 第 11 章 Bellman-Ford 与 DP 在 RL 中的完整展开；
- Ha & Schmidhuber, *World Models*（2018）—— 本课程目标方向的入门论文；
- Dao et al., *FlashAttention* —— 第 04 章分块与第 05 章 logsumexp 的现代工程集大成；
- Vazirani, *Approximation Algorithms* —— 第 15 章的进阶理论。

---

**结语**：十五个章节的零件 + 本章的装配图，构成了你进入世界模型研究的**底层能力底座**。真正难的不是记住某个结构——是**在看到问题时，脑子里能自动亮起"这该用树/堆/图/哈希/DP"的那盏灯**。那盏灯不靠背，靠**反复手写 + 反复测量**点亮。现在，关掉这本书，去写一个你自己的东西：一个 beam search、一个 KD 树、一个小世界模型。**能跑起来的那一刻，这门课才算真正学完。**

↩︎ 回到 [README 总索引](README.md) ｜ 上一章 [第 15 章 NP 难与近似算法](15-NP难与近似算法.md)
