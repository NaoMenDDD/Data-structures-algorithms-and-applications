# 第 05 章 栈

> 前面三章讲的是「怎么把一堆元素存起来」。从这一章开始，我们关心的是**「操作的顺序」**：元素按什么规则进出。
>
> **栈（stack）** 的规则只有一条：**后进先出（LIFO, Last-In-First-Out）**。就这么简单，简单到你会怀疑它有什么好讲的。
>
> 但事实是：**栈是整本书里最有"魔力"的数据结构**。它自己没什么花样，却能一举解决一大类问题——函数调用、括号匹配、表达式求值、深度优先搜索、回溯、以及——**深度学习的自动微分**。当你看到「递归」「回溯」「计算图」这些词时，背后站着的几乎都是栈。
>
> 这一章的一个隐藏彩蛋：**反向传播就是一个栈的遍历**。学完你就懂为什么。

---

## 本章目标

读完并跑完本章代码，你应该能够：

1. 说清栈的 ADT，并用**数组**和**链表**两种方式实现，比较它们的取舍；
2. 解释**函数调用栈（call stack）**的机制，以及为什么递归太深会「栈溢出」；
3. 用栈解决三个经典问题：**括号匹配、表达式求值、单调栈**；
4. 说明「**任何递归都能改写成显式栈 + 循环**」，并知道何时该这么做；
5. 理解**单调栈**这一「用栈维护有序性」的高阶技巧，并会求「下一个更大元素」「柱状图最大矩形」；
6. 建立关键直觉：**自动微分（autograd）的底层就是栈**，反向传播就是计算图的反向拓扑遍历。

---

## 前置知识

- 第 02 章（数组实现）与第 03 章（链表实现）—— 栈的两种底层正好是它们；
- 第 00 章的函数与调用机制（有助于理解调用栈）；
- 一点点表达式语法知识（运算符优先级、结合性）。

---

## 5.1 LIFO：栈的抽象与两种实现

### ADT

栈只在一端操作，那一端叫**栈顶（top）**。核心操作只有三四个：

| 操作 | 语义 | 复杂度 |
|---|---|---|
| `push(x)` | 把 `x` 压入栈顶 | 数组：摊还 $O(1)$；链表：$O(1)$ |
| `pop()` | 弹出并返回栈顶元素 | $O(1)$ |
| `top()` / `peek()` | 只看栈顶、不弹出 | $O(1)$ |
| `empty()` / `size()` | 是否为空 / 元素个数 | $O(1)$ |

注意：栈**不提供**随机访问，也不提供「取第 $i$ 个」—— 这是**故意的限制**。限制越少，越不容易出错；栈的价值恰恰在于它「只让你从一端操作」，从而天然匹配一大类「后进先出」的过程。

### 两种实现

栈的两种实现在前两章都写过了，这里直接复用：

- **`ArrayStack<T>`**：用动态数组。栈顶就是数组末尾，`push`/`pop` 就是 `push_back`/`pop_back`。`push` 摊还 $O(1)$（扩容摊还），**内存连续、缓存友好**。
- **`LinkedStack<T>`**：用单链表，栈顶就是链头。`push`/`pop` 就是头插/头删，**每次严格 $O(1)$**（无扩容时的突发代价），但每个元素多一个指针、内存不连续。

| 维度 | `ArrayStack` | `LinkedStack` |
|---|---|---|
| `push`/`pop` | 摊还 $O(1)$ | 严格 $O(1)$ |
| 最坏单次 | 扩容时 $O(n)$ | $O(1)$ |
| 内存 | 连续、可能空闲 | 每元素多一指针、碎片 |
| 缓存 | **友好** | 不友好 |
| 适用 | 绝大多数场景 | 实时性要求极高、容量难估 |

> 对照第 03 章的实测（数组比链表快上百倍）：**除非你有明确的实时性理由，栈一律优先用数组实现。** `std::stack` 默认底层就是 `std::deque`（分段连续数组），也是这个道理。

跑 `code/05_stack.cpp`：

```
===== ArrayStack (动态数组) =====
push 1..10 后: size=10, capacity=16 (发生了倍增扩容)
[OK] LIFO 顺序正确, 逆序弹出 10..1
[OK] 空栈操作抛 std::out_of_range
[OK] 模板对 std::string 同样工作
===== LinkedStack (单链表) =====
[OK] 头插/头删实现 LIFO, pop 得到 25,16,9
[OK] 100 次弹栈, 两种实现输出序列完全一致
```

---

## 5.2 栈与函数调用：调用栈

**这是栈最重要的应用，没有之一。** 每当程序调用一个函数，运行时会：

1. 在**调用栈（call stack）**上压入一个**栈帧（stack frame）**，里面存：参数、局部变量、返回地址；
2. 执行函数体；
3. 函数返回时弹出该栈帧，控制权回到「返回地址」处继续执行。

```
   调用 f(3) 时调用栈的样子（从下往上长）:

   ┌──────────────┐
   │ main 的帧     │  ← 最先压入，最后弹出
   ├──────────────┤
   │  f(3) 的帧    │
   ├──────────────┤
   │  f(2) 的帧    │  ← 递归：f 又调用 f
   ├──────────────┤
   │  f(1) 的帧    │  ← 栈顶；遇到 base case 后开始逐层弹出
   └──────────────┘
```

**「后进先出」在这里体现得淋漓尽致**：最后被调用的函数最先返回。递归不过是一个函数反复调用自己，把许多同名的栈帧叠起来。

### 为什么递归太深会「栈溢出（stack overflow）」？

调用栈的空间是**有限**的（Linux 默认几 MB）。每层递归占一个帧，递归深度 = 帧数。若递归深度是 $10^5$ 或 $10^6$，栈帧就会撑爆有限的栈空间 → 程序崩溃（`Segmentation fault`）。

**解法**：把递归改写成「显式栈 + 循环」。显式栈是用 `new`/`std::vector` 在**堆**上分配的，堆比栈大得多，且深度可控。这就是 5.6 节的内容，也是编译器、解释器（如 Python 的递归限制）、DFS 实现的通用做法。

---

## 5.3 经典应用一：括号匹配

**问题**：给定含 `()[]{}` 的字符串，判断括号是否**正确配对、正确嵌套**。

**栈的思路**：从左到右扫描：

- 遇到**左括号** → 压栈；
- 遇到**右括号** → 检查栈顶是否是**匹配的左括号**：是则弹出，否则（栈空或类型不符）失败；
- 扫描结束 → 栈必须**恰好为空**。

**为什么栈对？** 因为括号的嵌套关系是**后进先出**的：最近出现的左括号，必须最先被闭合。`([{}])` 里，`{` 最先闭合、`(` 最后闭合 —— 正是 LIFO。

跑 `code/05_stack.cpp`：

```
  "([{}])" -> balanced  (期望 balanced)
  "([)]" -> NOT balanced  (期望 NOT balanced)     ← 交错嵌套也抓得住
  "()" -> balanced
  "(" -> NOT balanced
  ")" -> NOT balanced
[OK] 11 组用例全部正确 (含空串、交错嵌套、单独右括号)
[OK] 错误定位: "([)]" 首个错误在下标 2
```

> 这个算法不只是「考试题」：**编译器的语法分析、编辑器的括号高亮、JSON/XML 解析器的初步校验，都用它。** 实际上，整个「嵌套结构」的解析（包括代码块、HTML 标签、甚至 Python 的缩进块）都离不开栈。

---

## 5.4 经典应用二：表达式求值

计算机不喜欢中缀表达式（`1 + 2 * 3`），因为它要处理**优先级**和**括号**这两件麻烦事。**后缀表达式（逆波兰记法）** `1 2 3 * +` 则不需要括号、天然适合栈求值。于是求值分两步：

**① 中缀 → 后缀**（用栈暂存运算符，经典的 Shunting-yard 算法）：

- 遇到**数字** → 直接输出；
- 遇到**运算符** → 弹出栈顶所有「优先级 ≥ 当前」的运算符输出（左结合时），再把当前运算符压栈；
- 遇到**左括号** → 压栈；遇到**右括号** → 一直弹出到左括号为止；
- 扫描结束 → 弹出栈中剩余运算符。

**② 后缀求值**（栈的主场）：

- 遇到**数字** → 压栈；
- 遇到**运算符** → 弹出**两个**操作数（注意顺序：先弹出的是**右**操作数！），运算，把结果压回；

跑 `code/05_expression_eval.cpp`：

```
===== 中缀 -> 后缀 =====
  1+2*3  ->  1 2 3 * +
  (1+2)*3  ->  1 2 + 3 *
  2^3^2  ->  2 3 2 ^ ^          ← 幂是右结合
  8/4/2  ->  8 4 / 2 /          ← 减/除是左结合
===== 后缀求值 / 整式中缀求值 =====
  1+2*3 = 7        (1+2)*3 = 9      2^3^2 = 512
  10/4 = 2.5       2*(3+4)*(5-1) = 56
[OK] 7 个整式求值全部正确
[OK] 除零与括号不匹配均正确抛错
```

三个**易错点**：

1. **弹出顺序**：`a - b` 对应的后缀是 `a b -`，弹出时栈顶是 `b`（右操作数）、次顶是 `a`（左操作数）。写反了结果会变成 `b - a`。**减法和除法不满足交换律，这个坑特别致命。**
2. **结合性**：`-`、`/` 是左结合（`8/4/2 = (8/4)/2`），而 `^`（幂）是右结合（`2^3^2 = 2^(3^2)`）。转后缀时对左结合要弹出「优先级 ≥」的，对右结合要弹出「优先级 >」的。
3. **多位数与小数**：词法分析要把连续的字符合并成一个数字 token（`12` 不能被当成 `1` 和 `2`）。

> **AI 关联**：表达式求值是「语法树求值的栈版本」。而深度学习框架在**追踪/图模式下**构建计算图，本质也是在解析一串运算、用栈或递归组织成表达式树。你写 `z = (x + y) * w` 时，框架内部就在做类似的事。

---

## 5.5 经典应用三：单调栈

**单调栈（monotonic stack）** 是栈的一个高级变体：栈内元素**始终保持单调**（递增或递减）。它能把一大类「找左右第一个更大/更小元素」的问题从 $O(n^2)$ 降到 $O(n)$。

**例题：下一个更大元素（Next Greater Element）** —— 对每个元素，找它右边第一个比它大的元素。

**暴力**：对每个 $i$，往右扫描找第一个更大的 → $O(n^2)$。

**单调栈**：维护一个**从栈底到栈顶递减**的栈，存**下标**：

- 遍历每个元素 $a[i]$：当栈非空且 $a[\text{栈顶}] < a[i]$ 时，说明 $a[i]$ 就是栈顶元素的「下一个更大元素」——弹出栈顶、记录答案；
- 然后把 $i$ 压栈。

每个元素最多进栈、出栈各一次，总复杂度 $O(n)$。

**为什么单调？** 栈里存的是「还没找到更大元素、且它们的值递减」的那些位置。当来了一个更大的 $a[i]$，它一次就能「结算」掉栈里所有比它小的元素——因为对它们来说，$a[i]$ 正是右边第一个更大的。这就是单调栈的精髓：**用一个有序的栈，在元素"过期"时一次性批量结算。**

跑 `code/05_monotonic_stack.cpp`：

```
===== 下一个更大元素 =====
===== 柱状图最大矩形 =====
===== 随机对拍 (固定种子 mt19937) =====
```

（程序用 $O(n^2)$ 暴力版做**随机对拍**，确保单调栈答案正确——这是很值得学的工程习惯：**写优化算法时，永远留一个暴力版对拍**。）

**例题：柱状图中最大矩形**（LeetCode 84 题）：对每根柱子，向左右扩展，能形成的最大矩形高度受限于「左右第一个比它矮的柱子」。这正是「左右第一个更小元素」问题，用单调栈 $O(n)$ 解决。它是**计算机视觉里「最大内接矩形」**的经典子问题。

---

## 5.6 递归与显式栈的互相转化

**定理**：任何递归算法都能用「显式栈 + 循环」改写（这是可计算性的基本结论）。

**为什么你可能想改**：

1. **避免栈溢出**：显式栈在堆上，深度可控；
2. **控制内存布局**：显式栈可以按需扩容、可以复用；
3. **可中断/可恢复**：显式栈的状态可以保存、序列化（协程、生成器、迭代器都靠这个）；
4. **避免函数调用开销**：某些场景下循环比递归快。

`code/05_call_stack_autodiff.cpp` 把阶乘从递归改写成显式栈，直观看到「进程的栈」和「你手里的栈」是一回事：

```
  20! = 2432902008176640000（递归） == 2432902008176640000（显式栈）
  6! 的显式栈展开: 1 * 2 * 3 * 4 * 5 * 6 = 720
  -> 递归深度 = n = 6（每层一个帧），显式栈把「进程栈」换成了「堆上的栈」
```

> **一个反直觉的建议**：递归的可读性往往更好，**不要为了"性能"盲目把递归改循环**。只有当栈深度可能很大（如处理深链表、深树）、或者需要可中断/可恢复时，才值得改成显式栈。

---

## 5.7 复杂度对照

| 操作/算法 | 复杂度 | 说明 |
|---|---|---|
| `push`/`pop`（数组栈） | 摊还 $O(1)$ | 扩容摊还 |
| `push`/`pop`（链表栈） | $O(1)$ | 严格，无突发 |
| 括号匹配 | $O(n)$ 时间、$O(n)$ 空间 | 最坏全左括号 |
| 中缀→后缀 / 后缀求值 | $O(n)$ 时间、$O(n)$ 空间 | $n$ = token 数 |
| 下一个更大元素（单调栈） | $O(n)$ 时间、$O(n)$ 空间 | 每元素进出栈一次 |
| 柱状图最大矩形（单调栈） | $O(n)$ 时间、$O(n)$ 空间 | 同上 |
| 递归调用的空间 | $O(\text{深度})$ 栈帧 | 深递归 → 栈溢出风险 |

---

## 常见错误与陷阱

1. **`pop()` 时不检查空栈。** 空栈弹出是未定义行为（或抛异常，取决于实现）。**每次 `pop`/`top` 前先确认非空**，或让实现抛 `std::out_of_range`。

2. **后缀求值弹出操作数顺序写反。** 见 5.4 的三个易错点之一 —— 减/除法不满足交换律，是重灾区。

3. **括号匹配漏掉「扫描结束栈非空」的检查。** `"(()"` 扫描完栈里还剩一个 `(`，必须判为不匹配。只检查「取右括号时能否匹配」是不够的。

4. **单调栈里存值 instead of 存下标。** 很多单调栈问题需要「两个元素之间的距离/宽度」（如柱状图最大矩形），这时**必须存下标**，光存值算不出宽度。

5. **混淆 `std::stack` 的 `top()` 语义。** `std::stack::top()` 返回栈顶的**引用**，不弹出；`pop()` **只弹出、不返回**。所以常见写法是 `auto x = s.top(); s.pop();`（两步），而不是 `s.pop()` 拿返回值。

6. **递归深度估算错误导致栈溢出。** 对 $n=10^5$ 的链式结构做递归遍历会崩。果断改显式栈，或用尾递归（但 C++ 不保证尾调用优化）。

7. **用栈去解决不规则嵌套的问题。** 栈擅长「规则嵌套」（每个开都有对应闭），但对「乱序」的关系无能为力。用错数据结构会很别扭。

8. **忘记栈是「受限」容器，反而当成通用容器用。** 如果你发现自己要访问栈的中间元素，那说明你其实需要的不是栈（也许是双端队列或数组）。

---

## 与 AI / 世界模型的连接

栈是「过程」的数据结构。而深度学习充满了「过程」——前向、反向、搜索、解析。所以栈无处不在。

### 1. ★ 自动微分（autograd）就是栈

这是本章与 AI 最直接、最深刻的连接。PyTorch 求梯度的机制：

- **前向**：每做一次张量运算，就往一条**磁带（tape）**里**压入**一个记录（这个运算、它的输入、它对该输入的局部导数）——磁带本质就是一个**栈**（或可追加的数组）；
- **反向**：从损失出发（梯度置 1），沿磁带**逆序**回传梯度。

`code/05_call_stack_autodiff.cpp` 用 ~40 行实现了这套机制，并对同一个函数 $f = (xy+x)+\sin x$ 验证了梯度：

```
  ∂f/∂x    = 3.58385  （解析 y+1+cos(x) = 3.58385）
  ∂f/∂y    = 2        （解析 x = 2）
  磁带长度 = 6 个结点（前向每步压一个）
  -> 这就是 PyTorch autograd 的骨架：前向建图（压栈），反向回传（逆序出栈）
```

它还演示了**为什么反向必须按拓扑序**：计算图是 **DAG**（有向无环图），同一个中间量（如这里的 $x$）会被多个后继共享，**它的梯度必须等所有后继都回传完才能累加完整**。用显式栈做一次 **DFS 求后序、再逆序**，就得到合法的反向拓扑序。这不是巧合——**「对计算图做反向拓扑遍历」正是所有自动微分框架的核心。**

### 2. DFS、回溯与搜索

栈 = DFS 的天然载体（第 10 章）。从迷宫求解、数独、到 **MCTS（蒙特卡洛树搜索）** 的展开与回溯，都是栈上的「深入 → 碰壁 → 回退」。世界模型里「想象（imagination）rollout」的搜索部分也依赖这种遍历。

### 3. 梯度检查点（gradient checkpointing）

训练大模型显存不够时，会**不保存中间激活**，需要时**重新计算**（用时间换显存）。这需要记录「计算到哪了、怎么重算」——又是一套栈式的过程管理。

### 4. 图编译器与遍历

把模型编译成高效执行计划（如 TorchScript、XLA、TVM）时，要对计算图做**拓扑排序、算子融合、内存规划**——全是图的遍历，而遍历的载体是栈。

### 5. 推理引擎的「请求栈」

一些系统用栈来管理嵌套的子任务（如 Agent 调用子 Agent、工具调用里再触发工具调用）。栈的「后进先出」正好匹配「子任务先完成」的语义。

**一句话**：栈教你的不只是「怎么倒着取元素」，而是**「如何组织一个有先后依赖的过程」**——而深度学习从计算图到自动微分到搜索，全是这样的过程。

---

## 本章小结

- 栈的规则只有一条 **LIFO**，但它的价值在于「限制」——只允许在一端操作，天然匹配一大类过程；
- 数组实现 `push` 摊还 $O(1)$、缓存友好；链表实现严格 $O(1)$、无突发。**默认选数组**；
- **函数调用栈**是栈最重要的应用；递归深度 = 栈帧数，太深会**栈溢出**；
- 三个经典应用：**括号匹配**（嵌套解析）、**表达式求值**（中缀→后缀→求值，注意弹出顺序与结合性）、**单调栈**（找左右第一个更大/更小，$O(n^2)\to O(n)$）；
- **任何递归都能改成「显式栈 + 循环」**，但可读性优先，别盲目改；
- ★ **自动微分的底层是栈**：前向压栈建计算图，反向按拓扑序逆序回传 —— 这是本章最值得记住的 AI 连接。

---

## 练习

### 基础

1. 用 `std::stack<int>` 实现「十进制转二进制」，再改成「转任意 $2\sim36$ 进制」。
2. 给 `code/05_stack.cpp` 的 `ArrayStack` 增加一个 `min()` 操作，返回栈中最小值，要求 $O(1)$。（提示：额外维护一个「最小值栈」。）
3. 实现「用两个栈模拟一个队列」（FIFO）。分析每个操作的摊还复杂度。
4. 判断一个字符串是否是「回文」，分别用**栈**和**双指针**实现，比较两者空间复杂度。

### 进阶

5. 把 `code/05_expression_eval.cpp` 扩展为支持**一元负号**（如 `-3+2`、`2*-3`）和**函数**（如 `sin(...)`、`max(...)`），并补充测试。
6. 用单调栈实现「每日温度」问题：给定温度数组，对每天求「还要等几天才更暖」；再实现「下一个更小元素」和「左右第一个更小元素」的通用版本。
7. 把「柱状图最大矩形」扩展到「**二维矩阵中最大的全 1 矩形**」（LeetCode 85 题）：对每一行用「柱状图」方法，说明为什么总复杂度是 $O(mn)$。
8. 实现**用显式栈的树的中序遍历**（不用递归），并解释为什么中序比前序/后序更需要技巧（提示：需要一个「访问状态」标记）。

### 挑战

9. **手写一个迷你反向模式自动微分框架**（比 `code/05_call_stack_autodiff.cpp` 更完整）：支持 `+ - * / sin cos exp log pow`、支持**张量**（而非标量）、支持**一维向量**的参数。用它求一个两层 MLP 的梯度，并与数值梯度逐元素比对。
10. **实现「迷你计算图 + 拓扑排序 + 常量折叠」**：把一串运算构造成 DAG，做拓扑排序，合并重复子表达式（CSE），并折叠常量。这是图编译器的核心步骤。- 想一想世界模型的 rollout 为什么要做这些优化。
11. **用显式栈实现一个支持「暂停/恢复」的遍历器**：即一个对象，每次调用 `next()` 返回遍历的下一个元素，内部状态可保存、可序列化。这对应深度学习里「可中断的数据加载/搜索」。

<details>
<summary>参考答案（要点）</summary>

**1.** 对 $n$ 反复 `% base` 得到低位、`/ base` 缩小，低位先得 → 压栈；最后弹出即高位在前。转 36 进制时用 `0-9a-z`。

**2.** 维护两个栈：主栈 `data` 和辅助栈 `mins`。`push(x)` 时 `mins.push(min(x, mins.empty()? x : mins.top()))`；`pop()` 时两个都弹；`min()` 返回 `mins.top()`。均为 $O(1)$。

**3.** 两个栈 `in`、`out`：入队压 `in`；出队时若 `out` 空，把 `in` 全部倒入 `out` 再弹出。每个元素最多被移动两次，**摊还 $O(1)$**。

**4.** 栈版：全部压栈、再逐个弹出与原串比较，$O(n)$ 空间。双指针：从两端往中间比，$O(1)$ 空间。**双指针更优**。

**5.** 一元负号：在词法层识别「处于表达式开头或运算符/左括号之后」的 `-` 为一元负号，转成一个特殊运算符或直接并入数字。函数：遇到标识符读入函数名，遇到左括号压栈、遇到对应右括号弹出并应用函数。

**6.** 「每日温度」：单调递减栈存下标；来一个更高温度时，弹出并结算「距离」。通用版：改比较方向即可。

**7.** 把矩阵的每一行视为「以该行为底的柱状图高度」（上方连续的 1 累加），对每行跑一次柱状图最大矩形（$O(n)$），共 $O(m)$ 行 → $O(mn)$。

**8.** 中序需要「先深入左子树、再访问根、再右子树」，用一个显式栈 + 状态标记（或经典的两段式：先一路压左孩子，再弹出一个访问其右子树）。

**9.** 关键点：结点存 `value`（张量）、`grad`、以及每个输入的**局部雅可比**或反传函数；`backward` 按拓扑逆序调用每个结点的反传函数，把上游梯度乘局部导数累加到下游。用中心差分验证。

**10.** 拓扑排序用 Kahn 算法或 DFS 后序；CSE 用哈希表按「(op, 输入结点 id)」去重；常量折叠：若某结点所有输入都是常量，则提前算出值并替换为常量。这三步能让图更小、更快。

**11.** 迭代器的状态就是一个「显式栈」（存还没处理完的结点）。把它存进对象，`next()` 就演一步；要保存/恢复，只需序列化这个栈。

</details>

---

## 延伸阅读

- **Sahni《数据结构、算法与应用（C++ 版）》第 2 版 第 8 章**「Stacks」：栈的 ADT、数组/链表表示、括号匹配与表达式求值的标准推导；
- **《算法导论》第 10.1 节**（栈与队列）：栈的数组/链表实现；
- **CSAPP 第 3 章**「程序的机器级表示」：函数调用栈帧的汇编级细节（想真正懂调用栈必读）；
- **Autograd 源码**（`torch/csrc/autograd`）：`Node`、`Edge`、拓扑序、`Engine::execute` —— 本章第 2 部分思想的工业级实现；
- **Reverse-mode autodiff 经典教程**：Baydin et al., *Automatic Differentiation in Machine Learning: a Survey*；
- 配套代码：`code/05_stack.cpp`、`code/05_expression_eval.cpp`、`code/05_monotonic_stack.cpp`、`code/05_call_stack_autodiff.cpp`。
