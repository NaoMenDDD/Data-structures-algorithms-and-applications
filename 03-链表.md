# 第 03 章 链表

> 上一章我们用「数组」实现了线性表：元素挤在一段连续内存里，随机访问 $O(1)$，但中间插入/删除要搬移一片元素，$O(n)$。
>
> 这一章换一种存法：**不要求元素连续**。每个元素住在一块独立的小内存里，用一根「指针」把下一块的地址记下来。于是插入/删除只需改两根指针、$O(1)$ 完成 —— 代价是失去随机访问，想找第 $i$ 个元素只能从头一个个数过去。
>
> 学会链表，你才算真正理解了「数据结构」这四个字：**同样一个抽象（线性表），换一种内存布局，就换来一组完全不同的时间权衡。** 而链表还是你第一次接触「手动管理内存」——这一点，是 C++ 与 Python 最不一样、也最能区分「会写」和「懂」的地方。

---

## 本章目标

读完并跑完本章代码，你应该能够：

1. 说清「数组表示」与「链表表示」在**时间、空间、缓存行为**三个维度上的取舍；
2. 独立写出一个**类型安全、无内存泄漏**的模板化单向链表（含迭代器、三/五法则）；
3. 理解并熟练运用**哨兵结点**这一「消灭边界情况」的核心技巧；
4. 实现双向链表与循环链表，并知道 LRU 缓存为什么**必须**用双向链表；
5. 手写链表经典算法：就地反转、快慢指针找中点、Floyd 判环、合并有序链、倒数第 $k$ 个、回文判定；
6. 用**实测数据**解释：为什么理论同为 $O(n)$ 的遍历，链表可能比数组慢上百倍；
7. 建立「代价意识」：何时该用链表，何时该用数组 —— 以及为什么现代工程里后者的出场率高得多。

---

## 前置知识

- 第 02 章的「ADT / 接口与实现分离」、数组表示、$O(1)$ 随机访问；
- 第 00 章的**指针、动态内存 `new`/`delete`、RAII、三/五法则**（本章大量使用，不熟一定回头补）；
- 第 01 章的渐进记号与摊还分析（本章末尾会用到）。

---

## 3.1 从数组到链表：三个痛点

回顾数组表示的线性表 `arrayList<T>`：

| 操作 | 数组表示 | 原因 |
|---|---|---|
| 随机访问 `get(i)` | $O(1)$ | 地址 = 基址 + $i \times \text{sizeof}(T)$，一步算出 |
| 尾部插入 `push_back` | 摊还 $O(1)$ | 倍增扩容摊还分析（第 02 章） |
| **中间/头部插入、删除** | $O(n)$ | 必须把后方元素整体搬移一格 |
| 空间 | 预分配容量，可能浪费 | 扩容留有闲置槽位 |

前两行是数组的长处，第三行是它的**硬伤**：如果你有一个 $10^6$ 元素的表，却频繁在头部插入，每次都搬移 $10^6$ 个元素，代价无法接受。

链表的思路：**既然搬移元素的本质原因是「元素必须紧挨着」，那就干脆不要求它们紧挨着。** 让每个元素住在一块独立内存里，额外记一个「下一个元素在哪」的地址。这样：

- 插入：新元素随便住哪；只要把「前一个的 next」改成指向它、「它的 next」改成指向原来的后继 —— 两步，$O(1)$。
- 删除：把「前一个的 next」直接跨过它指向「它的后继」—— 一步，$O(1)$。
- 随机访问：糟了，内存里没有「第 $i$ 个」的概念了，只能从头顺着 next 一个个数 —— $O(n)$。

一句话总结这个权衡：

$$
\boxed{\text{数组：访问快、修改慢} \qquad \text{链表：修改快、访问慢}}
$$

天下没有免费的午餐，只有**把代价挪到你更不在乎的地方**。

---

## 3.2 结点与指针：链表的原子

链表的最小单位叫**结点（node）**。一个存 `int` 的单向结点长这样：

```
        ┌───────────────┐
        │  data: int    │      数据域（payload）
        ├───────────────┤
        │  next: node*  │      指针域：指向下一个结点
        └───────────────┘
                │
                ▼
        ┌───────────────┐
        │  data: int    │
        ├───────────────┤
        │  next: node*  │──────────►  ... ──► nullptr（链尾）
        └───────────────┘
```

一条链就是「从头指针 `head` 出发，顺着 `next` 一路走到底，最后一个是 `nullptr`」。C++ 里用结构体表示：

```cpp
struct node {
    int   data;
    node* next;          // 自引用指针：node 指向同类型
};
```

在 C++17 里我们会把 `data` 的类型泛化成模板参数 `T`，并把 `next` 用 `nullptr` 表示终点（C++11 起用 `nullptr` 而不是 `NULL`/`0`）。

> **为什么 `next` 的类型写成 `node*` 自己？** 这叫**自引用（self-referential）结构**。因为 `node*` 只是一个指针（大小固定），编译器不需要知道 `node` 的完整大小就能声明它，所以结构体可以「包含指向自己的指针」。但不能包含自己的**对象**（`node next;`）—— 那会无限递归、大小无穷。

---

## 3.3 单向链表

### 3.3.1 接口（ADT）

和数组表 `arrayList<T>` 保持一致的接口，才能体现「换实现对上层透明」：

| 操作 | 语义 | 链表代价 |
|---|---|---|
| `size()` / `empty()` | 元素个数 / 是否为空 | $O(1)$（单独维护 `size_`） |
| `front()` | 首元素 | $O(1)$ |
| `back()` | 尾元素 | $O(n)$（要走到尾） |
| `get(i)` | 第 $i$ 个（0-based） | $O(i)$ |
| `indexOf(x)` | 第一个等于 $x$ 的位置 | $O(n)$ |
| `push_front(x)` | 头插 | $O(1)$ |
| `push_back(x)` | 尾插 | $O(n)$（无尾指针时） |
| `insert(i, x)` / `erase(i)` | 在第 $i$ 位插入/删除 | 找到前驱 $O(i)$，改指针 $O(1)$ |
| `reverse()` | 就地反转 | $O(n)$ 时间、$O(1)$ 空间 |

注意 `push_front` 是链表**独有**的 $O(1)$ 操作：数组表头插要搬移全部元素，链表改一根指针就完事。反之 `push_back` 在链表上是 $O(n)$（除非额外维护**尾指针**）—— 这又是一次「代价的搬家」。

### 3.3.2 三个必须记牢的指针操作

链表代码 90% 的 bug 来自这三处**顺序错误**：

**① 头插：先接后改，不能先改。**

```cpp
// ✅ 正确：先让新结点指向旧头，再让 head 指向新结点
node* p = new node(v, head);   // 构造时 next 直接接上旧头
head = p;

// ❌ 错误：先改 head，旧头就再也找不到了（内存泄漏）
head = new node(v, nullptr);   // head 已改，旧头地址丢失！
```

**② 插入中间：先连新结点的后继，再改前驱的 next。**

```cpp
// 目标：在 prev 之后插入 value
node* p = new node(v, prev->next);   // 1. 新结点接上「原来 prev 的后继」
prev->next = p;                      // 2. 再让 prev 指向新结点

// ❌ 若反过来先写 prev->next = p，则 prev->next 已变，
//    下一行再读 prev->next 拿到的是 p 自己 —— 自环！
```

**③ 删除：先保存后继，再释放。**

```cpp
node* doomed = prev->next;
prev->next = doomed->next;   // 1. 跨过被删结点
delete doomed;               // 2. 再释放（顺序不能反，否则读的是已释放内存）
```

这三条合起来是一句口诀：**「先接线，后断电」**。凡是修改 `next` 指针，永远先把「还没接上的那一段」记住。

### 3.3.3 完整实现

完整可编译的 `chain<T>`（含**前向迭代器**（只能 `++`，不能 `--` 或随机跳）、深拷贝/移动、$O(n)$ 就地反转）见 **`code/03_chain.cpp`**。核心片段：

```cpp
// 尾插：用「指向指针的指针」免去对 head 的特殊判断 —— 一个非常 C++ 的技巧
void push_back(const T& v) {
    node** tail = &head_;
    while (*tail) tail = &(*tail)->next;   // 一路摸到「最后一个 next 的位置」
    *tail = new node(v, nullptr);
    ++size_;
}

// 就地反转：三指针滚动
void reverse() {
    node* prev = nullptr;
    node* cur  = head_;
    while (cur) {
        node* nxt = cur->next;   // 1. 记住后继
        cur->next = prev;        // 2. 掉头
        prev = cur;              // 3. 前进
        cur  = nxt;
    }
    head_ = prev;
}
```

`node** tail` 这个写法值得琢磨：`tail` 是「指向某个 `next` 指针的指针」。它一开始指向 `head_`，之后不断指向「当前结点内的 `next` 字段」。这样 `*tail = ...` 无论是改头指针还是改尾结点的 next，写法都统一，就不用写 `if (空表) ... else ...` 的分支了。这是 C++ 里消除边界判断的常用手法。

跑起来看输出：

```
尾插 10..50 : [10 -> 20 -> 30 -> 40 -> 50]
头插 0      : [0 -> 10 -> 20 -> 30 -> 40 -> 50]
get(3) = 30, indexOf(30) = 3
```

---

## 3.4 哨兵结点：把边界情况消灭在源头

写链表时最烦的是这些**特例**：

- 空表插入？
- 在**第一个**位置插入（要改 `head`）？
- 删除**第一个**元素（又要改 `head`）？
- 删除**最后一个**元素（前驱的 next 要置空）？

每个特例都要单开一个 `if`，代码又长又容易错。

**哨兵（sentinel / dummy node）** 的思路：在链表开头（或首尾）放一个**不放真实数据**的哑结点，让它永远存在。于是「空表」不再特殊 —— 因为总有一个哨兵在，`head` 永远不空。

```
   真实数据结构:       哨兵        node0      node1      node2
   ┌──────┐          ┌──────┐    ┌─────┐    ┌─────┐    ┌─────┐
   │ head ├─────────►│ dummy├───►│  A  ├───►│  B  ├───►│  C  ├──► nullptr
   └──────┘          └──────┘    └─────┘    └─────┘    └─────┘
                       ↑
                  永远非空，所有插入/删除都作用在它之后的链上，
                  连「删掉唯一元素」都只需改 dummy.next
```

用哨兵后，「在位置 1 插入」和「在位置 5 插入」变成同一段代码；「删第一个」和「删中间一个」也变成同一段代码。**边界判断消失了，bug 也随之消失。** 代价是多一个结点（几乎可忽略）。

> 这正是「哨兵」在本章 `03_linked_list_problems.cpp` 里解决「删除倒数第 n 个」时大放异彩的原因 —— 如果没有哨兵，删头要单独处理。

---

## 3.5 双向链表

单向链表有个致命缺陷：**给定一个结点，无法 $O(1)$ 删掉它**。因为删除需要它的**前驱**，而单向链表只能从头一路找前驱，$O(n)$。

**双向链表（doubly linked list）** 给每个结点多存一个 `prev` 指针：

```
       ┌───────────────────┐        ┌───────────────────┐
       │ prev │ data │ next│◄──────►│ prev │ data │ next│◄──────► ...
       └───────────────────┘        └───────────────────┘
```

- 删除**已知结点**：改 `p->prev->next` 和 `p->next->prev` 两根指针，$O(1)$；
- 可以**反向**遍历（双向迭代器，支持 `--`）；
- 代价：每个结点多 8 字节；插入/删除要改 **4** 根指针（而非 2 根），更容易写错。

**双向链表最经典的应用就是 LRU 缓存**（本章最后一题）。要「$O(1)$ 淘汰任意一个已知元素」，单向链表做不到，双向链表是标准答案。

### 循环哑结点：双向链表的极致优雅

把「双向」和「哨兵」结合起来，再让整条链**首尾相连成环**，就得到一种几乎**没有边界情况**的结构：

```
         ┌─────────────────────────────────────────────┐
         ▼                                             │
   ┌───────────┐     ┌───────┐     ┌───────┐     ┌───────┐
   │ sentinel  │◄───►│ node0 │◄───►│ node1 │◄───►│ node2 │
   └───────────┘     └───────┘     └───────┘     └───────┘
         ▲                                             │
         └─────────────────────────────────────────────┘
   sentinel.next = 首结点，sentinel.prev = 尾结点；
   空表时 sentinel.next == sentinel.prev == &sentinel（自指环）
```

于是插入/删除只需要两个**原子操作**：

1. **在 `before` 之前插入 `p`**：`p->prev = before->prev; p->next = before; before->prev->next = p; before->prev = p;`
2. **在环形链中摘掉 `p`**：`p->prev->next = p->next; p->next->prev = p->prev; delete p;`

头、尾、中间、空表，**全部**走这两条路径 —— 一个 `if` 都不需要。完整的 `DList<T>` 实现见 **`code/03_doubly_linked_list.cpp`**。Linux 内核的 `list_head`、C++ 的 `std::list`，骨子里都是这个结构。

它的反转也异常简洁（交换每个结点的 `prev`/`next` 即可）：

```cpp
void reverse() {
    for (Node* p = sentinel_->next; p != sentinel_; p = p->prev)
        std::swap(p->prev, p->next);
    std::swap(sentinel_->prev, sentinel_->next);
}
```

---

## 3.6 循环链表与约瑟夫环

**循环链表**就是让尾结点的 `next` 指回头结点（而不是 `nullptr`），首尾相接成环。它适合「轮询（round-robin）」类问题。

最经典的应用是**约瑟夫环（Josephus problem）**：

> $n$ 个人围成一圈，从第 1 个开始报数，每报到第 $k$ 个，那人出圈；然后从下一个人重新报 1。问最后剩下谁？

用循环链表模拟最直观：一路报数前进，报到 $k$ 就摘掉该结点，最后剩下的就是答案。实现见 **`code/03_circular_free_list.cpp`**：

```
  n=5, k=2 -> 幸存者 = 3   （出圈顺序 2, 4, 1, 5）
  n=7, k=3 -> 幸存者 = 4
  n=1, k=9 -> 幸存者 = 1
```

> **小技巧**：约瑟夫问题有 $O(n)$ 的**数学递推解**。设 $J(n)$ 是 $n$ 人、每 $k$ 个出圈时的幸存者**下标（0-based）**，则
> $$J(1) = 0, \qquad J(n) = (J(n-1) + k) \bmod n.$$
> 直观理解：$n$ 人出圈一人后，剩下 $n-1$ 人、从「出圈者的下一位」重新开始 —— 相当于把整个环**旋转**了一下。这条递推把 $O(nk)$ 的模拟降到 $O(n)$，是「用一点数学换掉一整个数据结构」的好例子（练习里会让你手推并验证）。

### 删除循环链表前必须「断环」

一个真实陷阱：循环链表不能用普通链表的删除循环：

```cpp
// ❌ 对循环链表会死循环：走到尾结点后又绕回头，永远不结束
while (head) { node* n = head->next; delete head; head = n; }

// ✅ 先找到尾结点、把 tail->next 置空（断环），再按普通链表删
```

---

## 3.7 自由表与对象池

链表的结点是 `new` 出来的。如果频繁 `new`/`delete`，会反复调用系统分配器 —— 那是有锁、有元数据开销的昂贵操作。

**自由表（free list）** 的思路：删除结点时，**不还给系统**，而是把它挂进一张「备用链表」；下次需要结点时，直接从这张表里**摘一个复用**。

```
   allocate():                         release(p):
   自由表非空? ──是──► 摘下表头复用      p->next = freeHead;
        │                (O(1))         freeHead = p;        (O(1))
        └──否──► 才真正 new 一个
```

这就把反复的「分配/释放」从系统调用降成了**链表操作**。实现见 `code/03_circular_free_list.cpp` 第 2 部分：

```
  申请100+释放100: 系统分配 = 100, 自由表 = 100
  再申请100:      系统分配 = 100（未再增长）, 复用次数 = 100
```

注意第二次申请 100 个时，系统分配次数**没有增加** —— 全部命中了复用。这就是**对象池 / 内存池**的最小原型，也是现代深度学习框架的命脉之一（见后文「与 AI 的连接」）。

---

## 3.8 数组 vs 链表：复杂度与实测

### 复杂度对照

| 操作 | 数组 `arrayList` | 单链表 `chain` | 双链表 `DList` |
|---|---|---|---|
| 随机访问 `get(i)` | **$O(1)$** | $O(i)$ | $O(i)$ |
| 头部插入/删除 | $O(n)$ | **$O(1)$** | **$O(1)$** |
| 尾部插入/删除 | 摊还 **$O(1)$** | $O(n)$ | **$O(1)$** |
| 已知结点处插入/删除 | — | $O(1)$（插入）/ $O(n)$（删除） | **$O(1)$** |
| 查找 `indexOf(x)` | $O(n)$ | $O(n)$ | $O(n)$ |
| 内存 | 连续、可能有闲置容量 | 每结点多 1 个指针、碎片化 | 每结点多 2 个指针 |
| 缓存友好度 | **高** | 低 | 低 |

### 为什么链表实测可能慢几十上百倍？

理论上，数组遍历和链表遍历都是 $O(n)$、加法次数完全相同。但实测呢？跑 `code/03_cache_list_vs_array.cpp`（本机实测，$n = 4{,}194{,}304$）：

```
实现                时间(ms)    有效带宽(GB/s)  说明
--------------------------------------------------------------------------
数组 vector<int>    0.908       18.48               顺序访问，cache 命中率高
链表 LNode*         311.472     0.05                指针追逐，几乎全 miss

链表 / 数组 耗时比 = 343.16x
```

**链表慢了三百多倍**（具体倍数与你机器的 CPU/内存有关，但数量级如此）。原因：

1. **数组有空间局部性**：读 `a[0]` 时硬件顺带把整条 cache line（64 字节 = 16 个 int）搬进 cache，之后 15 次访问几乎免费；预取器还能提前把后面的线拉进来。
2. **链表要「指针追逐（pointer chasing）」**：每个结点的 `next` 指向一块「随机」地址，每次都要等一次主存访问（约 100 ns），预取器完全使不上劲。

这解释了三条极重要的工程事实：

- 为什么 `std::vector` 在**绝大多数**场景下比 `std::list` 更快，哪怕中间插入 `list` 才是 $O(1)$ —— 因为**常数因子和缓存效应常常盖过渐进复杂度**；
- 为什么 PyTorch 张量坚持「连续内存」，哪怕 `transpose` 后要多做一次 `contiguous()` 拷贝；
- 为什么 GPU 极度偏爱**连续、可合并（coalesced）**的访存 —— 链表的随机访存会把带宽优势彻底葬送。

> **启示**：渐进复杂度告诉你「规模变大时谁赢」，但它**不告诉你常数因子**。当两个算法复杂度相同时，赢家由**内存访问模式**决定。这是从「会算法」到「会工程」的关键一跃。

---

## 3.9 经典链表算法

以下算法全部在 **`code/03_linked_list_problems.cpp`** 中实现并自测。它们是面试高频题，也是训练「指针思维」的最佳素材。

### ① 就地反转

- **迭代（三指针）**：$O(n)$ 时间、$O(1)$ 空间，见 3.3.2。
- **递归**：语义是「反转以 `head` 开头的链，返回新头」。

```cpp
Node* reverseRec(Node* head) {
    if (!head || !head->next) return head;
    Node* newHead = reverseRec(head->next);
    head->next->next = head;   // 让后继反过来指向自己
    head->next = nullptr;      // 自己成为新尾
    return newHead;
}
```
递归深度 = 链长，长链可能**栈溢出** —— 工程里优先用迭代。

### ② 快慢指针找中点

`fast` 每次走 2 步、`slow` 每次走 1 步，`fast` 到尾时 `slow` 恰在中点。$O(n)$ 时间、$O(1)$ 空间。

```cpp
while (fast && fast->next) { slow = slow->next; fast = fast->next->next; }
```

### ③ Floyd 判环（龟兔赛跑）

- **判有没有环**：`slow` 走 1、`fast` 走 2。若有环，二者必在环内相遇（`fast` 每步追近 `slow` 一格，不会跳过）。
- **找环入口**：相遇后把一个指针放回**头**，两指针**同速**前进，再次相遇处即入口。证明：设头到入口长 $a$、入口到相遇点长 $b$、环长 $L$。相遇时 `slow` 走了 $a+b$，`fast` 走了 $a+b+kL$，又速度是 2 倍关系，故 $2(a+b)=a+b+kL$，得 $a = (k-1)L + (L-b)$ —— 即「从头走到入口」等于「从相遇点继续走到入口」。

### ④ 倒数第 $k$ 个

`first` 先走 $k$ 步，然后 `first`、`second` 同速走；`first` 到空时 `second` 恰在倒数第 $k$ 个。一次遍历，$O(n)$。

### ⑤ 合并两条有序链表

标准「哨兵 + 尾指针」归并，$O(n+m)$：

```cpp
Node dummy(0);
Node* tail = &dummy;
while (a && b) {
    if (a->val <= b->val) { tail->next = a; a = a->next; }
    else                  { tail->next = b; b = b->next; }
    tail = tail->next;
}
tail->next = a ? a : b;    // 接上剩余
```

### ⑥ 删除倒数第 $n$ 个

用哨兵消除「删头」的特例，见 3.4。

### ⑦ 回文判定

找中点 → 反转后半 → 逐一比较 → （好习惯）再反转回来恢复。$O(n)$ 时间、$O(1)$ 空间。

### ⑧ 两条链表相交

先各算长度，让长的先走**长度差**，再同速走，第一个相同的结点就是交点。$O(n+m)$。

### ⑨ LRU 缓存（★ 工程核心）

**需求**：容量固定为 $c$ 的缓存，`get`/`put` 都要 $O(1)$；容量满时淘汰「最久未使用（Least Recently Used）」的那一项。

**做法**：哈希表 + 双向链表。

- **哈希表** `key → 结点指针`：$O(1)$ 判断 key 在不在、拿到对应结点；
- **双向链表**：维护访问顺序，`front` = 最近使用，`back` = 最久未用；
  - `get` 命中 → 把结点**移到最前**（$O(1)$，因为双向链表能 $O(1)$ 摘下任意已知结点）；
  - `put` 新键 → 插到最前；若超额 → **淘汰尾部**（$O(1)$）。

**为什么必须双向链表？** 因为「把已知结点移到最前」需要 $O(1)$ 摘下它，而摘下需要它的**前驱** —— 单向链表给不了 $O(1)$ 前驱。这是双向链表不可替代的场景。

实现（`std::list` 的 `splice` 是 $O(1)$ —— 只改指针、不搬元素）：

```cpp
int get(int key) {
    auto it = pos_.find(key);
    if (it == pos_.end()) return -1;
    items_.splice(items_.begin(), items_, it->second);   // O(1) 提到最前
    return it->second->second;
}
void put(int key, int value) {
    auto it = pos_.find(key);
    if (it != pos_.end()) { it->second->second = value;
                            items_.splice(items_.begin(), items_, it->second); return; }
    items_.emplace_front(key, value);
    pos_[key] = items_.begin();
    if (items_.size() > cap_) { pos_.erase(items_.back().first); items_.pop_back(); }
}
```

运行输出：

```
put(1,100).put(2,200): MRU [2:200, 1:100] LRU
get(1)=100 之后:        MRU [1:100, 2:200] LRU     ← 1 被提升为最近
put(3,300) 触发淘汰:    MRU [3:300, 1:100] LRU     ← 2 被淘汰
```

---

## 常见错误与陷阱

1. **忘记释放内存 / 只 `delete` 头结点。** 链表必须逐个 `delete`。`chain` 的析构函数里要遍历整条链释放。用 ASan 编译（`-fsanitize=address`）能立刻抓出泄漏。

2. **浅拷贝导致双重释放（double free）。** 如果你没写拷贝构造函数，编译器生成的默认版本只复制 `head_` 指针 —— 两条链共用同一批结点，析构时同一块内存被 `delete` 两次，程序崩溃。**链表类必须遵守三/五法则**（拷贝构造、拷贝赋值、析构，外加移动）。见第 00 章。

3. **指针操作顺序错误。** 三条口诀「先接线，后断电」见 3.3.2。写完后**画图**核对每个结点的 `next` 指向谁，是最有效的排错手段。

4. **删除后继续使用悬垂指针。** `delete p` 之后，`p` 仍指向那块（已被回收的）内存，再访问是未定义行为。删除后养成把指针置空（`p = nullptr`）的习惯。

5. **循环链表用普通删除循环 → 死循环。** 见 3.6，删前必须断环。

6. **`for (p = head; p->next; p = p->next)` 漏掉尾结点。** 条件写成 `p->next` 会在最后一个结点就停下，处理不到它。遍历一律用 `for (p = head; p; p = p->next)`，判据是 `p` 本身非空。

7. **在遍历中删除当前结点后继续 `p = p->next`。** `delete` 后 `p->next` 已经是悬垂读取。正确做法：先 `next = p->next; delete p; p = next;`，或者用「指向指针的指针」`node**` 技巧。

8. **自作聪明地优化常数因子却引入了 $O(n^2)$。** 例如在循环里反复 `size()` 若它内部是遍历实现的，就会退化成 $O(n^2)$。链表的 `size()` 一定要用**单独维护的计数器**做到 $O(1)$。

---

## 与 AI / 世界模型的连接

链表在 AI 系统里的戏份和数组不同：**它很少作为「存数据的主力」，却频繁作为「管理关系的骨架」。**

### 1. 计算图节点与自动微分

深度学习框架的**计算图**里，每个张量运算是一个节点，节点之间通过「输入/输出边」相连。图的邻接关系常用**指针**表达（PyTorch 的 autograd `Node` 就有 `next_edges`/`inputs` 之类的指针字段）。反向传播沿这张图遍历 —— 这正是「用图（指针结构）表达关系」的思路，第 10 章会深入。

### 2. LRU / KV cache 淘汰 —— 本章最直接的落点

大模型**推理**时，为了避免重复计算，会缓存每个已生成 token 的 Key/Value 向量，叫 **KV cache**。它的容量受显存限制；当上下文很长或并发请求很多时，必须**淘汰**一部分。生产级推理引擎（vLLM 的 `PagedAttention`、SGLang 的 `RadixAttention` 等）都在管理这种「缓存块」的分配与淘汰 —— 其思想内核就是本章的 **LRU + 哈希 + 双向链表**（以及自由表做块复用）。

### 3. 自由表 ↔ 显存分配器（Caching Allocator）

PyTorch 的 **Caching Allocator** 就是显存版的自由表：张量释放时，显存**不还给 CUDA 驱动**（`cudaFree` 很慢且会同步），而是挂进一张空闲块链表；下次申请同尺寸显存时直接复用，把 `cudaMalloc` 的调用次数从「每次操作」降到「偶尔」。这正是 3.7 节自由表思想的工业级实现。

### 4. 内存分配器本身

`malloc`/`free` 内部维护的「空闲链表（free list）」、分箱（segregated bins）、伙伴系统（buddy system），都是链表/自由表的变体。理解链表，是理解「内存从哪里来」的第一步。

### 5. 为什么 GPU 讨厌链表

GPU 的算力来自**成千上万个线程同时访存**。它的内存控制器擅长「一大批地址连续的请求合并成一次大事务」。链表结点的地址是**随机、分散**的，无法合并，带宽利用率暴跌（正是 3.8 节实测的「指针追逐」在 GPU 上被放大得更惨）。这就是为什么：

- 稀疏张量在 GPU 上要用 **CSR/COO 这种「数组化的图」**（第 04 章）表达，而不是指针链表；
- GNN 的消息传递要先「按边列表聚合」，而不是跟着指针跳。

**一句话**：链表教给你「用指针表达任意关系」的抽象能力（计算图、缓存、分配器都靠它），同时也让你亲眼看到「指针的代价」（缓存不友好）。在 AI 系统里，你常常两者都要：**用指针结构想清楚逻辑，用连续数组实现出来跑得快。**

---

## 本章小结

- 链表用**指针**把散落的内存块串起来，换来插入/删除 $O(1)$，代价是**失去随机访问**；
- 链表代码的 bug 几乎都出在**边界**（空表/头/尾）和**指针操作顺序**；**哨兵结点**能消灭大部分边界；
- **双向链表**把「$O(1)$ 删除任意已知结点」变成可能，是 LRU 缓存的必需品；**循环哑结点**是最优雅的实现；
- **循环链表**适合轮询类问题（约瑟夫环）；**自由表/对象池**把昂贵的系统分配降为 $O(1)$ 链表操作；
- 复杂度相同 ≠ 速度相同：**缓存局部性**能让数组比链表快几百倍。渐进复杂度告诉你「谁赢」，内存访问模式决定「赢多少」；
- 链表在 AI 里主要作为**关系骨架**（计算图、LRU/KV cache、自由表/分配器），而在**算数据**时往往要让位给连续数组。

---

## 练习

### 基础

1. 在 `code/03_chain.cpp` 的基础上，给 `chain<T>` 增加一个**尾指针 `tail_`**，使 `push_back` 变成 $O(1)$。想一想：加上尾指针后，`pop_back` 会不会变慢？为什么？（提示：删尾需要新尾的前驱。）
2. 手写「删除单向链表中值为 `x` 的**所有**结点」，要求只遍历一遍。
3. 给定两条**无环**单链表，判断它们是否相交，并返回交点。为什么「先把一条链的尾接到另一条链的头，再判环」也能求交点？
4. 用 `DList<T>`（`code/03_doubly_linked_list.cpp`）实现一个「栈」，说明为什么用链表做栈比数组更自然。

### 进阶

5. 实现 **Floyd 判环**并验证：对 $n=100$、环入口在中点的链表，返回的入口是否正确。再实现「哈希表判环」版本，比较两者空间复杂度。
6. 用**数学递推** $J(n)=(J(n-1)+k)\bmod n$ 重写约瑟夫问题，使复杂度从 $O(nk)$ 降到 $O(n)$，并与 `code/03_circular_free_list.cpp` 的模拟结果逐一比对（$n$ 从 1 到 200，$k$ 从 1 到 20）。
7. 实现**归并排序链表版**（自底向上，$O(1)$ 额外空间），与数组版归并排序比较。
8. 用**双指针法**判断链表是否为回文，要求 $O(1)$ 额外空间，并在结束时**恢复链表原状**。

### 挑战

9. **实现一个带「过期时间」的 LRU（TTL-LRU）**：除容量外，每个条目还有一个过期时刻，`get` 时若已过期则视为不存在并删除。想一想：如果过期时间不单调递增，还能保持 $O(1)$ 吗？
10. **实现「$O(1)$ 空间、$O(n)$ 时间」的链表随机抽一个结点**（蓄水池抽样 Reservoir Sampling）。这个算法在**流式数据/流式经验回放**里非常重要 —— 想一想为什么世界模型的 replay buffer 常用它。
11. **自己实现一个简化版「显存块分配器」**：固定 4 种块大小（1/2/4/8 MB），每种维护一条自由表，`alloc(size)` 找最小够用的桶、`free` 挂回对应桶；统计 10000 次随机 alloc/free 后的系统分配次数，与「每次直接 new」对比。

<details>
<summary>参考答案（要点）</summary>

**1.** 加 `tail_` 后 `push_back` 变 $O(1)$（直接 `tail_->next = new node(...)`，再更新 `tail_`）。但 `pop_back` 会退化成 $O(n)$：删掉尾结点后，新的尾结点是**倒数第二个**，而单向链表求它的前驱要 $O(n)$。**代价守恒**：$O(1)$ 尾插换来 $O(n)$ 尾删。

**2.** 用「指向指针的指针」一遍过：
```cpp
void removeAll(node*& head, int x) {
    node** link = &head;
    while (*link) {
        if ((*link)->data == x) { node* d = *link; *link = d->next; delete d; }
        else                     link = &(*link)->next;
    }
}
```

**3.** 若两条链相交，则「把 A 的尾接到 B 的头」会形成一个**环**（因为相交后共用的尾巴会绕回来）。于是「是否相交」等价于「接成一条后是否有环」。用 Floyd 判环即可。

**4.** 栈是 LIFO，只在**一端**增删。链表头插/头删都是 $O(1)$，且不用预留容量、不用扩容。数组栈虽也是摊还 $O(1)$，但需要预估/扩容，链表则天然按需分配。

**5.** Floyd 判环：$O(1)$ 空间。哈希判环：边走边把结点地址塞进 `unordered_set`，重复出现即有环，$O(n)$ 空间。前者空间优，后者能直接给出「重复的结点」。

**6.** 递推实现：
```cpp
int josephusFast(int n, int k) {
    int j = 0;                       // J(1) = 0，0-based
    for (int i = 2; i <= n; ++i) j = (j + k) % i;
    return j + 1;                    // 转成 1-based
}
```
与模拟结果应完全一致。

**7.** 自底向上归并：从 `width=1` 开始，每轮把相邻两段长 `width` 的有序链归并，`width` 翻倍。全程只改指针、不递归、不用栈，$O(1)$ 额外空间。

**8.** 找中点、反转后半、比较、再把后半反转回来（用 `reverseIter` 调两次）。

**9.** TTL-LRU：只要过期时间是**单调递增**的，最先过期的就是最久未用的（尾部），可以在 `get`/`put` 时从尾部批量清除过期项，均摊 $O(1)$。若过期时间不单调，则「最久未用」与「最先过期」不再一致，需要额外结构（如过期时间小顶堆），无法纯靠 LRU 顺序 $O(1)$ 完成。

**10.** 蓄水池抽样：遍历第 $i$ 个（从 1 数）时，以概率 $1/i$ 把它替换成当前答案。数学上每个元素被选中的概率都是 $1/n$，且只需 $O(1)$ 空间、一次遍历，**不需要事先知道 $n$**。流式 replay buffer 数据是无界的、逐个到来的，正好符合这个模型。

**11.** 关键点：`alloc` 时先在对应桶的自由表里找（$O(1)$ 摘头）；`free` 时挂回对应桶；只有桶空时才向系统要内存。统计 `systemAllocs` 会发现远小于 10000 次。

</details>

---

## 延伸阅读

- **Sahni《数据结构、算法与应用（C++ 版）》第 2 版 第 6 章**「Linear Lists — Linked Representation」：链表的 ADT、单/双/循环链表、以及「自由表」的经典论述；
- **Sahni 第 2 版 第 10 章**（Skip Lists and Hashing）开头关于链表的回顾；
- **《算法导论》第 10 章**（基本数据结构）关于链表、哨兵、自由表的讨论；
- **Andrei Alexandrescu, "Modern C++ Design" / CppCon 演讲**：关于「哨兵」与「指针稳定性」的工程实践；
- **PyTorch `CachingAllocator` 源码**（`c10/cuda/CUDACachingAllocator.cpp`）：工业级自由表的真实实现；
- **Linux `list.h`**（内核双向循环链表）：`list_head` 的教科书级实现，与本章的循环哑结点一脉相承；
- 配套代码：`code/03_chain.cpp`、`code/03_doubly_linked_list.cpp`、`code/03_circular_free_list.cpp`、`code/03_linked_list_problems.cpp`、`code/03_cache_list_vs_array.cpp`。
