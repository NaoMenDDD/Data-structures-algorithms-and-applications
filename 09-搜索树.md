# 第 09 章 搜索树

> 有序数组能 $O(\log n)$ 二分查找，但**插入/删除**要挪动半个数组，$O(n)$；哈希表能 $O(1)$ 查找，却**不支持「范围查询 / 按序取出」**。现实里我们经常同时想要两样：**既能快速查找，又能动态增删，还要能按序遍历**——字典要排序、数据库要范围扫描、推荐要取「某区间内」的候选。
>
> **搜索树（search tree）**就是为这个需求量身定做的：它把「有序」编码进树的形状，于是**中序遍历天然升序**，查找/插入/删除只走一条从根到叶的路径。最朴素的**二叉搜索树（BST）**只用了「左小右大」一条性质，就能在 $O(h)$ 内完成全部操作——代价是 $h$ 可能退化成 $O(n)$。
>
> 这一章讲透四件事：**BST 的原理与退化**、**AVL 树怎么用旋转把高度锁死在 $O(\log n)$**、**跳表怎么用抛硬币代替旋转**、**B 树怎么用「胖结点」把树高压到常数层**。它们共同回答一个问题：**如何让「有序 + 动态」既快又稳**——这正是世界模型里键值缓存、向量索引、训练数据管理的底层骨架。

---

## 本章目标

读完并跑完本章代码，你应该能够：

1. 说清**搜索树 ADT**（`insert` / `search` / `remove` / `min` / `max` / `successor` / `predecessor` / `rangeQuery`）为何是「有序动态集合」的最优抽象；
2. 手写 **BST 的插入、查找、删除**，讲清删除的**三种情形**（叶子 / 单孩子 / 双孩子用中序后继顶替）；
3. 用**上下界递归**正确校验一棵树是不是 BST，并说清「只比父子」为什么是错的；
4. ★ 亲手见证 **BST 的退化**：升序插入 $1..N$ 树高 $= N-1$，查找退化到 $O(n)$；
5. 掌握 **AVL 树**的不变式（平衡因子 $\in\{-1,0,+1\}$）与**四种旋转**（LL/RR/LR/RL），实测它把高度压到 $1.44\log_2 n$ 以内；
6. 理解**跳表**用「抛硬币 + 层」替代旋转，实现期望 $O(\log n)$ 的增删查，实测比纯链表快约 $3000\times$；
7. 理解 **B 树 / B+ 树**的「胖结点」思想，说明它为什么把树高压到 $\log_{几百} n$，以及数据库为何偏爱 B+ 树；
8. 说清 **BST / AVL / 红黑树 / 跳表 / B 树**的取舍，以及它们各自在 AI 系统里的落点。

---

## 前置知识

- 第 07 章（树与二叉树）—— 二叉树的遍历、高度、递归建树是本章的骨架；
- 第 08 章（堆）—— 「用局部操作维护全局性质」的思想，与旋转/分裂同源；
- 第 01 章（算法分析）—— 递归式求树高、期望复杂度（跳表）需要一点概率直觉；
- 一点点**概率**（跳表抛硬币）与**磁盘 I/O 的直觉**（B 树）。

---

## 9.1 搜索树 ADT：我们要的到底是什么？

一个**有序动态集合**（sorted dynamic set）需要支持：

| 操作 | 语义 | 目标复杂度 |
|---|---|---|
| `insert(k)` | 插入键 $k$ | $O(\log n)$ |
| `remove(k)` | 删除键 $k$ | $O(\log n)$ |
| `search(k)` / `contains(k)` | 查找键 $k$ | $O(\log n)$ |
| `min()` / `max()` | 最小 / 最大键 | $O(\log n)$ |
| `successor(k)` / `predecessor(k)` | 大于 / 小于 $k$ 的最接近键 | $O(\log n)$ |
| `rangeQuery(lo, hi)` | 列出 $[lo,hi]$ 内所有键 | $O(\log n + s)$，$s$ = 输出个数 |
| **中序遍历** | 按升序枚举全部键 | $O(n)$ |

**三种朴素结构都有短板**：

| 结构 | `search` | `insert` | `remove` | `rangeQuery` |
|---|---|---|---|---|
| 有序数组 | $O(\log n)$ 二分 | $O(n)$ 移位 | $O(n)$ 移位 | $O(\log n + s)$ |
| 有序链表 | $O(n)$ | $O(n)$ 找位 | $O(n)$ 找位 | $O(n)$ |
| 哈希表 | $O(1)$ 期望 | $O(1)$ 期望 | $O(1)$ 期望 | **做不到** |
| **搜索树**（平衡时） | $O(\log n)$ | $O(\log n)$ | $O(\log n)$ | $O(\log n + s)$ |

哈希表输在**没有顺序**：它把键打散到桶里，所以「比 $k$ 大的最小键」「$[lo,hi]$ 里有哪些」这类问题无从下手。**搜索树的全部价值，就在于它把「有序」显式编码进结构**——一旦有序，范围查询、前驱后继、按序遍历全都顺理成章。

> **一句话**：搜索树 = **能动态增删的、可二分查找的、有序数组**。它要的就是「有序数组的查询能力 + 链表的增删能力」。

---

## 9.2 二叉搜索树（BST）：一条性质撑起一切

**BST 的定义**只有一句话：

> 对**每个**结点 $x$，其**左子树**里所有键 $< x.\text{key}\le$ 其**右子树**里所有键。

这条「左小右大」的性质，直接给出了查找算法：

```cpp
bool search(Node* r, int k) {
    while (r) {
        if (k == r->key) return true;
        r = (k < r->key) ? r->left : r->right;   // 迭代版，省栈
    }
    return false;
}
```

**插入**就是「查找到空位就插」；**中序遍历**（左 → 根 → 右）自然得到升序序列：

```cpp
void inorder(Node* r, std::vector<int>& out) {
    if (!r) return;
    inorder(r->left, out);
    out.push_back(r->key);
    inorder(r->right, out);
}
```

跑 `code/09_bst.cpp` 的前半段：

```
=== 1. 插入 / 中序 / 查找 ===
  中序遍历（即升序）: 10 20 25 30 35 40 45 50 55 60 65 70 75 80 85
  查找 45 = 1，查找 46 = 0 ✓
  min = 10，max = 85
  树高 = 3（15 个结点，理想约 log2(16)=4）
```

插入了 15 个键、又重复插了 50 和 30（被忽略），中序恰好是升序——**这就是 BST 的「有序性来自形状」**。

### 删除：三种情形

删除是 BST 最微妙的部分，必须分三种情况：

1. **叶子**：直接删掉，父结点指针置空；
2. **只有一个孩子**：用那个孩子「顶替」自己的位置；
3. **有两个孩子**：用**中序后继**（右子树的最小键）替换自己的键，然后**递归删掉那个后继**——后继一定没有左孩子，于是退化成情形 1 或 2。

```cpp
Node* remove(Node* r, int k) {
    if (!r) return nullptr;
    if (k < r->key)      r->left  = remove(r->left, k);
    else if (k > r->key) r->right = remove(r->right, k);
    else {
        if (!r->left)  { Node* t = r->right; delete r; return t; }  // 情形①②
        if (!r->right) { Node* t = r->left;  delete r; return t; }  // 情形②
        Node* succ = findMin(r->right);        // 情形③：中序后继
        r->key = succ->key;
        r->right = remove(r->right, succ->key);
    }
    return r;
}
```

> **为什么用中序后继而不是前驱？** 两者都正确。用后继（右子树最小）或前驱（左子树最大）都能保持 BST 性质。选后继是习惯——本文统一用后继。

跑 `code/09_bst.cpp` 的删除部分：

```
=== 4. 删除（三种情形）===
  删叶子 10 ✓
  删单孩子结点 20（25 顶上）✓
  删双孩子结点 30（中序后继顶替）✓
  删除后中序: 25 35 40 45 50 55 60 65 70 75 80 85
```

代码还和 `std::set` 做了 **100 轮 × 300 次随机增删**的对拍，每轮结束后 BST 的中序都必须与 `std::set` 完全一致——`std::set` 本身通常就是红黑树，这是拿「工业级平衡树」给我们的手写 BST 当基准。

```
=== 5. 与 std::set 随机对拍 ===
  100 轮 × 300 次随机增删：BST 中序 == std::set ✓
```

### 上界 / 下界校验：只比父子是错的

一个经典陷阱：想检查「这棵树是不是 BST」，很多人写：

```cpp
// ✗ 错误：只保证局部父子关系
bool badIsBST(Node* r) {
    if (!r) return true;
    if (r->left  && r->left->key  >= r->key) return false;
    if (r->right && r->right->key <= r->key) return false;
    return badIsBST(r->left) && badIsBST(r->right);
}
```

**这漏掉了「跨层」的约束**。反例：根 $10$，左孩子 $5$，左孩子的**右孩子** $15$。局部看每个父子都合法，但 $15$ 落在根的左子树里却 $>10$——破坏 BST 性质。正确做法是携带**上下界**：

```cpp
bool isBST(Node* r, long lo, long hi) {
    if (!r) return true;
    if (r->key <= lo || r->key >= hi) return false;   // 必须严格落在 (lo, hi) 内
    return isBST(r->left, lo, r->key)                 // 左子树的上界收紧为 r->key
        && isBST(r->right, r->key, hi);               // 右子树的下界收紧为 r->key
}
```

每往下一层，可用区间 $(lo, hi)$ 就收窄一点。这也解释了为什么 BST 的「有序」是**全局**性质，而不是局部性质。

---

## 9.3 ★ BST 的退化：为什么还要平衡树？

BST 的操作复杂度都是 $O(h)$（$h$ 为树高）。**当树「长得均匀」时 $h = O(\log n)$；但当输入是升序时，BST 退化成一条链表，$h = n-1$**。

跑 `code/09_bst.cpp` 的退化实验（依次升序插入 $1..N$）：

```
=== 6. ★ 退化：按升序插入 1..N ===
  升序插入 1..8 -> 树高 = 7（N-1 = 7，退化成链表！）
  升序插入 1..1000 -> 树高 = 999（N-1 = 999，退化成链表！）
  升序插入 1..100000 -> 树高 = 99999（N-1 = 99999，退化成链表！）
```

代码用 `assert(height(chain) == N - 1)` 把这件事钉死。**这就是问题所在**：升序（或近似有序）的数据在现实中极其常见（日志按时间戳、ID 递增、有序流），BST 一遇到它们就完蛋，查找从 $O(\log n)$ 崩到 $O(n)$，和链表一样慢。

> 这引出本章后面三个主角的共同目标：**无论输入什么顺序，都把树高锁在 $O(\log n)$**。
> - **AVL / 红黑树**：确定性——用旋转主动修复平衡；
> - **跳表**：概率性——用抛硬币随机决定结构；
> - **B 树**：多路——用「胖结点」把树高压到对数底很大。

---

## 9.4 AVL 树：用旋转把高度锁死

**AVL 树**（Adelson-Velsky & Landis, 1962）是历史上第一个自平衡 BST。它的核心是一条**不变式**：

> 对**每个**结点，其**左右子树高度差（平衡因子 BF）** $\in \{-1, 0, +1\}$。

一旦插入/删除破坏不变式，就用**旋转（rotation）**在 $O(1)$ 内局部修复。

### 四种失衡 + 对应旋转

看「从失衡结点往下走的前两步方向」：

| 情形 | 直观 | 修复 |
|---|---|---|
| **LL** | 新结点在**左**孩子的**左**边 | **右旋** |
| **RR** | 新结点在**右**孩子的**右**边 | **左旋** |
| **LR** | 新结点在**左**孩子的**右**边 | **先左旋左孩子，再右旋** |
| **RL** | 新结点在**右**孩子的**左**边 | **先右旋右孩子，再左旋** |

口诀：**LL/RR 单旋，LR/RL 双旋**。

旋转只改几个指针，$O(1)$：

```cpp
Node* rotateRight(Node* y) {          // 右旋：y 的左边过重
    Node* x = y->left;
    y->left = x->right;
    x->right = y;
    pull(y); pull(x);                 // 先 pull 低的 y，再 pull 新根 x
    return x;                         // x 成为子树新根
}
```

插入时，**沿递归回溯的路径逐层检查 BF 并旋转**即可。关键性质：**旋转后子树的「高度」恢复到插入前的值**，所以上层不会连锁失衡——一次插入最多 $O(\log n)$ 次旋转，但通常只要 1 次。

跑 `code/09_avl.cpp` 的旋转定点测试（三种插入各触发一种旋转）：

```
=== 2. 四种失衡各自触发对应旋转 ===
   LL (右旋)：插入后根 = 20，中序 10 20 30 ✓
   RR (左旋)：插入后根 = 20，中序 10 20 30 ✓
   LR (先左旋再右旋)：插入后根 = 20，中序 10 20 30 ✓
   RL (先右旋再左旋)：插入后根 = 20，中序 10 20 30 ✓
```

四组输入 `{30,20,10}`、`{10,20,30}`、`{30,10,20}`、`{10,30,20}` 分别是 LL/RR/LR/RL，插入后根都回到中间的键 $20$，中序都是 `10 20 30`。

### ★ 升序插入不再退化

同一个「升序插入 $1..N$」实验，换 AVL 来做：

```
=== 1. ★ 升序插入 1..N（BST 会退化成链，AVL 不会）===
      N    BST高度(=N-1)   AVL高度   AVL理论上界 1.44*log2(N+2)
     8   7   4   4.78358
     1000   999   10   14.3549
     100000   99999   17   23.9179
```

BST 高度是 $N-1$（灾难），AVL 高度却只有 $10$（$N=1000$）和 $17$（$N=10^5$），**稳稳落在 $1.44\log_2(N+2)$ 的理论上界之内**。这就是旋转的威力：无论你怎么插，它都自动把树「扭」回平衡。

**高度界**：可以证明 $n$ 个结点的 AVL 树高度 $h < 1.44\log_2(n+2) - 0.328$，所以查找/插入/删除都是严格的 $O(\log n)$。代码用 `checkAvl` 在**每一次**操作后校验「BF $\in\{-1,0,1\}$ + height 字段正确 + 中序 == `std::set`」：

```
=== 3. 与 std::set 随机对拍（每插入后必查平衡）===
  100 轮 × 400 次随机增删：全程平衡因子合法 + 中序 == std::set ✓
  过程中出现的最大树高 = 9（键域 0..299，log2(300) ≈ 8.22882，始终 O(log n)）
```

### 删除更麻烦，但同样能修

删除可能触发**连锁旋转**（一路修到根），因为删掉一个结点会让整条路径上的高度都变化。代码做了定向测试：

```
=== 4. 删除后仍保持平衡（删除触发连锁旋转）===
  删 7 个结点后：剩余 13 个，高度 5，仍平衡 ✓
```

**小结**：AVL 树用「每个结点多存一个 height」为代价，换来了「高度被不变式锁死在 $O(\log n)$」。它的优点是**平衡严格、查询快**（适合读多写少）；缺点是增删时旋转较多。红黑树（§9.7）放宽了平衡条件，旋转更少、增删更快，是 STL `map`/`set` 的实现。**两者都是「确定性平衡树」家族的代表**。

---

## 9.5 跳表：用抛硬币代替旋转

**跳表（skip list，Pugh 1989）**换了个思路，不维护任何「平衡」，而是**在有序链表上随机地叠「快速通道」**：

- 最底层是一条**包含全部元素**的有序链表；
- 每个结点以概率 $p$（通常 $1/2$）「长到上一层」，形成更稀疏的链表；
- 层数期望 $O(\log n)$。

查找就像**坐电梯**：从最高层的 `head` 出发，**能往右就往右，不能就下移一层**。

```
层级3:  head ────────────────────→ 25 ──→ NIL
层级2:  head ────────→ 9 ────────→ 25 ──→ NIL
层级1:  head ──→ 3 ──→ 9 ──→ 17 ──→ 25 ──→ NIL
层级0:  head → 3 → 6 → 7 → 9 → 12 → 17 → 19 → 21 → 25 → 26 → NIL
```

**为什么是概率的？** 因为「有没有上一层」是**每次插入时抛硬币**决定的，整棵树是**随机的**——不需要任何旋转/再平衡。只要随机数够好，就极难退化，而且**实现比红黑树简单得多**。

```cpp
bool search(int key) const {
    Node* x = head_;
    for (int i = level_; i >= 0; --i) {            // 从当前最高层往下
        while (x->next[i] && x->next[i]->key < key)
            x = x->next[i];                        // 本层能往右就往右
    }
    x = x->next[0];                                // 落到最底层再看一步
    return x && x->key == key;
}
```

插入时先记下「一路上要接在哪」（`update[]` 数组），再逐层穿针引线；层数由抛硬币决定。跑 `code/09_skiplist.cpp`：

```
=== 1. 插入 / 查找 / 删除 ===
  当前元素: 3 6 7 9 12 17 19 21 25 26 
  查找 19 = 1，查找 4 = 0，size = 10 ✓
  删除 19 后：存在? 0，再删一次返回 0 ✓

=== 2. 与 std::set 随机对拍 ===
  100 轮 × 400 次随机增删：有序序列 == std::set ✓
```

### ★ 对比：有「快速通道」的跳表 vs 纯链表

同样的 $N=10^5$ 个键（升序插入），随机查 2000 次，统计**访问结点数**：

```
=== 3. ★ 有「快速通道」的跳表 vs 纯链表 ===
  插入 0..99999 后跳表层数 = 16（≈ log2(100000) = 16.6096）
  随机查 2000 次（键域 0..99999）：
     纯有序链表 平均步数 ≈ 49940.2
     跳表       平均步数 ≈ 16.42
     加速 ≈ 3041.42x
```

纯链表平均要走约 5 万步，跳表只要约 16 步——**约 $3000$ 倍的差距**。而且注意：这里跳表面对的也是**升序插入**（对链表最坏的输入），但因为它靠抛硬币而不是「输入顺序」来决定结构，**完全不受影响**。这正是它相比 BST 的结构性优势。

> **跳表的工程地位**：它是 **Redis 的 zset**、**LevelDB / RocksDB 的 MemTable** 的底层结构。为什么工程上偏爱它？因为它**好写**（没有旋转）、**支持范围查询**（找到 $lo$ 后沿最底层顺序走）、**并发友好**（各层指针可做无锁 CAS）。**AI 落点**：特征索引、近邻检索的候选集、分布式训练用的 KV 存储。

---

## 9.6 B 树 / B+ 树：为「块 I/O」而生的胖结点

前面所有结构都是**二叉**的：一个结点 1 个键、2 个孩子，每层只前进一点点，树高 $O(\log_2 n)$。但**磁盘上的数据按「块」存取**——一次 I/O 读一整块（4KB~16KB）。二叉树每层都要一次 I/O，$10$ 亿条记录要 $30$ 层 = $30$ 次 I/O，太慢。

**B 树**（Bayer & McCreight, 1972）的思想：**把结点做胖**——

> 一个结点存**几百个键**、几百个孩子，**恰好塞满一个磁盘块**。

于是树高从 $\log_2 n$ 降到 $\log_{几百} n$：$10$ 亿条记录只要 **3~4 层**，查一条只需 3~4 次 I/O。

**最小度数 $t$ 的 B 树**（CLRS 记号）的性质：

- 每个结点最多 $2t-1$ 个键、$2t$ 个孩子；**非根结点至少 $t-1$ 个键**；
- **所有叶子在同一层**（B 树是**完美平衡**的）。

**插入用「预分裂（preemptive split）」**：一路向下时，**遇见满结点（$2t-1$ 个键）就先把它裂成两半、把中间键顶进父结点**。于是真正落笔时，叶子一定不满，**永远不用回溯**——这和 AVL 的「回溯修复」形成了鲜明对比。

```cpp
void insertNonFull(BNode* x, int key) {
    int i = (int)x->keys.size() - 1;
    if (x->leaf) {
        // 在有序位置插入 key
    } else {
        while (i >= 0 && key < x->keys[i]) --i;
        ++i;                                              // 应下探的孩子下标
        if ((int)x->children[i]->keys.size() == 2*t_ - 1) {
            splitChild(x, i);                             // 下探前先裂
            if (key > x->keys[i]) ++i;
        }
        insertNonFull(x->children[i], key);
    }
}
```

跑 `code/09_btree.cpp`：

```
=== 1. 插入 / 查找 / 中序（t = 3）===
  中序遍历（升序）: 5 6 7 10 12 17 20 30
  查找 17 = 1，查找 11 = 0，size = 7，高度 = 1 ✓

=== 2. 与 std::set 随机对拍 + 校验 B 树不变式 ===
  200 组随机测试：中序 == std::set，且「2t-1 上界 / t-1 下界」全部满足 ✓
```

200 组随机测试里，代码逐结点校验「键数 $\le 2t-1$」和「非根结点键数 $\ge t-1$」两条不变式。

### ★ 为什么「胖结点」能大幅降低树高

同样的升序插入 $1..N$（对 BST 是灾难），B 树的高度：

```
=== 3. ★ 为什么「胖结点」能大幅降低树高 ===
      N        B树(t=3)高   BST高(=N-1)   B树(t=8)高   理想二叉 log2(N)
     1000     5        999        2          9.96578
     100000     9        99999        5          16.6096
     1000000     11        999999        6          19.9316
```

$10^6$ 条记录：BST 升序插入高度是 $999999$（$N-1$）；B 树 $t=3$ 只要 $11$ 层，$t=8$ 更是只要 $6$ 层。**把「每层能带几个键」从 1 提到 $2t-1$，树高就从 $\log_2 n$ 压到 $\log_{2t} n$**。当 $t$ 取几百（刚好一个磁盘块），$10$ 亿条记录也只要 3~4 层。

### B 树 vs B+ 树（工程上的选择）

| | B 树 | **B+ 树** |
|---|---|---|
| 数据存放 | 所有结点都可能有数据键 | **只有叶子存数据**，内部结点只存路由键 |
| 查找命中 | 可能在内部结点提前命中 | 一定走到叶子 |
| 内部结点大小 | 含数据，偏大 | 只含路由键，**更小 -> 一层装更多键 -> 树更矮** |
| 范围 / 顺序扫描 | 需中序遍历，跨层跳 | **叶子成链，顺序扫即可**，天然支持 `ORDER BY` |

**结论**：正因为「内部结点更小（树更矮）+ 叶子成链（范围查询快）」，**MySQL InnoDB、PostgreSQL、Oracle 的索引全部用 B+ 树**。

---

## 9.7 五种结构的取舍：一张表看清

| 结构 | 查找 | 插入 | 删除 | 平衡方式 | 范围查询 | 典型用途 |
|---|---|---|---|---|---|---|
| **BST** | $O(h)$，最坏 $O(n)$ | $O(h)$ | $O(h)$ | 无（会退化） | ✓ | 教学、小型有序集合 |
| **AVL 树** | $O(\log n)$ | $O(\log n)$ | $O(\log n)$ | 旋转（严格） | ✓ | 读多写少、查得快 |
| **红黑树** | $O(\log n)$ | $O(\log n)$ 旋转少 | $O(\log n)$ | 染色 + 旋转（松弛） | ✓ | STL `map`/`set`、`std::multimap` |
| **跳表** | 期望 $O(\log n)$ | 期望 $O(\log n)$ | 期望 $O(\log n)$ | 抛硬币（无旋转） | ✓ | Redis、MemTable、并发 KV |
| **B / B+ 树** | $O(\log_t n)$ **次 I/O** | 同 | 同 | 分裂 / 合并 | ✓（B+ 尤优） | 数据库、文件系统 |

**怎么选？**

- **内存里的小规模**：`std::map`/`std::set`（红黑树）几乎是默认答案；
- **读远多于写、要求极致查询**：AVL；
- **需要并发、想实现简单、要范围查询**：跳表；
- **数据在磁盘 / 量极大**：B+ 树；
- **只是查在不在、不需要顺序**：哈希表（第 13 章）比以上都快，但丢掉了有序性。

> **一个重要的思想**：AVL 用**旋转**、跳表用**抛硬币**、B 树用**分裂**——三个完全不同的机制，目标却是同一个：**把「高度」或「路径长度」压到 $O(\log n)$**。**数据结构的设计，本质是选择「用什么代价维护什么不变式」**。

---

## 9.8 复杂度对照

| 操作 / 结构 | BST | AVL | 红黑树 | 跳表 | B 树（$t$） |
|---|---|---|---|---|---|
| `search` | $O(h)$ | $O(\log n)$ | $O(\log n)$ | 期望 $O(\log n)$ | $O(\log_t n)$ 磁盘访问 |
| `insert` | $O(h)$ | $O(\log n)$ | $O(\log n)$ | 期望 $O(\log n)$ | $O(\log_t n)$ |
| `remove` | $O(h)$ | $O(\log n)$ | $O(\log n)$ | 期望 $O(\log n)$ | $O(\log_t n)$ |
| `min`/`max` | $O(h)$ | $O(\log n)$ | $O(\log n)$ | $O(\log n)$ 期望 | $O(\log_t n)$ |
| `successor`/`pred` | $O(h)$ | $O(\log n)$ | $O(\log n)$ | $O(\log n)$ 期望 | $O(\log_t n)$ |
| 中序遍历 | $O(n)$ | $O(n)$ | $O(n)$ | $O(n)$（底层） | $O(n)$ |
| 额外空间 / 结点 | 2 指针 | 2 指针 + height | 2 指针 + 1 bit 色 | 平均 $\approx 2$ 指针 | $2t$ 指针 + $2t-1$ 键 |
| 最坏保证 | ✗ | ✓ | ✓ | ✗（概率） | ✓ |

（$h$ = 实际树高，最坏 $n-1$；$t$ = B 树最小度数。）

---

## 常见错误与陷阱

1. **BST 删除忘了分三种情形。** 叶子、单孩子、双孩子必须分开处理。双孩子用「中序后继 / 前驱」顶替时，**别忘了递归删掉那个后继**，否则会出现重复键。

2. **只比父子校验 BST。** 如 §9.2 所述，必须携带**上下界**递归，保证「整棵左子树 $<$ 根」。这是最经典的错误之一。

3. **BST 遇到有序输入就退化。** 升序插入 $1..N$ 会得到高度 $N-1$ 的链。**任何真实使用都应换成平衡树**（`std::map`/`set` 或跳表），不要手搓裸 BST 上生产。

4. **AVL 旋转后忘了更新 height / BF。** 每次旋转都要 `pull` 受影响的两个结点（**先 pull 低的那层，再 pull 新的根**，顺序反了会算错）。这是自写 AVL 最常见的 bug。

5. **AVL 的 LR / RL 只旋一次。** LR 必须**先对左孩子左旋、再对根右旋**；RL 对称。只旋一次会「越修越歪」。

6. **AVL 删除忘了可能连锁旋转。** 删除会让整条路径高度变化，检查要一路回溯到根，不像插入通常一次就够。

7. **跳表忘了在删空最高层后收缩 `level`。** 否则 `search` 会从一堆空层开始，白白多走几步（不影响正确性，但影响性能）；另外**随机数质量差会退化**。

8. **B 树分裂时中间键的下标搞错。** $2t-1$ 个键的中间键是下标 $t-1$，左半留 $t-1$ 个、右半 $t-1$ 个、中间 1 个上浮。差一位就破坏「所有叶子同层」的不变式。

9. **把 B 树和 B+ 树混为一谈。** 数据库索引几乎都是 **B+ 树**（内部结点不存数据、叶子成链）。面试/工程里说「B 树」时要明确是哪一种。

10. **中文字符串用 `std::setw` 对齐会错位。** 本章表格故意把数字放前面、不依赖 `setw`——因为 `setw` 按**字节数**而非**显示宽度**算，一个汉字 3 字节但显示 2 宽（第 08 章已踩过这个坑）。

11. **递归删除 / 析构造成泄漏或悬垂。** BST、AVL、B 树的 `remove`/析构都要正确 `delete`（B 树要递归删所有孩子）。务必用 ASan 跑一遍（本章代码全部通过 `-fsanitize=address,undefined`）。

---

## 与 AI / 世界模型的连接

搜索树回答的是「**如何维护一个动态的有序集合**」。深度学习的系统层里，只要出现「按 key 快速找 / 按区间取 / 有序遍历」，背后几乎都是本章的结构。

### 1. ★ 键值缓存与向量数据库的索引

**向量数据库**（Faiss、Milvus、Weaviate）在近似最近邻检索之后，还要维护「向量 ID → 元数据」的映射，以及按 ID / 时间 / 类别做**范围过滤**。底层常用 **B+ 树或 LSM 树**（RocksDB）。**推理服务的 KV 缓存**（prefix cache）也靠有序结构做「前缀命中 + 淘汰」。

### 2. ★ 世界模型 / 强化学习里的「状态索引」

**经验回放池**若要支持「按状态相似度检索近邻」（如基于 KD-tree 或 HNSW 的优先回放），就需要**空间索引**；而**离散状态 / 动作空间**的快速查表常直接用一个有序结构做 ID 映射。**MCTS 的树节点转置表（transposition table）**用哈希或有序结构避免重复扩展——「同一个状态别搜两遍」正是 `std::set` 的语义。

### 3. ★ 训练数据与特征工程

- **特征哈希 / 类别特征的字典编码**：把字符串类别映射到整数 id，需要快速查找 + 有序遍历（离线统计频次）；
- **数据去重 / 采样**：用 `std::set` / 跳表维护「已见过的样本 id」，O(log n) 判重；
- **数据集划分**：按时间戳有序切分训练 / 验证集，B+ 树的范围扫描是天然工具。

### 4. ★ 跳表与并发系统

**分布式训练的参数服务器、KV 存储（Redis、RocksDB）**大量使用跳表——因为它**无锁实现容易**（各层指针可原子更新），在**多线程 / 多卡**环境下比旋转类平衡树更友好。理解「为什么工程界选跳表而非红黑树」，是系统思维的必修课。

### 5. ★ B 树与「内存层级」的第一性原理

B 树最深刻的一课是「**让每次内存访问值回票价**」：把结点做胖以摊薄每次访问的固定开销。这个思想在 AI 硬件里**处处可见**：

- **GPU 显存分层**（寄存器 / 共享内存 / L2 / HBM）：让数据尽量待在快的那层；
- **FlashAttention**：用**分块（tiling）**把访问从 HBM 搬到 SRAM，正是「块 I/O」思想的翻版；
- **KV-cache 分页（PagedAttention）**：把 KV 缓存切成「块」，像操作系统管理内存页一样管理——这和 B 树的「块」是同一个抽象。

> **一句话**：搜索树教你「**如何在动态变化中，始终维持有序、并让查询保持对数级**」。从数据库索引（B+ 树），到缓存 / 参数服务器（跳表），到训练数据管理（有序集合）——**只要系统要「有序 + 快」，就站着一棵搜索树**。

---

## 本章小结

- **搜索树 ADT** 的价值是「**有序数组的查询 + 链表的增删**」，是哈希表唯一做不到的「有序动态集合」；
- **BST** 靠「左小右大」一条性质撑起全部操作，$O(h)$；**中序 = 升序**；删除分**三种情形**（双孩子用中序后继）；
- ★ **校验 BST 必须用「上下界」递归**，只比父子会漏掉跨层约束；
- ★ **BST 会退化**：升序插入 $1..N$ 树高 $= N-1$，查找崩到 $O(n)$；
- **AVL 树**：不变式「BF $\in\{-1,0,+1\}$」+ **四种旋转**（LL/RR 单旋、LR/RL 双旋）；实测升序插入 $N=10^5$ 高度仅 $17$，锁在 $1.44\log_2 n$ 内；
- **跳表**：**抛硬币 + 分层**替代旋转，期望 $O(\log n)$；实测比纯链表快约 $3000\times$，且不受输入顺序影响；
- **B 树 / B+ 树**：**胖结点**把树高压到 $\log_t n$；$N=10^6$ 升序插入 B 树($t{=}8$) 仅 $6$ 层；数据库索引用 **B+ 树**（叶子成链、范围查询快）；
- **取舍**：内存小规模用红黑树（`std::map`）；读多写少用 AVL；要并发 / 实现简单用跳表；磁盘 / 海量用 B+ 树；
- ★ **AI 连接**：向量数据库索引、KV 缓存、经验回放的近邻检索、MCTS 转置表、数据去重、参数服务器（跳表）、内存层级（B 树 -> FlashAttention / PagedAttention）。

---

## 练习

### 基础

1. 在 `code/09_bst.cpp` 的 `BST` 上增加 `rangeCount(lo, hi)`（统计区间内键的**个数**，不输出），要求 $O(\log n)$，并说明为什么不能靠 `rangeQuery` 再数一遍来达到这个复杂度。
2. 手画一棵 7 个结点的 BST，然后依次执行 `insert(6)`、`remove(根)`，画出每一步的树形。
3. 用「上下界递归」校验下面这棵树的合法性，并指出它是不是 BST：根 $20$，左 $10$、右 $30$；$10$ 的右孩子是 $25$。
4. 给 `code/09_avl.cpp` 输出每种旋转**各发生几次**的计数器，跑一次随机测试，观察四种旋转的分布。

### 进阶

5. **实现红黑树的插入**：用「染色 + 旋转」维护「根黑、红结点不能有红孩子、各路径黑高相同」三条不变式，并与 `std::set` 对拍。
6. **实现跳表的范围查询**：找到 $lo$ 后沿最底层顺序输出直到超过 $hi$，分析复杂度 $O(\log n + s)$，并与 `code/09_btree.cpp` 的 B 树范围能力对比。
7. **实现 2-3-4 树（B 树 $t=2$）**，并说明它和红黑树的**一一对应**关系（每个红黑树都能唯一映射成 2-3-4 树）。
8. **AVL vs 跳表 性能对比**：对 $N=10^6$ 个键做「随机插入 + 随机查找 + 随机删除」，分别测 AVL 和跳表的总耗时，解释差异来源（旋转 vs 指针跳转、cache 局部性）。

### 挑战

9. **实现持久化（persistent）BST**：每次修改都返回一个新的根，**保留旧版本**（路径复制，$O(\log n)$ 每版本）。这是**函数式数据结构**与「模型检查点 / 版本化数据集」的核心思想。
10. **实现 B+ 树的范围扫描与批量加载**：支持「给定 $[lo,hi]$ 顺序输出」和「把有序数组 $O(n)$ 批量建成 B+ 树」（bulk loading），说明为什么数据库导入数据时用批量加载而非逐个插入。
11. **用跳表实现「带权重的 LRU 缓存」**：键按「最近使用时间」排序，容量满时淘汰最久未用的。对比哈希表 + 双向链表的经典 LRU 实现，讨论缓存淘汰策略与 `prefix cache` 的关系。

<details>
<summary>参考答案（要点）</summary>

**1.** 在每个结点额外维护「子树结点数 `cnt`」。`rangeCount(lo,hi)` 用「$\le hi$ 的个数 $-$ $< lo$ 的个数」两次下界查询得到，各 $O(\log n)$。不能先 `rangeQuery` 再 `size()`，因为那要输出 $s$ 个元素，$O(\log n + s)$ 已经包含了 $s$。

**2.** 关键在于画清楚删除双孩子结点时「中序后继顶替 + 递归删后继」两步。$6$ 插入后若造成依赖关系，注意 BST 不旋转，形状由插入顺序决定。

**3.** 用 `isBST(root, -∞, +∞)`：到 $10$ 的右子树时下界收紧为 $10$、上界仍为 $20$，$25 \ge 20$ 越界 → **不是 BST**。

**4.** 计数器放全局 / 传引用；随机测试下 LL 与 RR 约各占 $\sim 40\%$ 对称，LR/RL 各约 $10\%$，取决于插入序分布。

**5.** 红黑树插入：新结点染红 → 若父红则看叔叔颜色。叔叔红则「父叔染黑、祖父染红、上溯」；叔叔黑则按 LL/RR/LR/RL 旋转 + 染色。最后根强制染黑。

**6.** 先 `search_probe` 找到 $\ge lo$ 的结点，再沿 `next[0]` 顺序走直到 $> hi$。复杂度 $O(\log n + s)$，与 B+ 树叶子扫描同阶。

**7.** 2-3-4 树（$t=2$）的「3-结点 / 4-结点」展开成二叉形态，对应红黑树的「黑结点带红孩子」；两结点数相等，是经典等价。

**8.** AVL 每次增删有旋转 + height 维护，常数大但最坏有保证；跳表指针跳转多、cache 局部性差，但无旋转、常数小、期望够快。实测常互有胜负，取决于操作混合比例。

**9.** 路径复制：`insert` 时新建从根到叶路径上的每个结点（共享未改动的子树），返回新根。每版本 $O(\log n)$ 空间，可查询任意历史版本——即「不可变数据」与「版本化」。

**10.** 批量加载：先把有序数组按 $2t-1$ 个一组切成叶子，再自底向上建内部结点，$O(n)$，且结点填充率接近满；逐个插入是 $O(n\log n)$ 且填充率低（只有 $\sim 70\%$）。数据库导入 / 建索引都用批量加载。

**11.** 跳表按「最近使用时间」排序 + 每结点存 key；命中时删除旧位置、按新时间重插。淘汰即删最底层的头。对比经典 LRU：有序结构让「按时间序淘汰」$O(\log n)$，但也牺牲了哈希的 $O(1)$ 平均查找。

</details>

---

## 延伸阅读

- **Sahni《数据结构、算法与应用（C++ 版）》第 2 版 第 12 章**「Binary Search Trees」：BST 的 ADT、插入/删除三种情形、索引二叉搜索树；
- **Sahni 第 2 版 第 13 章**「Balanced Search Trees」：AVL 树、红黑树、2-3 树、B 树、B+ 树的经典论述；
- **Sahni 第 2 版 第 10 章**「Skip Lists and Hashing」：跳表的随机化设计与期望复杂度分析；
- **Adelson-Velsky & Landis (1962)** *An algorithm for the organization of information* —— AVL 树原始论文；
- **Pugh, W. (1990)** *Skip Lists: A Probabilistic Alternative to Balanced Trees* —— 跳表原始论文；
- **Bayer & McCreight (1972)** *Organization and Maintenance of Large Ordered Indices* —— B 树原始论文；
- **《算法导论》第 12 章**「二叉搜索树」、**第 13 章**「红黑树」、**第 18 章**「B 树」—— 严格的形式化证明；
- **Guibas & Sedgewick (1978)** *A Dichromatic Framework for Balanced Trees* —— 红黑树的统一框架；
- 配套代码：`code/09_bst.cpp`、`code/09_avl.cpp`、`code/09_skiplist.cpp`、`code/09_btree.cpp`。

---

**下一章**：第 10 章《图基础与遍历》—— 从「树」迈入更一般的「图」，讲图的表示（邻接矩阵 / 邻接表）、DFS/BFS、拓扑排序，以及它们在世界模型「状态转移图」上的直接对应。
