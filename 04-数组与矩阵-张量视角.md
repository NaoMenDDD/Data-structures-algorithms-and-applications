# 第 04 章 数组与矩阵：张量视角

> 前两章我们把线性表用数组和链表两种方式实现了一遍。从这一章开始，元素不再排成一条线，而是排成**网格**——矩阵，以及更高维的**张量**。
>
> 这一章是整本书里与你的研究方向**关系最直接**的一章。因为：**深度学习里的一切数据都是张量**。而张量的底层，就是「一维连续内存 + 一层地址映射」。搞懂 `shape` 和 `stride`，你就能解释 PyTorch 里一大半令人困惑的行为：为什么 `.transpose()` 免费、`.contiguous()` 要拷贝、`view` 有时报错、`NCHW` 和 `NHWC` 能让卷积快慢差好几倍。
>
> 一句话：**这一章教你「看穿」张量。**

---

## 本章目标

读完并跑完本章代码，你应该能够：

1. 写出多维数组「索引 → 一维偏移」的地址公式，说清**行主序**与**列主序**的区别；
2. 用 `shape + strides + offset` 描述任意张量布局，并解释 PyTorch 的 `view` / `transpose` / `permute` / `contiguous` / 广播各自做了什么；
3. 说清 `NCHW` 与 `NHWC` 的本质差异，以及它为什么会影响卷积性能；
4. 解释矩阵乘法**三种循环次序**的性能差异源于 cache，并写出最快的那种；
5. 掌握**特殊矩阵的压缩存储**（对称、三角、对角、带状）与**稀疏矩阵的 CSR/CSC** 表示；
6. 建立「布局即性能」的直觉：同一份数据、同一个算法，换个存储布局，速度可以差一个数量级。

---

## 前置知识

- 第 02 章：数组的内存连续性、动态数组、缓存局部性；
- 第 01 章：复杂度的基本记号（本章会用「FLOP 计数」论证三种循环次序等价）；
- 一点点线性代数（矩阵乘法 $C_{ij}=\sum_k A_{ik}B_{kj}$）。

---

## 4.1 多维数组的内存映射

### 4.1.1 从二维到一维：地址公式

计算机的内存是**一维**的（一个巨大的字节数组）。二维数组 `M[rows][cols]` 必须被「拍平」成一条线。这里有个约定问题：**先排完一行，再排下一行**，还是**先排完一列，再排下一列**？

- **行主序（row-major）**：一行一行地铺。C、C++、Python(NumPy/PyTorch) 默认。
- **列主序（column-major）**：一列一列地铺。Fortran、MATLAB、BLAS/LAPACK、Julia 默认。

对 $3 \times 4$ 矩阵 $M$：

$$
M=\begin{pmatrix} 0&1&2&3\\ 10&11&12&13\\ 20&21&22&23 \end{pmatrix}
$$

```
行主序物理内存:  0  1  2  3 | 10 11 12 13 | 20 21 22 23   （每行 4 个连续）
列主序物理内存:  0 10 20 | 1 11 21 | 2 12 22 | 3 13 23    （每列 3 个连续）
```

地址公式（**0-based** 索引）：

$$
\text{offset}_{\text{row-major}}(i,j) = i \cdot \text{cols} + j, \qquad
\text{offset}_{\text{col-major}}(i,j) = j \cdot \text{rows} + i.
$$

以 $M[1][2]=12$ 为例：行主序偏移 $= 1\cdot4+2=6$；列主序偏移 $= 2\cdot3+1=7$。两套约定给出的物理位置不同，**但逻辑矩阵完全一样**——这正是「布局是元数据、数据本身不变」的第一个例子。

跑 `code/04_matrix_rowcol.cpp` 可直接验证（含三维、$n$ 维、1-based 与 0-based 的换算）：

```
===== 2D: 3 行 x 4 列 =====
行主序物理内存:    0   1   2   3  10  11  12  13  20  21  22  23
列主序物理内存:    0  10  20   1  11  21   2  12  22   3  13  23
M[1][2] = 12, 行主序偏移 = 6, 列主序偏移 = 7
[OK] 24 个下标: 霍纳式与 stride 点积结果完全一致
```

### 4.1.2 一般 $n$ 维：霍纳法则

对形状 $(d_0, d_1, \dots, d_{n-1})$ 的 $n$ 维数组，行主序偏移可以写成**霍纳（Horner）展开**：

$$
\text{offset} = (\cdots((i_0 \cdot d_1 + i_1)\cdot d_2 + i_2)\cdots)\cdot d_{n-1} + i_{n-1}.
$$

这个递推式就是 NumPy 内部 `ravel_multi_index` 的核心。它的好处是**只用一个循环、不用先算 stride**。

---

## 4.2 步长（stride）：张量库的核心抽象

### 4.2.1 张量 = 存储 + 形状 + 步长 + 偏移

上面霍纳式展开后，每个维度前面的系数就是该维的**步长（stride）**：沿该维走一步，在底层存储里要走多远（以元素为单位）。于是：

$$
\boxed{\ \text{offset}(\text{index}) = \text{offset}_0 + \sum_{k} \text{index}_k \cdot \text{stride}_k\ }
$$

- **形状 `shape`**：每一维有多少个元素，决定「逻辑上有多大」；
- **步长 `strides`**：每一维走一格要跳过多少个元素，决定「逻辑索引怎么落到物理地址」；
- **偏移 `offset`**：这块视图从底层存储的第几个元素开始。

行主序的**标准步长**：最后一维步长为 1，往前依次乘以后面几维的大小：

```cpp
std::vector<long> standardStrides(const std::vector<long>& s) {
    std::vector<long> st(s.size());
    long acc = 1;
    for (long k = (long)s.size() - 1; k >= 0; --k) { st[k] = acc; acc *= s[k]; }
    return st;
}
```

$2\times3$ 张量的标准步长是 `[3, 1]`（第一维走一格跳 3 个元素，第二维走一格跳 1 个）。`shape + strides + offset` 这套抽象就是 PyTorch 张量的全部秘密（`tensor.stride()`、`tensor.storage_offset()` 是公开 API）。

### 4.2.2 视图操作：零拷贝的魔法

一旦把布局表达成 strides，**转置、换轴、切片全都可以靠改元数据完成，一个字节都不搬**：

| 操作 | 做什么 | 是否拷贝数据 |
|---|---|---|
| `transpose(a,b)` | 交换第 $a,b$ 维的 `shape` 与 `strides` | **否**（零拷贝） |
| `permute(order)` | 按 `order` 重排所有维度 | **否** |
| `view`/`reshape` | 改 `shape`，要求数据连续才能免拷贝 | 连续时否，否则**是** |
| `slice` | 调 `offset`、改 `shape`、必要时改 `strides` | **否** |
| `contiguous()` | 把非连续布局**真正物化**成连续 | **是** |

`transpose` 的实现短到令人怀疑（`code/04_tensor_stride.cpp`）：

```cpp
Tensor transpose(long a, long b) const {
    auto sh = shape_, st = strides_;
    std::swap(sh[a], sh[b]);       // 交换形状
    std::swap(st[a], st[b]);       // 交换步长
    return Tensor(data_, sh, st, offset_);   // 共享同一块 storage
}
```

实测：

```
A            shape=[2,3]  strides=[3,1]  contiguous=true
A^T          shape=[3,2]  strides=[1,3]  contiguous=false
验证 A[0][1]==1 == A^T[1][0]=1  ✓（转置未拷贝数据）
```

**PyTorch 里 `x.T` 或 `x.transpose(0,1)` 是 $O(1)$ 的**，因为它只改了 strides；`contiguous()` 才是 $O(n)$。理解了这一点，你就知道什么时候该担心拷贝、什么时候不该。

### 4.2.3 连续性（contiguity）

一个张量**连续（contiguous）**，当且仅当它的 `strides` 恰好等于标准行主序步长。转置后 `strides=[1,3]`，末维步长是 3 而不是 1，所以**不连续**。

为什么所有框架都盯着「连续」？因为：

- 连续张量的元素在内存里**顺序排列**，算子可以顺序访存、向量化、合并访存；
- 非连续张量要按 stride 跳着访问，**每次访问都可能是一个新 cache line**（第 03 章实测过：跨步访问能让求和慢 10 倍以上！）。

`code/04_tensor_stride.cpp` 实测（$2048\times2048$ 求和，加法次数完全相同）：

```
连续张量 a[i][j]        2.444 ms
跨步 view a^T[i][j]      24.745 ms
跨步 / 连续 耗时比 = 10.13x
```

**这就是「`.transpose()` 之后常要 `.contiguous()`」的全部理由**：多花一次 $O(n)$ 的拷贝，换回后续所有算子的顺序访存。是否划算，取决于后续要用它做多少次计算——这正是「布局即性能」的工程取舍。

### 4.2.4 NCHW vs NHWC

卷积网络的四维张量有两个常见布局：

- **NCHW**：`(batch, channels, height, width)`，PyTorch 默认；
- **NHWC**：`(batch, height, width, channels)`，TensorFlow 默认，也是很多 GPU 芯片（如某些 ARM、TPU）偏好的布局。

它们描述的是**同一份数据**，只是 strides 不同。用 `permute` 换布局是零拷贝的（`code/04_tensor_stride.cpp`）：

```
NCHW  shape=[2,3,4,5]  strides=[60,20,5,1]   contiguous=true
NHWC  shape=[2,4,5,3]  strides=[60,5,1,20]   contiguous=false
x[1][2][3][4] 偏移 = 119（NCHW）, y[1][3][4][2] 偏移 = 119（NHWC）  ← 同一个物理元素
```

同一个元素的物理偏移一致，说明 `permute` 没搬数据。但**访存模式**变了：

- NCHW 下，一次取一个像素的**所有通道**要跨 stride 20（不连续）；
- NHWC 下，通道维在最后、stride = 1，一个像素的通道**连续存放**。

卷积核常常需要「一个空间位置上的所有通道」一起算，所以 NHWC 对这种访问更友好。cuDNN 会针对不同布局选出不同的 kernel——**这就是同一模型换个内存格式，速度能差几成的底层原因**。

### 4.2.5 广播（broadcasting）= 步长 0

想让形状 `(3,4)` 的张量加上形状 `(4,)` 的向量，NumPy/PyTorch 会自动「广播」：把 `(4,)` 看成 `(1,4)`，再沿第 0 维复制 3 份。但框架**并不会真的复制**——它把被广播那一维的 stride 设成 **0**。

**步长为 0 的维度意味着「沿着它走一步，地址不动」**，于是逻辑上访问 $3\times4$ 个位置，物理上只读那 4 个数。广播是零拷贝的，靠的就是 stride = 0 这条技巧。

> 记住这个统一图景：**`view`、`transpose`、`slice`、`broadcast` 都是「改 strides」；只有 `contiguous`/`copy` 是「搬数据」。** 这是阅读张量框架源码、定位性能问题的基本功。

---

## 4.3 矩阵乘法与 cache：循环次序的威力

矩阵乘法 $C = A\times B$ 是深度学习算力的绝对大头（GEMM）。对 $n\times n$ 方阵，浮点运算次数固定是 $2n^3$ —— **不管你怎么写循环，运算次数都一样**。那为什么性能能差十几倍？

因为三重循环有 $3! = 6$ 种次序，访问模式的 cache 友好度天差地别。以行主序存储、$C_{ij}=\sum_k A_{ik}B_{kj}$ 为例，看右侧乘数 $B$ 的访问：

| 循环次序 | 内层访问 | $B$ 的访存模式 | 相对耗时（实测 $n=768$） |
|---|---|---|---|
| `i-k-j` | `C[i][j] += A[i][k]*B[k][j]` | $B$ 顺序、$C$ 顺序 | **1.0x（最佳）** |
| `i-j-k` | `C[i][j] += A[i][k]*B[k][j]` | $B$ **跨列**（stride $=n$） | 5.4x |
| `j-k-i` | 同上，次序换 | $C,A$ **跨行** | 15.1x（最差） |

跑 `code/04_matmul_cache_order.cpp`（$n=768$，三种次序结果完全一致）：

```
循环次序       耗时(s)    相对 ikj   正确?
i-j-k (较差)         0.674        5.434x   OK
i-k-j (最佳)         0.124        1.001x   OK
j-k-i (最差)         1.868       15.064x   OK
结论: 三种次序的浮点运算次数完全相同 (都是 2n^3),
      差异纯粹来自内存访问的局部性 (cache line 命中率)。
```

**同一个算法、同样的运算次数，最差次序的耗时是最好次序的 15 倍。** 这不是算法问题，是**内存问题**。工程上的解法：

- **循环分块（blocking / tiling）**：把大矩阵切成能塞进 cache 的小块，块内做乘法，块间移动 —— 这是所有高性能 BLAS（如 OpenBLAS、MKL、cuBLAS）的核心；
- **打包（packing）**：把 $B$ 的一小块「转置、重排」成对当前循环友好的临时缓冲；
- 这正是 **FlashAttention** 的思想源头：不做完整的 $n\times n$ 注意力矩阵，而是分块、在 SRAM 里边算边合并（第 01 章提到过）。

> **对你的启示**：研究世界模型时，你会频繁面对「显存放不下、算得太慢」的问题。答案几乎总是同一类：**改变数据布局与计算分块，让访存与计算重叠、提高 cache/片上内存命中率。** 这背后就是本章这一节的原理。

---

## 4.4 特殊矩阵的压缩存储

很多矩阵有特殊结构，可以用远少于 $n^2$ 的空间存下来。

| 类型 | 结构 | 存储量 | 索引映射 |
|---|---|---|---|
| 对称矩阵 | $A_{ij}=A_{ji}$ | $\approx n^2/2$ | 只存下三角，$i\ge j$ 直接存，$i<j$ 查 $A_{ji}$ |
| 上/下三角 | 半边为 0 | $\approx n^2/2$ | 按行（或列）连续存非零半三角 |
| 对角矩阵 | 只有主对角线非零 | $n$ | 一维数组 `d[i]` |
| 三对角 | 只有 $|i-j|\le1$ 非零 | $3n$ | 三条对角线分别存 |
| 带状（带宽 $b$） | $|i-j|\le b$ | $\approx(2b+1)n$ | 每条带一维存 |

压缩的关键永远是**「设计一个数组下标到矩阵下标的双向映射」。** 以对称矩阵为例，若按行主序存下三角（含对角）：

$$
\text{offset}(i,j)\ (i\ge j) = \frac{i(i+1)}{2} + j.
$$

> **AI 关联**：注意「只存一半」在神经网络里无处不在 —— 注意力矩阵 $QK^\top$ 是**对称**的（或可对称化），因果掩码让它变成**三角**的。FlashAttention 的块级实现正是利用了「很多块可以跳过（因果掩码下块全为 0）」和「对称/三角结构」来省算力。

---

## 4.5 稀疏矩阵：三元组与 CSR

当矩阵**绝大多数元素是 0** 时（稀疏），用稠密数组存是巨大浪费。两种经典表示：

**① 三元组（coordinate / COO）**：存一个 `(row, col, value)` 列表，按行主序排序。

```
A (4×5, nnz=7):
  (0,0,1) (0,3,2) (1,1,3) (2,3,4) (2,4,5) (3,0,6) (3,2,7)
```

**② CSR（Compressed Sparse Row）**：用三个数组紧凑表示：

- `rowPtr`：长度 `rows+1`，`rowPtr[i]..rowPtr[i+1]` 是第 $i$ 行的非零区间；
- `colIdx`：长度 `nnz`，每个非零元的列号（每行内升序）；
- `vals`：长度 `nnz`，对应的值。

```
rowPtr = [0, 2, 3, 5, 7]
colIdx = [0, 3, 1, 3, 4, 0, 2]
vals   = [1.00, 2.00, 3.00, 4.00, 5.00, 6.00, 7.00]
```

CSR 的精髓是**「用连续的数组表达稀疏的图/关系」**——彻底避开了第 03 章那种指针追逐。于是它能向量化、能上 GPU。`code/04_sparse_csr.cpp` 演示了：

- 三元组 ↔ CSR 互转；
- **稀疏转置**（$O(\text{nnz})$ 的「快速转置」）；
- **稀疏矩阵-向量乘** $y = Ax$，复杂度 $O(\text{nnz})$ 而非 $O(\text{rows}\times\text{cols})$：

```
===== 稀疏矩阵-向量乘 y = A x =====
  y[0] sparse=9.00  dense=9.00
  ...
[OK] 稀疏 SpMV 与稠密结果一致 (复杂度 O(nnz) 而非 O(rows*cols))
```

> **AI 关联**：CSR 是 **GNN（图神经网络）消息传递**的工业标准存储格式（PyG 的 `torch_sparse`、DGL 都用它），也是**稀疏注意力**、**嵌入表（embedding table）**、**推荐系统特征**的底层结构。当模型里出现「一个大矩阵但只有很少非零」时，你要条件反射地想：「能不能用 CSR/CSC 省下几十倍显存和算力？」

CSC（Compressed Sparse Column）就是把上面那套按**列**来组织，适合「按列切片」的场景（如类 LAPACK 的列主序算法）。

---

## 常见错误与陷阱

1. **混淆行主序与列主序，导致跨语言/跨库传数据时结果全错。** C/Python 与 Fortran/MATLAB/BLAS 的默认约定相反。把 C 数组直接喂给一个期望列主序的 BLAS 例程，会得到「转置了」的结果。**排错第一步永远是：确认对方的存储约定。**

2. **误以为 `transpose` 会复制数据。** 它不会（$O(1)$）。但**后续对非连续张量做逐元素运算会变慢**——因为访存变成跨步。在 PyTorch 里典型症状是「`.transpose()` 之后某算子慢了几倍」。

3. **在非连续张量上调用 `view`。** `view` 要求内存连续（本质是「按另一种形状重新解释同一段连续内存」），非连续时会报错。解法是先 `.contiguous()`，或用 `reshape`（它在必要时自动拷贝）。**理解 `view` vs `reshape` 的区别 = 理解连续性。**

4. **下标计算时把 `stride` 和维度大小搞混。** 记住：`stride[k]` 是「走一格跳多远」，等于**它后面所有维度的乘积**（行主序）。一个常见 bug 是用 `d_k`（第 $k$ 维大小）当步长。

5. **`int` 溢出导致大张量地址算错。** $100000\times100000$ 的索引会超过 `int`（约 21 亿）上限。**大张量索引一律用 64 位（`int64`/`long`/`size_t`）**。PyTorch 因此有 `int32`/`int64` 索引模式之分。

6. **稀疏矩阵里存了显式的 0。** CSR 的 `nnz` 应当只数**非零元**。若不小心把结构性的 0 也存进去，`nnz` 虚高，SpMV 白算。浮点计算产生的「接近 0」的元素是否清理，是稀疏化时要权衡的（清了省算力，清了又可能影响数值）。

7. **三重循环写矩阵乘却不考虑 cache 次序。** 直接写 `i-j-k` 会慢好几倍（见 4.3）。要么用分块，要么直接调 BLAS——**永远不要手写朴素三重循环去跑大矩阵乘。**

8. **以为「对称矩阵只存一半」总是省。** 只存一半会让**并行/向量化**变难（索引映射 $i(i+1)/2+j$ 有整数除法、跨行跳跃）。有时为了算得快，反而**存全**。空间与速度的权衡，视场景而定。

---

## 与 AI / 世界模型的连接

这一章的主题几乎就是「深度学习系统的底层」。逐条对照：

### 1. 张量 = 存储 + shape + strides（最核心的一条）

```python
x = torch.randn(2, 3, 4)
x.shape        # torch.Size([2, 3, 4])        逻辑形状
x.stride()     # (12, 4, 1)                   步长
x.storage_offset()   # 0                       偏移
x.T.stride()   # 换轴后 stride 变了，数据没动
```

- 为什么 `.transpose()` 是 $O(1)$、`.contiguous()` 是 $O(n)$？
- 为什么 `.view()` 有时报错、`.reshape()` 却能自动处理？
- 为什么广播不占额外内存？

**答案全部是 strides。** 掌握它，你读 PyTorch 源码、看性能剖析（profiler）时就不再是「黑盒调 API」。

### 2. NCHW / NHWC 与卷积性能

cuDNN、TensorRT 里都有 `NHWC` 选项。换布局能显著影响卷积速度，因为不同布局下「同一空间位置多通道」的访存是否连续不同（4.2.4）。**部署推理引擎时，选对内存格式常常是免费的性能提升。**

### 3. GEMM / cuBLAS / 分块 / FlashAttention

矩阵乘是 Transformer 的算力主体。高性能实现全是**分块 + 打包 + 提高 cache/共享内存命中率**（4.3）。FlashAttention 就是把「分块思想」用到注意力上，把 $O(n^2)$ 的中间矩阵从显存搬到片上内存、分块累加，从而大幅省显存、提速。你会在读世界模型的注意力变体时反复遇到这个思路。

### 4. 稀疏张量：GNN、稀疏注意力、嵌入表

CSR/CSC（4.5）是稀疏数据的通用语言：

- **GNN**：邻接矩阵稀疏，消息传递就是稀疏矩阵乘 + 聚合；
- **稀疏注意力**（Longformer、BigBird）：只算部分注意力对；
- **嵌入表 / 推荐**：巨大但每行样本只激活少数特征（超大稀疏矩阵乘）。

### 5. 特殊结构与「省一半」

因果注意力是**三角**的、注意力得分常可**对称**化（4.4）。利用这些结构能省下近一半算力。**识别问题里的对称性/稀疏性/带宽结构，是研究里最能「白赚」性能的地方。**

### 6. 世界模型里的时空张量

世界模型处理视频/交互序列时，张量常是 `(B, T, C, H, W)`（batch、时间、通道、高、宽）。**时间维是否连续、通道维放哪**，直接决定时空注意力和卷积核的访存效率。你会经常在「`(B,T,C,H,W)` vs `(B,C,T,H,W)`」之间权衡——现在你知道，选的其实是 **strides**。

**一句话**：如果说前两章教你「怎么存一堆数」，这一章教你「**怎么把数摆成网格并高效地读它们**」——而这正是深度学习性能的全部战场。

---

## 本章小结

- 多维数组必须拍平成一维；**行主序**（C/Python）与**列主序**（Fortran/MATLAB/BLAS）是两套约定，地址公式不同；
- 张量 = 一维存储 + `shape` + `strides` + `offset`；地址 = $\text{offset} + \sum_k \text{index}_k\cdot\text{stride}_k$；
- **`transpose`/`permute`/`slice`/`broadcast` 都是改 strides（零拷贝）；只有 `contiguous` 搬数据**；
- **连续性** = strides 等于标准行主序；跨步访存会因 cache 变得很慢（实测 10x+）；
- **NCHW vs NHWC** 是同一数据的两种 strides，影响卷积访存效率；
- 矩阵乘三种循环次序运算次数相同，但**最差比最好慢 15 倍**（全是 cache）；解法是分块/打包，这催生了 FlashAttention；
- 特殊矩阵（对称/三角/对角/带状）靠**索引映射**压缩存储；稀疏矩阵用 **CSR/CSC** 把「图」压成连续数组，是 GNN / 稀疏注意力 / 嵌入表的底层。

---

## 练习

### 基础

1. 手算 $4\times5$ 矩阵中 `M[3][2]` 在行主序、列主序下的偏移，再用 `code/04_matrix_rowcol.cpp` 里的函数验证。
2. 写出 $3\times4\times5$ 张量行主序的标准 `strides`，并用「最后一维步长为 1、往前累乘」的规则核对。
3. 给 `Tensor::permute` 补一个断言：`order` 必须是 `0..rank-1` 的一个排列。思考为什么 permute 必须要求它是排列（否则会怎样）。
4. 把 `code/04_sparse_csr.cpp` 的 CSR 补一个 `CSC` 版本，并实现 CSC 格式的 SpMV。

### 进阶

5. 实现一个**真正零拷贝的切片** `slice(dim, start, end)`：只改 `offset`、`shape`、`strides`，不拷贝数据（提示：新 `offset = offset_ + start*strides[dim]`，新 `shape[dim] = end-start`）。用断言验证切片后的逻辑值与「拷贝版」一致。
6. 实现**广播加法**：`(3,4)` 加 `(4,)`，靠把 `(4,)` 的第 0 维设成 stride 0 实现零拷贝广播，并验证结果与「显式扩展后相加」一致。
7. 给矩阵乘加上**分块（blocking）**：把 $n\times n$ 分成 $b\times b$ 小块，内层用 `i-k-j`。测不同 $b$（16/32/64/128）的耗时，画出「块大小 vs 时间」的趋势，并解释为什么太小（cache 装不满）和太大（装不下）都不好。
8. 用 `code/04_tensor_stride.cpp` 的 `Tensor` 实现「按列求和」在 NCHW 与 NHWC 下的耗时对比，验证「NHWC 下按通道求和更快」。

### 挑战

9. **实现一个最小 FlashAttention**（CPU 版即可）：不要显式构造 $n\times n$ 的注意力矩阵，而是分块计算 $QK^\top$、在线（online softmax）累积得到 $\text{softmax}(QK^\top)V$。用分块版与朴素版对比数值一致性与峰值内存。
10. **为稀疏矩阵设计一个「行分块」的 SpMV**（类似 CSR 的 block-row 变体），让每行内的列索引连续、便于向量化，并与朴素 CSR SpMV 比性能。
11. **实现一个「张量布局转换器」**：给定任意 shape/strides 的张量，写一个通用 `toContiguous`（本文件已有雏形），再写一个通用的「按任意目标布局重排」的函数，并证明对同一物理数据，任意两次布局变换的复合仍等价于直接变换（即布局变换构成一个作用在元数据上的群）。

<details>
<summary>参考答案（要点）</summary>

**1.** $M[3][2]$：行主序 $=3\times5+2=17$；列主序 $=2\times4+3=11$。

**2.** 形状 $(3,4,5)$：从后往前累乘，$st_2=1,\ st_1=5,\ st_0=20$，即 `[20,5,1]`。

**3.** `order` 必须是 $0..rank-1$ 的排列，否则新 `shape`/`strides` 会重复包含某一维而漏掉另一维，逻辑张量的元素个数与底层存储不再对应（会读到错位的数据、甚至越界）。断言：`std::is_permutation(order.begin(), order.end(), iota)`。

**4.** CSC：`colPtr`（长度 `cols+1`）+ `rowIdx` + `vals`。SpMV 时对每一列，把 `vals[j]*x[col]` 累加到 `y[rowIdx[j]]`。

**5.** 
```cpp
Tensor slice(long dim, long start, long end) const {
    auto sh = shape_, st = strides_;
    long newOffset = offset_ + start * strides_[dim];
    sh[dim] = end - start;
    return Tensor(data_, sh, st, newOffset);
}
```
注意 `strides` 不变，只有 `offset` 和该维 `shape` 变。

**6.** 广播：把 `(4,)` 视为 `shape=[1,4], strides=[0,4]`，再与 `(3,4)` 相加（两维分别取「较大」的 shape，较小且为 1 的那维 stride 置 0）。验证时与「把 `(4,)` 复制 3 份成 `(3,4)` 再相加」逐元素比较。

**7.** 分块后内层 `i-k-j` 访问的 $A,B$ 块都能驻留 cache；块太小则每次只有少量数据、cache 利用率低；块太大则块本身装不进 cache，退化成跨步访问。最佳 $b$ 由 cache 大小决定（经验值：让「$A$ 的一块 + $B$ 的一块 + $C$ 的一块」小于 L1/L2）。

**8.** NCHW 下按通道（第 1 维，stride 大）求和是跨步访问；NHWC 下通道在最后一维、stride = 1，顺序访问更快。

**9.** FlashAttention 核心是**在线 softmax**：逐块算 $S=QK^\top$，维护运行最大值 $m$ 与运行和 $\ell$，每来一块就按新 $m$ 重标定并累积到输出 $O$。全程不物化 $n\times n$ 矩阵，峰值内存从 $O(n^2)$ 降到 $O(n)$（或 $O(n\cdot d)$）。数值上分块版与朴素版应在浮点误差内一致。

**10.** Block-row SpMV：把每行再切成「列号连续的小段」，段内列号连续 ⇒ 可向量化；再对多行做 tiling 以复用 $x$ 的 cache。本质与稠密矩阵分块同理。

**11.** 因为「布局变换」只作用在元数据 $(shape, strides, offset)$ 上、不触碰底层数据，所以任意两次变换的复合就是把元数据连续变换两次，等价于直接变换（可对同一物理数组验证：两条路径得到的逻辑值逐元素相同）。

</details>

---

## 延伸阅读

- **Sahni《数据结构、算法与应用（C++ 版）》第 2 版 第 7 章**「Arrays and Matrices」：多维数组映射、特殊矩阵压缩、稀疏矩阵的三元组与转置；
- **NumPy 文档「Internal memory layout of an ndarray」**：`shape`/`strides`/`contiguity` 的权威说明；
- **PyTorch 文档「Tensor Views」与 `torch.as_strided`**：官方对「视图 = 改 strides」的解释；
- **《Computer Systems: A Programmer's Perspective》(CSAPP) 第 6 章「The Memory Hierarchy」**：分块（blocking）优化矩阵乘的经典推导；
- **FlashAttention 论文**（Dao et al., 2022）：IO-aware 注意力，本章分块思想的高峰；
- **CSR 格式**：`scipy.sparse` 文档、DGL/PyG 的稀疏张量说明；
- 配套代码：`code/04_matrix_rowcol.cpp`、`code/04_matmul_cache_order.cpp`、`code/04_sparse_csr.cpp`、`code/04_tensor_stride.cpp`。
