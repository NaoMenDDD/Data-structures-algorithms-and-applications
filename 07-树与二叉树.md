# 第 07 章 树与二叉树

> 前面六章讲的都是**线性结构**：数组、链表、栈、队列。它们的共同点是「一个接一个」，每个元素最多一个前驱、一个后继。
>
> **树（tree）** 打破了这个限制：一个结点可以有**多个孩子**。于是「层级」「分支」「嵌套」这些概念第一次有了自然的表示。树是本书后半程的**枢纽**——搜索树、堆、并查集、B 树、决策树、语法树、KD-树，全都是树的不同形态。
>
> **二叉树（binary tree）** 是最重要的一种树：每个结点最多两个孩子。它简单到能用一行结构体定义，却又深刻到撑起了**编译器（抽象语法树）、数据库索引（B+ 树）、机器学习（决策树 / GBDT）、空间检索（KD-树）**。这一章我们把二叉树「一次性吃透」：存储、遍历、重建、性质、以及两个高阶技巧（Morris 遍历、KD-树）。

---

## 本章目标

读完并跑完本章代码，你应该能够：

1. 说清树的**术语**（根、叶、度、深度、高度、满/完全二叉树），并区分「高度按边数」和「按结点数」两种约定；
2. 掌握二叉树的**链式**与**顺序（数组）**两种存储，知道完全二叉树为什么适合用数组；
3. 熟练写出**四种遍历**（前序/中序/后序/层序）的**递归**版本，并说清它们各自的用途；
4. 把前序/中序/后序遍历**改写成非递归**（显式栈）版本，掌握后序遍历的三种写法；
5. 会**由前序 + 中序重建**二叉树，说明为什么「前序 + 后序」无法唯一确定一棵树；
6. 求树的**高度、直径、平衡、对称**，验证经典结论 **$n_0 = n_2 + 1$**（叶子数 = 度为 2 的结点数 + 1）与「$n$ 个结点的**最小**高度 $=\lceil\log_2(n+1)\rceil-1$」；
7. 掌握 **Morris 遍历**与**线索二叉树**，用 $O(1)$ 额外空间做中序遍历；
8. 求**最近公共祖先（LCA）**，并用 **KD-树**把二叉树的思想延伸到**高维空间检索**，亲眼看到**维度灾难**。

---

## 前置知识

- 第 05 章（栈）—— 非递归遍历全靠显式栈；
- 第 06 章（队列）—— 层序遍历靠队列；
- 第 03 章（链表）—— 链式二叉树的结点就是一个「带两个 next 的链表结点」；
- 第 01 章（算法分析）—— 递归式的复杂度分析（树高、$O(n\log n)$ 建树）。

---

## 7.1 树的术语与二叉树

**树** 是 $n$ 个结点的有限集合：有一个特殊的**根（root）**，其余结点分成若干**互不相交**的子树，每棵子树又是一棵树。**关键的「不相交」**意味着：树里**没有环**，且任意两个结点之间**恰有一条路径**。

常用术语（务必记牢，后面全靠它们）：

| 术语 | 含义 |
|---|---|
| **根（root）** | 唯一的顶层结点，没有父结点 |
| **叶（leaf）** | 没有孩子的结点，度为 0 |
| **度（degree）** | 一个结点拥有的孩子个数；**结点的度 ≤ 2** 的树是二叉树 |
| **父/子/兄弟** | 直接的上层 / 下层 / 同父结点 |
| **路径长度** | 两结点间边的条数 |
| **深度（depth）** | 从根到该结点的边数（根深度 0） |
| **高度（height）** | 从该结点到最远叶子的边数（叶高度 0） |
| **层（level）** | 深度相同的一批结点 |

**两种高度的约定**（很容易搞混，一定要在代码里注释清楚）：

- **按边数**：空树高度 $= -1$，单个结点高度 $= 0$；
- **按结点数**：空树高度 $= 0$，单个结点高度 $= 1$。

本章代码统一用**按边数**，注意 `height(nullptr) == -1`。

**几种特殊二叉树**：

- **满二叉树（full）**：每个结点要么是叶、要么有两个孩子；
- **完全二叉树（complete）**：除最后一层外全满，且最后一层结点**从左到右连续**排列。**完全二叉树能用数组紧凑存储**（7.2 节）；
- **完美二叉树（perfect）**：所有叶子在同一层，结点数 $= 2^{h+1}-1$。

---

## 7.2 二叉树的存储

### 链式存储（最常用）

每个结点存「值 + 左孩子指针 + 右孩子指针」：

```cpp
struct Node {
    int   val;
    Node* left;
    Node* right;
};
```

访问任意结点只需 $O(1)$；每个结点多两个指针的开销（8 或 16 字节）。

### 顺序存储（数组）

把结点按**层序**编号（这里用**下标从 0 开始**），则下标 $i$ 的结点：

- 左孩子下标 $2i+1$，右孩子下标 $2i+2$，父结点下标 $\lfloor (i-1)/2 \rfloor$。

**只有完全二叉树才适合**用数组存——否则稀疏的树会浪费大量空位（一棵退化成链的 $n$ 结点树需要 $2^n$ 大小的数组！）。**这个「下标算术」正是堆（第 08 章）的底层**，也是 KV cache 分页、线段树的基础。

`code/07_tree_properties.cpp` 里的 `buildCompleteFromLevel` 就是用这套下标从层序数组构造完全二叉树：

```cpp
Node* buildCompleteFromLevel(const vector<int>& a, int i, int n) {
    if (i >= n) return nullptr;
    Node* r = new Node(a[i]);
    r->left  = buildCompleteFromLevel(a, 2 * i + 1, n);   // 左孩子
    r->right = buildCompleteFromLevel(a, 2 * i + 2, n);   // 右孩子
    return r;
}
```

| 维度 | 链式 | 顺序（数组） |
|---|---|---|
| 定位孩子 | 指针，$O(1)$ | 下标算术，$O(1)$ |
| 空间 | 每结点 2 指针 | 完全二叉树零浪费；稀疏树灾难 |
| 缓存 | 差（结点分散） | **好**（连续内存） |
| 适用 | 任意二叉树 | 完全二叉树（堆、线段树） |

---

## 7.3 四种遍历：递归版

「遍历」= 按某种规则**每个结点访问且只访问一次**。二叉树有三种深度优先的次序，区别只在**根的访问时机**：

| 遍历 | 次序 | 一句话记法 |
|---|---|---|
| **前序（preorder）** | 根 → 左 → 右 | 先记根 |
| **中序（inorder）** | 左 → 根 → 右 | 中间记根 |
| **后序（postorder）** | 左 → 右 → 根 | 最后记根 |

```cpp
void preorderRec(Node* r, vector<int>& out) {
    if (!r) return;
    out.push_back(r->val);        // 根
    preorderRec(r->left, out);    // 左
    preorderRec(r->right, out);   // 右
}
```

（中序、后序只需把「根」那一行挪到中间 / 最后。）

**它们的用途**（不只是考试题）：

- **前序**：复制一棵树、序列化、打印目录结构（先输出父再输出子）；
- **中序**：**二叉搜索树的中序是升序序列**（第 09 章的核心）；表达式树的中序还原中缀表达式；
- **后序**：**先算孩子再算父**——释放内存、计算子树高度/直径、表达式树的求值、**反向传播（计算图的后序即反向拓扑序）**。

**第四种：层序（level order）** —— 用**队列**「一层一层」访问，是 **BFS** 在树上的特例：

```cpp
vector<vector<int>> levelOrder(Node* root) {
    vector<vector<int>> res;
    if (!root) return res;
    queue<Node*> q; q.push(root);
    while (!q.empty()) {
        int sz = q.size();                 // 当前层的结点数
        vector<int> level;
        for (int i = 0; i < sz; ++i) {     // 一次处理完一整层
            Node* cur = q.front(); q.pop();
            level.push_back(cur->val);
            if (cur->left)  q.push(cur->left);
            if (cur->right) q.push(cur->right);
        }
        res.push_back(std::move(level));
    }
    return res;
}
```

`sz = q.size()` 这个「先把当前层数量记下来」的技巧，是「层序分组」的关键——它把队列里「已有的本层结点」和「新入队的下层结点」分开了。

---

## 7.4 非递归遍历：把递归的「栈」提出来自己管

第 05 章说过：**任何递归都能改写成「显式栈 + 循环」**。遍历正是最好的例子——而且改写的三种遍历难度依次递增，是理解「显式栈」的绝佳教材。

### 前序：最简单

把根压栈；每次弹出一个、访问它，然后**先压右、后压左**（这样左孩子先出栈）：

```cpp
st.push(root);
while (!st.empty()) {
    Node* cur = st.top(); st.pop();
    out.push_back(cur->val);
    if (cur->right) st.push(cur->right);   // 先压右
    if (cur->left)  st.push(cur->left);    // 后压左 => 左先访问
}
```

### 中序：一路向左，弹一个、转向右

```cpp
Node* cur = root;
while (cur || !st.empty()) {
    while (cur) { st.push(cur); cur = cur->left; }   // 走到最左
    cur = st.top(); st.pop();
    out.push_back(cur->val);                         // 访问根
    cur = cur->right;                                // 转向右子树
}
```

### 后序：三种写法

后序最难，因为「访问根」必须等**左右子树都处理完**。三种经典写法：

**写法 A（取巧）**：按「根-右-左」做**镜像前序**（压栈顺序改成先左后右），最后把结果**整体反转** → 得到「左-右-根」：

```cpp
st.push(root);
while (!st.empty()) {
    Node* cur = st.top(); st.pop();
    out.push_back(cur->val);
    if (cur->left)  st.push(cur->left);    // 先压左
    if (cur->right) st.push(cur->right);   // 后压右 => 右先出 => 得到 根-右-左
}
std::reverse(out.begin(), out.end());
```

**写法 B（单栈 + 记录上次访问的结点）**：模拟递归的「回溯」——只有当**右子树已处理完**（`top->right == last`）才输出根：

```cpp
Node* cur = root; Node* last = nullptr;
while (cur || !st.empty()) {
    while (cur) { st.push(cur); cur = cur->left; }
    Node* top = st.top();
    if (top->right && top->right != last) {
        cur = top->right;                  // 右子树还没处理，转进去
    } else {
        out.push_back(top->val);           // 右子树已处理（或为空），输出根
        last = top; st.pop();
    }
}
```

**写法 C（双栈 / 标记法）**：压入「(结点, 是否已展开)」二元组，未展开的重新压入并压两个孩子。第 05 章的 `backwardDFS` 就用了这个套路。

三种写法结果完全一致（代码里有断言）：

```
前序(递归): [ 1 2 4 5 3 6 7 ]
前序(迭代): [ 1 2 4 5 3 6 7 ]
中序(递归): [ 4 2 5 1 6 3 7 ]
中序(迭代): [ 4 2 5 1 6 3 7 ]
后序(递归): [ 4 5 2 6 7 3 1 ]
后序(迭代-反转): [ 4 5 2 6 7 3 1 ]
后序(迭代-单栈): [ 4 5 2 6 7 3 1 ]
层序: [1] [2,3] [4,5,6,7] 
LCA(4,5)=2  LCA(4,6)=1  LCA(6,7)=3
```

> **为什么值得学非递归？** 不只是「省栈」——**递归遍历的深度 = 树高**，遇到退化成链的树会**栈溢出**。显式栈把深度放到堆上，可控。而且显式栈的遍历可以**中途暂停、恢复、序列化**（迭代器、协程），这是递归做不到的。

---

## 7.5 由「前序 + 中序」重建二叉树

**问题**：给一棵树的前序和中序序列，重建这棵树。

**思路**：前序的**第一个**元素必是**根**；在中序里找到根的位置，**左边是左子树的中序、右边是右子树的中序**；据此也能切出左右子树的**前序**区间，递归下去：

```cpp
Node* buildPreIn(pre, pl, pr, in, il, ir, inPos) {
    if (pl > pr) return nullptr;
    int rootVal = pre[pl];
    int k = inPos.at(rootVal);              // 根在中序中的位置（哈希表 O(1)）
    int leftLen = k - il;                   // 左子树结点个数
    Node* root = new Node(rootVal);
    root->left  = buildPreIn(pre, pl+1, pl+leftLen, in, il, k-1, inPos);
    root->right = buildPreIn(pre, pl+leftLen+1, pr, in, k+1, ir, inPos);
    return root;
}
```

用**哈希表**把「在中序里找根」从 $O(n)$ 降到 $O(1)$，总复杂度 $O(n)$。

**为什么「前序 + 后序」不行？** 因为对于「只有一个孩子」的结点，前序和后序都**无法区分这个孩子是左还是右**。例如中序不同、前序后序却相同的树不止一棵。**必须有一个「中序」来提供左右信息**。

> **反序列化 / 序列化树**是模型保存、AST 存储的基础。前端框架（React 的虚拟 DOM diff）、编译器的语法树持久化，都在做类似的「序列化—重建」。

---

## 7.6 树的经典性质与问题

### 高度、直径、平衡、对称

这四个都是「后序遍历」的天然应用（先搞定孩子，再算自己）：

- **高度**：`1 + max(高(左), 高(右))`；
- **直径（diameter）**：任意两结点的最长路径。**关键观察**：最长路径一定「穿过」某个结点，长度 = 该结点左右子树高度之和 $+2$。一次后序遍历即可 $O(n)$ 求出（而不是「对每个结点求高度再取 max」的 $O(n^2)$）；
- **平衡（balanced）**：任一结点左右子树高度差 ≤ 1。用「返回 $-1$ 表示已不平衡」**剪枝**，仍是 $O(n)$，不必对每棵子树重复求高度；
- **对称（symmetric）**：`mirror(左, 右)` 镜像比较——`a->val == b->val && mirror(a->left, b->right) && mirror(a->right, b->left)`。

> **易错点**：一棵「形状满」的树**未必值对称**。例如层序 `{1,2,3,4,5,6,7}`：形状是完美二叉树，但左孩子是 2、右孩子是 3，`isSymmetric` 应为 **false**。真正值对称的完美二叉树要让孩子成镜像，如 `{1,2,2,3,4,4,3}`。**这个坑我在这份代码里真实踩过并修正了**（见 7.6 代码注释）。

### 经典结论：$n_0 = n_2 + 1$

**内容**：任何非空二叉树，**叶子结点数 = 度为 2 的结点数 + 1**。

**证明**（一句话版）：设结点总数 $n = n_0 + n_1 + n_2$。从上往下数「孩子边」共有 $n_1 + 2n_2$ 条；从下往上数「每个非根结点恰有一条入边」共 $n - 1 = n_0 + n_1 + n_2 - 1$ 条。两者相等 → $n_0 = n_2 + 1$。$\blacksquare$

代码对 200 棵**随机生成的 BST** 逐一验证：

```
随机 BT 上 n0 = n2 + 1 验证通过（200 次）
```

### $n$ 个结点的最小高度

**内容**：$n$ 个结点的二叉树最小高度（按边数）= $\lceil \log_2(n+1) \rceil - 1$。直觉：让每一层都塞满，层数最少。

```
n=1..64 完全二叉树高度 = ceil(log2(n+1))-1 验证通过
```

> **这个结论是「平衡树」的动机**：$n$ 个结点的**平衡**二叉树高度 $O(\log n)$，所以查找是 $O(\log n)$；而**不平衡**的树（如按顺序插入的 BST）高度退化成 $O(n)$，查找变成 $O(n)$——这正是第 09 章 AVL / 红黑树要解决的问题。

---

## 7.7 Morris 遍历与线索二叉树：$O(1)$ 空间的遍历

前面的非递归遍历都用了 $O(h)$ 的栈。有没有办法用 **$O(1)$ 额外空间**做中序遍历？有，而且很美。

### 核心思想：借用「空闲的右指针」

中序遍历时，一个结点的「下一个」是它的**中序后继**。对于**右指针为空的叶子/结点**，这个指针是浪费的——何不**临时用它指向自己的中序后继**？

- 若当前结点有左子树，找到左子树的**最右结点**（中序前驱）`pred`：
  - 若 `pred->right == null`：**第一次**到达，把 `pred->right = cur`（建线索），进入左子树；
  - 若 `pred->right == cur`：**第二次**到达（左子树走完了），**拆除线索**（复原树结构）、访问 `cur`、进入右子树；
- 若当前结点无左子树：直接访问，进入右子树。

每个结点被访问两次、每条边走两遍，仍是 $O(n)$，但**额外空间 $O(1)$**：

```cpp
Node* pred = cur->left;
while (pred->right && pred->right != cur) pred = pred->right;
if (!pred->right) { pred->right = cur; cur = cur->left; }   // 建线索
else { pred->right = nullptr; out.push_back(cur->val); cur = cur->right; }  // 拆线索
```

```
Morris 中序遍历 == 有序序列，且不改动树结构
```

（代码里跑了两遍遍历，断言结果相同，证明它**复原了树结构**。）

### 线索二叉树（threaded binary tree）

如果**永久地**把这些空闲右指针存成线索（用 `rightThread` 布尔标记区分「线索」还是「真孩子」），就得到一个**线索二叉树**：中序遍历可以**不用栈、不用递归**，$O(1)$ 空间直接沿着线索走。

```
线索二叉树无栈中序遍历: 1 2 3 4 5 6 7 
```

| 方案 | 时间 | 额外空间 | 是否改动树 |
|---|---|---|---|
| 递归 / 显式栈 | $O(n)$ | $O(h)$ | 否 |
| **Morris** | $O(n)$（常数更大） | $O(1)$ | 遍历时临时改，遍历后复原 |
| **线索二叉树** | $O(n)$ | $O(1)$ | 永久改动（存线索） |

> **落地价值**：内存极度受限的嵌入式系统、以及「遍历过程中不能有任何分配」的实时场景。**在 AI 里**，这类「把结构信息编码进指针本身」的思想，和「用连续数组存不规则结构（CSR、paged KV cache）」是同一哲学——**用更聪明的内存布局省掉额外开销**。

---

## 7.8 最近公共祖先（LCA）

**问题**：二叉树里两个结点 $p, q$ 的**最近公共祖先**，是最低的、同时是二者祖先的结点。

**朴素递归**（一次遍历，$O(n)$）：

```cpp
Node* lca(Node* root, Node* p, Node* q) {
    if (!root || root == p || root == q) return root;
    Node* L = lca(root->left,  p, q);
    Node* R = lca(root->right, p, q);
    if (L && R) return root;        // p、q 一左一右 => root 就是分叉点
    return L ? L : R;               // 都在某一侧，往上传递
}
```

**核心洞见**：若 $p, q$ 分别落在某结点的**左右两侧**，该结点就是 LCA；若都在同一侧，答案在那个子树里。返回值「向上传递」的写法很值得体会。

```
LCA(4,5)=2  LCA(4,6)=1  LCA(6,7)=3
```

> **AI 关联**：LCA 在**树形结构**里无处不在——**语法树里两个变量的最近公共作用域**、**决策树里两个样本的最近公共分裂点**、**AST 里两个子表达式的公共子表达式识别（CSE）**。更一般地，「在树上找两个东西的汇合点」是分层聚类、层次化 RL 的常见子问题。

---

## 7.9 KD-树：把二叉树伸进高维空间

普通二叉树按「值的比较」分叉（左小右大）。**KD-树（k-dimensional tree）** 换了分叉依据：**每一层轮流挑一个维度作切分轴，按该维坐标把点集切成两半**。

```
   2 维 KD-树（根在第 0 维切，孩子在第 1 维切 ...）:

                    (x 轴切分)
            ┌──────────┼──────────┐
        左半区                右半区
            │                     │
        (y 轴切分)            (y 轴切分)
        ┌───┴───┐             ┌───┴───┐
      更下     更上          更下     更上
```

**构造**：每层选当前维度，用 `nth_element` 取**中位数**作切分点——保证树**平衡**，高 $\approx \log_2 n$，总建树 $O(n \log n)$。

**查询（1-NN）**：分两步——**先深入"近"侧**，再用「目标到切分面的距离」判断**"远"侧是否可能更近**，不可能就**整块剪掉**：

```cpp
nnSearch(nearChild, target, best);                       // 先走近侧
if (diff * diff < best.dist2) nnSearch(farChild, ...);   // 剪枝：远侧够不着才搜
```

这颗树在 2 维里剪枝效果惊人（200000 个点，平均只访问约 25 个）：

```
=== 2. 剪枝有多强？访问结点数（2 维, N=200000） ===
  N = 200000，查询 500 次
  KD-树平均访问结点数 = 24.6（暴力要访问全部 200000 个）
  平均只访问 N 的 0.0123% —— 即少访问约 99.99% 的结点
```

**k-NN**：把「当前最优」换成一个**大小为 $k$ 的最大堆**，堆顶就是「当前第 $k$ 近距离」，作为剪枝半径。代码里 $k$-NN 与暴力排序逐一比对，完全一致。

### ★ 维度灾难：KD-树的阿喀琉斯之踵

剪枝依赖「切分面能把空间分成明显更近/更远的两半」。但维度一高，点的距离**趋于集中**（「几乎所有点都差不多远」），切分面再也排除不掉远侧——**KD-树退化成暴力搜索**：

```
  dim  平均访问结点数   占 N 比例
  ---  --------------   ---------
   2             21      0.001
   4             62      0.003
   8            913      0.046
  16          19458      0.973
  32          20000      1.000
```

**这张表是全章最该记住的 AI 连接**：它解释了为什么**近似最近邻（ANN）**、**LSH**、**HNSW**、**向量数据库（Faiss / Milvus / ScaNN）** 要存在——在几百上千维的 embedding 空间里，精确 KD-树没用，必须**用一点点精度换取可扩展性**。第 13 章（哈希 / LSH）会接着讲。

| 维度 | KD-树 | 暴力 |
|---|---|---|
| 建树 | $O(n\log n)$ | — |
| 1-NN 查询（低维） | 期望 $O(\log n)$ | $O(n)$ |
| 1-NN 查询（高维） | 退化 $O(n)$ | $O(n)$ |
| 空间 | $O(n)$ | $O(n)$ |

---

## 7.10 复杂度对照

| 操作/算法 | 时间复杂度 | 空间 | 备注 |
|---|---|---|---|
| 四种遍历（递归/迭代） | $O(n)$ | $O(h)$ 栈 | $h$ = 树高 |
| 层序遍历（BFS） | $O(n)$ | $O(w)$ 队列 | $w$ = 最大宽度 |
| Morris / 线索遍历 | $O(n)$ | $O(1)$ | 常数更大 |
| 由前序+中序重建 | $O(n)$ | $O(n)$ | 哈希表定位根 |
| 高度 / 直径 / 平衡 / 对称 | $O(n)$ | $O(h)$ | 后序一次遍历 |
| LCA（朴素） | $O(n)$ | $O(h)$ | 单次查询 |
| KD-树 建树 | $O(n\log n)$ | $O(n)$ | 中位数切分 |
| KD-树 1-NN（低维） | 期望 $O(\log n)$ | $O(\log n)$ | 高维退化 $O(n)$ |

---

## 常见错误与陷阱

1. **高度约定不统一。** 「空树高度是 $-1$ 还是 $0$？」必须**在代码开头注释清楚并全文件统一**。混用会导致 `1 + max(...)` 到处差 1，且极难 debug。本章统一用**边数**（空树 $-1$）。

2. **直径写成「对每个结点求高度再取 max」。** 那样是 $O(n^2)$。正解是**一次后序遍历**，在递归里同时更新「穿过当前结点的最长路径」和「返回给父结点的高度」。

3. **平衡判断重复求高度。** `abs(height(l) - height(r)) <= 1 && isBalanced(l) && isBalanced(r)` 会重复遍历，退化成 $O(n^2)$。用「返回 $-1$ 表示已不平衡」一次搞定。

4. **把「形状对称」当成「值对称」。** 见 7.6 的坑：完美二叉树未必值对称。`isSymmetric` 判的是**值的镜像**。

5. **非递归后序写错访问时机。** 后序必须等**右子树处理完**才输出根。写法 B 里 `last` 变量就是「右子树是否已处理」的标记，漏了它会输出错序或死循环。

6. **重建时忘了「前序 + 后序」不够。** 只有前序+后序无法唯一确定树，必须有一个中序。别在只有前后序的数据上硬重建。

7. **字符串字面量里混入全角引号。** 写中文注释/输出时，`"..."` 用了全角的 `“ ”` 或 `" "` 会**直接编译报错**（字符串提前结束）。本章代码里就踩过一次。**中文里的引号一律用「」或代码里转义**。

8. **KD-树的切分轴记错。** 是 `depth % dim`（按**深度**轮换），不是按结点编号。写错会导致树严重不平衡、剪枝失效。

9. **KD-树查询忘了先走「近」侧。** 顺序反了（先走远侧）虽然结果仍对，但**剪枝效果大幅下降**（当前最优不紧）。**先近后远**才让剪枝半径尽快收缩。

10. **高维数据上迷信 KD-树。** 维度 > 20 左右，KD-树的剪枝就基本失效（见 7.9 的表）。此时应转向 ANN 方法，而不是调 KD-树的参数。

---

## 与 AI / 世界模型的连接

树是「层级」的数据结构，而 AI 里到处都是层级：语法的层级、空间的层级、决策的层级、任务的层级。

### 1. ★ 决策树与 GBDT

**决策树**就是一棵**二叉树**，每个内部结点是一个**特征阈值判断**（「年龄 > 30？」），每条边是一次分裂，叶子是预测。**中序遍历**能按顺序列出所有分裂点；**后序遍历**能自底向上做**剪枝**（先算子树，再决定是否保留这个分裂）。**随机森林 / GBDT / XGBoost / LightGBM** 是树模型的集大成者，至今仍是表格数据（推荐、风控、特征工程）的王者。**树的分裂点选择**本质上和 KD-树选切分轴是同一件事——**用某个维度的阈值把样本分成更"纯"的两半**。

### 2. ★ KD-树与向量检索

第 7.9 节的 KD-树是**最近邻检索**的最小内核。在 AI 里，它对应：

- **KNN 分类 / 检索**：embedding 空间里找最相似的样本；
- **点云处理**：3D 感知、SLAM、NeRF 里找邻域点；
- **世界模型的状态检索**：在「状态 / 观测 embedding」空间里找相似的历史状态，做检索增强的规划或想象。

而**维度灾难**那张表解释了为什么工程上最终都用 **HNSW / IVF / PQ（乘积量化）** 这类 ANN——**理论最优 ≠ 工程最优**，这是深度学习系统里反复出现的主题。

### 3. ★ 抽象语法树（AST）与图编译器

你写的每一行代码，编译器第一件事就是把它解析成 **AST（抽象语法树）**。PyTorch 的 `torch.compile`、TVM、XLA 会把计算变成图 / 树，然后做**遍历**来优化：

- **前序遍历**：打印 / 序列化表达式；
- **后序遍历**：计算子树、做**常量折叠**、释放内存；
- **中序遍历**：还原中缀表达式（可读输出）。

**「对 AST / 计算图做后序遍历」就是编译器优化的基本动作**，而它正是本章 7.4 节的内容。

### 4. 分层强化学习与选项（options）

**分层 RL** 把任务拆成「高层策略选子目标、低层策略完成子目标」，天然是一棵树（任务分解树）。**Options framework**、**HIRO**、**FeUdal Networks** 都用树 / 层级结构组织时间尺度上的抽象。**世界模型的想象 rollout** 也可以组织成树（一次想象多种未来分支）。

### 5. 场景树与物理仿真

3D 仿真、游戏引擎、机器人仿真里，场景用**树**组织（世界 → 刚体 → 关节 → 部件），空间关系用**八叉树 / KD-树**加速碰撞检测与邻域查询。世界模型要在这样的结构化世界里做预测，树是不可或缺的表示。

### 6. 概率图模型里的树

**树宽（treewidth）** 小 = 图上推断可高效进行。HMM 的前向后向、CRF 的维特比，本质都是**在树 / 链上做动态规划**——**树是「可高效推断」的图**。

**一句话**：二叉树教你「**如何用分叉组织信息**」。从决策树到 AST 到 KD-树，分叉的维度变了（特征、语法、空间坐标），但「用层级剪掉一半可能性」的思想一以贯之。

---

## 本章小结

- **树** 是「无环 + 任意两点唯一路径」的层级结构；**二叉树** 每结点最多两个孩子；
- **链式存储**通用、**顺序存储**只适合完全二叉树（但它是堆的底层）；下标关系 $2i+1,\ 2i+2,\ (i-1)/2$；
- **四种遍历**：前/中/后序（DFS，靠栈）+ 层序（BFS，靠队列）；后序是「先算孩子」的关键；
- 非递归遍历 = 把递归的栈提出来自己管；**后序有三种写法**（镜像反转、单栈+last、双栈标记）；
- **前序 + 中序**可 $O(n)$ 重建；**前序 + 后序**不行（无法区分单孩子的左右）；
- 经典性质：**$n_0 = n_2 + 1$**；$n$ 结点最小高度 $= \lceil\log_2(n+1)\rceil - 1$（平衡树的动机）；高度/直径/平衡/对称都能一次后序遍历 $O(n)$；
- **Morris 遍历 / 线索二叉树**：$O(1)$ 空间做中序，靠「借用空闲右指针存后继」；
- **LCA** 用「一左一右即分叉点」的递归思想；
- ★ **KD-树** 把二叉树拓展到高维检索；**维度灾难**解释了 ANN / LSH / 向量数据库为何必要。

---

## 练习

### 基础

1. 写出二叉树**前序/中序/后序/层序**遍历的**递归**版本，并在一棵手工构造的树上验证四者关系（提示：$n_0=n_2+1$ 可以顺便验证）。
2. 用**一个队列 + 一个栈**实现「之字形层序遍历」（LeetCode 103）：第 1 层左→右、第 2 层右→左、交替进行。
3. 求二叉树中**所有叶子到根路径**（每条路径是一个 `vector<int>`）。用前序 + 回溯实现，比较与「记录父指针 + 向上回溯」两种写法。
4. 给 `code/07_tree_properties.cpp` 增加「**求两个结点的距离**」：距离 $=$ `depth(p) + depth(q) - 2 * depth(LCA(p,q))`。用本章的 `height` 思路实现 `depth`。

### 进阶

5. **不用全局变量**求直径：写一个函数，**同时返回**子树高度和子树内最大直径（返回 `pair<int,int>` 或结构体），避免 7.6 那种「用引用传 `best`」的写法。比较两种风格。
6. 实现「**判断一棵树是否为另一棵树的子树**」：把子树**序列化**成字符串，然后用 KMP / `std::string::find` 匹配（这叫「树的哈希 / 树的同构」）。
7. **序列化 / 反序列化二叉树**：设计一个字符串格式（如 LeetCode 的层序带 `null` 标记），实现双向转换，并保证 `deserialize(serialize(t)) == t`。
8. 实现 **KD-树的删除**：删除一个点时，用「该维度下子树中的最小/最大值替代」的策略维护 KD-树性质。解释为什么删除比插入难得多。

### 挑战

9. **实现一个支持「最近邻 + 范围查询」的 KD-树**：新增 `rangeQuery(center, radius)`，返回球内所有点，并统计剪枝比例。对比「球查询」与「k-NN」的剪枝效率差异。
10. **把 KD-树扩展成「Ball Tree」**：用超球（而非超平面）划分空间，体会它在高维下为什么比 KD-树更稳。做一个 dim = 2/8/16 的对比实验。
11. **实现一个迷你「AST 解释器 + 常量折叠 + 公共子表达式消除」**：把 `z = (a+b)*c + (a+b)/d` 解析成二叉树，做后序遍历求值、折叠常量、识别重复子树 `(a+b)`，输出优化后的表达式。这就是图编译器的最小内核。

<details>
<summary>参考答案（要点）</summary>

**1.** 直接用本章 `preorderRec/inorderRec/postorderRec/levelOrder`；在一棵 5 结点小树上手工核对序列，统计叶子与度为 2 的结点数验证 $n_0=n_2+1$。

**2.** 层序用队列；用一个 `bool leftToRight` 翻转每层的输出方向：取出一层后，若该层需「右→左」则把结果 `reverse`，或改用 deque 头插/尾插。

**3.** 前序 + 回溯：进入结点时 `path.push_back(v)`；到叶子时记录 `path`；返回前 `path.pop_back()`。父指针法：先一遍遍历记 `parent[]` 和 `depth[]`，再从每个叶子沿父指针向上收集。

**4.** `depth(node)`：从根递归下去累加即可；或先求 LCA，再用公式 $d(p)+d(q)-2d(\text{LCA})$。

**5.** 递归返回 `{本子树高度, 本子树内最大直径}`；在合并时用两个孩子的高度更新直径、用 `1+max` 更新高度。优点是**无副作用、可并行、易测试**。

**6.** 序列化必须**唯一且无歧义**（前序 + 空标记）。然后把小树的串在大树串里做子串匹配。注意边界：要在序列化里区分结点值，避免 `12` 匹配到 `1,2`。

**7.** 层序 + `null` 占位：`[1,2,3,null,null,4,5]`。反序列化用队列按层建孩子，遇到 `null` 跳过。关键是**空孩子也要占位**，否则无法还原形状。

**8.** 若删除的是叶子，直接删；若有右子树，用「右子树中该维度的最小值」替代，并递归删除那个最小值；若只有左子树，用「左子树中该维度的最大值」替代。比插入难在于**要维护每一维的切分不变量**。

**9.** 范围查询：递归时先判断当前结点的切分面是否与球**相交**（`|target[axis]-pt[axis]| <= radius`），相交才搜远侧。球查询的剪枝通常比 k-NN 更松（因为固定半径），访问结点更多。

**10.** Ball Tree 每个结点维护一个**球心 + 半径**包住子树所有点；查询时若「目标到球心距离 $-$ 球半径 $>$ 当前最优」则整球剪枝。高维下球比超平面的「排除能力」保持得更久。

**11.** 解析用栈 / 递归下降建二叉表达式树；求值用**后序**；常量折叠：后序时若某结点两孩子都是常数则算出并替换；CSE：用一个哈希表按「(op, 左子树, 右子树)」建键，重复则复用同一子结点。

</details>

---

## 延伸阅读

- **Sahni《数据结构、算法与应用（C++ 版）》第 2 版 第 11 章**「Trees」：树的定义、二叉树、遍历、线索树、以及树的表示；
- **《算法导论》第 12 章**（二叉搜索树）与第 10.4 节（有根树表示）——树的遍历与存储的系统论述；
- **Knuth, TAOCP Vol.1 第 2.3 节**：二叉树与树的经典性质、$n_0=n_2+1$；
- **Morris, J. (1979)** *Traversing Binary Trees Simply and Cheaply* —— Morris 遍历原始论文；
- **Bentley, J. L. (1975)** *Multidimensional Binary Search Trees Used for Associative Searching* —— KD-树的原始论文；
- **Breiman et al., 1984** *Classification and Regression Trees*（CART）—— 决策树的经典；
- **Chen & Guestrin, 2016** *XGBoost* —— 树模型在现代机器学习里的巅峰；
- 配套代码：`code/07_binary_tree.cpp`、`code/07_tree_properties.cpp`、`code/07_kdtree.cpp`。
