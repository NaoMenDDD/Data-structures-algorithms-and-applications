# 第 06 章 队列与双端队列

> 上一章讲的是**后进先出（LIFO）**的栈。这一章讲它的「镜像」——**先进先出（FIFO）**的**队列（queue）**。
>
> 栈像一摞盘子：最后放的先取。队列像排队买饭：先来的先走。两个规则都简单得不能再简单，但队列承担了一件栈做不到的事——**它把「产生」和「消费」解耦了**。生产者只管往队尾塞，消费者只管从队头取，两边不必同步、不必等待。这一解耦，正是**流水线、缓冲区、消息系统**的根基。
>
> 而在深度学习里，队列无处不在：**DataLoader 的预取、推理服务的请求排队与连续批处理（continuous batching）、KV cache 的淘汰、强化学习的经验回放池**……这一章就从「一堆数字排队」讲到「世界模型的训练管线」。

---

## 本章目标

读完并跑完本章代码，你应该能够：

1. 说清队列的 ADT，理解 FIFO 与 LIFO 的本质区别，知道为什么队列能「解耦生产者与消费者」；
2. 用**循环队列（circular queue）**在 $O(1)$ 时间内完成入队/出队，并说清两种**判空/判满**方案（牺牲一格 vs 计 `size`）的取舍；
3. 理解**双端队列（deque）**比队列多了什么能力，为什么它能在两端 $O(1)$ 操作；
4. 掌握**单调队列**这一「用双端队列维护单调性」的高阶技巧，用 $O(n)$ 解决**滑动窗口最大值/最小值**；
5. 理解**环形缓冲（ring buffer）**的固定容量、覆盖式语义，并亲手实现一个**经验回放池**；
6. 建立关键直觉：**缓冲区（buffer）是数据结构，也是系统设计里的"解耦器"**——DataLoader 预取、请求队、KV cache 淘汰，都是同一个思想的不同面孔。

---

## 前置知识

- 第 02 章（数组实现）与第 03 章（链表实现）—— 队列的两种底层；
- 第 05 章（栈）—— 对照着学，理解「受限容器」的价值；
- 一点点的**均摊分析**（第 01 章）—— 循环队列、双栈队列都靠它。

---

## 6.1 队列 ADT：FIFO 与「生产—消费」解耦

队列在一端**入队（enqueue）**、另一端**出队（dequeue）**。入队的那端叫**队尾（back/tail）**，出队的那端叫**队首（front/head）**。

| 操作 | 语义 | 复杂度 |
|---|---|---|
| `push(x)` / `enqueue(x)` | 把 `x` 放到**队尾** | 数组（摊还）$O(1)$；链表 $O(1)$ |
| `pop()` / `dequeue()` | 取出并删除**队首**元素 | （循环）$O(1)$；（朴素数组）$O(n)$ |
| `front()` / `peek()` | 只看队首 | $O(1)$ |
| `back()` | 只看队尾 | $O(1)$ |
| `empty()` / `size()` | 空否 / 元素个数 | $O(1)$ |

**为什么队列比栈"更有用"？** 栈的 LIFO 匹配「嵌套」结构（调用、括号、回溯），而队列的 FIFO 匹配「按到达顺序公平处理」的语义。一旦你想让**两件事以不同速度运行却又不互相拖死**，你就需要队列：

```
   生产者(快)  ──push──►  [ 队 列 / 缓冲 区 ]  ──pop──►  消费者(慢)
   每步 O(1)，不必等            积压的在这里排队              按自己的节奏取
```

**朴素数组实现队列的坑**：如果队首永远在数组下标 0，每次出队都要把后面所有元素前移 → $O(n)$/次。解法就是下面的循环队列。

---

## 6.2 循环队列：把数组「首尾相接」

**循环队列（circular queue / ring buffer）** 用一个固定大小的数组，配合两个下标 `head`（队首）、`tail`（下一个写入位置），让下标**回绕**：

```cpp
head = (head + 1) % N;     // 出队后，队首前进（走到头就回到 0）
tail = (tail + 1) % N;     // 入队后，写指针前进
```

这样出队也变成 $O(1)$，不搬任何元素。但绕圈带来一个新问题：**怎么区分「空」和「满」？** 因为绕圈之后 `head == tail` 既可能是空、也可能是满。有两种经典方案：

### 方案 A：牺牲一个存储单元

底层数组开 `capacity + 1` 格，**永远留一个空位**：

- **空**：`head == tail`；
- **满**：`(tail + 1) % N == head`（其中 $N = \text{capacity} + 1$）。

好处是**不额外存变量**；代价是浪费一格空间，且可用容量是底层大小的 $N-1$。

### 方案 B：额外维护一个 `size` 计数器

不浪费格子，空/满由计数器唯一决定：

- **空**：`size == 0`；**满**：`size == capacity`。

好处是**逻辑最直观、容量全用满**；代价是多存一个 `size_t`（8 字节），每次操作多维护一次加减。

跑 `code/06_circular_queue.cpp`（两种方案并排对照）：

```
===== 两种判满方案对照 =====
----- 方案A 牺牲一格 (capacity = 5) -----
  填满后 size=5, full=1, front=0, back=4
  [OK] 回绕后 FIFO 顺序完全正确
----- 方案B 计 size (capacity = 5) -----
  填满后 size=5, full=1, front=0, back=4
  [OK] 回绕后 FIFO 顺序完全正确

[OK] 容量 0: 空且满, push 返回 false
[OK] 容量 1: 放一个后即满, 弹出后可复用

===== 模板鲁棒性 (std::string) 与异常 =====
[OK] std::string 元素进出队正确
[OK] 空队列 pop/front 抛 std::out_of_range

全部测试通过。
```

> 这个「回绕写指针」的动作，就是**环形缓冲**的核心——它是后面 6.5 节经验回放池、也是操作系统里各种 I/O 缓冲区的底层。

---

## 6.3 双端队列（deque）：两端都能进出

**双端队列（double-ended queue，deque）** 是队列的「加强版」：**两端都能 $O(1)$ 入队和出队**。

| 操作 | 语义 |
|---|---|
| `push_back` / `pop_back` | 在**尾部**进出 |
| `push_front` / `pop_front` | 在**头部**进出 |
| `front` / `back` | 看两端 |
| `[]` | 随机访问（`std::deque` 提供） |

栈和队列都是 deque 的特例：**只用 `push_back`/`pop_back` 就是栈，只用 `push_back`/`pop_front` 就是队列。** 所以 `std::deque` 是 C++ 里最通用的线性容器之一（`std::stack` 和 `std::queue` 默认都拿它当底层）。

**为什么 deque 能两端 $O(1)$？** 标准实现是「**分段连续数组**（a map of fixed-size chunks）」：把元素分成若干固定大小的块，`push_front` 不够就往前面挂一块，`push_back` 不够就往后面挂一块。这样两端插入不需要像动态数组那样搬动全部元素。代价是随机访问要多一层间接（先找块、再找块内偏移），比 `vector` 略慢。

> **一句话对比**：`vector` 只有尾端高效，`deque` 两端都高效但随机访问稍慢，`list` 两端高效但完全不连续。**deque 的定位就是「我要在两头都干活」**——而下一节的单调队列正是这样。

---

## 6.4 单调队列：滑动窗口的 $O(n)$ 解法

**单调队列**是双端队列最漂亮的用法之一，和第 05 章的**单调栈**是一对孪生兄弟。它的规格是：**队列里的元素始终保持单调（递增或递减）**，从而能以 $O(1)$ 拿到「当前窗口的最值」。

**例题：滑动窗口最大值**（LeetCode 239）。给定数组和窗口大小 $k$，返回每个长度 $k$ 的窗口里的最大值。

例如 `nums = [1,3,-1,-3,5,3,6,7], k = 3` → `[3,3,5,5,6,7]`。

**暴力**：每个窗口扫一遍求 max → $O(n \cdot k)$。当 $k$ 接近 $n$ 时退化成 $O(n^2)$。

**单调队列**：维护一个**从队首到队尾递减**的队列，里面存**下标**（不是值！）：

1. **入窗口**：把队尾所有「值 $\le$ 新元素」的下标弹出——新元素更大、且更晚过期，那些小的永远不可能再当最大值了；
2. **出窗口**：若队首下标已经滑出窗口（`front + k <= i`），弹出队首；
3. **取答案**：窗口形成后（`i >= k-1`），队首下标对应的值就是当前窗口最大值。

```
   窗口 [1,3,-1] 扫过时的单调队列(存下标, 值递减):
   i=0: nums[0]=1   -> dq=[0]          (窗口未满)
   i=1: nums[1]=3   -> 弹出 0(1<=3), dq=[1]
   i=2: nums[2]=-1  -> dq=[1,2]        窗口 [1,3,-1] 满 -> 队首=1 -> max=3
   i=3: nums[3]=-3  -> dq=[1,2,3]      i-k=0, 队首1 未过期 -> max=3
   i=4: nums[4]=5   -> 弹出 3,2,1      dq=[4]  -> max=5
   ...
```

每个下标**最多进队一次、出队一次**，所以总复杂度 $O(n)$，空间 $O(k)$。

跑 `code/06_monotonic_deque.cpp`：

```
=== 1. 滑动窗口最大值 ===
  nums = [1,3,-1,-3,5,3,6,7], k = 3
  每个窗口的最大值 = [3,3,5,5,6,7]
  （暴力与单调队列结果一致 ✓）

=== 2. 滑动窗口最小值 + 均摊分析实证 ===
  n = 100000, k = 17
  首个窗口 max = 690, min = -866
  全场（含前后边界）队尾弹出总次数 = 94569 < n = 100000  -> 均摊 O(1)/元素，整体 O(n) ✓

=== 3. 用两个栈实现队列（均摊 O(1)）===
  交错 push/pop 序列 1,2,3 |pop| 4,5  -> 严格 FIFO 输出 ✓
  push 100000 次后全部 pop：总搬迁 = 100000 ≈ push 次数（每个元素最多搬一次）-> 均摊 O(1) ✓

=== 4.（附）单调队列 O(n) vs 优先队列 O(n log k) ===
  两者结果一致；deque 版 O(n)、heap 版 O(n log k)，
  但 heap 更通用（窗口可任意增删/带优先级）——工具互补，不是二选一。
```

**求最小值**只需把所有比较反过来（维持**递增**队列）。上面第 2 部分的 94569 < 100000 正是「每个元素最多出队一次」的实证——这就是 $O(n)$ 的来源。

### 单调队列 vs 优先队列

用**最大堆**也能求滑窗最大值：堆顶是全局最大，遇到「过期元素」就**惰性删除**（检查堆顶下标是否滑出窗口，是就弹出）。两者对比：

| 方案 | 复杂度 | 适用 |
|---|---|---|
| 单调队列 | $O(n)$ | 窗口**整体平移**（过期顺序规整、先进先出） |
| 优先队列（堆） | $O(n \log k)$ | 窗口可**任意增删**、元素带任意优先级 |

**要点**：单调队列之所以能降到 $O(n)$，是因为它利用了「窗口是 FIFO 滑动」这个额外结构。一旦窗口的过期顺序不再规整（比如带优先级、随机删除），就只能退回堆。**没有免费的午餐，只有对问题结构更深的利用。**

### 顺带一提：用两个栈实现队列

第 05 章的练习里提过「用两个栈模拟队列」，`code/06_monotonic_deque.cpp` 第 3 部分给了完整实现。要点：`in` 栈只管入队，`out` 栈只管出队；`out` 空时把 `in` **整体倒过去**（翻转两次 = FIFO）。每个元素一生最多被搬一次，所以**摊还 $O(1)$**：

```
  push 100000 次后全部 pop：总搬迁 = 100000 ≈ push 次数（每个元素最多搬一次）
```

这就是「栈与队列可以互相模拟」的经典结论，也是很多语言实现队列时「两块可翻转缓冲」的由来。

---

## 6.5 环形缓冲：固定容量的「记忆」

**环形缓冲（ring buffer）** 是循环队列的一个变体，语义上强调**「容量固定 + 写满即覆盖最旧」**：

- `push(x)`：写入；若已满，**覆盖最旧的元素**（而不是拒绝或扩容）；
- 容量不变，`size` 恒为 $\min(\text{已写入数},\ \text{capacity})$；
- 逻辑顺序永远「从旧到新」，`oldest()` / `newest()` 两端 $O(1)$。

它天然是一段「**定长的、最近的记忆**」。丢掉最旧的、保留最新的——这正是**经验回放池（experience replay buffer）**要干的事。

### 经验回放池：DQN 能训起来的关键组件

在 DQN（Mnih et al., 2013/2015）里，智能体每一步产生一条**转移（transition）**：

```
   (state, action, reward, next_state, done)
```

全部塞进一个**固定容量的环形缓冲**。训练时**随机采样**一个 mini-batch 来学。为什么用队列（FIFO）而不是直接按顺序学？两个核心理由：

1. **打破时间相关性（temporal correlation）**：连续几步的样本高度相似（相关性极强）。若按顺序学，等价于一直朝同一方向做梯度下降，训练会剧烈震荡甚至发散。**随机化采样**让 mini-batch 近似 i.i.d.（独立同分布），梯度才稳定。
2. **提高样本效率**：一条经验可以被反复采样、多次利用，不必用完即弃。

**容量 = 记忆的长度**，它直接控制「旧策略数据 vs 新策略数据」的混合比：

- **容量太大** → 池里囤着大量**过时策略**产生的样本。因为 DQN 是 **off-policy** 的（用旧策略的数据学当前策略），旧数据的目标值已经不准，会让价值估计**滞后**；
- **容量太小** → 遗忘久远经验，容易**灾难性遗忘（catastrophic forgetting）**，也丧失了「打破相关性」的效果。

跑 `code/06_replay_buffer.cpp`：

```
======== 06 经验回放池（环形缓冲）========

=== 1. RingBuffer 的基本语义 ===
  填入 [10,20,30,40]，size=4 full=true
  再 push 50 后：最旧 = 20，最新 = 50（10 被覆盖 ✓）
  逻辑顺序: 20,30,40,50
  容量 1：每次 push 都覆盖，size 恒为 1 ✓

=== 2. 容量压力：push 100000 条进容量 1000 的池 ===
  push 100000 条，容量 1000 -> 覆盖 99000 次 (= 100000 - 1000)
  池中最旧 state = 99000，最新 state = 99999（FIFO 遗忘旧数据 ✓）

=== 3. 均匀采样 mini-batch（无放回） ===
  采样 32 条（容量 200）：batch 内 state 无重复 ✓
  该 batch 的 reward 之和 = 2956
  大量采样的平均 state = 100.17（理论中位数附近 ~99.5，说明均匀）

=== 4. 分布漂移：旧经验 vs 新经验 ===
  旧策略数据（满池 500 条）平均 reward = -0.0207485
  改进策略写入 250 条后（一半旧、一半新）平均 reward = 0.481552
  再写 250 条（旧数据全部被覆盖）平均 reward = 0.992766
  -> 容量决定的「记忆长度」直接控制新旧数据的混合比例：
     容量太大 -> 采样到大量过时(off-policy)样本，价值估计滞后；
     容量太小 -> 遗忘久远经验，易灾难性遗忘。这是要调的超参。

全部断言通过，程序正常结束。
```

第 4 部分用一个「策略改进 → reward 均值从 0 漂移到 1」的抽象实验，直观展示：**随着新数据涌入，旧数据被逐步覆盖，池的平均统计量单调上升**。这就是 off-policy 学习里「数据分布漂移」的现实问题——而**环形缓冲的容量就是控制它的旋钮**。

> **延伸**：真实实现里还有两个常见增强——
> - **优先经验回放（Prioritized Experience Replay, PER）**：按 TD 误差给样本加权，误差大的多采样。底层常用**二叉求和树**或**线段树**维护权重（第 07/08 章会出现）。
> - **n-step / 多步回放**：一条样本里打包连续 $n$ 步，缓解长期信用分配。这需要队列把相邻的 transition **拼接**起来。

---

## 6.6 复杂度对照

| 操作/算法 | 时间复杂度 | 空间 | 说明 |
|---|---|---|---|
| 队列 `push`/`pop`（循环数组） | $O(1)$ | $O(n)$ | 下标回绕，不搬元素 |
| 队列 `push`/`pop`（链表） | $O(1)$ | $O(n)$ | 每元素多一指针 |
| deque 两端 `push`/`pop` | $O(1)$ | $O(n)$ | 分段连续数组 |
| 单调队列滑窗最值 | $O(n)$ 总 | $O(k)$ | 每元素进出队各一次 |
| 优先队列滑窗最值 | $O(n\log k)$ | $O(k)$ | 堆 + 惰性删除，更通用 |
| 两栈实现队列 | 摊还 $O(1)$ | $O(n)$ | 每元素最多搬一次 |
| 环形缓冲 `push`/`sample(k)` | $O(1)$ / $O(k)$ | $O(\text{capacity})$ | 覆盖最旧；采样无放回 |

---

## 常见错误与陷阱

1. **朴素数组实现队列，出队时整体前移。** 每次 $O(n)$，一不小心就把整个程序拖成 $O(n^2)$。**用循环队列**，让 `head` 回绕。

2. **循环队列判空/判满搞混。** 记住：**牺牲一格方案里满的判据是 `(tail+1)%N == head`，不是 `tail == head`**（后者是空）。改任何一处都要同步改三处判据（`empty`/`full`/`size`）。

3. **取 `size()` 时算错回绕。** `size = (tail + N - head) % N`；写 `tail - head` 会在回绕时得到负数（`size_t` 下更会变成一个巨大的正数）。

4. **单调队列里存值而不是存下标。** 存值就没法判断「是否滑出窗口」。**必须存下标**——这一点和第 05 章单调栈的坑一模一样。

5. **单调队列比较符方向写反。** 求最大值要「弹出所有 $\le$ 新元素」的队尾（维持递减）；求最小值反过来。方向和「队列单调方向」一定要想清楚再写。

6. **环形缓冲的 `push` 返回值语义不清。** 是「是否发生覆盖」，还是「是否成功」？回放池是覆盖式（永远成功），普通循环队列是拒绝式（满则失败）。**同一个结构，两种语义，先定义清楚再用。**

7. **随机采样有放回，导致一个 batch 里同一条样本重复多次。** 多数实现用**部分洗牌（Fisher-Yates）**做无放回采样，$O(\text{batch})$。若用「每次独立 `rand()%n`」会有重复，影响梯度质量。

8. **采样用的随机数发生器不可复现。** 训练实验结果要能复现，就必须给 RNG **固定种子**（本章代码用确定性 LCG，同一 seed 输出完全一致）。

9. **忘了 `std::queue` 没有迭代器 / 不能随机访问。** 它只暴露 `front`/`back`/`push`/`pop`。要遍历或随机访问，说明你需要的其实是 `deque` 或 `vector`。

---

## 与 AI / 世界模型的连接

队列是「**解耦器**」——它让快慢不同的两个过程各自按自己的节奏跑。深度学习到处都是这种「快慢不匹配」，所以队列无处不在。

### 1. ★ DataLoader 的预取（prefetch）

训练时 GPU 算得飞快，但**读数据、解码、增广**很慢（磁盘 I/O、JPEG 解码、CPU 增广）。若串行执行，GPU 大部分时间在**饿着等数据**。PyTorch 的 `DataLoader(num_workers=K, prefetch_factor=m)` 用一组**工作进程 + 队列**：工作进程提前把 $K \cdot m$ 个 batch 塞进队列，GPU 一空就从队头取——**用队列把数据生产和模型计算解耦**。这就是 6.1 节那张生产者—消费者图在真实系统里的样子。

### 2. ★ 推理服务的请求队列与连续批处理（continuous batching）

LLM 推理服务（vLLM、TensorRT-LLM 等）同时收到成百上千个请求，每个请求的生成长度还不一样。系统把请求放进**队列**，由调度器组织成 batch。传统做法是「静态批处理」：一个 batch 里所有请求必须**等最慢的那个**生成完才能一起返回，短请求被长请求拖死、GPU 利用率低。

**连续批处理（continuous batching，又叫 iteration-level scheduling）** 用队列 + 双端结构解决它：

- **队头**：某个序列生成结束后**立即出队**、返回结果；
- **队尾**：新请求**立即入队**、补进当前正在进行的 batch 的空位。

于是 batch 的组成**每个 decoding step 都在变**，GPU 始终满载。这里的「哪些请求还在、谁先到、谁先完成」正是**FIFO 队列**；而「在两端增删」正是 **deque** 的能力。

### 3. ★ KV cache 淘汰

Transformer 自回归生成时，会把每个 token 的 key/value 缓存起来（**KV cache**）避免重复计算。但 cache 有**容量上限**，超出就要**淘汰**。既然要淘汰，就要一个策略——而任何「淘汰策略」背后都是一个**定长缓冲**：

- **FIFO 淘汰**：走最旧的（环形缓冲，和本章的回放池如出一辙）；
- **LRU 淘汰**：走最久没被用到的（第 03 章我们用**哈希表 + 双向链表**实现过 LRU，这里的 deque 与之高度相关）；
- **注意力分数驱动的淘汰**（如 H2O、StreamingLLM 保留「注意力汇聚点」）：按重要性而非时间淘汰。

**共同点**：都需要一个**有界的、记住"最近/最重要"的数据结构**——这正是环形缓冲与双端队列的用武之地。

### 4. ★ 经验回放池（本章 6.5 节）

强化学习（Q-learning、DQN、SAC，以及世界模型里想象 rollout 的训练）几乎必备。**世界模型的"记忆"往往就是一个大容量回放池**：它决定了模型能"回忆"多久之前的世界状态。梦（Dreamer）、TD-MPC 这类世界模型方法，都在探索「用什么结构组织经验、如何采样才有代表性」——PER、n-step、基于模型的优先级，都是在这个环形缓冲上做的文章。

### 5. BFS：图的层序遍历（第 10 章的伏笔）

**广度优先搜索（BFS）** 的载体就是队列：从源点出发，先访问所有距离 1 的点、再距离 2 的点……「**按距离分层**」恰好就是 FIFO。而栈对应的是 DFS。**世界模型里做规划、可达性分析、状态空间 BFS 展开**，都直接用队列。

### 6. 任务调度与消息队列

分布式训练的梯度同步、参数服务器、以及各种 Agent 框架的**工具调用任务队列**，本质都是队列——生产者（各 worker / 各子任务）入队，消费者（参数服务器 / 调度器）出队。**队列是并发系统里最基础的原语之一**。

**一句话**：栈教你组织「有依赖的过程」，**队列教你解耦「速度不匹配的两端」**。从 DataLoader 到推理服务到经验回放，深度学习的工程栈里，队列是隐藏的主角。

---

## 本章小结

- **队列 = FIFO**，核心价值是**解耦生产者与消费者**；朴素数组实现出队是 $O(n)$，用**循环队列**回绕下标降到 $O(1)$；
- **循环队列判空/判满**有两套方案：**牺牲一格**（省变量、浪费一格）和**计 `size`**（容量全用、多存一个变量）；
- **双端队列（deque）** 两端 $O(1)$，栈和队列都是它的特例；底层是分段连续数组；
- **单调队列**用 deque 维护单调性，把**滑动窗口最值**从 $O(nk)$ 降到 $O(n)$；要点是**存下标**、想清楚比较方向；它利用的是「窗口 FIFO 滑动」这一结构，一旦过期顺序不规整就得退回**优先队列**；
- **环形缓冲**是「固定容量的最近记忆」，**写满覆盖最旧**；它就是**经验回放池**的底层，容量直接控制「旧数据/新数据」的混合比与 off-policy 的分布漂移；
- **两栈实现队列**摊还 $O(1)$：每个元素一生最多被搬一次；
- ★ **AI 连接**：DataLoader 预取、推理服务连续批处理、KV cache 淘汰、经验回放池、BFS —— 全是队列思想的不同面孔。

---

## 练习

### 基础

1. 用 `std::queue<int>` 实现 BFS，求一张无向图上「从源点到所有点的最短（边数）距离」。解释为什么 BFS 得到的一定是「最少边数」。
2. 给 `code/06_circular_queue.cpp` 的 `CircularQueueCounted<T>` 增加一个 `resize(newCap)` 方法：把当前元素按 FIFO 顺序搬到新缓冲区，保持队首语义。分析复杂度。
3. 实现一个「**滑动窗口平均值**」：给定数组和窗口 $k$，用 $O(n)$ 时间求每个窗口的均值；再改成「滑动窗口中位数」（提示：需要两个堆，见第 08 章）。
4. 用单调队列重做「滑动窗口最小值」，并对随机数组用暴力版**对拍**验证（学第 05 章代码里的工程习惯）。

### 进阶

5. 实现一个**带优先级的双端队列**：元素有优先级，出队时先出高优先级；同优先级内 FIFO。说明为什么这**不能**用朴素 deque 做（提示：需要每层一个 deque，或一个「deque 的 deque」）。
6. 把 `code/06_replay_buffer.cpp` 的 `ReplayBuffer` 扩展为**优先经验回放（PER）**：给每条样本维护一个「TD 误差」权重，按权重采样（提示：用**二叉求和树**或**前缀和 + 二分**），并实现「重要性采样权重（IS weight）」的校正。
7. 实现**用两个队列模拟一个栈**。分析复杂度，并与「两栈实现队列」比较：为什么前者不能做到摊还 $O(1)$？
8. 实现一个**限流器（rate limiter）**：固定窗口 + 环形缓冲记录最近时间戳，判断「最近 1 秒内请求是否超过 N 次」。这就是 API 网关和 LLM 服务限流的最小内核。

### 挑战

9. **实现一个支持续批处理（continuous batching）的极简调度器**：模拟若干长度不一的生成请求到达，每个 decoding step 输出「当前 batch 里有哪些请求」，并计算 GPU 利用率（batch 平均占用 / 最大并发）。对比「静态批处理」与「连续批处理」的利用率差距，用数据说明为什么后者能省一大笔推理成本。
10. **实现一个 n-step 经验回放池**：每条样本打包连续 $n$ 步的 `(state, action, reward, ..., next_state)`，支持重叠窗口与边界处理（episode 结束要截断，不能跨 episode 拼接）。这是世界模型训练里很常见的一步。
11. **设计一个 KV cache 淘汰实验**：构造一串「带局部性」的键访问序列（有的键反复被访问、有的只用一次），分别用 **FIFO** 和 **LRU** 淘汰，统计命中率。解释为什么真实注意力里 LRU 类策略通常优于 FIFO（提示：注意力的「汇聚点」会被反复访问）。

<details>
<summary>参考答案（要点）</summary>

**1.** 从源点入队、距离 0；每次出队一个点 $u$（距离 $d$），把其未访问邻居距离置 $d+1$ 并入队。因为 BFS 按距离**从近到远**扩展，每个点**第一次被访问时**的层数就是最短边数（图无权重，多走一步只会更远）。

**2.** 新开 `newCap+1` 的缓冲，从旧 `head` 起按逻辑顺序逐个 `push`，更新 `head=0`、`tail=size`。若 `newCap < size`，可选择截断最旧或抛异常（先定义语义）。复杂度 $O(\text{size})$。

**3.** 滑窗均值：维护「窗口和」，入一个加、出一个减，$O(n)$。滑窗中位数：维护**两个堆**（大顶存较小一半、小顶存较大一半），配合**惰性删除**处理过期元素；每次取两堆顶的平均/较大者，$O(n\log k)$。

**4.** 把第 2 部分的 `windowMinDeque` 与对应暴力在随机数据上逐窗口比对，断言相等即可。

**5.** 朴素 deque 只有一种顺序，表达不了「多级优先级」。做法：维护**若干 deque**（每个优先级一个），出队时从最高优先级、非空的 deque 头部取；或一个「deque of deques」+ 位图索引。

**6.** 每条样本存一个正权重 $p_i$；用二叉求和树能 $O(\log n)$ 做「按权重采样」和「更新权重」。采样概率 $P(i)\propto p_i^\alpha$，需要乘以 IS 权重 $(1/(N\cdot P(i)))^\beta$ 修正偏差（$\beta$ 从 0 退火到 1）。

**7.** 栈：`push` 直接进「入队」（记 `q`）；`pop` 时把 `q` 里除最后一个之外的全部倒入另一个队列，取出最后一个，再倒回。单次 $O(n)$，且**没有摊还优势**（因为每次都要倒 $n-1$ 个），整体 $O(n)$/次。两栈队列之所以摊还 $O(1)$，是因为元素搬一次后就**长期留在 `out` 栈**、不必再搬。

**8.** 用环形缓冲存最近 $N$ 次请求的时间戳；新请求到来时，先弹掉所有早于 `now-1s` 的时间戳，再看剩余个数是否 $< N$。$O(1)$ 摊还。

**9.** 静态批处理：等全 batch 最长的请求结束才换批，短请求占着位置不出，利用率 $\approx \text{mean}/\max$。连续批处理：每个 step 结束后立即把完成的请求出队、把等待的新请求补进来，利用率趋近 1（受限于最大并发）。用「长度分布 + 最大并发」模拟，输出两种策略的平均 batch 占用。

**10.** 用一个环形缓冲存「单步 transition」；采样时随机取一个起点，向后**连续取 $n$ 条**，若中途遇到 `done` 就**在 done 处截断**（不足 $n$ 步就用剩余步，或按约定丢掉）。关键是**不能越过 episode 边界**。

**11.** 命中率看「重复访问」的比例。FIFO 会淘汰掉「马上又要被访问」的热点键（因为它只看进入时间，不看使用频率）；LRU 把最近用过的留在队尾附近，热点键很少被淘汰。真实注意力里存在被持续关注的「汇（sink）」token，LRU 类策略对它们的保护更好。

</details>

---

## 延伸阅读

- **Sahni《数据结构、算法与应用（C++ 版）》第 2 版 第 9 章**「Queues」：队列 ADT、数组/链表表示、循环队列的判空/判满讨论；
- **《算法导论》第 10.1 节**（栈与队列）：数组、链表实现；
- **Knuth, TAOCP Vol.1**（环形缓冲与顺序分配的经典论述）；
- **Mnih et al., 2015** *Human-level control through deep reinforcement learning*（Nature）—— 经验回放池的原始出处；
- **Schaul et al., 2016** *Prioritized Experience Replay* —— PER，以及它背后的求和树；
- **vLLM 论文**（Kwon et al., 2023）*Efficient Memory Management for Large Language Model Serving with PagedAttention* —— 连续批处理与 KV cache 管理；
- 配套代码：`code/06_circular_queue.cpp`、`code/06_monotonic_deque.cpp`、`code/06_replay_buffer.cpp`。
