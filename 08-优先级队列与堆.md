# 第 08 章 优先级队列与堆

> 队列的规则是「先进先出」——**完全公平**。但现实里我们常常想要**不公平**：急诊室先看重症，操作系统先跑高优先级任务，束搜索先扩展高得分候选。这时你需要的是**优先级队列（priority queue）**：**每次取出的永远是优先级最高的元素**，跟它什么时候进来的无关。
>
> 优先级队列的标准实现是**二叉堆（binary heap）**——一棵用数组存的完全二叉树。它把栈和队列都「内卷」了一遍：`push`、`pop` 都是 $O(\log n)$，`top` 是 $O(1)$，而且**可以 $O(n)$ 建堆**。堆排序、Huffman 编码、Top-k、数据流中位数，全是它的舞台。
>
> 这一章是「树」第一次真正发挥威力：**一个看似朴素的结构，撑起了排序、压缩、检索三大领域**。

---

## 本章目标

读完并跑完本章代码，你应该能够：

1. 说清优先级队列的 ADT，并解释它为什么**不能**用「有序数组 + 二分」高效实现（插入 $O(n)$）；
2. 掌握二叉堆的**完全二叉树 + 数组存储**表示，熟记下标关系 $2i+1,\ 2i+2,\ \lfloor(i-1)/2\rfloor$；
3. 手写 **sift-up** 与 **sift-down**，实现 $O(\log n)$ 的 `push`/`pop`；
4. **证明并实测**「自底向上建堆是 $O(n)$ 而非 $O(n\log n)$」；
5. 实现**堆排序**，并说出它「最坏 $O(n\log n)$ + 原地」的定位，以及为什么平均比快排慢；
6. 用最小堆实现 **Huffman 编码**，验证它是最优前缀码，且平均码长满足 $H \le L < H+1$；
7. 用大小为 $k$ 的堆解决 **Top-k**（$O(n\log k)$）与**流式 Top-k**；
8. 用**对顶堆**（一个大顶堆 + 一个小顶堆）求**数据流中位数**（插入 $O(\log n)$、查询 $O(1)$）。

---

## 前置知识

- 第 07 章（树与二叉树）—— 完全二叉树的数组表示是堆的骨架；
- 第 01 章（算法分析）—— 级数求和 $\sum h/2^{h+1}$ 用于建堆的 $O(n)$ 证明；
- 一点点**信息论**直觉（熵）—— 帮助理解 Huffman 的最优性。

---

## 8.1 优先级队列 ADT：为什么不用「有序数组」？

优先级队列只需要支持三个操作：

| 操作 | 语义 | 目标复杂度 |
|---|---|---|
| `push(x)` / `insert` | 插入一个元素 | $O(\log n)$ |
| `pop()` / `extractMin/Max` | 取出并删除**优先级最高**的元素 | $O(\log n)$ |
| `top()` / `peek` | 看优先级最高的元素 | $O(1)$ |

**先想想朴素方案为什么不行**：

| 方案 | `push` | `pop` | `top` |
|---|---|---|---|
| 无序数组 | $O(1)$ | $O(n)$ 找最值 | $O(n)$ |
| **有序数组** | $O(n)$ 插入移位 | $O(1)$ | $O(1)$ |
| 有序链表 | $O(n)$ 找位置 | $O(1)$ | $O(1)$ |
| **二叉堆** | $O(\log n)$ | $O(\log n)$ | $O(1)$ |

有序结构把代价压在了 **`push`** 上（$O(n)$ 移位）；无序结构压在了 **`pop`** 上（$O(n)$ 找最值）。**堆把两个操作都做到 $O(\log n)$**——这才是优先级队列的最优平衡点。

> `std::priority_queue` 就是堆的封装。默认 `std::less` → **大顶堆**（堆顶最大）；要小顶堆就传 `std::greater`。这个约定一定要记牢，写反了方向就全反了。

---

## 8.2 二叉堆：完全二叉树 + 数组

**二叉堆**是满足两个条件的二叉树：

1. **结构性质**：是一棵**完全二叉树**（除最后一层外全满，最后一层从左到右连续）；
2. **堆序性质**：每个结点都不低于（大顶堆）或不高于（小顶堆）它的两个孩子。

**关键技巧**：完全二叉树可以用**数组**紧凑存储，无需指针：

```
        9                下标:  0  1  2  3  4  5  6
      ╱   ╲              值  :  9  7  8  5  3  1  2
     7     8
    ╱ ╲   ╱
   5   3 1

  下标 i 的左孩子 = 2i+1    右孩子 = 2i+2    父结点 = ⌊(i-1)/2⌋
```

- **零指针开销**、**内存连续（缓存友好）**、**不用递归**（下标算术直接定位）；
- 数组本身就是**层序**，所以「完全二叉树」这个约束就是为了让数组不出现空洞。

> 这正是第 07 章「顺序存储只适合完全二叉树」的落地——堆是它最重要的应用。

---

## 8.3 核心操作：sift-up 与 sift-down

所有堆操作都由两个「修复」原语组合而成：

**sift-up（上浮）**：把末尾的新元素往上挪，直到不再比父结点优先级高。

```cpp
void siftUp(std::size_t i) {
    while (i > 0) {
        const std::size_t p = (i - 1) / 2;
        if (!comp(data_[p], data_[i])) break;   // 父 >= 子，堆序已满足
        std::swap(data_[p], data_[i]);
        i = p;
    }
}
```

**sift-down（下沉）**：把堆顶的元素往下挪，每次都和孩子中优先级更高的那个交换。

```cpp
void siftDown(std::size_t i) {
    for (;;) {
        std::size_t l = 2*i+1, r = 2*i+2, best = i;
        if (l < n && comp(data_[best], data_[l])) best = l;
        if (r < n && comp(data_[best], data_[r])) best = r;
        if (best == i) break;
        std::swap(data_[best], data_[i]);
        i = best;
    }
}
```

有了这两个原语：

- **`push(x)`**：放到数组末尾（保持完全二叉树结构），然后 **sift-up**；
- **`pop()`**：把堆顶和末尾交换、删除末尾（拿到堆顶），然后对新堆顶 **sift-down**。

两者都只沿树的一条路径走，长度 $= $ 树高 $= O(\log n)$。跑 `code/08_heap.cpp`：

```
=== 1. 堆的基本操作 ===
  push 0..9 后堆顶（最大）= 9
  依次弹出：9 8 7 6 5 4 3 2 1 0 
  小顶堆堆顶（最小）= 1 ✓
  5000 个随机数建堆后依次弹出 == 降序排序 ✓
```

> **易错点**：`sift-up` 里如果用 `>` 而不是「父 < 子才交换」，或者 `sift-down` 里两个孩子的比较方向写反，**大顶堆就会变成小顶堆**——而且小数据量下可能碰巧「看起来对」，一到大的随机测试就崩。

---

## 8.4 ★ 建堆为什么是 $O(n)$？

**问题**：把 $n$ 个无序元素变成堆。最直接的想法是对每个元素 `push`，那是 $O(n\log n)$。但有一个**自底向上**的做法，能在 $O(n)$ 完成：

```cpp
void heapify() {
    for (std::size_t i = data_.size() / 2; i-- > 0; )
        siftDown(i);        // 从最后一个「内部结点」倒着往前，逐个下沉
}
```

**为什么是 $O(n)$？** 关键在于**不同高度的结点数量不同**：高度为 $h$ 的结点大约有 $n/2^{h+1}$ 个，而高度为 $h$ 的结点下沉代价是 $O(h)$。总代价：

$$
\sum_{h=0}^{\lfloor\log n\rfloor} \frac{n}{2^{h+1}} \cdot O(h)
= O\!\left(n \sum_{h\ge 0} \frac{h}{2^{h+1}}\right)
= O(n)
$$

因为 $\sum_{h\ge0} h/2^{h+1} = 1$（等比级数的导数），**常数收敛**。直觉：**大部分结点都在底层、沉不了几步**；顶层结点虽然能沉很久，但数量极少。

`code/08_heap.cpp` 用**精确比较次数**把这件事钉死：

```
        n  push(升序)  push(随机)   heapify  heapify/n  push升序/nlog2n
     4096      40974        9252      7639      1.865         0.834
    65536     917522      149570    123172      1.879         0.875
  1048576   18874390     2390361   1971853      1.881         0.900
 16777216  369098778    38261973  31558798      1.881         0.917
```

读这张表：

- **`heapify` 列 / n ≈ 1.88**，随 $n$ 增长**稳定**——这就是 $O(n)$（常数 $\approx 2$）；
- **`push(升序)` 列 / (n·log₂n)** 从 0.834 爬到 0.917，**趋向 1**——证实逐次 push 在**最坏输入**下是 $\Theta(n\log n)$；
- **`push(随机)`** 却小得多：随机元素上浮距离很短，**平均**接近 $O(n)$。这提醒我们：$O(n\log n)$ 是**最坏上界**，实际常常更便宜。

**结论**：`heapify` 的价值是**把最坏情况从 $O(n\log n)$ 压到 $O(n)$**。这就是堆排序第一阶段、以及「构建初始堆」都用它的原因。

---

## 8.5 堆排序（heapsort）

有了建堆和 sift-down，堆排序就是顺水推舟：

1. **建堆**：把数组原地调整成大顶堆（$O(n)$）；
2. **反复取最大**：把堆顶（全局最大）和「当前堆的最后一个元素」交换，堆大小减一，再对堆顶 sift-down（$O(\log n)$）。第 $i$ 次交换后，数组末尾第 $i$ 个位置就被**最终确定**了。

```cpp
for (std::size_t end = n; end-- > 1; ) {
    std::swap(a[0], a[end]);      // 当前最大 -> 末尾
    siftDown(a, end, 0);          // 剩余部分恢复堆序
}
```

跑 `code/08_heap_sort.cpp`：

```
=== 1. 正确性（含大量随机与边界用例）===
  300 组随机用例：原地堆排序 == std::sort == O(n) 空间版 ✓
  空数组 / 单元素 / 全相等 / 已排序：均正确 ✓

=== 2. 性能对比（n = 2,000,000）===
  83.0632 ms   <- heapSort    (原地)
  12.2563 ms   <- std::sort   (内省排序)
  比值 = 6.78x（堆排序更慢）
```

**堆排序的三个优点**：最坏 $O(n\log n)$（不像快排会退化到 $O(n^2)$）、**原地**（$O(1)$ 额外空间）、无递归。

**那为什么比 `std::sort` 慢 6 倍多？** 因为 sift-down 在数组里**跳跃访问**（下标 $2i+1$、$2i+2$），每次比较都可能落在新的 cache line 上，**cache 局部性差、常数大**。而 `std::sort` 是「快排 + 堆排 + 插入排序」的**内省排序（introsort）**：平均走快排（顺序访问、cache 友好），只有递归过深才回退堆排兜底。

> **一句话定位**：堆排序是「**保证 + 原地**」的安全牌；追求平均速度用快排。两者互补——这正是 `std::sort` 把堆排当兜底的原因。

---

## 8.6 Huffman 编码：最小堆 + 贪心

**Huffman 编码**是最小堆最漂亮的组合应用，也是文件压缩（gzip、PNG、JPEG 的熵编码阶段）的理论基础。

**目标**：给每个符号分配一串 0/1 变长码，使**平均码长最短**，且保证**前缀码**性质（任何码不是另一个码的前缀，从而能无歧义解码）。

**贪心算法**：

1. 每个符号建叶子结点（权 = 频率），全放进**最小堆**；
2. 反复取出**权最小的两个**结点，合并成新结点（权 = 二者之和）放回堆；直到只剩一个（根）；
3. 从根出发，向左记 0、向右记 1，走到叶子即得码。

```cpp
while (pq.size() > 1) {
    HNode* a = pq.top(); pq.pop();      // 最小
    HNode* b = pq.top(); pq.pop();      // 次小
    HNode* m = new HNode(a->freq + b->freq, -1);
    m->left = a; m->right = b;
    pq.push(m);
}
```

**为什么最优？** 核心是「**频率低的符号更深**」——最早被合并的（合并次数最多的）永远是频率最小的，于是它们被放到最深处、码最长。这就是贪心正确性的直观来源。

跑 `code/08_huffman.cpp`（英文字母频率表）：

```
    e    12.70     3   100
    t     9.06     3   000
    a     8.17     4   1110
    o     7.51     4   1101
    i     6.97     4   1011
    ...
    q     0.10     9   001001001
    z     0.07     9   001001000

=== 2. 平均码长 vs 定长编码 vs 信息熵 ===
  定长编码 (ceil(log2 26))      = 5.0000 bit/符号
  Huffman 平均码长 L            = 4.2050 bit/符号
  信息熵 H = -Σ p·log2(p)        = 4.1757 bit/符号
  压缩比 (定长/Huffman)          = 1.1891x
  冗余 L - H                     = 0.0293 bit/符号

  [OK] 前缀码性质成立（无码是另一码的前缀）
  [OK] 满足信息论界 H <= L < H+1（Huffman 最优性的体现）

=== 3. 编解码往返验证 ===
  原文: thisisaverygoodworldmodel（25 字符）
  编码: 110 bit（定长需 125 bit）
  解码: thisisaverygoodworldmodel
```

**三个关键数字**：定长编码要 5 bit/符号；Huffman 只要 **4.2050** bit/符号（省约 16%）；而信息论下界——**熵**——是 4.1757。Huffman 只比理论极限多 0.03 bit，**几乎最优**。

**信息论定理**：任何前缀码的平均码长 $L$ 满足 $H \le L < H + 1$。Huffman 是**最优前缀码**，达到这个下界附近。代码里用断言 `H <= L < H+1` 把它钉住。

> **AI 关联**：Huffman / 算术编码是「**用更短的字表示更常见的东西**」。在深度学习里，这个思想出现在**分词（BPE）里高频 token 用短 id**、**模型量化里压缩常见权重**、以及**信息瓶颈 / 最小描述长度（MDL）** 等理论里。

---

## 8.7 Top-k：大小为 $k$ 的堆

**问题**：从 $n$ 个数里找出**最大的 $k$ 个**（或第 $k$ 大）。

**暴力**：全部排序取前 $k$ → $O(n\log n)$。

**正解**：维护一个**大小为 $k$ 的最小堆**，扫一遍：

- 堆没满 $k$ 个：直接塞；
- 堆满了：拿当前元素和**堆顶**（当前第 $k$ 大）比，比它大就**替换堆顶**。

```cpp
for (int x : a) {
    if (minHeap.size() < k) minHeap.push(x);
    else if (x > minHeap.top()) { minHeap.pop(); minHeap.push(x); }
}
```

每个元素只与堆顶比较一次，仅当更大才做一次 $O(\log k)$ 替换，**总复杂度 $O(n\log k)$**。当 $k \ll n$ 时远优于全排序。

跑 `code/08_topk.cpp`：

```
=== 2. 流式 Top-k（数据不可回头）===
  收到 5 -> 当前 3 个最大: [5]  第 3 大 = -1
  收到 9 -> 当前 3 个最大: [9,5,2]  第 3 大 = 2
  收到 7 -> 当前 3 个最大: [9,7,5]  第 3 大 = 5
  收到 8 -> 当前 3 个最大: [9,8,7]  第 3 大 = 7
  最终 3 个最大 = [9,8,7] ✓（全程只需 O(k) 内存）

=== 3. 性能对比（n = 20,000,000）===
  k = 10：  堆 top-k 9.05 ms  vs  partial_sort 12.03 ms（加速 1.33x）
  k = 1000：堆 top-k 9.87 ms  vs  partial_sort 12.74 ms（加速 1.29x）
```

**流式场景是堆的主场**：数据一条条到达、不能回头重扫、内存放不下全部——这时大小为 $k$ 的堆是**唯一**的一次扫描方案。

> **★ AI 落点**：
> - **Top-k / nucleus ($\text{top-}p$) 采样**：从词表 logits 里取前 $k$ 个候选（LLM 解码的默认操作）；
> - **Beam Search**：每步保留得分最高的 $k$ 条候选序列；
> - **推荐召回 / 向量检索**：从海量候选中取 top-k 相似项；
> - **优先经验回放（PER）**：按 TD 误差取 top-k 高价值样本。
>
> 这些操作**每一步、每个 token** 都要做，所以 $O(n\log k)$ 的堆实现是实打实的性能关键路径。

---

## 8.8 对顶堆：数据流中位数

**问题**：数据源源不断到来，随时能回答「当前中位数是多少」。

**思路**：把数据一分为二，用两个堆夹住中间：

- **`lo`：大顶堆**，存**较小的一半**（堆顶 = 小半部分的最大值）；
- **`hi`：小顶堆**，存**较大的一半**（堆顶 = 大半部分的最小值）；
- **不变式**：`lo.size() == hi.size()` 或 `lo.size() == hi.size() + 1`。

于是中位数 $=$ `lo.top()`（奇数个）或 `(lo.top()+hi.top())/2`（偶数个）。

**插入**：先塞进 `lo`，再把 `lo` 的最大值挪给 `hi`，最后若 `hi` 更大就把 `hi` 的最小值挪回去——三步保证**大小平衡**与**「`lo` 里每个元素 ≤ `hi` 里每个元素」**。

```cpp
void addNum(int x) {
    lo_.push(x);                                   // ① 先进小半部分
    hi_.push(lo_.top()); lo_.pop();                // ② 把小半的最大值挪给大半
    if (hi_.size() > lo_.size()) {                 // ③ 再平衡
        lo_.push(hi_.top()); hi_.pop();
    }
}
```

跑 `code/08_median_two_heaps.cpp`：

```
=== 1. 逐个插入，中位数实时更新 ===
  插入  6 -> 中位数 = 6.0   (n = 1)
  插入 10 -> 中位数 = 8.0   (n = 2)
  ...
  插入  0 -> 中位数 = 3.0   (n = 11)

=== 2. 与「每次排序求中位数」对拍 ===
  200 组随机数据流：对顶堆中位数 == 每次排序 == 全程不变式成立 ✓
=== 3. 单调序列（最容易暴露平衡 bug）===
  1..1001 递增：中位数 = 501.0（应为 501）
  1001..1 递减：中位数 = 501.0（应为 501）
  1..10 偶数个：中位数 = 5.5（应为 5.5）
```

**复杂度**：`addNum` $O(\log n)$、`median` $O(1)$。对比「每来一个数就排序」的 $O(n\log n)$/次，或「维护有序数组插入」的 $O(n)$/次——**对顶堆是数据流中位数的标准解**。

> **变体**：求**滑动窗口中位数**时，两个堆要配合**惰性删除**处理「滑出窗口」的过期元素（第 12 章排序会再提）。**AI 落点**：训练监控里的滑动 P50/P90/P99 分位数、流式异常检测。

---

## 8.9 复杂度对照

| 操作/算法 | 时间复杂度 | 空间 | 备注 |
|---|---|---|---|
| 堆 `top` | $O(1)$ | — | |
| 堆 `push` / `pop` | $O(\log n)$ | — | sift-up / sift-down |
| **建堆 `heapify`** | **$O(n)$** | — | 自底向上；常数 $\approx 2$ |
| 逐个 push 建堆（最坏） | $O(n\log n)$ | — | 升序输入 |
| 堆排序 | $O(n\log n)$ 最坏 | $O(1)$ | 原地、非递归 |
| Huffman 建树 | $O(n\log n)$ | $O(n)$ | $n$ = 符号数 |
| Top-k | $O(n\log k)$ | $O(k)$ | 大小 $k$ 的堆 |
| 数据流中位数 | 插入 $O(\log n)$ / 查询 $O(1)$ | $O(n)$ | 对顶堆 |
| 多路归并 $k$ 个有序数组 | $O(N\log k)$ | $O(k)$ | $N$ = 元素总数 |

---

## 常见错误与陷阱

1. **大顶堆 / 小顶堆方向搞反。** `std::priority_queue<T>` 默认是**大顶堆**；要小顶堆必须写成 `priority_queue<T, vector<T>, greater<T>>`。手写堆时，`comp(a,b)` 的含义要全文件统一（本章约定：`comp` = 「a 的优先级低于 b」）。

2. **建堆用逐个 push，白白损失 $O(n)\to O(n\log n)$。** 只要一次性拿到全部数据，就该用 `heapify`。

3. **`sift-down` 里忘了比较**两个孩子中的更优者**。** 应该先选「优先级更高的孩子」，再和父结点比。直接和左孩子比会在很多情况下出错。

4. **下标关系记错。** 0 基下是左 $2i+1$、右 $2i+2$、父 $\lfloor(i-1)/2\rfloor$；1 基则是 $2i$、$2i+1$、$\lfloor i/2\rfloor$。**两套混用是重灾区**，一开始就要选定并注释。

5. **`pop` 时忘了处理空堆 / 只剩一个元素。** `pop` 后若堆非空才 `sift-down`；空堆 `top` 是未定义行为。

6. **Top-k 用了大顶堆。** Top-k **最大**的 $k$ 个要用**小顶堆**（堆顶是当前的「门槛」，方便替换）；求最小 $k$ 个才用大顶堆。方向反了逻辑就全错。

7. **对顶堆忘记「跨堆的大小关系」。** 只维护 size 平衡还不够，必须保证 **`lo` 的最大值 ≤ `hi` 的最小值**。本章的三步插入同时保证了这两点；自己写时容易只做其中一半。

8. **Huffman 树把「频率」当成「深度」的排序依据。** Huffman 之所以最优，是因为它让**低频符号更深**。若误按频率从大到小优先合并，会得到一个合法但**非最优**的码。

9. **中文字符串用 `std::setw` 对齐会错位。** `std::setw` 按**字节数**而非显示宽度计算，一个汉字占 3 字节但显示宽度是 2。本章 Huffman 表格因此**故意不依赖 `setw` 对齐**，而是把数字放前面。

10. **在函数式/引用传 `best` 的写法里漏掉更新。** 求直径、堆校验这类「递归 + 外部累加变量」的写法，要注意所有分支都更新它。

---

## 与 AI / 世界模型的连接

优先级队列是「**资源有限时按重要性取舍**」的数据结构。深度学习里到处都是「从一大堆候选里挑最好的几个」，所以堆无处不在。

### 1. ★ Top-k / nucleus 采样与 Beam Search

LLM 每生成一个 token，都要从几万维的词表 logits 里**挑出前 $k$ 个候选**——这就是 8.7 节的 Top-k。而 **Beam Search** 每步对**所有**部分序列扩展、再保留得分最高的 $k$ 条，正是「维护大小为 $k$ 的候选堆」的过程。**这两个操作在推理里每一步都跑，是性能关键路径**，所以工程上会用 partial sort、基数选择等更快的 Top-k（第 12 章）。

### 2. ★ A\* 与规划：优先队列是搜索的引擎

**A\* 搜索**维护一个**优先队列（open list）**，按 $f = g + h$（已走代价 + 启发式估计）出队——**永远先扩展最有希望的状态**。这就是第 11 章图算法的主角，也是**世界模型做规划（MPC、树搜索）**的核心：在想象出的未来里，用优先队列反复挑「最有希望的轨迹」扩展。**MCTS / AlphaZero 的树搜索**也用优先队列组织「该访问哪个结点」。

### 3. ★ Huffman 与熵编码

Huffman 是**无损压缩**的基石。在 AI 里：

- **模型权重压缩 / 量化**：高频/常见的权重用更少的比特；
- **分词（BPE / Unigram）**：高频子词用短 id；
- **信息论视角**：交叉熵损失本质上就是「用你的模型编码数据的平均码长」，Huffman 的 $L \ge H$ 和「模型永远无法比真实熵编码得更好」是同一个道理。

### 4. 优先经验回放（PER）

第 06 章的经验回放池若按 **TD 误差**给样本定优先级，就需要一个能「按优先级采样、并动态更新优先级」的结构——底层常用**二叉求和树 / 线段树**（一种堆的亲戚）。堆的「快速取最值 + 快速更新」正是它需要的能力。

### 5. 事件驱动仿真与调度

**世界模型 / 物理仿真**里，事件按时间排序处理（离散事件仿真）；**分布式训练**里任务按优先级调度；**推理服务**里请求按优先级排队——都是优先级队列。**堆是「按重要性排序的引擎」**。

### 6. 分位数与流式统计

训练时监控 loss / reward 的**滑动 P90、P99**，异常检测里的分位数阈值，都是 8.8 节对顶堆的近亲（扩展成两个堆或求和树）。**要在数据流上实时算分位数，堆是标准工具**。

**一句话**：堆教你「**如何在资源有限时，始终抓着最重要的那一个**」。从 LLM 的 Top-k 采样，到 A\* 的 open list，到 PER 的优先级树——**只要出现「挑最好的」，背后几乎都站着一个堆**。

---

## 本章小结

- **优先级队列**的核心是「取优先级最高者」；有序数组把代价压在 push（$O(n)$），堆把 push/pop 都做到 $O(\log n)$；
- **二叉堆** = 完全二叉树 + 数组存储 + 堆序性质；下标 $2i+1,\ 2i+2,\ \lfloor(i-1)/2\rfloor$；
- **两个原语**：`sift-up`（push 用）、`sift-down`（pop 用），都是沿一条路径 $O(\log n)$；
- ★ **`heapify` 是 $O(n)$**：底层结点多但沉得浅，$\sum n h/2^{h+1}$ 收敛；实测常数 $\approx 1.88$；
- **堆排序**：$O(n\log n)$ 最坏 + 原地，但 cache 局部性差，实测比 `std::sort` 慢约 6 倍——定位是「保证 + 原地」；
- **Huffman**：最小堆 + 贪心，最优前缀码，平均码长满足 $H \le L < H+1$（实测 4.205 vs 熵 4.176）；
- **Top-k**：大小 $k$ 的堆，$O(n\log k)$；流式场景是唯一解；
- **对顶堆**：大顶堆（小半）+ 小顶堆（大半），数据流中位数插入 $O(\log n)$、查询 $O(1)$；
- ★ **AI 连接**：Top-k/nucleus 采样、Beam Search、A\* open list、Huffman/熵编码、PER、分位数监控。

---

## 练习

### 基础

1. 用 `std::priority_queue` 实现「合并 $k$ 个有序链表」，并分析复杂度（本章代码演示的是有序数组版本）。
2. 给 `code/08_heap.cpp` 的 `BinaryHeap` 增加一个 `changeKey(index, newValue)` 操作（就地改值并修复堆序），分析复杂度，并说明它在 Dijkstra 里的用途。
3. 用堆实现「**数组中第 $k$ 大元素**」，比较与「维护大小 $k$ 的堆」两个思路的复杂度。
4. 手工构造一个小顶堆，画出它的数组表示，然后演示一次 `push`（sift-up）和一次 `pop`（sift-down）的全过程。

### 进阶

5. **实现 `d` 叉堆**（每个结点 $d$ 个孩子），比较不同 $d$ 对 `push`/`pop` 的常数影响。解释为什么 $d=4$ 有时比 $d=2$ 快（提示：cache line）。
6. **用堆实现「带懒删除的滑动窗口最大值」**：堆里存 `(值, 下标)`，取最大值时弹出下标已滑出窗口的堆顶。对比第 06 章单调队列的复杂度与适用场景。
7. **实现「可合并堆」的入门版**：把两个二叉堆合并成一个（可以接受 $O(n)$ 重建），说明为什么二叉堆做merge很慢，以及**左偏树 / 配对堆**如何做到 $O(\log n)$。
8. **Huffman 的变体**：实现「**$k$ 叉 Huffman 编码**」（每次合并 $k$ 个而非 2 个），并处理「符号数不足」时需要补「空符号」的边界。

### 挑战

9. **实现「Top-k with 基数选择」**：对 32 位整数，按位做**基数选择（radix select）**，在期望 $O(n)$ 时间找出第 $k$ 大。对比堆的 $O(n\log k)$，并说明为什么 GPU 上的 Top-k 常用这类方法。
10. **实现「双关键字优先队列」**：元素有主、次两个优先级；出队先比主键、主键相同再比次键。用它实现「**带优先级的任务调度器**」，模拟不同优先级的任务到达与完成，统计平均等待时间。
11. **实现一个「流式 P-quantile 估计器」**：用两个堆（或 t-digest 的思路）在单遍、有限内存下估计任意分位数，并与精确分位数比较误差。这是训练监控里算 P99 的真实需求。

<details>
<summary>参考答案（要点）</summary>

**1.** 把每个链表的头结点入堆；每次弹出最小结点接到结果尾部，再把该结点的下一个入堆。$O(N\log k)$，$N$ 为总结点数。

**2.** 改值后，若变大了就 sift-up、变小了就 sift-down（二选一，取决于堆类型）。Dijkstra 里「松弛」一条边时要更新某点的最短距离，`decreaseKey` 让它 $O(\log n)$。

**3.** 维护大小 $k$ 的小顶堆，扫完堆顶即第 $k$ 大，$O(n\log k)$、$O(k)$ 空间。另一种是 quickselect，期望 $O(n)$、但最坏 $O(n^2)$（第 12 章）。

**4.** 小顶堆数组如 `[1,3,2,7,5,4]`。`push(0)`：放末尾 → sift-up 一路换到根。`pop`：根与末尾交换、删末尾、新根 sift-down 选更小孩子交换。

**5.** 左孩子 $di+1$、父 $\lfloor(i-1)/d\rfloor$。$d$ 大则树矮、sift 步数少，但每步要比较 $d$ 个孩子。$d=4$ 常更快是因为「比较的 $d$ 个孩子大概率在同一 cache line」。

**6.** 懒删除堆：取最大值时，若堆顶下标 $\le i-k$ 就弹掉。复杂度 $O(n\log n)$（每个元素进出堆一次）。比单调队列 $O(n)$ 慢，但**更通用**（窗口可任意增删）。

**7.** 二叉堆合并需把一个堆的元素逐个插入另一个，$O(n\log n)$。左偏树用「合并（merge）」作为基本操作，递归合并右路径，$O(\log n)$；配对堆用多叉 + 两遍合并，均摊 $O(\log n)$。

**8.** $k$ 叉 Huffman 每次合并 $k$ 个权最小者。需满足 $(n-1) \bmod (k-1) == 0$，否则先补几个权为 0 的虚符号，保证树是满 $k$ 叉。

**9.** 基数选择：从最高位起，统计每一位为 0/1 的个数，判断第 $k$ 大落在哪一侧，只递归那一侧。期望 $O(n)$。GPU 上它比堆更适合并行（每个线程独立统计桶）。

**10.** 用一个比较函数先比主键再比次键；堆的所有比较都走它。调度器：按优先级堆组织到达的任务，模拟时间推进取任务，统计等待时长。

**11.** 维护两个堆夹住目标分位（如 P99 用一个「小半堆 + 大半堆」按分位比例分配大小）；用插值给出估计。与精确分位数比较，误差通常很小且内存 $O(1/\epsilon)$。

</details>

---

## 延伸阅读

- **Sahni《数据结构、算法与应用（C++ 版）》第 2 版 第 11 章**「Trees」（堆与优先级队列部分）与**第 15 章**「Sorting」（堆排序）；
- **《算法导论》第 6 章**「堆排序」——建堆 $O(n)$ 的严格证明（本章 8.4 节就是它的直观版）；
- **Williams, J. W. J. (1964)** *Algorithm 232: Heapsort* —— 堆与堆排序的原始论文；
- **Huffman, D. A. (1952)** *A Method for the Construction of Minimum-Redundancy Codes* —— Huffman 编码原始论文；
- **Cover & Thomas, _Elements of Information Theory_** —— $H \le L < H+1$ 的熵编码理论；
- **Musser, D. (1997)** *Introspective Sorting and Selection Algorithms* —— `std::sort` 的内省排序；
- 配套代码：`code/08_heap.cpp`、`code/08_heap_sort.cpp`、`code/08_huffman.cpp`、`code/08_topk.cpp`、`code/08_median_two_heaps.cpp`。
