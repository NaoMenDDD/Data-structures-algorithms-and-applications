# 第 13 章 哈希与散列

> 数组最迷人的性质是**按下标访问是 $O(1)$**：`a[i]` 一步到位，不用扫、不用比。可现实里我们要找的往往不是「第 $i$ 个」，而是「**键 = 张三**的那条记录」「**URL = xxx** 是否见过」「**向量 v** 的近邻是谁」。哈希（散列）的全部野心就是——
>
> $$\text{任意键 } k \;\xrightarrow{\;\text{哈希函数 } h\;}\; \text{下标 } i = h(k) \bmod m \;\xrightarrow{\;\text{直接访问}\;}\; \text{值}$$
>
> **把「按键查找」硬生生变成「按下标访问」。** 这一章分两幕：
>
> **第一幕（精确）**：哈希表。理想是「一键一位、互不冲突」，但键空间远大于桶空间（鸽巢原理），**冲突不可避免**——于是有**链地址法**和**开放定址法**两套解法，有**负载因子 $\alpha$** 这个核心指标，有**再哈希**这个维持性能的生死线。搞懂这三件事，你就理解了 `std::unordered_map`、Python `dict`、数据库索引、缓存的一切底层。
>
> **第二幕（近似）**：当「精确」太贵时，我们用哈希**换**空间与速度。**布隆过滤器**回答「这个元素**可能**在吗」（无假阴性、有假阳性，几比特/元素）；**局部敏感哈希（LSH）**回答「这两个东西**像不像**」（让相似键落入同一桶），把「找近重复」从 $O(n^2)$ 砍到近线性。
>
> "放弃精确、换取规模"——这是近似算法的入门范式，也是**十亿级向量检索、推荐召回、爬虫去重、缓存穿透防护**的共同底座。

---

## 本章目标

读完并跑完本章代码，你应该能够：

1. 说清**哈希表的基本模型**：键 $\to$ 哈希函数 $\to$ 下标 $\to$ 桶，并解释为什么**冲突不可避免**（鸽巢原理）；
2. 对比**好哈希函数与坏哈希函数**：说清「字符求和」为什么会把字母异位词（`"ab"` / `"ba"`）撞到一起，而 **djb2 / FNV-1a** 引入「位置权重」后为什么不会；
3. 手写**链地址法哈希表**：`put / get / erase`、负载因子、再哈希，并理解「查找代价 $\approx 1+\alpha$」；
4. 手写**开放定址法哈希表**（线性探测 / 双重散列），说清为什么**删除必须留墓碑（tombstone）**、为什么「一次簇聚」让线性探测在 $\alpha$ 大时急剧退化；
5. 理解**再哈希**：为什么「桶数翻倍 + 全部重新分布」能均摊 $O(1)$（与 `vector` 扩容同理），以及「桶数取 2 的幂用位运算取模」与「取素数更鲁棒」的取舍；
6. 用**实测对拍**验证自己的哈希表与 `std::unordered_map` **行为完全一致**，并用基准比较链地址 / 开放定址 / `unordered_map` 的插入与查找耗时；
7. 手写**布隆过滤器**（$m$ 比特 + $k$ 哈希、双重散列模拟 $k$ 个独立哈希），实测「**零假阴性、有假阳性**」，验证假阳性率公式 $\approx(1-e^{-kn/m})^k$ 与最优 $k^\*=({m}/{n})\ln2$；
8. 用 **MinHash** 估计两个集合的 **Jaccard 相似度**，用**分带（banding）**把「找近重复」从 $O(n^2)$ 降到近线性，并理解候选概率 $1-(1-s^r)^b$ 这条 **S 形曲线**；
9. 建立一张「精确哈希 vs 近似哈希（布隆 / LSH）」的选型直觉，并说清它们在**大模型 / 向量检索 / 推荐**中的落点。

---

## 前置知识

- 第 01 章（算法分析）—— 平均 / 最坏、**均摊分析**（再哈希与 `vector` 扩容同理）；
- 第 02 章（线性表与数组）—— 「下标直接寻址」是哈希表的物理基础；
- 第 03 章（链表）—— 链地址法的「桶链」；
- 一点点**概率**：生日悖论、均匀分布假设、期望探测次数；以及「**双射**」「**随机置换**」的直觉（MinHash 的前提）。

---

## 13.1 从「按下标访问」到「按键访问」

先看一个对比，把哈希表的价值钉死：

| 需求 | 朴素解法 | 代价 | 哈希解法 |
|---|---|---|---|
| 找「第 $i$ 个元素」 | 数组下标 | $O(1)$ | 本来就这么快 |
| 找「键 = k 的元素」（**无序**） | 从头扫到尾 | $O(n)$ | **平均 $O(1)$** |
| 找「键 = k 的元素」（**有序数组**） | 二分 | $O(\log n)$ | 平均 $O(1)$，且插入不用挪位 |
| 找「键 = k 的元素」（**平衡树**） | 沿树下降 | $O(\log n)$ | 平均 $O(1)$，但**丧失有序性** |

**哈希表用「丧失有序性」换来了「查找 / 插入 / 删除平均 $O(1)$」。** 这是一个明确的取舍：需要「按序遍历」「找比 k 大的最小键」时，用平衡树（第 09 章）；只需要「精确命中」时，用哈希。

哈希表的物理结构非常朴素：**一个数组（桶数组）+ 一个把键映射成下标的函数**。

```
键 k  --h(k)-->  下标 i = h(k) mod m  --buckets[i]-->  值（或桶链）
```

### 为什么冲突不可避免

数组只有 $m$ 个位置，可键的取值空间（字符串、整数……）**几乎总是远大于 $m$**。由**鸽巢原理**：把超过 $m$ 只鸽子放进 $m$ 个笼子，必有两只同笼——**冲突（collision）是必然的，不是意外**。

于是问题的核心从「怎么消除冲突」变成「**怎么优雅地处理冲突**」。这就是本章的两个主角：链地址法与开放定址法。

---

## 13.2 哈希函数：把键映射成下标

哈希函数 $h$ 的质量直接决定表的好坏。一个「好」哈希要满足：

- **确定**：同一个键永远给同一个值；
- **快**：不能比查找本身还贵；
- **均匀**：把键「打散」到各个桶，尽量不扎堆；
- **雪崩**：键改一个字符，哈希值应大幅变化（否则相邻键会撞在相邻桶）。

### 反面教材：字符求和

```cpp
static std::size_t hashSumOfChars(const std::string& s) {
    std::size_t h = 0;
    for (unsigned char c : s) h += c;      // 只加，不管位置
    return h;
}
```

它的问题一目了然：**字母异位词必然同哈希**——`"ab"` 与 `"ba"` 的字符和都是 $97+98$。实测（对 $100$ 取模）：

```
求和哈希(坏) -> ab:95 ba:95 abc:94 acb:94 bac:94 cab:94 cba:94
```

`abc / acb / bac / cab / cba` 六个排列**全部撞在 94**。现实中「异位词恰好同现」的场景（比如词袋、拼写变体）会让这类哈希表退化成链表。

### 两个经典好哈希

**djb2**（Daniel J. Bernstein）——极简，乘一个魔数 $33$ 再累加：

```cpp
static std::size_t hashDjb2(const std::string& s) {
    std::size_t h = 5381;
    for (unsigned char c : s) h = h * 33 + c;    // h*33 = (h<<5)+h
    return h;
}
```

**FNV-1a**——先 xor 再乘一个素数：

```cpp
static std::size_t hashFnv1a(const std::string& s) {
    std::size_t h = 1469598103934665603ULL;
    for (unsigned char c : s) { h ^= c; h *= 1099511628211ULL; }
    return h;
}
```

两者的共同点是**引入了「位置权重」**：越靠后的字符在结果里被乘了越多次，因此不同排列几乎不可能同哈希。实测：

```
djb2        -> ab:8 ba:40 abc:63 acb:95 bac:19 cab:7 cba:39
FNV-1a      -> ab:76 ba:34 abc:9 acb:11 bac:75 cab:59 cba:45
```

六个异位词的哈希两两不同。**「加法可交换、乘法不平移」**——这就是好哈希与坏哈希的分水岭。

> **工程提示**：C++ 里 `std::hash<int>` 对整数常是恒等映射（$h(k)=k$），所以**整数键一定要自己再混合一次**。经典做法是乘一个黄金比例常数 $2654435761 = \lfloor 2^{32}/\varphi\rfloor$（Fibonacci hashing），或者套一个 `splitmix64` 强混合函数（见 13.8 节）。否则「连续的键 $0,1,2,\dots$」会全部落在连续的桶里，配合「取模 2 的幂」就退化了。

---

## 13.3 冲突处理（一）：链地址法

**链地址法（separate chaining）**：每个桶挂一条链表，冲突的键就往链上加。查找时先算桶号，再在链上线性找。

```cpp
class ChainingTable {
    std::size_t idx(int k) const {
        return static_cast<std::size_t>(std::hash<int>{}(k)) & (buckets_.size() - 1);  // 桶数恒为 2 的幂 -> 位运算取模
    }
    std::vector<std::vector<std::pair<int,int>>> buckets_;   // 每个桶一条链（这里用 vector 当链）
    std::size_t size_ = 0;
    double maxLoad_;
};
```

| 操作 | 代价 |
|---|---|
| 插入 | $O(1)$（头插）；查找确认是否已存在则 $O(1+\alpha)$ |
| 查找（命中） | $\approx 1+\alpha$（$\alpha$ = 平均链长 = 元素数 / 桶数） |
| 删除 | $O(1+\alpha)$，**最简单**（不必留墓碑） |

**优点**：实现简单；**负载因子可以 $>1$**；删除干净利落；适合「元素数量波动大」的场景。
**缺点**：每个结点带指针，**缓存不友好**（查找时反复跳内存）；链太长会退化。

### 实测：$\alpha$ 决定平均链长

固定插入 $n=100000$ 个随机 `int`，只改**再哈希阈值** `maxLoad`，观察最终 $\alpha$ 与查找代价：

```
=== 2. ★ 链地址法：负载因子 α 决定平均链长 ===
     maxLoad=0.5  α=0.38147  桶数=262144  最长链=5  平均查找探测=0.37995
     maxLoad=1  α=0.762924  桶数=131072  最长链=7  平均查找探测=0.7646
     maxLoad=4  α=3.0517  桶数=32768  最长链=12  平均查找探测=3.03055
```

读法：阈值 $0.5$ 时表被撑到 $262144$ 个桶，$\alpha\approx0.38$，平均只需探测 $0.38$ 个结点；阈值放到 $4$（几乎不再哈希），表停在 $32768$ 桶，$\alpha\approx3.05$，平均要探 $3.03$ 个——**正好 $\approx\alpha$**。

> 注意「平均查找探测」略小于 $\alpha$：因为查找的键**大多不在表里**（用的独立随机查询），未命中时要扫完整条链，但空桶命中概率高，统计被拉低。数量级上就是「**查找代价 $\approx 1+\alpha$**」。

---

## 13.4 冲突处理（二）：开放定址法

**开放定址法（open addressing）**：不用链表，冲突了就「**换个座位**」——按某个**探测序列**在表里往后找一个空槽。所有元素都存在**同一个连续数组**里。

三种探测方式：

| 探测 | 第 $i$ 次探测位置 | 特点 |
|---|---|---|
| **线性探测** | $(h_1+i)\bmod m$ | 简单、缓存友好；但会产生**一次簇聚** |
| **二次探测** | $(h_1+i^2)\bmod m$ | 缓解簇聚；但可能探不到所有槽 |
| **双重散列** | $(h_1+i\cdot h_2)\bmod m$ | 用第二个哈希 $h_2$ 决定步长，**最接近均匀** |

**为什么 $\alpha$ 必须 $<1$？** 因为开放定址**必须留有至少一个空槽**，否则查找会陷入死循环。所以它比链地址法更「怕满」。

**为什么删除要留墓碑？** 这是开放定址最反直觉、也最容易写错的地方。假设线性探测插入了 $A$（桶 3）、$B$（桶 4），现在删掉 $A$。若直接把桶 3 置为「空」，那么下次查找 $B$ 时，探测到桶 3 发现是空的，就会**误判 $B$ 不存在**——因为探测链被「挖断」了。

解法的核心代码：

```cpp
static constexpr unsigned char EMPTY_S = 0, USED = 1, TOMB_S = 2;   // 三态，绝不把墓碑当空位

bool get(int k, int& out, long long& probes) const {
    probes = 0;
    for (std::size_t i = 0; i < keys_.size(); ++i) {
        const std::size_t pos = probe(k, i);
        ++probes;
        if (state_[pos] == EMPTY_S) return false;               // 撞到真空位 -> 一定不存在
        if (state_[pos] == USED && keys_[pos] == k) { out = vals_[pos]; return true; }
    }
    return false;
}
```

- **查找**遇到 `EMPTY_S` 才能停；遇到 `TOMB_S` 要**继续探**；
- **删除**把 `USED` 改成 `TOMB_S`（墓碑），而不是 `EMPTY_S`；
- **插入**应优先复用**探测路径上第一个墓碑**（记在 `firstFree` 里），否则墓碑会越积越多、表越来越慢；
- **再哈希**（扩容）时墓碑全部消失，表被「压实」。

### 实测：$\alpha$ 大时线性探测急剧退化

固定 $n=100000$，扫三档阈值，每档比线性探测与双重散列：

```
=== 3. ★ 开放定址：α 越大，线性探测的「扎堆」越明显 ===
     负载因子阈值   探测方式   实际α     平均查找探测
     0.5       	线性探测   	0.381462   	1.80232
     0.5       	双重散列   	0.381466   	1.7095
     0.75       	线性探测   	0.381466   	1.79854
     0.75       	双重散列   	0.38147   	1.70454
     0.9       	线性探测   	0.762932   	9.84034
     0.9       	双重散列   	0.762932   	4.6976
```

$\alpha\approx0.38$ 时两者差不多（都 $\sim1.7\!\sim\!1.8$）；但阈值放到 $0.9$、表涨到 $\alpha\approx0.76$ 时，**线性探测 $9.84$ 次 vs 双重散列 $4.70$ 次**——差了一倍多。

**根因是一次簇聚（primary clustering）**：线性探测下，一旦形成一段连续占用的「簇」，任何散列到这段里的键都会**加到这个簇的尾巴上**，让簇越滚越长，后续键排长队。双重散列的步长由 $h_2$ 决定，能把键**撒开**，所以 $\alpha$ 越大优势越明显。

---

## 13.5 负载因子与再哈希

**负载因子 $\alpha = \text{元素数} / \text{桶数}$** 是哈希表的核心旋钮。理论代价（教科书公式）：

$$\text{链地址法查找} \approx 1+\alpha, \qquad \text{线性探测查找} \approx \tfrac12\!\left(1+\tfrac{1}{(1-\alpha)^2}\right)$$

两条曲线在 $\alpha\to1$ 时**爆炸式上升**。所以工程做法是：**$\alpha$ 超过阈值就再哈希（rehash）——桶数翻倍 + 把所有元素重新分布。**

```cpp
void rehash(std::size_t newCap) {
    std::vector<std::vector<std::pair<int,int>>> nb(newCap);
    for (auto& chain : buckets_)
        for (auto& p : chain)
            nb[std::hash<int>{}(p.first) & (newCap - 1)].push_back(p);
    buckets_.swap(nb);              // 一次性换掉，旧表随作用域销毁
}
```

**为什么单次 $O(n)$ 的再哈希，均摊到每次插入还是 $O(1)$？** 与 `std::vector` 扩容**完全同理**：从空表插入 $n$ 个元素，总代价 $\approx n + n/2 + n/4 + \dots < 2n$，均摊到每个元素是常数。这就是**均摊分析**的经典应用。

两个工程取舍：

- **桶数取「2 的幂」**：`h(k) & (m-1)` 比 `h(k) % m` 快得多（位运算 vs 除法）。代价是「坏哈希 + 低位规律」时容易退化（比如 `h(k)=k` 且 $m=2^{20}$，低位相同的键全撞一起）。**所以取幂时必须先把哈希值充分混合**。
- **桶数取「素数」**：对「低位有规律」的坏哈希更鲁棒（素数不整除键的规律），代价是取模稍慢。老式实现（如 Java 早期 `HashMap`）偏爱素数。

---

## 13.6 实战：对拍 + 基准

### 对拍：与 `std::unordered_map` 行为完全一致

「行为一致」是哈希表正确性的黄金标准。用 $20$ 万次随机「插入 / 删除 / 查找」，把链地址法与开放定址法都和 `unordered_map` 逐次比对：

```
=== 4. ★ 随机操作对拍：与 std::unordered_map 完全一致 ===
  20 万次随机「插入 / 删除 / 查找」：链地址法与开放定址都与 unordered_map 一致 ✓
```

每次操作都 `assert`：插入后值要一致、删除的返回值（是否删掉了）要一致、查找命中与值都要一致。**这一步逼出了代码里一个真实的 bug**：`OpenTable::put` 最初「一遇到空槽或墓碑就插」，没继续往后探测该键是否已存在——于是**同一个键可能被插两份**。墓碑的存在让「已存在」的判断更微妙，必读代码里 `firstFree` 的那段逻辑。

### 基准：$n=500000$ 插入 + $10^6$ 次查找

```
=== 5. ★ 基准：n = 500,000 随机 int，插入 + 100 万次查找 ===
     链地址法      插入 64.3205 ms  查找 9.89286 ms
     开放定址(线性)插入 23.7282 ms  查找 11.9074 ms
     开放定址(双重)插入 14.1282 ms  查找 12.889 ms
     std::unordered_map  插入 30.2785 ms  查找 19.2964 ms
```

几个值得记住的结论：

- **插入**：开放定址（尤其双重散列）**远快于**链地址法。因为开放定址把元素存在**一整块连续内存**里，写的时候只有一次 cache miss；链地址法每插一个结点都要 `malloc`（分配 + 指针跳转），开销大。
- **查找**：链地址法在这里**反而最快**。因为链结点一旦建好，命中时经常在链头（新插入的在链头），指针跳转虽多但可预测；而开放定址在 $\alpha\approx0.38$ 下仍要走几次探测，且每次探测都可能 miss。
- **`unordered_map`**：插入和查找都比我们的手写版慢一些（它是通用库实现，有结点分配、迭代器、哈希泛型等开销），但它**通用、健壮**，是默认选择。
- **一句话**：**开放定址「连续内存」→ 缓存友好、插入快；链地址法「指针灵活」→ 删改方便、命中快。** 工程里到底用哪个，看读写比与是否有删除。

---

## 13.7 布隆过滤器：用几比特回答「可能在吗」

到这里，「精确」之路走到头了：想 $O(1)$ 精确查在不在，至少得把键（或它的某种表示）存下来。但如果允许**一点点错误**呢？

**布隆过滤器（Bloom filter）** 只用一个**比特数组**和 **$k$ 个哈希函数**：

- **插入 $x$**：算出 $k$ 个位置，全部置 $1$；
- **查询 $x$**：检查这 $k$ 个位置。**只要有一个是 $0$ $\Rightarrow$ 一定不在；全是 $1$ $\Rightarrow$ 可能在。**

```cpp
class BloomFilter {
    std::vector<std::uint64_t> bits_;     // 比特数组
    std::size_t m_;                        // 总比特数
    int k_;                                // 哈希函数个数
public:
    void add(std::size_t h1, std::size_t h2) {
        // 双重散列模拟 k 个独立哈希：第 i 个位置 = (h1 + i*h2) % m
        for (int i = 0; i < k_; ++i) set((h1 + static_cast<std::size_t>(i) * h2) % m_);
    }
    bool query(std::size_t h1, std::size_t h2) const {
        for (int i = 0; i < k_; ++i)
            if (!get((h1 + static_cast<std::size_t>(i) * h2) % m_)) return false;  // 有一位 0 -> 一定不在
        return true;                                                                // 全 1 -> 可能在
    }
};
```

### 两个关键性质

| 性质 | 说明 |
|---|---|
| **零假阴性** | 在集合里的元素，插入时把 $k$ 位都置了 $1$，之后**永远查得到**（比特只会 $0\to1$，不会被清）；「在的必说在」。 |
| **有假阳性** | 一个从未插入的元素，它的 $k$ 个位置**可能恰好都被别的元素置成了 $1$**，于是被误判为「在」。 |

用一个具体的尺寸实测（$m=100000$ 比特、$k=7$、插入 $n=10000$ 个）：

```
=== 1. ★ 布隆过滤器：在的必说在，不在的可能说在 ===
     m=100000 比特, k=7, 插入 n=10000 个元素
     假阴性数（在却说不在）= 0（必须为 0）
     实测假阳性率 = 0.00802   理论值 (1-e^{-kn/m})^k = 0.00819372
     填充率（置 1 的比例）= 0.50204
```

**假阴性严格为 $0$**（这是布隆的铁律）；假阳性率实测 $0.00802$，与理论公式 $0.0082$ **高度吻合**。

### 假阳性率公式与最优 $k$

理论推导（假设各哈希近似独立、各位被置 $1$ 的概率均匀）：

- 插入 $n$ 个元素、共 $kn$ 次置位，「某一位**仍是 $0$**」的概率 $\approx e^{-kn/m}$；
- 于是「$k$ 位**全被置 $1$**」的概率（即假阳性率）$\approx \left(1-e^{-kn/m}\right)^k$。

把这个式子对 $k$ 求最小，得到**最优哈希个数**：

$$k^\* = \frac{m}{n}\ln 2 \approx 0.693\,\frac{m}{n}$$

实测 $k$ 的扫描（$m/n=10$，理论最优 $k^\*\approx6.93$）：

```
=== 2. ★ 哈希函数个数 k 对假阳性率的影响 ===
     k      实测假阳性率      理论假阳性率
     1	0.1        	0.0951626
     2	0.07201        	0.0328585
     4	0.02296        	0.0118133
     6	0.01805        	0.00843621
     7	0.01127        	0.00819372
     8	0.014835        	0.00845547
     10	0.01833        	0.0101859
     12	0.02367        	0.0135606
     16	0.051945        	0.0270986
  -> 实测最优 k ≈ 7，理论最优 k* = (m/n)·ln2 ≈ 6.93147。k 太少 -> 冲突多；k 太多 -> 比特被填满，两者都会升。
```

**$\alpha$ 式的跷跷板**：$k$ 太小 $\Rightarrow$ 单个键置位太少、碰撞多，假阳性高；$k$ 太大 $\Rightarrow$ 一下子把大半比特填满，别人随便一查都是全 $1$，假阳性也高。**两端都贵、中间最低**——这就是那条 $k^\*=(m/n)\ln2$ 的由来。（实测 $k=7$ 最低，与理论 $6.93$ 一致。）

### 省了多少空间

```
=== 3. ★ 空间对比：布隆 vs 精确集合 ===
     存 1000000 个元素：
       精确哈希集合（存键）：约 4 MB（按 4 字节/键算）
       布隆过滤器（10000000 比特）：1.25 MB，且假阳性率约 0.00819372
```

存 $10^6$ 个 `int`，精确集合要 $\sim4$ MB；布隆用 $10$ 比特/元素只要 $1.25$ MB，**约 $1/40$ 的空间**（若键是长字符串、URL，省得更多），代价是 $0.8\%$ 的假阳性。**这笔买卖在「过滤掉绝大多数不存在查询」的场景里极其划算。**

### 能删吗？

**经典布隆不支持删除**——因为把某位清 $0$ 会**误伤**其他把它置 $1$ 的元素（它们本该查得到）。需要删除时用**计数布隆（counting Bloom）**：每个位置存一个小计数器，插入 $+1$、删除 $-1$，$>0$ 即「可能在」。代价是比特变字节，空间涨 $4\sim8$ 倍。

### 典型用处

缓存穿透防护（先查布隆，拦掉必然不存在的 key，保护后端 DB）、爬虫 URL 去重、数据库「先查布隆再看磁盘」、区块链 SPV 里的交易过滤、推荐系统「已读/已曝光」去重。

---

## 13.8 局部敏感哈希（LSH）：让「相似」落到同一桶

布隆回答「**在不在**」，LSH 回答「**像不像**」。

**普通哈希追求「不同键分散」**（如果两个不同的键撞到一起，那是坏事）。
**LSH 追求「相似的键聚集」**（相似键**故意**撞到同一桶，不相似的尽量分开）——**目标正好相反**。

### MinHash：用签名估计 Jaccard 相似度

两个集合 $A,B$ 的 **Jaccard 相似度**：

$$J(A,B) = \frac{|A\cap B|}{|A\cup B|}$$

直接算要存下两个集合全部元素，对于「文档 = 词集合」动辄几万维，很贵。**MinHash** 用一个巧妙的概率事实把它们压成定长「签名」：

> 取一个**随机哈希（近似随机置换）** $h$，则
> $$P\big(\min_{x\in A} h(x) = \min_{y\in B} h(y)\big) = J(A,B)$$
> 直观：$A\cup B$ 里哈希值最小的那个元素，**恰好在 $A\cap B$ 里**的概率，正是 $|A\cap B|/|A\cup B|$。

于是取 $k$ 个随机哈希 $h_1,\dots,h_k$，每个集合得到一个 $k$ 维签名（第 $i$ 维 = $\min_{x\in S} h_i(x)$）。**两个签名相同的维度占比**就是 $J$ 的估计。签名越长越准，误差 $\sim O(1/\sqrt{k})$。

实测（$A=\{0,\dots,199\}$、$B=\{100,\dots,299\}$，真 $J=100/300=0.3333$）：

```
=== 4. ★ MinHash：用签名估计 Jaccard 相似度 ===
    真 Jaccard(A,B) = 0.333333（|交|=100, |并|=300）
     签名长度 k   估计值      误差
     16		0.4375    0.104167
     64		0.3125    -0.0208333
     256		0.300781    -0.0325521
     1024		0.367188    0.0338542
  -> 签名越长，估计越准（误差 ~ O(1/sqrt(k))）。MinHash 的妙处：
     P(min h_i(A) == min h_i(B)) = Jaccard(A,B) —— 签名相同的比例就是相似度。
```

$k=16$ 时误差达 $0.10$；$k=64/256/1024$ 时估计都在真值 $0.333$ 附近翻飞，量级 $\sim\pm0.03$（$\propto 1/\sqrt{k}$）。**没有存任何原始集合**，只用 $k$ 个整数就估出了相似度。

> **一个真实的坑**：最初用**仿射哈希** $(a_i x + b_i)\bmod p$ 当 MinHash，结果估计**系统性偏低**（$0.283$ vs 真值 $0.333$）且不收敛。原因是仿射哈希在「$A=\{0..199\},B=\{100..299\}$ 这种**有结构的连续小整数**」上**不满足 min-wise 独立性**。换成 **`splitmix64`**（对 64 位整数做充分混合的强双射，`(seed_i + x)` 喂进去）后估计立刻无偏（$0.33337$）。**LSH 的哈希必须足够「随机」，不能有结构偏差。**

```cpp
// splitmix64：把 64 位整数充分打乱的小双射（seed 只做加法偏移，再整体混合）
static std::uint64_t splitmix64(std::uint64_t z) {
    z += 0x9e3779b97f4a7c15ULL;
    z = (z ^ (z >> 30)) * 0xbf58476d1ce4e5b9ULL;
    z = (z ^ (z >> 27)) * 0x94d049bb133111ebULL;
    return z ^ (z >> 31);
}
// 第 i 个 MinHash 用 seed[i] 偏移：h_i(x) = splitmix64(seed[i] + x)
```

### 分带（banding）：把候选对筛出来

有了签名，怎么**快速**找出「签名相似」的一对对文档？**不要两两比**（$O(M^2)$）。做法是**分带**：

把 $k$ 维签名**切成 $b$ 段、每段 $r$ 行**（$k=br$）。**只要任意一段完全相同，就把这对文档判为「候选近邻」**（再进候选集里做精细比较）。为了快速「按段找相同」，给每段算一个哈希、塞进哈希桶，同桶即候选。

**为什么这是对的？** 设两个文档真实相似度为 $s$：

- 单行（一个 MinHash）相同的概率 $=s$；
- 一段 $r$ 行**全相同**的概率 $=s^r$；
- 一段**不相同**的概率 $=1-s^r$；
- $b$ 段**都不相同**的概率 $=(1-s^r)^b$；
- 于是**至少有一段相同（成为候选）的概率**：

$$P(s) = 1 - (1-s^r)^b$$

这是条 **S 形曲线**：$s$ 高（真的很像）时 $P\to1$（必被抓成候选）；$s$ 低时 $P\to0$（基本不打扰）。**$b,r$ 一起决定这条 S 曲线的「拐点」和「陡峭度」**——即「相似度多少以上才值得比较」。

### 实测：找近重复文档

造 $30$ 对「基础文档 + 加噪副本」的近重复，再掺 $100$ 篇随机噪声，共 $M=160$ 篇。签名 $k=128$（$b=32$ 段 × $r=4$ 行）：

```
=== 5. ★ LSH 分带：在文档集合里找近重复 ===
     文档数 M = 160（含 30 对近重复 + 噪声），签名 k=128，分带 b=32 段 × r=4 行
     真近重复对 = 30，被找回 = 30  召回率 = 1
     候选对总数 = 88，其中真近重复 = 30  精度 = 0.340909
  -> 只比较「候选对」就把 O(M^2)≈12720 次两两比较砍到 88 次，且近重复基本没漏。
     b 段 r 行决定「相似度阈值」：候选概率 = 1 - (1 - s^r)^b，是条 S 形曲线。
```

- **召回率 $=1$**：$30$ 对近重复**全部**被找回；
- **候选对从 $12720$（$O(M^2)$）砍到 $88$**，缩小了 $100$ 多倍；
- 精度 $0.34$：$88$ 个候选里 $30$ 个是真近重复，其余 $58$ 个是「碰巧有一段撞上」的假候选——没关系，**后面用完整签名（或原始特征）对候选做精细比较即可排除**。LSH 的角色是**廉价地「筛候选」**，把昂贵的两两比较从「全体」缩到「少数候选」。

**一句话总结 LSH**：用**便宜的、会误伤的哈希**做粗筛，把 $O(M^2)$ 的两两比较降到「近线性」，precision 换 recall 的可控权衡。

---

## 常见错误与陷阱

1. **整数键直接当哈希用**：`std::hash<int>` 常是恒等，连续键配「取模 2 的幂」→ 全撞连续桶。**务必先混合**（乘黄金比常数 / `splitmix64`）。
2. **开放定址删除时把槽置为空**：会切断探测链，导致**后插的元素查不到**。必须留**墓碑**，查找遇到墓碑要**继续探**。
3. **开放定址插入时不找「已存在的键」**：只探到第一个空槽就插，会**插入重复键**。（我们的对拍就是被这个 bug 卡住的。）正确做法：探测路径上记下第一个墓碑，一路探到空槽再决定是「覆盖已有的」还是「插到墓碑/空槽」。
4. **墓碑越积越多**：墓碑位置反复插入新元素会「回收」一部分，但删除密集时墓碑会拖慢查找——**再哈希（扩容）会一次性清空所有墓碑**，是必要的维护。
5. **$\alpha$ 长期逼近 $1$ 不再哈希**：开放定址会退化成 $O(n)$。**阈值 $\sim0.75$ 就该翻倍扩容**。
6. **`rehash` 用错的模数**：扩容后必须用**新桶数**重新计算所有键的下标，不能沿用旧的。
7. **布隆过滤器误用于「需要删除」**：清位会误伤他人。要删除请用计数布隆。
8. **布隆过滤器当作「精确判断」**：它的「在」只意味着「**可能**在」；「不在」才是确定。把假阳性当真的用会出 bug。
9. **布隆尺寸拍脑袋**：$m$ 要按目标假阳性率与预计 $n$ 反推（$m\approx-\tfrac{n\ln p}{(\ln2)^2}$），$k$ 取 $(m/n)\ln2$。$n$ 会超预期就得多留余量。
10. **LSH 用「弱随机」哈希**：仿射哈希在结构化数据上有偏（见 13.8 的坑）。**MinHash 的哈希要足够雪崩。**
11. **LSH 只调 $b$ 忘调 $r$**：$b,r$ 一起决定 S 曲线。$r$ 越大越「严」（一段全同更难），$b$ 越大召回越高但候选越多。要按「目标相似度阈值」反推 $b,r$。
12. **拿 LSH 结果直接当最终答案**：LSH 给的是**候选**，precision 有限，务必对候选做一次精确比较。

---

## 与 AI / 世界模型的连接

哈希看起来「和神经网络八竿子打不着」，其实它是**大规模系统里无处不在的地基**，尤其在你关心的**世界模型 / 表征学习**里：

- **向量检索与 ANN（近似最近邻）**：世界模型 / 多模态模型会把图像、文本、动作编码成 embedding，检索「最像的那个」是核心操作。十亿级向量没法暴力比，工业界的 FAISS / ScaNN 用的正是 **LSH / 乘积量化（PQ）+ 倒排** 这一族「哈希+分桶」近似术。**本章的 LSH 分带就是这条线的入门形态。**
- **去重与数据清洗**：训练大模型前，海量网页 / 图文对要做**近重复去除**。MinHash + LSH 是其中最经典的组合（Google 的 SimHash 与之同源），能在近线性时间里消掉冗余，**直接决定数据质量与训练成本**。
- **推荐召回**：推荐系统第一阶段要从千万候选里「捞」几百个给精排。**双塔 embedding + LSH / PQ** 是标准召回架构，本质就是「相似键落同桶」。
- **注意力 / KV-Cache 的近似**：长上下文推理里，KV-Cache 越来越大，出现了「**只保留/检索与当前 query 相似的 KV**」的稀疏注意力（如基于 LSH 的 Reformer）。这仍是「用哈希换近似」的思想。
- **Embedding 去重与记忆**：世界模型常维护「经验回放 / 情景记忆」，用哈希把「相似状态」聚到一格、避免存储爆炸，是 replay buffer 的常见优化（呼应第 06 章）。
- **缓存穿透防护**：线上推理服务的缓存前面挂一个布隆过滤器，拦掉**必然不存在**的 key，省下大量后端查询——**布隆是一线 ML 服务的标配**。
- **哈希作为「免费」的特征**：**特征哈希（hashing trick）** 把任意稀疏特征（词、n-gram、ID）哈希到固定维度的向量里，省去维护词表，是传统 ML / 推荐特征工程的经典手法，思想与本章同源。
- **可微分的反面**：哈希是**不可微、不可学**的。但「**可微哈希 / 可微分桶**」（如 VQ-VAE 的码本、软分配 Gumbel-Softmax）正是把「离散分桶」软化后接进神经网络的做法——理解硬哈希，才懂这些软化技巧在近似什么。

**一句话**：世界模型负责「把世界压成向量」，而**哈希 / LSH 负责「在十亿个向量里快速找到相似的那些」**。前者是表征，后者是检索，两者是同一套大规模系统的两条腿。

---

## 本章小结

- **哈希表模型**：键 $\to h(k)\bmod m \to$ 桶，把「按键查找」变成「按下标访问」，平均 $O(1)$；
- **冲突不可避免**（鸽巢原理），两套解法：
  - **链地址法**：桶挂链，查找 $\approx1+\alpha$，$\alpha$ 可 $>1$，**删除最简单**，但指针多、缓存差；
  - **开放定址**：探测找空槽，**$\alpha$ 必须 $<1$**，**删除要留墓碑**，省指针、缓存友好；线性探测有**一次簇聚**，**双重散列**把簇打散、$\alpha$ 大时优势明显；
- **好哈希**要均匀、雪崩、快：djb2（$h\!\cdot\!33+c$）、FNV-1a（xor 后乘素数）；**坏哈希**（字符求和）会让异位词全撞车；整数键**务必先混合**；
- **再哈希**（阈值 $\sim0.75$ 翻倍）：单次 $O(n)$、**均摊 $O(1)$**，与 `vector` 扩容同理；桶数「2 的幂 + 位运算」快、「素数」鲁棒，各有取舍；
- **布隆过滤器**：$m$ 比特 + $k$ 哈希，**零假阴性、有假阳性**，假阳性率 $\approx(1-e^{-kn/m})^k$，最优 $k^\*=(m/n)\ln2$，几比特/元素、**不支持删除**（除非计数布隆）；
- **MinHash**：用最小哈希签名估计 **Jaccard 相似度**（签名相同占比 $\approx J$），哈希要足够雪崩（仿射哈希会在结构化数据上有偏）；
- **LSH 分带**：$b$ 段 × $r$ 行，任一段相同即候选，候选概率 $1-(1-s^r)^b$ 是 **S 形曲线**；把「找近邻」从 $O(n^2)$ 降到近线性；
- **共同哲学**：**放弃精确、换取空间与速度**——近似算法的入门范式，也是向量检索 / 推荐召回 / 数据去重的底座。

---

## 练习

### 基础

1. 手动算：把键 $10,22,37,45$ 依次插入 $m=7$ 的哈希表（$h(k)=k\bmod7$），分别用**链地址法**和**线性探测**画出最终结构，并数出查找 $45$ 各需几次。
2. 解释为什么「字符求和」哈希一定把**任意一对字母异位词**映到同一个值，而 djb2 不会。举一个 djb2 也会冲突的具体例子（提示：$m$ 取小、键取巧）。
3. 用 $m=8,\ h(k)=k$（恒等哈希）、**取模 2 的幂**，把键 $0,8,16,24$ 插入，观察它们全落在哪个桶；再改成 $\times2654435761u$ 混合后观察。
4. 手写一个开放定址表，**故意**在删除时把槽置为空（不留墓碑），构造一个「删除后查不到后续插入元素」的例子，打印出来。
5. 用 $n=10000$、$m=100000$ 算布隆过滤器的最优 $k$ 与理论假阳性率；再用 $m=10000$（即 $m/n=1$）重算，比较两者。

### 进阶

6. **实现二次探测**（$(h_1+i^2)\bmod m$）的开放定址表，与线性探测、双重散列在 $\alpha=0.8$ 下比较平均查找探测次数。
7. **实现「计数布隆过滤器」**并支持删除：插入 $+1$、删除 $-1$、查询判 $>0$。验证「插入后删除，该元素查不到；但别的同槽元素不受影响」。
8. **反推布隆尺寸**：给定目标假阳性率 $p=0.01$ 与预计 $n=10^6$，算出所需的 $m$ 与 $k$，用代码构造并**实测**假阳性率是否达标。
9. **实现 SimHash**（另一种近重复哈希）：把文档的每个特征哈希成一个 $64$ 位向量，按位加权求和、取符号位得到 $64$ 位指纹；用**汉明距离**判近重复，与 MinHash + LSH 在召回率 / 速度上对比。
10. **扫 $b,r$ 的 S 曲线**：固定签名 $k=128$，取若干组 $(b,r)$，对真实相似度 $s$ 从 $0$ 到 $1$ 画（打印）$P(s)=1-(1-s^r)^b$，标出每组 $(b,r)$ 的「拐点」，说明如何按目标阈值选 $(b,r)$。
11. **对拍再延伸**：给你的 `ChainingTable` / `OpenTable` 加上 `size()`、迭代遍历（遍历所有键值对）两个接口，并与 `unordered_map` 遍历结果（排序后）对拍一致。

### 挑战

12. **实现一致性哈希（consistent hashing）**：把「键 $\to$ 桶」改成「键 $\to$ 环上的第一个节点」。验证「增删一个节点只影响 $O(1/N)$ 的键」——这是分布式缓存 / 分布式存储的分片基础，与「节点数变化时全体重分布」形成鲜明对比。
13. **手写一个带 LSH 的最近邻检索**：对一批随机向量，用**随机超平面 LSH**（$h(v)=\text{sign}(w\cdot v)$）建索引，查询给定向量的近邻，与暴力最近邻比对召回率，并测加速比。
14. **实现「矩阵乘积哈希（Count-Min Sketch）的兄弟」**：用 Count-Min Sketch 在 $O(\text{width}\times\text{depth})$ 空间里估计**流式数据里某元素的出现次数**（只高估不低估），实测在「一亿次随机 + 若干重尾元素」流上的误差。
15. **可微分桶**：实现 VQ-VAE 式的「最近码本 + 直通梯度（straight-through）」，理解「硬哈希 / 硬量化」与「可微软化」之间差了什么，并用手写小网络验证梯度能反传。

<details>
<summary>参考答案（要点）</summary>

**1.** $h$：$10\to3,\ 22\to1,\ 37\to2,\ 45\to3$。链地址法：桶 $3$ 挂 $[10,45]$，桶 $1$ 挂 $[22]$，桶 $2$ 挂 $[37]$；查 $45$：桶 $3$ 链上第 $2$ 个 $\Rightarrow$ $2$ 次。线性探测：$10$ 入 $3$，$22$ 入 $1$，$37$ 入 $2$，$45$ 想入 $3$ 被占 $\to4\to5$（空）入 $5$；查 $45$：探 $3,4,5$ 命中 $\Rightarrow$ $3$ 次。

**2.** 求和哈希只依赖「字符集合」不依赖次序，异位词集合相同 $\Rightarrow$ 值必相等。djb2 的 `h*33+c` 让「后出现的字符」被乘更多次，次序不同结果不同。djb2 也会冲突的例子：`m=1` 时一切皆撞；或两个不同串恰好同余（用生日悖论构造，$m$ 小时很快出现）。

**3.** 恒等 $+m=8$：$0,8,16,24$ 的 $k\bmod8$ 全是 $0$ $\Rightarrow$ 全挤桶 $0$。乘 $2654435761u$（黄金比）后，四个值的哈希被打散，落到不同桶。

**4.** 线性探测插 $A\to3$、$B\to4$、$C\to5$；删 $A$ 时把 $3$ 置空；查 $B$：探 $3$ 见空 $\Rightarrow$ 返回「不存在」——**实际 $B$ 还在 $4$**。这就是必须留墓碑的活证。

**5.** $m/n=10$：$k^\*=6.93\approx7$，$p\approx\left(1-e^{-0.7}\right)^7\approx0.0082$。$m/n=1$：$k^\*=\ln2\approx0.69$（取整 $1$），$p\approx\left(1-e^{-1}\right)^1\approx0.632$——**空间太小，假阳性高达 $63\%$**，所以布隆要「几比特/元素」以上才实用。

**6.** 二次探测避免一次簇聚，但会产生「二次簇聚」（同一起点走同一序列），$\alpha=0.8$ 时平均探测通常介于线性与双重之间；双重散列最好。代码可在 13.4 的 `probeCap` 里加一个 `i*i` 分支。

**7.** 计数布隆：数组元素是 `uint8_t` 计数器。插入对 $k$ 个位置 $+1$，删除 $-1$，查询看是否全 $>0$。删某元素只把它置过的位减 $1$，别人的贡献仍在，故不误伤。

**8.** $m=-\dfrac{n\ln p}{(\ln2)^2}=-\dfrac{10^6\cdot\ln0.01}{0.4805}\approx9.59\times10^6$ 比特 $\approx1.2$ MB；$k=\dfrac{m}{n}\ln2\approx6.64\approx7$。构造后插入 $10^6$ 个、查 $10^6$ 个新元素，实测假阳性应 $\approx1\%$。

**9.** SimHash：对每个特征 $f$ 取 `hash(f)` 的 64 位，按每位 $0/1$ 对向量 $+w_f/-w_f$ 累加，最后每位取符号得指纹；两文档指纹的**汉明距离**小即相似。相比 MinHash 更省（定长 64 位 / 文档）、可用汉明距离快速近似；但理论保证不如 MinHash 清晰（SimHash 的「随机超平面」解释）。

**10.** 每组 $(b,r)$ 的曲线在 $s\approx(1/b)^{1/r}$ 附近最陡（拐点）。想让「相似度 $>0.8$ 才算候选」，就选使拐点落在 $0.8$ 的 $(b,r)$：$r$ 大更严、$b$ 大更松。可打印若干 $(b,r)$ 的曲线比较。

**11.** 加 `size()` 返回 `size_`；遍历：链地址法双层循环（桶 $\times$ 链），开放定址单层扫 `state_==USED` 的槽。把结果存进 `vector<pair>` 排序后与 `unordered_map` 的快照比较，应完全一致。

**12.** 一致性哈希：把「节点」和「键」都哈希到 $[0,2^{32})$ 的环上，键归「顺时针第一个节点」。增删节点只重分布相邻一段（期望 $1/N$ 的键），远优于「取模」时几乎全量重分布。用虚拟节点（每物理节点放多个环上点）改善负载均衡。

**13.** 随机超平面 LSH：取 $L$ 组随机超平面，每组用若干比特签名，同桶即候选。对查询取同桶候选做精确距离排序，与暴力 top-k 比召回率；通常能拿到 $>90\%$ 召回 + 数量级加速。

**14.** Count-Min Sketch：$d$ 个哈希 + $w$ 个计数器行，更新时每行 $+1$，查询取每行计数**最小值**（因为哈希只增不减，故只高估）。误差 $\le \varepsilon N$ w.p. $1-\delta$，取 $w=\lceil e/\varepsilon\rceil,\ d=\lceil\ln(1/\delta)\rceil$。

**15.** VQ-VAE：编码器输出 $z_e$，在码本里找**最近邻** $e_k$（硬哈希）；前向用量化值，反向梯度**直通**回编码器（`z_q = z_e + (z_q-z_e).detach()`）。可微软化的极端是「对所有码本做 softmax 加权」。手动小网络 + 数值梯度比对，验证编码器能收到梯度。

</details>

---

## 延伸阅读

- **Sahni《数据结构、算法与应用（C++ 版）》第 2 版 第 10 章**「Skip Lists and Hashing」：跳表与哈希表的实现与分析（哈希函数、链地址法、开放定址、再哈希）；
- **《算法导论》第 11 章**「Hash Tables」——链地址法 / 开放定址的严格期望分析、通用哈希（universal hashing）、完全哈希（perfect hashing）；
- **Bloom, B. H. (1970)** *Space/Time Trade-offs in Hash Coding with Allowable Errors* —— 布隆过滤器原始论文；
- **Broder, A. (1997)** *On the resemblance and containment of documents* / **Broder et al. (1998)** —— MinHash 与文档相似度；
- **Indyk, P. & Motwani, R. (1998)** *Approximate nearest neighbors: towards removing the curse of dimensionality* —— LSH 的理论奠基；
- **Charikar, M. (2002)** *Similarity estimation techniques from rounding algorithms* —— SimHash；
- **Cormode & Muthukrishnan (2005)** *An improved data stream summary: the Count-Min sketch* —— 流式频率估计；
- **Johnson, Douze & Jégou (2019)** *Billion-scale similarity search with GPUs*（FAISS）—— 十亿级向量检索的工程实现；
- **Reformer（Kitaev et al., 2020）** *Reformer: The Efficient Transformer* —— 用 LSH 做稀疏注意力；
- 配套代码：`code/13_hashing.cpp`、`code/13_bloom_lsh.cpp`。

---

**下一章**：第 14 章《算法设计策略》—— 把前面散落的手段收成一张**方法论地图**：**分治**（归并 / 快选 / 大整数乘法 / 快速傅里叶变换的骨架）、**动态规划**（背包 / 编辑距离 / 最长公共子序列 / 维特比解码）、**回溯与分支限界**（N 皇后 / 旅行商 / 剪枝），并落到**隐马尔可夫模型（HMM）与维特比算法**这一世界模型 / 序列建模的经典工具上——讲清「暴力搜索 / 记忆化 / 递推 / 剪枝」如何把一个指数级问题变成多项式。
