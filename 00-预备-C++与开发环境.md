# 第 00 章 预备：C++ 与开发环境

> 这一章不是"数据结构"，但没有它，后面的每一章都会读得磕磕绊绊。数据结构课程里 90% 的困惑其实来自三件事：**模板看不懂、指针/内存管不清、不会测量性能**。本章把这三件事一次性补齐，并把它们和"为什么深度学习框架长这样"联系起来。

---

## 本章目标

学完本章，你应当能够：

1. 说清"接口与实现分离"是什么意思，并写出一个带私有实现细节的类；
2. 正确写出一个管理资源的类：构造、析构、拷贝、移动、`Rule of 0/3/5`；
3. 看懂并写出函数模板与类模板，理解"编译期多态"与"运行期多态"的区别；
4. 区分指针与引用，知道 `new/delete`、`unique_ptr`、`shared_ptr` 各自的使用场景与代价；
5. 理解 RAII，并能解释"为什么 RAII 是 C++ 最重要的设计思想"；
6. 用 `-std=c++17 -O2 -Wall -Wextra` 与消毒器编译程序，用 gdb/valgrind/perf 定位问题；
7. 用 `std::chrono` 做一次**可信的**性能测量，并说出微基准的三个常见陷阱。

## 前置知识

- 会写 C 风格的循环、数组、函数；
- 知道 `int`、`double`、`char`、`bool` 这些基本类型；
- 不需要任何数据结构基础。

---

## 0.1 为什么"数据结构"要先讲 C++

数据结构研究的是 **"数据怎么组织、操作怎么实现、代价是多少"**。用 Python 也能写数据结构的实验，但你会发现两件事：

1. 你**看不见**代价。Python 的 `list.append` 是摊还 $O(1)$，但你没机会亲手实现"扩容"这一步，于是永远只是背结论。
2. 你**改不动**实现。想换一种哈希冲突策略？CPython 的 `dict` 不给你这个口子。

C++ 的价值在于：**它是那种"能把代价暴露给你"的语言**。你能决定元素是连续存放还是用指针串起来、能决定扩容是 2 倍还是 1.5 倍、能数出到底搬了多少次元素。这些细节，恰恰是后来决定"你的模型能不能训得动、推理延迟是多少"的东西。

同时，C++ 也是现代 AI 基础设施的语言：PyTorch、TensorFlow、ONNX Runtime、TensorRT、vLLM、FAISS、Arrow 的核心都是 C++。你不需要成为 C++ 专家，但**必须能读懂并修改这些系统的关键部分**。

---

## 0.2 接口与实现分离：数据结构的核心思维

抽象数据类型（ADT, Abstract Data Type）的思想只有一句话：

> **使用者只依赖"能做什么"（接口），不依赖"怎么做"（实现）。**

以"线性表"为例，接口是：

| 操作 | 语义 |
|---|---|
| `size()` | 元素个数 |
| `get(i)` | 取第 i 个元素 |
| `set(i, x)` | 改第 i 个元素 |
| `insert(i, x)` | 在第 i 个位置插入 |
| `erase(i)` | 删除第 i 个元素 |
| `indexOf(x)` | 返回 x 第一次出现的下标 |

至于底层是"一整块连续数组"还是"一串用指针连起来的节点"——**接口不变**。这就是为什么同一份使用线性表的代码，可以今天用数组版、明天换成链表版而不用重写。

在 C++ 里，接口与实现的分离通过 `class` 的 `public` / `private` 完成：

```cpp
// 只暴露行为，隐藏数据
class LinearList {
public:
    virtual int  size() const = 0;
    virtual int  indexOf(const int& x) const = 0;
    virtual void insert(int i, const int& x) = 0;
    virtual void erase(int i) = 0;
    virtual ~LinearList() = default;   // 见 0.3：基类析构必须是虚的
};
```

> **一个原则**：类里的 `private` 部分，将来应当可以整体替换掉而不影响任何使用者。如果你改一个私有成员的类型，结果半个项目编译不过，说明接口没设计好。

**与 AI 的连接**：`torch.nn.Module` 就是典型的接口。你写 `forward(x)`，PyTorch 负责反向传播、设备搬运、混精。你不关心 `forward` 内部是卷积还是注意力——接口不变，实现可换。这就是"接口与实现分离"在框架层面的体现。

---

## 0.3 类与封装：从"能用"到"用对"

### 0.3.1 构造函数与析构函数

```cpp
class Buffer {
public:
    explicit Buffer(size_t n) : data_(new double[n]), n_(n) {}   // 构造：获取资源
    ~Buffer() { delete[] data_; }                                 // 析构：释放资源
private:
    double* data_;
    size_t  n_;
};
```

`explicit` 阻止 `Buffer b = 1024;` 这种隐式转换，避免莫名其妙的编译通过。**构造函数获取资源、析构函数释放资源**，这对函数叫做 RAII（见 0.5）。

### 0.3.2 拷贝、移动与 Rule of 0/3/5

编译器默认生成的拷贝是"逐成员拷贝"。对 `Buffer` 这种持有裸指针的类，逐成员拷贝会导致**两个对象指向同一块内存**，析构时二次释放 → 崩溃。所以：

- **Rule of 3**：如果你需要自定义**析构函数**、**拷贝构造**、**拷贝赋值**中的任意一个，通常三个都需要。
- **Rule of 5**：C++11 之后再加上**移动构造**与**移动赋值**。
- **Rule of 0**：最好的情况是——**不要手写任何一个**。把资源交给 `std::vector`、`std::string`、`unique_ptr` 去管，编译器生成的版本自动正确。

```cpp
class Buffer {
public:
    explicit Buffer(size_t n) : data_(n, 0.0) {}   // Rule of 0：用 vector 管内存
    double& operator[](size_t i) { return data_[i]; }
    size_t  size() const { return data_.size(); }
private:
    std::vector<double> data_;
};
```

这版**不需要**写析构、拷贝、移动，编译器全给你生成对。**Rule of 0 应作为默认选择**，只有在必须管理裸资源（句柄、显存、文件描述符）时才升级到 Rule of 5。

移动语义的意义：拷贝是"复制一份数据"，移动是"把资源偷过来，把源对象置空"。

```cpp
std::vector<double> a(1'000'000, 1.0);
std::vector<double> b = std::move(a);   // O(1)：只搬了三个指针，不复制一百万个 double
// 此后 a 处于"有效但未指定"状态，不要再用它的内容
```

`= default` 与 `= delete` 用来显式表达意图：

```cpp
class MutexLike {
public:
    MutexLike() = default;
    MutexLike(const MutexLike&) = delete;            // 锁不可拷贝
    MutexLike& operator=(const MutexLike&) = delete;
};
```

### 0.3.3 `const` 正确性

`const` 是给**编译器**看的契约，它能在编译期帮你抓出"不小心改了一个不该改的对象"：

```cpp
class Buffer {
public:
    double at(size_t i) const { return data_.at(i); }  // 尾部的 const：不修改本对象
private:
    std::vector<double> data_;
};
```

**经验法则**：所有不修改对象状态的成员函数都加尾随 `const`。这样 `const Buffer&` 才能调用它们，也是后面写"只读视图""迭代器"的基础。

---

## 0.4 模板：编译期多态

### 0.4.1 函数模板

```cpp
template <typename T>
const T& myMax(const T& a, const T& b) { return (a < b) ? b : a; }

myMax(3, 5);          // T = int
myMax(2.5, 1.5);      // T = double
myMax<std::string>("a", "b");  // 显式指定
```

模板不是"运行时的泛型"，而是**编译器为每种用到的类型各生成一份代码**（实例化）。

### 0.4.2 类模板

```cpp
template <typename T>
class Stack {
public:
    void push(const T& x) { data_.push_back(x); }
    T pop() {
        T v = data_.back();     // 先拷贝（或移动）
        data_.pop_back();
        return v;
    }
    bool empty() const { return data_.empty(); }
private:
    std::vector<T> data_;
};

Stack<int> si;
Stack<std::string> ss;
```

> **为什么 STL 全靠模板**：如果 `std::vector` 是运行期多态（基类 + 虚函数），那么每次 `push_back` 都要一次虚调用、每个元素都要一次动态分派、且元素类型必须能"装箱"。模板让这些开销全部消失——**这是 C++ 容器能比手写循环还快的根本原因**。

### 0.4.3 编译期多态 vs 运行期多态

| | 编译期多态（模板） | 运行期多态（虚函数） |
|---|---|---|
| 绑定时机 | 编译期 | 运行期（虚表） |
| 调用开销 | 零（可内联） | 一次间接跳转 |
| 代码体积 | 每种类型一份（膨胀） | 一份 |
| 能否异构容器 | 不能 | 能（基类指针） |
| 典型用途 | 容器、算法、算子 | 框架插件、模块系统 |

**与 AI 的连接**：这是同一个问题在框架层的翻版。PyTorch 的算子既要在 CPU 上跑、又要在 CUDA 上跑，早期方案是运行期多态（`at::Backend` 分派），但热点路径必须靠**模板 + 编译期分派**才能达到峰值性能。你在读 PyTorch 源码时会看到大量 `AT_DISPATCH_FLOATING_TYPES` 之类的宏——本质就是"为每种标量类型实例化一份模板代码"。

---

## 0.5 指针、引用、智能指针与 RAII

### 0.5.1 指针 vs 引用

```cpp
int x = 1, y = 2;
int* p = &x;    // 指针：可以重新指向、可以为 nullptr、可以算术运算
int& r = x;     // 引用：必须绑定、不可重新绑定、语法上像本体

*p = 10;   // x 变成 10
r  = 20;   // x 变成 20
```

**选择建议**：函数参数若只是"借用且非空"，用 `const T&`；若"可能为空"或"需要改变指向"，用指针。

### 0.5.2 裸 `new/delete` 的问题

```cpp
double* p = new double[n];
// ... 中间万一抛异常或者 return 了 ...
delete[] p;        // 忘了这一行 → 内存泄漏
// 或者：delete p;  ← 用了标量的 delete 释放数组 → 未定义行为
```

`new/delete` 要求你**手工配对**，一旦控制流变复杂（异常、多返回值）就容易出错。这就是智能指针存在的理由。

### 0.5.3 智能指针

```cpp
#include <memory>

// 独占所有权：不能拷贝，只能移动
std::unique_ptr<double[]> buf(new double[n]);
std::unique_ptr<Buffer> b = std::make_unique<Buffer>(1024);
// 离开作用域自动 delete[]/delete，不需要写任何释放代码

// 共享所有权：引用计数，最后一个持有者释放
std::shared_ptr<Buffer> s1 = std::make_shared<Buffer>(1024);
std::shared_ptr<Buffer> s2 = s1;      // 引用计数变 2
// 注意：shared_ptr 有原子计数开销，且无法表达"互相引用"（会泄漏）

// 打破循环引用：weak_ptr 不增加引用计数
std::weak_ptr<Buffer> w = s1;
if (auto locked = w.lock()) { /* 对象还活着 */ }
```

**三个智能指针的选择**：
- `unique_ptr`：默认选择，零开销，表达"独占"；
- `shared_ptr`：确实需要共享生命周期时才用（有原子操作代价）；
- `weak_ptr`：打破 `shared_ptr` 的循环引用，或做"观察者"。

### 0.5.4 RAII：C++ 最重要的设计思想

**RAII（Resource Acquisition Is Initialization，资源获取即初始化）**：把资源的生命周期绑定到对象的生命周期上。

```cpp
class ScopedTimer {
public:
    explicit ScopedTimer(const char* name)
        : name_(name), start_(std::chrono::high_resolution_clock::now()) {}
    ~ScopedTimer() {   // 无论正常返回还是抛异常，析构一定会执行
        auto end = std::chrono::high_resolution_clock::now();
        double ms = std::chrono::duration<double, std::milli>(end - start_).count();
        std::cout << name_ << " 用时 " << ms << " ms\n";
    }
private:
    const char* name_;
    std::chrono::high_resolution_clock::time_point start_;
};

void f() {
    ScopedTimer t("f");       // 构造：开始计时
    // ... 无论这里做什么，哪怕抛异常 ...
}                             // 析构：自动打印耗时
```

RAII 的三条推论：

1. **不需要手写 `finally`**；
2. **异常安全**变成默认行为（不用在每个 `return` 前清理）；
3. 资源种类可以任意扩展：内存、文件、锁、socket、**GPU 显存**。

**与 AI 的连接**：这是"为什么 PyTorch 的张量不需要你手动 `free`"的答案。`torch::Tensor` 的析构函数通过引用计数（类似 `shared_ptr`）释放显存；CUDA 的流、事件、显存池也都是 RAII 对象。你在写 C++/CUDA 扩展时，如果手动 `cudaMalloc` 却忘了 `cudaFree`，训练跑几小时后就会 OOM——而 `torch::Tensor` 从设计上就不给你这个机会。

---

## 0.6 异常与异常安全

```cpp
#include <stdexcept>

double at(const std::vector<double>& v, size_t i) {
    if (i >= v.size()) throw std::out_of_range("下标越界");
    return v[i];
}
```

异常安全分三个等级：

| 等级 | 保证 | 例子 |
|---|---|---|
| 基本保证（basic） | 不泄漏、对象仍处于有效状态（内容可能已变） | 大多数操作 |
| 强保证（strong） | 失败时状态完全回滚，如同没调用过 | `vector::push_back`（容量足够时） |
| 不抛保证（no-throw） | 承诺不抛异常 | 析构函数、`swap`、移动构造 |

**三条实用规则**：

1. **析构函数绝不应该抛异常**（否则栈展开时可能 `std::terminate`）；
2. 先做可能失败的事（分配内存、打开文件），再做不会失败的事（改指针），这样容易拿到强保证——这就是"`copy-and-swap`"惯用法；
3. 不确定时，至少做到**基本保证**：不泄漏、不留半死不活的对象。

---

## 0.7 迭代器与 STL 的统一接口

迭代器是"指针的抽象"：它让你用同一套代码遍历数组、链表、哈希表。

```cpp
std::vector<int> v{1, 2, 3, 4, 5};

for (std::vector<int>::iterator it = v.begin(); it != v.end(); ++it)
    std::cout << *it << ' ';

for (int x : v) std::cout << x << ' ';          // range-for，等价写法

auto it = std::find(v.begin(), v.end(), 3);     // 算法只依赖迭代器，不依赖容器
```

迭代器有五个"能力等级"（`input` / `output` / `forward` / `bidirectional` / `random_access`），能力越强，能用的算法越多：

- `std::vector`、`std::deque`、`std::array`、原生指针 → **随机访问**（`it + n`、`it[n]`、`<` 比较）；
- `std::list`、`std::map` → **双向**（只能 `++`/`--`，不能跳）；
- `std::forward_list` → **前向**。

**这直接决定了复杂度**：第 02 章的数组能用 `O(log n)` 二分查找，第 03 章的链表不行——不是算法不好，是迭代器能力不够。

> **重要**：容器"失效规则"。`std::vector` 扩容后所有迭代器、指针、引用**全部失效**；`std::list` 只有被删除元素的迭代器失效。这是后面写链表/哈希表时最容易踩的坑。

---

## 0.8 工具链：编译、调试、剖析

### 0.8.1 编译

```bash
# 基础：C++17 + 二级优化 + 打开常用警告
g++ -std=c++17 -O2 -Wall -Wextra code/02_array_list.cpp -o /tmp/ds02

# 开发期（推荐）：抓越界、悬垂指针、未定义行为
g++ -std=c++17 -O1 -g -fsanitize=address,undefined code/03_chain.cpp -o /tmp/ds03
/tmp/ds03
```

- `-Wall -Wextra`：打开大部分警告。**不要忽略警告**，数据结构代码里的警告往往就是 bug 的预告。
- `-fsanitize=address,undefined`（ASan/UBSan）：运行期检查数组越界、释放后使用、整数溢出、未定义行为。**代价是慢 2–3 倍，但值得**——它抓 bug 的效率比 printf 高一个数量级。
- `-g`：生成调试信息，供 gdb 使用。

最小 `CMakeLists.txt`：

```cmake
cmake_minimum_required(VERSION 3.16)
project(ds_study CXX)
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
add_compile_options(-Wall -Wextra)

add_executable(demo code/02_array_list.cpp)
```

### 0.8.2 调试与剖析

```bash
gdb /tmp/ds02                # 崩溃后 bt 看调用栈、print 看变量
valgrind --leak-check=full /tmp/ds02   # 内存泄漏与非法访问（比 ASan 慢，但更全）
perf stat /tmp/ds02          # 指令数、cache miss、分支预测失败率
perf record ./prog && perf report       # 热点函数
```

**与 AI 的连接**：`perf` / `nsys` / `ncu`（Nsight Compute）是同一类东西在不同层次上的实现。你未来调优化器、看 kernel 是不是 memory-bound，靠的就是这套"用数据而不是直觉"的方法。

---

## 0.9 如何科学地测量性能

这一段是本章最重要的内容，因为**大多数性能结论的错误都源于糟糕的测量**。

### 三个陷阱

**陷阱一：编译器把你的代码优化没了。** 如果你算了半天却从不使用结果，`-O2` 下整段代码会被删掉。

```cpp
volatile double sink;               // volatile 阻止"消除无用计算"
double t = 0;
for (int i = 0; i < n; ++i) t += a[i];
sink = t;                           // 让结果"逃逸"出编译器视野
```

**陷阱二：没有热身。** 第一次运行要经历缺页中断、cache 冷启动、分支预测器预热，耗时往往高出数倍。

**陷阱三：单次测量噪声太大。** 正确做法是**重复多次取最小值或中位数**：

```cpp
template <typename F>
double bestOf(const char* name, F fn, int warmup = 3, int reps = 10) {
    for (int i = 0; i < warmup; ++i) fn();                 // 热身
    double best = 1e300;
    for (int i = 0; i < reps; ++i) {
        auto t0 = std::chrono::high_resolution_clock::now();
        fn();
        auto t1 = std::chrono::high_resolution_clock::now();
        double ms = std::chrono::duration<double, std::milli>(t1 - t0).count();
        if (ms < best) best = ms;                          // 取最小值
    }
    std::cout << name << ": " << best << " ms\n";
    return best;
}
```

> 为什么取**最小值**而不是平均值？因为干扰（其他进程、中断、频率波动）只会让耗时**变长**，最小值最接近"这段代码真正需要的时间"。

### 内存访问模式：一个必须亲手体验的现象

```cpp
// 顺序访问：硬件预取器友好，i = 0,1,2,...,n-1 一趟走完，共 n 次加法
for (size_t i = 0; i < n; ++i) sum += a[i];

// 跨步访问（stride = 16）：每个 pass 走 n/16 次，跑 16 个 pass
// -> 加法次数同样是 n，只是「访问顺序」不同（每次都落在新的 cache line 上）
for (size_t pass = 0; pass < 16; ++pass)
    for (size_t i = pass; i < n; i += 16) sum += a[i];
```

**关键：让两者的加法次数完全相同**（都是 $n$ 次）——否则比的就不是「内存访问模式」，而是「谁做的活儿少」。**在同样计算量的前提下**，耗时仍可能差 **数倍以上**，差别不在"算法复杂度"，而在**内存层次**（L1/L2/L3/DRAM 的延迟差 10–100 倍）。

**与 AI 的连接**：这正是"为什么 batch 的存储布局（NCHW vs NHWC）能让卷积快 30%"、"为什么 FlashAttention 要做分块"、"为什么稀疏张量要用 CSR 而不是坐标列表"的底层原因。**在大模型时代，性能瓶颈往往不是计算量，而是数据搬运量。** 从第 4 章开始，我们会反复回到这一点。

---

## 常见错误与陷阱

1. **Rule of 3 用错**：自定义了析构却用默认拷贝 → 双重释放。
2. **忘记 `explicit`**：`Buffer b = 1024;` 意外编译通过。
3. **析构函数抛异常** → `std::terminate`。
4. **`delete` 数组用了 `delete` 而不是 `delete[]`** → 未定义行为。
5. **`shared_ptr` 循环引用** → 内存永不释放（用 `weak_ptr` 打破）。
6. **扩容后继续用旧迭代器/引用**（`vector` 尤其危险）。
7. **模板代码写得太"聪明"**：`const T&` 与 `T&&` 重载混淆，在 `T = std::string` 时参数错配。**简单直接优先。**
8. **忘了 `#include <algorithm>`** 就用 `std::sort`/`std::reverse`/`std::max_element`。
9. **测量时被优化器删掉代码**，得出"耗时 0 ms"的荒谬结论。
10. **只看平均值不看分布**：p99 延迟才是线上系统的关键指标。

---

## 与 AI / 世界模型的连接

把本章的知识点映射到你未来的工作：

| 本章概念 | 在 AI 系统里的落点 |
|---|---|
| 接口与实现分离 | `nn.Module.forward`、`Dataset.__getitem__`、算子注册表（ATen） |
| Rule of 0/5 + RAII | `torch::Tensor` 的引用计数、CUDA 流/事件、显存池、文件句柄 |
| 模板（编译期多态） | `AT_DISPATCH_FLOATING_TYPES`、kernel 的 dtype 特化、`cutlass` 的模板元编程 |
| 虚函数（运行期多态） | 后端分派（CPU/CUDA/MPS）、优化器基类、插件式模块 |
| `unique_ptr`/`shared_ptr` | 计算图节点所有权、参数共享（embeddings tying）、KV cache 块管理（`vLLM` 的 block manager） |
| `const` 正确性 | 只读推理路径、`torch.no_grad()` 语义 |
| 迭代器与失效规则 | 张量视图（view）与底层存储的别名关系；in-place 操作导致的其他视图失效 |
| ASan/UBSan/perf | 自定义算子调试、`nsys`/`ncu` 剖析 CUDA kernel |

**一个具体例子：为什么要自己写 C++/CUDA 扩展？**

1. **自定义算子**：框架没有的算子（如某种稀疏注意力、特定的数据增强）；
2. **融合**：把 `add → relu → dropout` 三个 kernel 合成一个，减少显存往返（kernel fusion）；
3. **部署与延迟**：推理侧要求低延迟、少依赖，Python 解释器开销不可接受；
4. **数据预处理**：从海量原始数据里解析样本往往是训练瓶颈（GPU 利用率低不是因为算得慢，而是因为数据供给慢）。

`pybind11` 是最常用的"把 C++ 暴露给 Python"的工具，一行代码就能把一个 C++ 函数变成 Python 可调用对象：

```cpp
// 概念示意（需安装 pybind11，本资料代码不含此依赖）
PYBIND11_MODULE(fast_ops, m) {
    m.def("dot", &fast_dot, "快速点积");   // Python 侧即可 fast_ops.dot(a, b)
}
```

**但先别急着写扩展**：首选是"用现成算子组合"（框架会做图优化），其次是 `torch.compile` 之类的自动融合，最后才轮到手动写 kernel。**过早优化是万恶之源**——这句话在 AI 系统里尤其成立。

---

## 本章小结

- **接口与实现分离**是数据结构的第一原则：使用者只依赖行为。
- **Rule of 0 是默认选择**；只有管理裸资源时才用 Rule of 5。
- **模板 = 编译期多态**，零开销但代码会膨胀；虚函数 = 运行期多态，灵活但有间接跳转。
- **RAII 把资源生命周期绑定到对象生命周期**，是异常安全与"不用手写 free"的基础。
- **智能指针**：`unique_ptr` 默认、`shared_ptr` 慎用、`weak_ptr` 破环。
- **迭代器有五个能力等级**，直接决定可用算法与复杂度；容器有失效规则。
- **测量要先热身、取最小值、防优化**；内存访问模式常比算法复杂度更影响实际性能。
- 用 `-Wall -Wextra` + ASan/UBSan 编译，用 gdb/valgrind/perf 定位问题。

---

## 练习

### 基础

1. 写一个 `explicit` 的 `Circle(double r)` 类，提供 `area() const`，并解释去掉 `explicit` 后 `Circle c = 3.0;` 为什么会编译通过。
2. 指出下面代码的错误：
   ```cpp
   class A { int* p; public: A(int n): p(new int[n]) {} ~A(){ delete p; } };
   ```
3. 举一个必须用 `weak_ptr` 而不能用 `shared_ptr` 的场景。
4. `std::vector<int> v(10); v.push_back(1);` 之后，`v.size()` 是多少？如果之前保存了 `v.begin()`，还能用吗？
5. 解释 `const std::vector<int>& v` 与 `std::vector<int>& const v` 的区别（提示：后者其实是非法的）。

### 进阶

6. 为 `Buffer` 类实现 Rule of 5，并用 `assert` 验证移动后源对象处于空状态。
7. 写一个 `ScopedFile`，构造时打开文件、析构时关闭，并保证不会泄漏（即使中途 `throw`）。
8. 用 `std::chrono` 比较 `std::vector<double>` 的 `sum via index` 与 `sum via iterator`，解释差异（提示：`-O2` 下应当几乎无差异）。
9. 用 ASan 运行一段"越界写入"的代码，观察报错信息，并说明它比"程序莫名崩溃"好在哪里。
10. 写一个模板函数 `printAll(const Container&)`，要求对 `vector`、`list`、`map` 都能工作，并解释它为什么依赖迭代器。

### 挑战

11. 设计一个 `ObjectPool<T>`：用 RAII 借出/归还对象，避免频繁 `new/delete`。给出接口与实现，并分析摊还复杂度。（提示：这正是内存分配器与 PyTorch caching allocator 的雏形。）
12. 用 `perf stat` 测量顺序访问与跨步访问的 `cache-misses`，把数字与耗时对应起来，写一段解释。
13. 解释这个现象：把 `std::vector` 的扩容因子从 2 改成 1.5，摊还复杂度仍是 $O(1)$，但实测常数变大。为什么？（提示：内存回收器与"能否复用刚释放的块"。）

<details><summary>参考答案与提示</summary>

**1.** `explicit` 禁止"单参数构造函数的隐式转换"。去掉后 `Circle c = 3.0;` 会调用 `Circle::Circle(double)`，把 `3.0` 当半径——语义上说得通，但会掩盖参数类型错误。**建议所有单参数构造函数都加 `explicit`**。

**2.** 两处错误：`delete p;` 应为 `delete[] p;`；同时缺少拷贝构造/赋值，默认逐成员拷贝会造成双重释放（Rule of 3 被违反）。

**3.** 父节点持有子节点、子节点又需要回指父节点的树结构（如自动微分图的 `parent` 指针、GUI 的父子关系）。若两边都用 `shared_ptr`，引用计数永不归零。

**4.** `size()` 是 11（先构造 10 个默认元素，再 push 一个）。之前保存的 `v.begin()` **已失效**——`push_back` 可能触发扩容，所有迭代器/引用作废。

**5.** `const std::vector<int>& v` 是"绑定到 const 对象的引用"（合法）；`std::vector<int>& const v` 是"const 的引用"，引用本身不可改，而引用天生不可重绑定，所以修饰符是多余的——但 C++ 允许（`typedef` 场景下才有意义）。

**6.** 要点：移动构造/移动赋值要把源的指针置 `nullptr`，析构里 `delete[] nullptr` 是安全的。验证：`assert(src.data() == nullptr)`。

**7.** 要点：成员为 `FILE*` 或 `std::ifstream`；构造里若打开失败就 `throw`；析构里 `close()` 且不抛异常。用 `std::ifstream` 直接得到 Rule of 0 的版本。

**8.** `-O2` 下两者生成几乎相同的汇编，因为迭代器会被内联成指针运算。**结论：不要为了"性能"拒绝使用迭代器**；可读性优先。

**9.** ASan 会打印"哪一行越界、越界多少字节、是谁分配的、内存块多大"，而不用 ASan 时可能表现为"偶尔结果错""换个编译器就好"。

**10.** 依赖 `begin()/end()` 与 `++`、`*`、`!=` 这几个迭代器操作——凡提供这些操作的容器都能工作。这是 STL 算法与容器解耦的机制。

**11.** 要点：`ObjectPool` 内部维护空闲链表（第 03 章会讲）；`acquire()` 优先从空闲链表取，取不到再 `new`；`release()` 放回。摊还 $O(1)$；归还的对象不析构，因此适合"构造/析构昂贵"而"可重置"的对象。PyTorch 的 caching allocator 正是这个思路：显存块回收后不还给驱动，而是留在池里复用。

**12.** 顺序访问的 `cache-misses` 显著低于跨步访问，`LLC-load-misses` 差别尤其大；耗时比大致等于 DRAM 延迟与 cache 延迟之比。

**13.** 扩容因子越小，释放的旧块越小，越容易被分配器复用（不会轻易触发 `mmap`/`munmap` 系统调用）；而 2 倍扩张在稳定增长阶段释放的块尺寸"跳变"更大，反而可能触发系统调用与缺页。这是"理论复杂度相同、实测常数不同"的典型例子。

</details>

---

## 延伸阅读

- Sahni，《数据结构、算法与应用（C++版）》**第 2 版 第 1 章 C++ Review**（类、模板、引用、异常的基础），**第 4 章 Performance Measurement**（测量方法学）。*章节号以第 2 版为准，不同版次可能略有差异。*
- C++ Core Guidelines（尤其 "R" 系列，资源管理）：搜索 "C++ Core Guidelines R.1"，讲 RAII 与所有权。
- 《Effective Modern C++》（Scott Meyers）第 1–3 章：类型推导、`auto`、智能指针。
- `code/` 目录下本章配套程序：`00_raii_smart_ptr.cpp`、`00_template_class.cpp`、`00_benchmark_chrono.cpp`，可编译运行。
