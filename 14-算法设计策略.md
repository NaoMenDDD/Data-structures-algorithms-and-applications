# 第 14 章 算法设计策略

> 前面每一章都在讲「**某个数据结构 + 某个算法**」：哈希表怎么建、最短路怎么走、排序怎么排。这一章换一个视角——把散落一地的具体算法收成**四张方法论地图**：**分治（Divide & Conquer）**、**动态规划（Dynamic Programming）**、**回溯（Backtracking）**、**分支限界（Branch & Bound）**。
>
> 它们的价值不在于「又学会了一道题」，而在于：**下次遇到一个从没见过的新问题，你脑子里能先亮起一盏灯**——「这个问题能不能切成独立的同类子问题？」（分治）「不同路径会不会反复遇到同一个子状态？」（DP）「我能不能一边枚举一边剪枝？」（回溯 / 分支限界）。
>
> 更关键的是：这四种策略**今天仍然是 AI 的骨架**。维特比解码（序列上的 argmax DP）、CTC、CRF、束搜索、乃至规划与世界模型里的 rollout，用的都是这里的思想。**DP 是「在庞大解空间里，用记忆化把指数压成多项式」的通用魔法**——这正是大模型之外，AI 里最朴素也最强大的武器。

---

## 本章目标

读完并跑完本章代码，你应该能够：

1. 建立一张 **「什么特征的问题用哪种策略」的决策地图**（分治 / DP / 回溯 / 分支限界 / 贪心的判据）；
2. **分治**：用**循环不变式**讲清二分查找为什么对，并说清 `lower_bound / upper_bound` 的区别；手写**快速幂**与**矩阵快速幂**，理解「指数 / 线性递推都能二分」；用**主定理**分析 $T(n)=aT(n/b)+f(n)$；
3. 手写 **Karatsuba 大整数乘法**（4 次子乘法降到 3 次），实测乘法次数随位数的增长，验证 $O(n^{\log_2 3})\approx O(n^{1.585})$；
4. **动态规划**：说清 **最优子结构 + 重叠子问题** 两要素；从**朴素递归**（指数）走到**记忆化**再到**递推**，用调用次数实测「子问题重叠」；手写 **0/1 背包、LCS、编辑距离、LIS**，并会**回溯还原**具体方案；
5. 说清 **自顶向下（记忆化递归）** 与 **自底向上（递推）** 两种 DP 写法的取舍；
6. **回溯**：用位掩码手写 **N 皇后**（实测 $n=8$ 有 $92$ 解）、**数独求解**（行/列/宫剪枝），并验证枚举**子集数 $=2^n$、排列数 $=n!$**；
7. **分支限界**：为 TSP 设计一个**下界**，用「下界 $\ge$ 当前最优就回退」把 $(n-1)!$ 的暴搜砍掉几个数量级，并理解**启发式给上界、下界给保证**的「夹逼」结构；
8. 手写 **HMM 的维特比解码**（序列上的 max-product DP）与**前向算法**（序列上的 sum-product DP），并与暴力枚举对拍——这是通往 **CRF / CTC / 序列世界模型** 的钥匙。

---

## 前置知识

- 第 01 章（算法分析）—— $O(\cdot)$、**递归式与主定理**、均摊；
- 第 08 章（堆）/ 第 11 章（图）—— 贪心的例子（Huffman、Dijkstra、Kruskal）已在那里出现；
- 第 12 章（排序/选择）—— 归并、快排、快选本身就是分治的范本；
- 一点点**概率**：HMM 的概率乘积、对数概率（防止下溢）。

---

## 14.1 四把锤子：遇到问题先亮哪盏灯

| 策略 | 一句话核心 | 典型问题 | 复杂度特征 | 解的性质 |
|---|---|---|---|---|
| **分治** | 切成**独立**的同型子问题，再合并 | 归并/快排、二分、快速幂、Karatsuba、FFT | 主定理 $T(n)=aT(n/b)+f(n)$ | 精确 |
| **动态规划** | 分治 + **记忆化**（子问题**重叠**） | 背包、LCS、编辑距离、LIS、维特比 | 状态数 $\times$ 转移代价 | 精确（最优） |
| **回溯** | **深度优先枚举 + 剪枝** | N 皇后、数独、子集/排列、走迷宫 | 最坏指数，剪枝后常可行 | 精确（可行解/最优） |
| **分支限界** | 回溯 + **界函数**剪枝 | TSP、整数规划、作业调度 | 最坏指数，界好则快 | 精确最优 |
| **贪心** | 每步取**局部最优** | 区间调度、Huffman、Kruskal/Dijkstra | 常 $O(n\log n)$ | 需**证明**，否则只是近似 |

**三条判据，先问自己：**

1. **子问题独立吗？** 独立 $\Rightarrow$ **分治**（一半规模的问题算完不管另一半）；
2. **子问题重叠吗？** 重叠（同一子状态反复出现）$\Rightarrow$ **DP**（算一次存起来）。**分治和 DP 的共同祖先是「递归 + 拆分」，区别只在「要不要存子问题答案」。**
3. **解空间是一棵「可选可退」的树吗？** 是 $\Rightarrow$ **回溯**；若还能给每个部分解算一个「乐观下界」$\Rightarrow$ **分支限界**。

而**贪心**是个「危险的朋友」：它最快，但**不一定对**。背包用贪心（按性价比）会错，而分数背包、Huffman、最小生成树用贪心恰好对——**区别在于有没有「拟阵」结构 / 交换论证**。本章会用 TSP 与背包把这几把锤子串起来对比。

---

## 14.2 分治：把大问题切成同型的小问题

分治三步曲：**Divide（拆）— Conquer（递归解）— Combine（合并）**。它的复杂度由**主定理**给出。分治的经典范本（归并、快排、快选）第 12 章已讲透，这里补三个「非排序」的分治。

### 14.2.1 二分查找：循环不变式

二分的正确性靠**循环不变式**：**答案永远落在区间 $[lo, hi)$ 内**。

```cpp
int lowerBound(const std::vector<int>& a, int target, long long& comps) {
    int lo = 0, hi = static_cast<int>(a.size());          // 区间 [lo, hi)
    while (lo < hi) {
        const int mid = lo + (hi - lo) / 2;               // 防溢出写法
        ++comps;
        if (a[mid] < target) lo = mid + 1;                // mid 及其左侧排除
        else                 hi = mid;                    // mid 可能是答案，保留
    }
    return lo;                                            // lo == hi 即答案
}
```

- `lower_bound`：第一个 $\ge$ target 的位置（`a[mid] < target` 才右移）；
- `upper_bound`：第一个 $>$ target 的位置（`a[mid] <= target` 才右移）。

两者只差一个 `<` 和 `<=`，但**语义天差地别**：用 `upper - lower` 即得 target 的**出现次数**。实测比较次数（200 次随机查询平均）：

```
===== 1) 二分查找：比较次数 vs O(log n) =====
  n=16	二分=4	线性=12	log2(n)=4
  n=128	二分=7	线性=97	log2(n)=7
  n=1024	二分=10	线性=790	log2(n)=10
  n=16384	二分=14	线性=12652	log2(n)=14
  n=262144	二分=18	线性=194308	log2(n)=18
  [断言通过] lower_bound/upper_bound 与 std 一致
```

二分比较次数**恰等于 $\log_2 n$**（每轮砍一半）；线性查找在 $n=262144$ 时要 $19$ 万次——**差了一万倍**。这就是 $O(\log n)$ 与 $O(n)$ 的鸿沟。

### 14.2.2 快速幂与矩阵快速幂

朴素算 $a^n$ 要乘 $n$ 次。**二分**（快速幂）只要 $\log n$ 次：

$$a^n = \begin{cases}(a^{n/2})^2 & n \text{ 偶}\\ a\cdot(a^{(n-1)/2})^2 & n \text{ 奇}\end{cases}$$

```cpp
long long powFast(long long a, long long n) {
    if (n == 0) return 1;
    const long long half = powFast(a, n / 2);   // 只递归一次
    const long long sq = half * half;
    return (n & 1) ? sq * a : sq;
}
```

**同样的二分思想能加速「线性递推」。** 斐波那契满足矩阵递推：

$$\begin{bmatrix}F(n+1)\\F(n)\end{bmatrix}=\begin{bmatrix}1&1\\1&0\end{bmatrix}^{n}\begin{bmatrix}F(1)\\F(0)\end{bmatrix}$$

于是 $F(n)$ 只需 $\log n$ 次 $2\times2$ 矩阵乘法——**指数和递推都能二分**。实测与朴素 $O(n)$ 版一致：

```
===== 2) 快速幂：朴素 O(n) vs 二分 O(log n) =====
  2^10 = 1024	快速幂一致: yes
  2^20 = 1048576	快速幂一致: yes
  2^30 = 1073741824	快速幂一致: yes
  2^40 = 1099511627776	快速幂一致: yes
  费马小定理: 123456789^(p-1) mod p = 1 (应为 1) -> OK
  ---- 矩阵快速幂求斐波那契：O(log n) vs 朴素 O(n) ----
  F(10) = 55	一致性: yes
  F(50) = 12586269025	一致性: yes
  F(90) = 2880067194370816120	一致性: yes
```

取模版快速幂（`powMod`）是 RSA、哈希、组合数取模的发动机——`123456789^(p-1) mod p = 1` 正是**费马小定理**。

> **「快速幂」的本质**：把 $n$ 写成二进制 $n=\sum b_i 2^i$，答案 $a^n=\prod_{b_i=1} a^{2^i}$，而 $a^{2^i}$ 由反复平方得到。**它和「二分」是同一件事的两种说法。**

### 14.2.3 Karatsuba：3 次子乘法的魔法

朴素竖式乘法 $n$ 位数 $\times$ $n$ 位数要 $n^2$ 次单位数乘法，$O(n^2)$。**Karatsuba（1960）** 把 $n$ 位数按 $10^{n/2}$ 拆成两半：

$$x=x_1 B+x_0,\quad y=y_1 B+y_0,\quad B=10^{n/2}$$
$$xy = z_2 B^2+z_1 B+z_0,\quad z_2=x_1y_1,\ z_0=x_0y_0$$

朴素要算 $x_1y_1,\ x_1y_0,\ x_0y_1,\ x_0y_0$ **四次**子乘法。Karatsuba 的洞见是 **$z_1=(x_1+x_0)(y_1+y_0)-z_2-z_0$**——**用 3 次乘法（$z_2,z_0,(x_1+x_0)(y_1+y_0)$）加几次减法**，换掉一次乘法。于是：

$$T(n)=3T(n/2)+O(n)\ \xrightarrow{\ \text{主定理}\ }\ O(n^{\log_2 3})\approx O(n^{1.585})$$

**指数 $1.585<2$**，所以位数越大越划算。用十进制大整数（`vector<int>` 存每位，避免 `long long` 溢出）实测乘法次数：

```
===== 3) Karatsuba：乘法次数对照（十进制大整数） =====
  (竖式阈值打底 = 16 位)
  d=16	竖式=256	Karatsuba=256	加速比=1.00	d^1.585=81	一致=yes
  d=32	竖式=1024	Karatsuba=784	加速比=1.31	d^1.585=243	一致=yes
  d=64	竖式=4096	Karatsuba=2214	加速比=1.85	d^1.585=729	一致=yes
  d=128	竖式=16384	Karatsuba=6614	加速比=2.48	d^1.585=2187	一致=yes
  d=256	竖式=65536	Karatsuba=20160	加速比=3.25	d^1.585=6562	一致=yes
  d=512	竖式=262144	Karatsuba=60148	加速比=4.36	d^1.585=19688	一致=yes
  d=1024	竖式=1048576	Karatsuba=180116	加速比=5.82	d^1.585=59064	一致=yes
  [断言通过] 与竖式结果一致；111111111^2=12345678987654321 校验通过
```

读法：竖式严格是 $d^2$（$1024^2=1048576$）；Karatsuba 的次数除以 $d^{1.585}$ 基本是**常数 $\approx 3.05$**（$256/81\approx3.16$、$180116/59064\approx3.05$）——这正说明它**精确地按 $n^{1.585}$ 增长**，常数来自「16 位打底」的阈值开销。位数越大，加速比越大（$1024$ 位时快 $5.8$ 倍）。**这就是「把对数从 2 降到 1.585」省出来的。**

> **历史注**：Karatsuba 是第一个「比竖式更快」的乘法，打破了「$O(n^2)$ 是乘法下界」的错觉。之后有 Toom–Cook（$O(n^{1.465})$）、**FFT 乘法**（$O(n\log n)$，用于 π 计算、大数库 GMP），但**分治的骨架始终是同一个**。

### 主定理速查

对 $T(n)=aT(n/b)+f(n)$，令 $c=\log_b a$（子问题总工作量的「临界指数」），比较 $f(n)$ 与 $n^c$：

| 情形 | 条件 | 结论 | 例子 |
|---|---|---|---|
| 1 | $f(n)=O(n^{c-\varepsilon})$（叶子主导） | $T=\Theta(n^{c})$ | 归并排序？不，看下 |
| 2 | $f(n)=\Theta(n^{c}\log^k n)$（均衡） | $T=\Theta(n^{c}\log^{k+1}n)$ | 归并 $a=2,b=2,f=n$：$T=\Theta(n\log n)$ |
| 3 | $f(n)=\Omega(n^{c+\varepsilon})$（合并主导） | $T=\Theta(f(n))$ | 二分 $a=1,b=2,f=1$：$T=\Theta(\log n)$ |

Karatsuba：$a=3,b=2,f(n)=O(n)$，$c=\log_2 3\approx1.585$，$f(n)=O(n^{1})$ 落在情形 1 $\Rightarrow T=\Theta(n^{1.585})$。

---

## 14.3 动态规划：记住已经算过的

DP 的两要素，**用一个背包问题讲最清楚**。

### 14.3.1 从递归到记忆化到递推

0/1 背包：每件物品选或不选，容量 `cap`，最大化价值。朴素递归：

```cpp
int knapRec(..., int i, int cap, long long& calls) {
    ++calls;
    if (i < 0 || cap == 0) return 0;
    if (w[i] > cap) return knapRec(w, v, i - 1, cap, calls);         // 装不下
    return std::max(knapRec(w, v, i - 1, cap, calls),                // 不选
                    v[i] + knapRec(w, v, i - 1, cap - w[i], calls)); // 选
}
```

这个递归**每次分叉成两支**，看似 $2^n$。但它其实只在算 $n\times(\text{cap}+1)$ 个**不同**的子问题 $(i, \text{cap})$——只是**同一个子问题被反复重算**。用「调用计数」一看便知：

```
===== 1) 0/1 背包：子问题重叠 = 指数 → 多项式 =====
  物品数 n=15 容量 cap=30 最优价值=48
  朴素递归调用次数 = 17586	记忆化 / 递推的子问题数 = 465
  -> 朴素递归指数爆炸（同一 (i,cap) 反复重算）；记忆化每个子问题只算一次，
     状态数 n*cap 就是 O(n*cap)。
  [断言通过] 3000 组随机：DP == 暴力枚举
```

**$17586$ 次调用 vs 只有 $465=15\times31$ 个不同子问题**——重算了近 $40$ 倍，且规模越大重叠越狠（指数 vs 多项式）。

**记忆化（自顶向下）**就是加一个 `memo[i][cap]` 缓存，算过直接返回：

```cpp
int& m = memo[i][cap];
if (m >= 0) return m;          // -1 表示未算过
```

**递推（自底向上）**则直接按顺序填表，还把二维压成一维（`f[c]` = 容量 $c$ 的最优价值，**倒序遍历容量**保证每件物品只用一次）：

```cpp
for (int i = 0; i < n; ++i)
    for (int c = cap; c >= w[i]; --c)          // 倒序 -> 0/1 而非完全背包
        f[c] = std::max(f[c], f[c - w[i]] + v[i]);
```

> **倒序 vs 正序是背包的关键分水岭**：倒序保证「本件物品只用一次」（0/1 背包）；正序则允许重复选取（完全背包）。一个循环方向，两种问题。

### 14.3.2 最长公共子序列 LCS

$f[i][j]$ = $a$ 前 $i$ 个、$b$ 前 $j$ 个的 LCS 长度：

$$f[i][j]=\begin{cases}f[i-1][j-1]+1 & a_i=b_j\\ \max(f[i-1][j],\,f[i][j-1]) & a_i\ne b_j\end{cases}$$

```
===== 2) 最长公共子序列 LCS =====
  A = ABCBDAB
  B = BDCABA
  LCS 长度 = 4	一个解 = "BCBA"
```

**还原具体 LCS**：从右下角回溯——「相等走对角、否则走向值较大的邻居」。填表 $O(nm)$、还原 $O(n+m)$。LCS 与**编辑距离**是同一张表的两面（一个求「最大保留」，一个求「最小改动」）。

### 14.3.3 编辑距离（Levenshtein）

把 $a$ 改成 $b$ 的最少「插入 / 删除 / 替换」次数：

$$f[i][j]=\begin{cases}f[i-1][j-1] & a_i=b_j\\ 1+\min(f[i-1][j-1],\,f[i-1][j],\,f[i][j-1]) & a_i\ne b_j\end{cases}$$

三项分别对应**替换 / 删除 / 插入**。边界 $f[i][0]=i$（全删）、$f[0][j]=j$（全插）：

```
===== 3) 编辑距离（Levenshtein） =====
  "kitten" -> "sitting" 距离 = 3
  编辑脚本：
插入 g
替换 e->i
替换 k->s
  -> 转移：相等取对角；否则 1 + min(替换, 删除, 插入)。
  [断言通过] 1500 组随机：DP == 暴力递归
```

（脚本按**回溯顺序**从终点往起点输出，所以自上而下读即「逆向执行」：$k{\to}s$、$e{\to}i$、末尾插 $g$。）$O(nm)$。编辑距离是**拼写纠错、语音识别打分、DNA 比对**的基础。

### 14.3.4 最长递增子序列 LIS：$O(n^2)\to O(n\log n)$

最直接的 DP：$f[i]=\max_{j<i,\,a_j<a_i}(f[j]+1)$，$O(n^2)$。但有个 **$O(n\log n)$ 的妙法**：维护 `tails[k]` = 「长度为 $k{+}1$ 的递增子序列的**最小可能结尾**」——它天然单调递增，于是新元素用**二分**找「第一个 $\ge a_i$」的位置替换：

```
===== 4) 最长递增子序列 LIS：O(n^2) vs O(n log n) =====
  数组 = 10 9 2 5 3 7 101 18
  LIS 长度 = 4（O(n^2) 也给 4），一条 LIS = 2 3 7 18
  -> tails 单调递增，用二分找「第一个 >= a[i]」的位置替换，O(n log n)。
  [断言通过] 4000 组随机：O(n log n) == O(n^2)
```

> **`tails` 不是答案本身**，它只维护「每个长度的最优结尾」。要**还原**具体 LIS，还得记 `parent` 指针（代码里已实现）。

### 自顶向下 vs 自底向上

| 维度 | 记忆化（自顶向下） | 递推（自底向上） |
|---|---|---|
| 写法 | 递归 + 缓存，贴近递推式的直觉 | 循环填表，需想清遍历顺序 |
| 子问题 | **只算用到的**（懒） | **全算**（可能浪费） |
| 开销 | 递归栈 + 缓存查找 | 无栈、缓存友好、常数小 |
| 适用 | 状态稀疏、转移复杂 | 状态稠密、要极致性能 |

两者复杂度同量级（都是「状态数 $\times$ 转移代价」），**先用记忆化想清楚，再用递推提速**是稳妥的开发顺序。

---

## 14.4 回溯：深度优先 + 剪枝

回溯 = **在解空间树上做深度优先搜索**，「试着走一步，不行就退回来」（这就是 DFS 的 naturally backtracking）。**它和暴力枚举的唯一区别是「剪枝」**：一旦当前部分解不可能扩展成完整解，立刻回退。

### N 皇后

在 $n\times n$ 棋盘放 $n$ 个皇后，互不攻击（不同行、不同列、不同对角线）。用**位掩码**把「列 / 主对角线 / 副对角线」的占用压进三个整数，冲突检测 $O(1)$：

```cpp
int avail = (~(cols | d1 | d2)) & ((1 << n) - 1);   // 本行可放的列
while (avail) {
    const int bit = avail & (-avail);               // 取最低位的 1
    avail ^= bit;
    nqueensCount(n, row + 1, cols | bit,
                 ((d1 | bit) << 1) & ((1 << n) - 1), // 对角线整体平移
                 (d2 | bit) >> 1, count);
}
```

实测解的个数（与已知结果一致）：

```
===== 1) N 皇后：解空间剪枝 =====
  n	解的个数	访问结点数
  4	2		16
  5	10		53
  6	4		152
  7	40		551
  8	92		2056
  9	352		8393
  10	724		35538
  11	2680		166925
  12	14200		856188
  n=8 的一个解（. 为空，Q 为皇后）：
    Q.......
    ....Q...
    .......Q
    .....Q..
    ..Q.....
    ......Q.
    .Q......
    ...Q....
  [断言通过] n=4 得 2 解、n=8 得 92 解（与已知结果一致）
```

$n=8$ 有 $92$ 个解（这是个著名的数）。注意**访问结点数远小于 $n!$**：$n=12$ 时 $12!\approx4.8$ 亿，而剪枝后只访问了约 $86$ 万个结点——**剪枝省了三个数量级**。

### 数独求解

逐格试探 $1..9$，用「行 / 列 / 宫」三个约束剪枝，不行就回退：

```
===== 2) 数独求解（回溯 + 行/列/宫剪枝） =====
  求解成功，试探次数 = 37652，解为：
    5 3 4 6 7 8 9 1 2
    6 7 2 1 9 5 3 4 8
    1 9 8 3 4 2 5 6 7
    8 5 9 7 6 1 4 2 3
    4 2 6 8 5 3 7 9 1
    7 1 3 9 2 4 8 5 6
    9 6 1 5 3 7 2 8 4
    2 8 7 4 1 9 6 3 5
    3 4 5 2 8 6 1 7 9
  [断言通过] 每行/列/宫都恰含 1..9 各一次
```

### 枚举的规模：子集 $2^n$、排列 $n!$

回溯能**恰好不漏不重**地枚举：

```
===== 3) 子集 / 全排列：回溯枚举的规模 =====
  n=3	子集数=8 (2^n=8)	排列数=6 (n!=6)
  n=4	子集数=16 (2^n=16)	排列数=24 (n!=24)
  n=5	子集数=32 (2^n=32)	排列数=120 (n!=120)
  n=6	子集数=64 (2^n=64)	排列数=720 (n!=720)
  n=7	子集数=128 (2^n=128)	排列数=5040 (n!=5040)
  n=8	子集数=256 (2^n=256)	排列数=40320 (n!=40320)
  n=9	子集数=512 (2^n=512)	排列数=362880 (n!=362880)
  n=10	子集数=1024 (2^n=1024)	排列数=3628800 (n!=3628800)
  [断言通过] 子集数恰为 2^n、排列数恰为 n!（回溯不漏不重）
```

**这就是回溯能解的边界**：$n$ 稍大，$2^n / n!$ 立刻爆炸。**剪枝是唯一的出路**——而剪枝的极致就是下面的分支限界。

---

## 14.5 分支限界：下界 + 当前最优

**分支限界（Branch & Bound）** = 回溯 + **一个「界函数」**。它在每个结点算一个**乐观下界 LB**（「从这里走下去，最好最好也就这么好了」）；**若 LB $\ge$ 当前已知最优，就整棵子树剪掉**。

用 **TSP**（旅行商：走遍每个城市恰好一次、回到起点、总路程最短）演示。**下界怎么来？** 对「还需要离开」的每个城市（当前城市 + 所有未访问城市），它至少要贡献一条出边 $\ge$ `minOut`。故：

$$\text{LB}=\text{已走代价}+\text{minOut(当前)}+\sum_{v\ \text{未访问}}\text{minOut}(v)$$

这个 LB **一定 $\le$ 真实最优**（否则会误剪掉最优解）。再配一个**启发式上界**（最近邻走法）初始化 `best`，让剪枝更早生效：

```
===== 2) 规模对比：暴力 (n-1)! vs 分支限界展开结点数 =====
  n	(n-1)!		最近邻		分支限界最优	展开结点数
  8	5040		317441		294254		1786
  9	40320		310550		272968		11335
  10	362880		322855		322855		44078
  11	3628800		334956		289571		12192
  12	39916800		344625		328496		113770
  13	479001600		289381		279367		126080
  14	6227020800		413337		339472		2017405
  -> 分支限界用「下界 + 当前最优」把 (n-1)! 的暴搜砍掉几个数量级；
     最近邻是「近似解」，通常比最优差几个百分点，却快得多。
```

$n=14$ 时 $(n-1)!=6.2\times10^9$，分支限界只展开约 $200$ 万结点——**省了 3000 倍**。而**最近邻启发式**（每步走最近的城市）给出的是「近似解」：

```
  n=11：最近邻 = 319159，最优 = 284362，多走 12.2369%
```

**上界 / 下界的「夹逼」结构**是这一节的灵魂：**启发式给上界（可行解），界函数给下界（保证）**，分支限界在两者之间**夹出精确最优**。这也解释了为什么「精确求解 NP 难问题」是可能的——**只要你的界足够紧、实例足够小/规整**。

> **与 AI 的连接**：分支限界就是「**带价值下界的 A\***」（第 11 章）。A\* 的 $f=g+h$、beam search 的宽度限制、MCTS 的上置信界 UCB——**都是「用估计函数引导搜索、砍掉无望分支」这一族思想**。世界模型里的**规划 / rollout** 本质也是在一棵未来分支树上做「有指导的前向搜索」。

---

## 14.6 把四把锤子串起来：TSP 与背包

同一个问题，换策略，差距立现：

| 问题 | 暴力 | DP | 回溯 | 分支限界 | 贪心（近似） |
|---|---|---|---|---|---|
| 0/1 背包 | $O(2^n)$ | $O(n\cdot\text{cap})$ | $O(2^n)$ | $O(2^n)$ | $O(n\log n)$（**错**） |
| TSP | $O(n!)$ | $O(2^n n^2)$（Bitmask DP） | $O(n!)$ | 常近线性 | 最近邻，差几 % |

**关键观察**：

- **背包有「容量」这个可加状态 $\Rightarrow$ DP 可行**（$O(n\cdot\text{cap})$）；TSP 的「已访问集合」也是可加状态 $\Rightarrow$ **状压 DP** $O(2^n n^2)$（比 $n!$ 好，但仍指数，因为 $2^n$）；
- **贪心在背包上错**（按性价比会「卡在」不是最优的组合），但在**分数背包**（可切分）上对——**问题结构变了，策略的正确性也变了**；
- **分支限界是「精确」与「可行」之间的工程妥协**：界越紧越快，但界越紧越贵算。

**选择顺序建议**：先问「有重叠子问题吗」→ DP；没有就「能剪枝吗」→ 回溯/分支限界；再不行就是 NP 难，转近似（第 15 章）。

---

## 14.7 维特比：DP 在序列上（与 AI 连接的重点）

前面所有 DP 都在「数组」「表」上。但**序列模型**里的 DP 一样重要，而且直接连着 AI。**隐马尔可夫模型（HMM）** 是最小的例子：

- 有一串**隐藏状态** $s_1,\dots,s_T$（如「雨天 / 晴天」）；
- 状态按**转移概率** $a_{ij}=P(s_t=j\mid s_{t-1}=i)$ 演化；
- 每步按**发射概率** $b_j(o_t)=P(o_t\mid s_t=j)$ 产生一个**观测** $o_t$（如「散步 / 购物 / 打扫」）。

给定观测序列，两个基本问题：

1. **解码**：最可能的隐藏状态序列是什么？—— **维特比（Viterbi）**，求 $\max$；
2. **打分**：这段观测的总概率是多少？—— **前向算法（Forward）**，求 $\sum$。

**维特比**就是「序列上的 max-product DP」：

$$\delta_t(j)=\Big[\max_i \delta_{t-1}(i)\,a_{ij}\Big]\,b_j(o_t)$$

```cpp
for (int t = 1; t < T; ++t)
    for (int j = 0; j < K; ++j) {
        double best = -inf; int arg = 0;
        for (int i = 0; i < K; ++i) {
            const double v = dp[t-1][i] + std::log(trans[i][j]);   // 取对数防下溢
            if (v > best) { best = v; arg = i; }
        }
        dp[t][j]   = best + std::log(emit[j][obs[t]]);
        bp[t][j]   = arg;                                          // 回溯指针
    }
```

**前向算法**则把上面的 `max` 换成 `sum`：

$$\alpha_t(j)=\Big[\sum_i \alpha_{t-1}(i)\,a_{ij}\Big]\,b_j(o_t)$$

用经典的「天气 / 活动」HMM 实测（观测 = Walk Shop Clean Walk Clean）：

```
===== 1) 经典 HMM：天气与活动 =====
  观测序列 = Walk Shop Clean Walk Clean
  维特比最优状态序列 = Sunny Rainy Rainy Sunny Rainy
  最优对数概率 = -7.63376
  前向算法总概率 = 0.00262897	暴力求和 = 0.00262897
  -> 维特比 = 序列上的「max」（最可能路径）；前向 = 序列上的「sum」（总概率）。
  [断言通过] 维特比 == 暴力最优；前向总概率 == 暴力求和
```

两者都用**暴力枚举 $K^T$ 条路径**对拍（$2000$ 组随机 HMM，$K=2..4,\ T=1..8$）：

```
  [断言通过] 2000 组随机 HMM：维特比最优 == 暴力；前向总概率 == 暴力求和
```

**这就是把指数 $K^T$ 压成 $O(TK^2)$ 的魔法**——和背包、编辑距离的 DP 完全同构，只是状态换成了「时刻 + 当前隐状态」。

> **维特比的「近亲」遍布 AI**：
> - **CRF**（条件随机场）解码 = 维特比的判别式版本；
> - **CTC**（语音/手写识别）= 带「空白符」的维特比；
> - **束搜索（beam search）** = 维特比 + 每步只保留 top-$B$（用召回换速度，正是第 13 章 LSH 的同款权衡）；
> - **前向算法 = sum-product 置信传播**在链上的特例，「对所有路径求和」也是 **margin 化 / EM 的 E 步** 的核心；
> - **序列世界模型的 rollout** = 在隐状态树上做带剪枝的前向搜索（分支限界的近亲）。
>
> **一句话**：维特比是「序列上的最短路」，前向是「序列上的配分函数」——**DP、图算法、概率推断，在这里是三而一的东西**。

---

## 常见错误与陷阱

1. **二分死循环**：`lo`/`hi` 更新不对称。记死两条——找「第一个 $\ge$」用 `hi=mid`（`mid` 保留）+ `lo=mid+1`；`mid` 一律写 `lo+(hi-lo)/2` 防溢出。
2. **二分区间开闭搞混**：`[lo,hi)` 半开区间最省心（`hi` 取 `n` 而非 `n-1`）。混用闭区间极易差一。
3. **快速幂忘记 `n=0` 的基准** 或 **`n&1` 的额外一次乘**：`a^0=1`；奇数要补乘一个 `a`。
4. **Karatsuba 在「位数」上算错 `half`**：`half=$\lfloor n/2\rfloor$`，`B=10^{half}`；两数位数不同要先**对齐到同长**再拆（代码里 `resize` 补零）。
5. **DP 忘记初始化边界**：编辑距离的 `f[i][0]=i`、`f[0][j]=j` 漏了，结果全错。
6. **背包循环方向错**：0/1 背包要**倒序**遍历容量；正序会变成「完全背包」（可重复选）。
7. **DP 数组下标越界**：状态是「前 $i$ 个」，表要开 `(n+1)` 大小，`f[0][*]` 是边界。
8. **记忆化忘了「未算过」标记**：`memo` 初值用 `-1`（而非 `0`），否则「值为 0 的合法子问题」会被误判为未算。
9. **回溯忘记「撤销」**：`used[v]=true` 之后递归完必须 `used[v]=false`（`pop_back`、清棋盘格子）。漏了会导致后续分支看到脏状态。
10. **位掩码 N 皇后`1<<n` 溢出**：$n\ge31$ 时 `int` 移位 UB；要么限定 $n$、要么用 `long long`/`__int128`。
11. **分支限界的界「不可采纳」**：界必须**不高估**真实代价（$\le$ 最优），否则会**误剪掉最优解**。界若高估，得到的是「近似」不是「精确」。
12. **分支限界忘记用上界初始化**：`best` 初值给 `INF` 会让剪枝几乎不生效；先用启发式算一个可行解当上界，剪枝立刻变狠。
13. **维特比用线性概率导致下溢**：$T$ 大时概率乘积趋近 $0$ 而变 `0.0`。**一律取对数**（$\log$ 把乘法变加法），只有「求和」的前向算法不能直接取 log（要用 log-sum-exp）。
14. **前向 / 维特比搞混**：维特比回溯**指针**给最优路径；前向没有「路径」，它的 $\alpha_t(j)$ 是「所有到 $j$ 的路径**概率之和**」。**别拿前向的结果去回溯路径**。
15. **递归深度爆炸**：DP 的递归版、回溯、分支限界都可能爆栈（$n$ 大时）。必要时改迭代或调大栈。

---

## 与 AI / 世界模型的连接

这一章的四种策略，**是现代 AI 里「非神经网络」那一半的骨架**：

- **DP = 序列建模与推断**：维特比 / 前向 / 后向 / Baum-Welch（EM）是 HMM 四大件，直接通向 **CRF、CTC、序列标注、语音识别**。**「维特比是序列上的最短路」** 这句话，把 DP、图算法、概率推断统一了起来。
- **DP = 强化学习与规划**：值迭代 / 策略迭代就是 **Bellman 方程上的 DP**（$V(s)=\max_a\sum P(s'|s,a)[r+\gamma V(s')]$），与背包、维特比的递推形式**一模一样**。你在本科学的那点 DP，是理解 RL 的入场券。
- **束搜索 / 分支限界 = 生成式模型的解码**：大模型的 `beam search`、`top-k / nucleus sampling`，都是「**在词元树上做带界的前向搜索**」。**分支限界给上界/下界、束搜索只留 top-$B$、A\* 用 $f=g+h$**——同一族思想的不同取舍。
- **分治 / FFT = 深度学习的地基**：`O(n\log n)` 的 FFT 是卷积、注意力的数学核心；Karatsuba 的「少算一次乘法」思想，在**可分离卷积、Winograd 卷积**里反复出现（用加法换乘法）。
- **回溯 = 组合优化与自动化**：编译器指令调度、SAT 求解、程序合成、神经架构搜索（NAS）里的离散搜索空间，都在「回溯 + 剪枝」的框架里。
- **「上界 / 下界 / 剪枝」= 世界模型的规划**：世界模型学到「状态转移」，规划器在**想象的未来**上做搜索（如 MuZero 的 MCTS、Dreamer 的 latent rollout）。砍掉无望分支、用启发式估计远景价值——**这就是分支限界思想在 latent space 里的复活**。
- **维特比 / 前向的「对数域」技巧**，在深度学习里就是 **log-sum-exp 稳定性**、**CTC loss 的实现**、**HMM 层的可微化**（把 Viterbi 软化成就可反传）。

**一句话**：神经网络负责「学一个函数近似器」，而**分治 / DP / 回溯 / 分支限界负责「在巨大离散空间里聪明地搜索与推断」**。世界模型恰恰是两者的交汇——**用网络学转移与价值，用搜索（DP / 分支限界 / MCTS）在它的想象里规划**。

---

## 本章小结

- **决策地图**：子问题**独立** → 分治；子问题**重叠** → DP；解空间是**可选可退的树** → 回溯；能给**界** → 分支限界；贪心最快但**需证明**；
- **分治**：Divide–Conquer–Combine；主定理 $T(n)=aT(n/b)+f(n)$ 对比 $f(n)$ 与 $n^{\log_b a}$；二分 $O(\log n)$、快速幂与**矩阵快速幂** $O(\log n)$、**Karatsuba** $O(n^{1.585})$（3 次子乘法换 1 次）；
- **DP 两要素**：**最优子结构 + 重叠子问题**；三态演进「朴素递归（指数）→ 记忆化（只算用到的）→ 递推（紧凑、快）」；背包 $O(n\cdot\text{cap})$、LCS / 编辑距离 $O(nm)$、LIS $O(n\log n)$；**背包倒序遍历容量**、会**回溯还原方案**；
- **回溯** = DFS + 剪枝；N 皇后（$n=8$ 有 $92$ 解）、数独（行/列/宫剪枝）；枚举子集 $2^n$、排列 $n!$——**这就是能解的边界**；
- **分支限界** = 回溯 + **界函数**；TSP 的 LB = 已走 + 各待离开城市的最小出边之和；**启发式给上界、界函数给下界，夹出精确最优**；与 A\*、beam search、MCTS 同族；
- **维特比**（序列上 max-product DP，$O(TK^2)$）与**前向**（sum-product DP）把 $K^T$ 压成多项式，是 CRF / CTC / 序列世界模型的核心；
- **统一视角**：DP、图最短路、概率推断是**三而一**的；搜索（回溯 / 分支限界 / MCTS）是**世界模型规划**的手段。

---

## 练习

### 基础

1. 用 $n=8$ 的已排序数组，手画一次 `lower_bound` 的区间收缩过程，标出每轮的 `lo/mid/hi`，验证「比较次数 $=\log_2 8=3$」。
2. 手算 $3^{13}$ 的快速幂每一步（写出 $13$ 的二进制与平方序列），数一共乘了几次；与朴素 $12$ 次对比。
3. 用 Karatsuba 的公式，手算 $12\times34$（$B=10$）的 $z_2,z_1,z_0$，验证 $=408$，并数用了几次「一位数乘法」。
4. 对一个 $n=4$、容量 $5$ 的背包，手填 DP 表，写出最优值对应的物品集合；再用**倒序**与**正序**各填一次，指出正序何时会「重复选同一件」。
5. 手算 `"cat"` $\to$ `"cut"` 与 `"cat"` $\to$ `"ct"` 的编辑距离，写出各自的最优操作序列。
6. 给 $n=4$ 的 N 皇后画出解空间树的前两层，圈出被剪掉的结点，说明剪枝条件。

### 进阶

7. **实现「带路径记录的 0/1 背包」**：不仅输出最优值，还输出选了哪些物品（用 `choice[i][c]` 或回溯 `f` 表）。
8. **实现状压 DP 解 TSP**：`f[mask][u]` = 从起点出发、访问过 `mask`、当前在 `u` 的最短路，验证与分支限界结果一致，比较两者在 $n=15$ 附近的耗时。
9. **给分支限界换一个更紧的下界**（用最小生成树 / 指派问题下界），实测 $n=14$ 的展开结点数能降多少。
10. **实现「迭代加深回溯」**：把 N 皇后改成「限深搜索 + 逐步加深」，对比它与纯 DFS 的结点数。
11. **实现前向-后向（Forward-Backward）**：给定观测序列，计算每个时刻处于每个状态的**后验概率** $P(s_t=j\mid o_{1:T})$，用它做「平滑」（比维特比的硬解码更软）。
12. **实现 Baum-Welch（EM）训练 HMM**：用前向-后向的期望计数迭代更新 `start/trans/emit`，在合成数据上验证「对数似然单调上升」。
13. **实现 log-sum-exp 版前向算法**：把前向搬到对数域，验证与线性版结果一致，并说明为什么它在大 $T$ 下不会下溢。

### 挑战

14. **实现 FFT 乘法**：把两个 $n$ 位整数（或两个多项式）用「求值 → 点乘 → 插值」做乘法，$O(n\log n)$，与 Karatsuba 比较位数增长时的耗时交叉点。
15. **实现 CTC 的前向算法**：在「目标序列 + 空白符」的扩展标签集上做前向 DP（即 CTC loss）。用 PyTorch 的 `nn.CTCLoss` 对拍你手写的值。
16. **实现可微维特比（软维特比）**：把 `max` 换成 `logsumexp`、把回溯换成「期望转移矩阵」，接到一个小序列标注网络里，验证梯度能反传（对比数值梯度）。
17. **实现 MuZero 式的「latent MCTS」迷你版**：用一个隐状态转移模型（可以就是 HMM）+ 值估计，在隐空间里做带 UCB 的树搜索，与「暴力前向搜索」在合成环境上比较样本效率。
18. **实现背包的 FPTAS**：用「价值缩放 + 舍入」把 0/1 背包变成 $(1+\varepsilon)$ 近似，实测近似比与 $\varepsilon$ 的关系——这是第 15 章「近似算法」的预热。

<details>
<summary>参考答案（要点）</summary>

**1.** `[0,8)`：`mid=4` 比较 1 次；假设 target 在左半，`hi=4`；`mid=2`；再收缩到 `[2,3)` 或 `[3,4)`，`mid=2`/`3`，第 3 次比较命中。总 $\log_2 8=3$ 次（二分比较次数 $=\lceil\log_2 n\rceil$ 量级）。

**2.** $13=1101_2$。平方序列：$a^{1},a^{2},a^{4},a^{8}$（每步平方，共 3 次平方）。按位取 $a^{1}\cdot a^{4}\cdot a^{8}$（3 次乘法）。共 $3+3=6$ 次乘法，朴素要 12 次。

**3.** $x=12\to x_1=1,x_0=2$；$y=34\to y_1=3,y_0=4$。$z_2=1\cdot3=3$，$z_0=2\cdot4=8$，$z_1=(1+2)(3+4)-3-8=21-11=10$。$xy=3\cdot100+10\cdot10+8=408$ ✓。一位数乘法 3 次（$1\cdot3,2\cdot4,3\cdot7$），朴素要 4 次。

**4.** 物品 $(w,v)$，容量 5。填 `f[c]`（倒序）：每件物品更新 `f[c]=max(f[c], f[c-w]+v)`。正序会在「同一件物品」上叠加 `f[c-w]`（已是本件更新过的），相当于允许多选，故 0/1 背包必须倒序。

**5.** `cat→cut`：替换 `a→u`，距离 1。`cat→ct`：删除 `a`，距离 1。脚本用回溯输出（逆向）。

**6.** 第 1 行放任意列（4 个分支）；第 2 行排除同列与两条对角线对应的列，只剩 2 个合法位置；不合法的分支立刻剪掉——这就是「不同列、不同对角线」。

**7.** 填 `f` 时若发生更新则记 `choice[i][c]=true`；最后从 `(n,cap)` 反向走：`choice` 为真则选第 $i$ 件并 `c-=w[i]`。

**8.** `f[1<<0][0]=0`；`f[mask|1<<v][v]=min(f[mask][u]+d[u][v])`；答案 `min_u f[full][u]+d[u][0]`。复杂度 $O(2^n n^2)$，$n=15$ 时 $2^{15}\cdot225\approx7\times10^6$，通常快于分支限界。

**9.** 用**最小生成树下界**（未访问城市构成的 MST 权重和）或**指派问题（匈牙利）下界**，比「最小出边和」紧得多，$n=14$ 展开结点数常能再降一个数量级。

**10.** 迭代加深从深度 1 开始限深 DFS，逐渐加深；在「解不深」时用更少内存，但会重复访问浅层结点——与 IDA\* 同族。

**11.** 后向 $\beta_t(j)=P(o_{t+1:T}|s_t=j)$；后验 $\gamma_t(j)\propto\alpha_t(j)\beta_t(j)$。归一化后即得每时刻状态分布，输入维特比的硬解对比更「软」。

**12.** E 步用 $\gamma$、$\xi_t(i,j)\propto\alpha_t(i)a_{ij}b_j(o_{t+1})\beta_{t+1}(j)$ 算期望计数；M 步归一化更新参数。每次迭代对数似然不减（EM 的保证）。

**13.** 前向对数版：$\log\alpha_t(j)=\text{logsumexp}_i(\log\alpha_{t-1}(i)+\log a_{ij})+\log b_j(o_t)$。log-sum-exp 用「减最大值再取 exp」的技巧防溢出。

**14.** FFT 乘法：把系数当多项式，取足够长的 $2^k$ 点，正变换 → 逐点相乘 → 逆变换，$O(n\log n)$。与 Karatsuba 的交叉点通常在几千位。

**15.** CTC：扩展标签 $l'$（每个标签间插空白），前向递推只允许「停在原地 / 前进一格 / 前进两格（跳过空白）」。与 `nn.CTCLoss`（`reduction='none'`）对拍。

**16.** 软维特比：把 `max`/`argmax` 换成 `logsumexp`，得到「转移的边际期望」；用这些软权重加权隐状态得可微输出。梯度用反向传播 + 数值梯度校验。

**17.** 隐 MCTS：每结点存 $(N,Q)$，用 $Q+U$（UCB）选子；展开时调转移模型生成下一隐状态、值模型估价值；回传更新。与穷举 rollout 比「达到同等回报所需的模拟次数」。

**18.** FPTAS：令 $K=\varepsilon\cdot v_{\max}/n$，把每个价值 $v_i'=\lfloor v_i/K\rfloor$，在缩放后的整数值上跑精确 DP（规模变 $O(n^2/\varepsilon)$），再乘回 $K$。近似比 $1-\varepsilon$。

</details>

---

## 延伸阅读

- **Sahni《数据结构、算法与应用（C++ 版）》第 2 版 第 16 章**「Dynamic Programming」与**第 17～19 章**（回溯、分支限界与各类经典问题）：背包、矩阵连乘、最短路、N 皇后、TSP 等；
- **《算法导论》第 4 章（分治与主定理）、第 15 章（动态规划）、第 34 章（NP 完全）**；
- **Karatsuba & Ofman (1962)** *Multiplication of many-digital numbers by automatic computers* —— 第一个次二次乘法；
- **Cooley & Tukey (1965)** *An algorithm for the machine calculation of complex Fourier series* —— FFT；
- **Bellman, R. (1957)** *Dynamic Programming* —— 动态规划的奠基（Bellman 方程、最优性原理）；
- **Viterbi, A. (1967)** *Error bounds for convolutional codes…* —— 维特比算法原始论文；
- **Rabiner, L. (1989)** *A tutorial on Hidden Markov Models…* —— HMM 最经典的教程（前向-后向、Baum-Welch、维特比）；
- **Land & Doig (1960)** *An automatic method of solving discrete programming problems* —— 分支限界原始论文；
- **Hart, Nilsson & Raphael (1968)** *A formal basis for the heuristic determination of minimum cost paths* —— A\*（本课第 11 章）；
- **Graves et al. (2006)** *Connectionist Temporal Classification* —— CTC，维特比思想在深度序列模型里的复活；
- **Silver et al. (2017)** *Mastering the game of Go without human knowledge*（AlphaZero）/ **Schrittwieser et al. (2020)** *Mastering Atari…*（MuZero）—— 用「学习到的模型 + MCTS 搜索」做规划，世界模型的代表作；
- 配套代码：`code/14_divide_conquer.cpp`、`code/14_dp_classics.cpp`、`code/14_nqueens_backtracking.cpp`、`code/14_branch_bound_tsp.cpp`、`code/14_viterbi_hmm.cpp`。

---

**下一章**：第 15 章《NP 难与近似算法》—— 当问题**没有**多项式精确算法时怎么办？我们先把「多项式时间」「归约」「NP 完全」这套**计算复杂性**的语言讲清楚，再转向**近似算法**（顶点覆盖的 2-近似、背包的 FPTAS）、**启发式**（模拟退火解 TSP）、以及**精确指数算法**（分支限界的极限），把第 14 章的「搜索」推到「问题本身的难度」这条终点线上。
