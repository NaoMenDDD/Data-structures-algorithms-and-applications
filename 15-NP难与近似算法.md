# 第 15 章 NP 难与近似算法

> 第 14 章的所有「精确」方法都有一个隐含前提：**这个问题是可解的**——能在多项式时间里求出最优解。可是有一大类问题（旅行商、背包、顶点覆盖、布尔可满足性 SAT、图着色、数独……），**至今没有人找到多项式的精确算法，而且有很强的证据表明：不可能有**（除非 P = NP）。
>
> 这一章讲两件事，正好是面对「难题」的**上半场和下半场**：
>
> **上半场——判断「难」**：把「多项式时间」「归约」「NP」「NP 完全」「NP 难」这套**计算复杂性**的语言讲清楚。它让你在看到一个新问题时，能判断「它到底是难是易」，而不是盲写一个 $O(2^n)$ 还浑然不觉。
>
> **下半场——对付「难」**：既然精确最优可能求不出，那就**退一步**。**近似算法**给出「保证离最优不超过 $2$ 倍 / $1+\varepsilon$ 倍」的解（顶点覆盖的 2-近似、背包的 FPTAS）；**启发式 / 元启发式**（模拟退火）用概率搜索在可接受时间里找到「足够好」的解；**精确指数算法**（分支限界、参数化算法）在小实例或特殊结构上仍求精确最优。
>
> 一句话贯穿全章：**「难」不是终点，是「换一种目标」的起点**——从「求最优」换成「求你保证得了的近似」或「求实践中的够好」。这是算法工程师最实用的一课，也是理解「为什么 AI 里有那么多近似与采样方法」的钥匙。

---

## 本章目标

读完并跑完本章代码，你应该能够：

1. 说清 **判定问题、P、NP、NP 完全、NP 难** 的定义与相互关系，解释「**验证**一个解」与「**求解**一个解」的区别，并知道 **P vs NP** 是未解难题（普遍认为 P $\ne$ NP）；
2. 理解**多项式归约（$\le_p$）**的含义，并说清 **Cook–Levin 定理**（SAT 是第一个 NP 完全问题）与「**NP 完全问题图谱**」（SAT → 3-SAT → 团 / 顶点覆盖 / 独立集 / 哈密顿回路 / TSP / 子集和……）；
3. 面对一个 NP 难问题，能列出**四条出路**：近似算法、启发式、精确指数（分支限界 / FPT）、限定特殊情形；
4. 定义**近似比 $\rho$**，手写**顶点覆盖的极大匹配 2-近似**，并**证明**它 $\le 2\cdot\text{OPT}$，实测其比值分布，构造**紧界**说明「2 无法再降」；
5. 手写**模拟退火（SA）**解 TSP：说清**邻域（2-opt）、接受准则 $\exp(-\Delta/T)$、几何降温**；实测它达到精确最优、大幅优于最近邻，并展示**起始温度「太冷冻住 / 太热乱走」**的 U 形现象；
6. 手写背包的 **FPTAS**：说清「按 $K=\varepsilon v_{\max}/n$ 缩放价值 + 在缩小价值域上精确 DP」的做法，解释它为什么是**多项式** $O(n^3/\varepsilon)$，而精确 DP 只是**伪多项式** $O(n\cdot\text{cap})$；
7. 建立一张**「复杂度全景图」**：P / NP / NP 完全 / NP 难 / 近似谱系（2-近似、$(1+\varepsilon)$-近似、PTAS、FPTAS、不可近似）的直觉地图。

---

## 前置知识

- 第 01 章（算法分析）—— $O(\cdot)$、「多项式时间」是本章的「容易」基准；
- 第 14 章（算法设计策略）—— DP、回溯、**分支限界**（本章的「精确指数算法」）；
- 第 11 章（图算法）—— 匹配、Hamilton 回路、顶点覆盖的图论背景；
- 一点点**离散数学 / 逻辑**：布尔公式、判定问题、图的语言。

---

## 15.1 为什么有的问题「注定」很难：从 P 到 NP

复杂性理论的出发点是**判定问题（decision problem）**：答案只有「是 / 否」两种。例：「这张图有没有长度 $\le L$ 的哈密顿回路？」「这堆物品能不能装进容量 $\text{cap}$、价值至少 $V$？」

> 为什么不研究「求最优」，而研究「判断是 / 否」？因为**判断是更弱的要求**——能求最优就一定能判断，反之未必。若连「判断」都难，那「求最优」只会更难。所有结论都能搬到优化问题上。

### 四个核心类

| 类 | 定义 | 直觉 |
|---|---|---|
| **P** | 能在**多项式时间**内**求解**（判定）的问题 | 「容易」——排序、最短路、匹配 |
| **NP** | 若答案是「是」，存在一个**多项式长度的证书**，能在**多项式时间**内**验证**它 | 「猜得到就能快速验」——解谜 |
| **NP 完全** | 属于 NP，且**所有 NP 问题都能多项式归约到它** | 「NP 里最难的一批」 |
| **NP 难** | **至少**和 NP 完全一样难（**不要求**自身属于 NP） | 「不比你最难的问题简单」 |

**关键区分：求解 vs 验证。** 数独就是最好的例子：**填好的**数独**验证**很简单（扫一遍看每行每列每宫）——用多项式时间就能检查，所以数独属于 NP；但从空盘**求解**却很难（我们靠回溯搜）。**「验证容易」不代表「求解容易」**——这正是 NP 与 P 的分界线。

### 归约：比较问题难度的标尺

**多项式归约 $A \le_p B$**：如果「用多项式次调用求解 $B$ 的算法」就能求解 $A$，就说 $A$ **归约**到 $B$，即 $A$ **不比** $B$ **难**（$B$ 至少和 $A$ 一样难）。

$$\text{若 } A\le_p B \text{ 且 } B\in P,\ \text{则 } A\in P \qquad\Longleftrightarrow\qquad \text{若 } A\le_p B \text{ 且 } B \text{ 难},\ \text{则 } A \text{ 也难}$$

**NP 完全的定义**就是「NP 里**最难的**」：$B$ 是 NP 完全 $\iff$ (1) $B\in\text{NP}$；(2) **每个** $A\in\text{NP}$ 都有 $A\le_p B$。

### Cook–Levin 定理与 P vs NP

1971 年 **Cook（及 Levin）** 证明：**SAT（布尔可满足性）是 NP 完全的**——这是第一个被证明「NP 里最难」的问题。它的意义在于**打破了鸡生蛋**：要证明别的 $B$ 是 NP 完全，只要证「**SAT $\le_p B$** 且 $B\in\text{NP}$」即可（因为 $A\le_p\text{SAT}\le_p B$ 传递）。**于是 SAT 成了整棵归约树的树根**。

**P vs NP 问题**：$P\stackrel{?}{=}NP$。若 $P=NP$，则所有 NP 问题（含 TSP、背包判定）都有多项式精确算法，密码学（RSA 基于大数分解难）将崩塌。**目前无人证明，但学界压倒性地相信 $P\ne NP$**——于是我们被迫走「近似 / 启发式」这条路。

---

## 15.2 NP 完全问题图谱

从 SAT 出发，逐层归约，得到一整棵「难兄难弟」家族：

```
SAT（Cook–Levin，根）
 └─ 3-SAT（限制每子句 3 个文字，仍是 NP 完全）
     ├─ 团 / 独立集 / 顶点覆盖   （互相可归约）
     ├─ 哈密顿回路 ──> 旅行商问题 TSP（判定版）
     ├─ 图着色（3-着色已是 NP 完全）
     ├─ 子集和 / 分区 / 背包（判定版）
     └─ 整数规划 / 集合覆盖 / 装箱
```

| 问题 | 是 / 否的问法 | 若 P=NP 则…… |
|---|---|---|
| **SAT** | 有无一组布尔赋值使公式为真？ | 能解所有 NP 问题 |
| **3-SAT** | 每个子句恰 3 个文字，同上 | 同上 |
| **团 / 独立集 / 顶点覆盖** | 有无大小 $\ge k$ 的团 / $\ge k$ 的独立集 / $\le k$ 的覆盖？ | 互相等价 |
| **哈密顿回路 / TSP** | 有无走遍所有点一次且总长 $\le L$ 的回路？ | 物流 / 路径规划变易 |
| **子集和 / 背包** | 有无子集和恰为 $S$ / 价值 $\ge V$ 且重量 $\le W$？ | 资源分配变易 |
| **图着色** | 能否用 $k$ 种颜色正常染色？ | 排程 / 寄存器分配变易 |

**注意：这些问题的「优化版」都是 NP 难的。** 例如「求最短哈密顿回路」（TSP）是 NP 难（甚至不含判定，因为只求最优值）；而「判断是否存在 $\le L$ 的回路」是 NP 完全。**NP 难是「难度上界」，NP 完全是「NP 里的 NP 难」。**

> **别被「NP 难」吓到**：NP 难只说明**最坏情况**下没有多项式算法（若 P$\ne$NP）。**具体的、有结构的实例**往往能快速精确求解（分支限界在 $n=14$ 的 TSP 上只展开 200 万结点），而**没结构的一般实例**才需要近似。

---

## 15.3 遇到 NP 难怎么办：四条出路

| 出路 | 做法 | 保证 | 代表 |
|---|---|---|---|
| **近似算法** | 求「保证在最优的 $\rho$ 倍内」的解 | 有**理论保证** | 顶点覆盖 2-近似、背包 FPTAS |
| **启发式 / 元启发式** | 用搜索/随机/进化「碰」好解 | **无**保证，实践常好 | 模拟退火、遗传、蚁群、局部搜索 |
| **精确指数算法** | 分支限界、参数化（FPT）、状压 DP | **精确**，但最坏指数 | 分支限界 TSP、Held–Karp $O(2^n n^2)$ |
| **限定特殊情形** | 限制输入结构，使其变多项式 | 精确 | 树上的 TSP / 区间的调度 / 小树宽的图 |

**选择顺序**（工程直觉）：研究性质 / 要保证 $\Rightarrow$ 近似算法；工业落地 / 要够快够好 $\Rightarrow$ 启发式；实例小或有结构 $\Rightarrow$ 精确指数；特殊领域 $\Rightarrow$ 找多项式特例。**下面三节各演示一条。**

---

## 15.4 近似算法：以顶点覆盖为例

**顶点覆盖**：选最少的顶点，使图中每条边至少有一个端点被选中。判定版（「有没有 $\le k$ 的覆盖」）是 NP 完全的，所以求最小覆盖是 NP 难的。

**近似比 $\rho$（最小化问题）**：若算法输出 $A$，最优为 $\text{OPT}$，且**总能**保证

$$A \le \rho\cdot \text{OPT}$$

就说它是 **$\rho$-近似**。$\rho$ 越接近 $1$ 越好；$\rho=1$ 就是精确（那是 NP 难，一般不指望）。

### ★ 极大匹配 2-近似

**做法**：找一组**极大匹配**（两两不相邻的边，贪心地能加就加），**把每条匹配边的两个端点都选进覆盖**。

```cpp
std::vector<char> approxMatching(const Graph& g) {
    std::vector<char> in(g.n, 0);
    for (const auto& e : g.edges)                 // 任取一条「两端都未覆盖」的边
        if (!in[e.first] && !in[e.second]) { in[e.first] = 1; in[e.second] = 1; }
    return in;
}
```

**先验证它是合法覆盖**：任何一条边，要么它在扫描时「两端都未覆盖」而被选中（两端入选），要么它的某端后来被他边选走（该端入选）——**总之至少一端被选**。

**再证明比值 $\le 2$**：

1. 设匹配边集合为 $M$（两两**不共享端点**），算法选了 $|A| = 2|M|$ 个顶点；
2. **最优解 $\text{OPT}$ 必须覆盖 $M$ 里的每一条边**（$M$ 是图里的边子集）；
3. 但 $M$ 的边**两两不相邻**，所以**一条顶点最多覆盖 $M$ 中的一条边**——于是 $\text{OPT}$ 至少要为 $M$ 的每条边各选一个端点，即 $|\text{OPT}| \ge |M|$；
4. 合起来：$|A| = 2|M| \le 2|\text{OPT}|$。**证毕。**

实测（含最优值对拍）：

```
===== 1) 单例演示：极大匹配 2-近似 =====
  图：n=12 顶点, 24 条边
  最优顶点覆盖 |OPT| = 7
  极大匹配近似   |A| = 10  （若有匹配需 2 倍上界）
  贪心(按度)     |G| = 7
  -> 两个结果都是合法覆盖，且匹配近似 <= 2*OPT。
```

**对比「贪心按度」**（每次选覆盖最多未覆盖边的顶点）：它常常更小，但**没有理论保证**。120 组随机图上的统计：

```
===== 2) 统计：近似比随图变稠密的变化 =====
  每顶点平均度	样本	匹配近似/OPT 均值	最大	贪心/OPT 均值	最大
  1.5		120	1.79222		2	1.01837		1.2
  3		120	1.60581		2	1.03105		1.28571
  5.25		120	1.43207		2	1.03624		1.33333
  7.5		120	1.3305		1.77778	1.02542		1.2
  10.5		120	1.23662		1.45455	1.01801		1.09091
```

- 匹配近似**稳定 $\le 2$**，且**最坏比 2 真的会出现**（稀疏图那一行）；
- 图越稠密，匹配近似越接近最优（$1.24$ 倍），因为匹配覆盖了更多结构；
- **贪心按度平均更好（$\sim1.02$），但当心它「没有保证」**——它可能在某些实例上更差。

### 2 倍界是紧的

```
===== 3) 2 倍界是「紧的」：构造最坏情形 =====
  6 条互不相邻的边：OPT = 6，匹配近似 = 12，比值 = 2
```

**$6$ 条互不相邻的边**（完美匹配）：最优每条边选 $1$ 个端点即 $6$；匹配近似每条边必须选 $2$ 个端点得 $12$——**刚好 $2$ 倍**。这说明**对一般图，$2$ 这个常数无法再改进**。（对特殊图类可以更好，但那不是「一般图」的结论。）

---

## 15.5 启发式：模拟退火解 TSP

当近似算法给不出「够好的保证」时，用**启发式**在解空间里「碰」好解。**模拟退火（Simulated Annealing, SA）** 是最经典的元启发式之一，灵感来自金属退火：**高温时原子乱动（探索），慢慢降温（收敛到低能态）**。

**三要素：**

1. **状态 + 邻域**：状态是一条巡回路径；邻居由 **2-opt** 生成——**反转路径中的一段**；
2. **接受准则**：若新解更好（$\Delta\le0$）直接接受；若更差，以概率 $\exp(-\Delta/T)$ 接受（$T$ 是温度）；
3. **降温**：$T \leftarrow \alpha T$（几何降温），$T\to0$ 时几乎只接受改进。

**灵魂在第 2 条**：**以一定概率接受「变差」的移动**，才能**跳出局部最优**——这正是它优于「爬山法」之处。

```cpp
if (delta <= 0 || u(rng) < std::exp(-static_cast<double>(delta) / T)) {
    if (delta > 0) ++accWorse;                      // 记录「接受变差」
    std::reverse(tour.begin() + i, tour.begin() + j + 1);   // 2-opt
    cur += delta;
    if (cur < bestLen) { bestLen = cur; best = tour; }
}
```

**温度要自适应**：本例先采样随机 2-opt 的「变差幅度」，取中位数 $D$，令 $T_0 = D/\ln 2$（此时「接受一个典型坏解」的概率约 $0.5$）。$n=9$ 时（与精确最优对拍）：

```
===== 1) 小规模：SA vs 精确最优（n=9） =====
  自适应起始温度 T0 = 69.2494
  精确最优     = 329
  最近邻(贪心) = 372  （比最优差 13.0699%）
  模拟退火     = 329  （达到最优 ✓）
  接受「变差」移动次数 = 221454（正是它帮助跳出局部最优）
```

**退火拿到了精确最优**，而且**接受了 22 万次「变差」移动**——正是它们把解从局部最优里拽了出来。$n=40$ 时（精确解不可行）：

```
===== 2) 中等规模：SA 显著优于最近邻（n=40） =====
  自适应起始温度 T0 = 118.301
  最近邻(贪心) = 634
  模拟退火     = 509  （比最近邻好 19.7161%）
  退火步数 = 4604700，接受变差 = 621641
```

**SA 比最近邻好近 20%**——但注意它**不讲「保证」**，只讲「实践常好」。

**温度的权衡**（U 形现象）：

```
===== 3) 起始温度：太冷「冻住」、太热「乱走」 =====
  参考温度 T* = 100.989（由「典型 Δcost」定）
  T0/T*		T0		终止长度	接受变差次数
  0.01		1.00989		479		158
  0.1		10.0989		469		3569
  1		100.989		469		72281
  10		1009.89		486		196006
  100		10098.9		486		261812
```

- $T_0$ 太小（$158$ 次接受变差）：几乎退化成**爬山**，困在局部最优（$479$）；
- $T_0\approx T^\*$（$7.2$ 万次接受变差）：**既能逃出局部最优、又来得及收敛**（$469$，最好）；
- $T_0$ 太大（$26$ 万次）：**前期乱走、后期来不及收敛**（$486$，反而更差）。

**「刚刚好」的温度，与「典型 $\Delta\text{cost}$」同量级**——这就是调参的核心直觉。

---

## 15.6 FPTAS：背包的「任意精度」近似

有些 NP 难问题允许**任意精度**的近似：给定 $\varepsilon>0$，能在**多项式时间**内给出 $(1-\varepsilon)\cdot\text{OPT}$ 的解。这叫 **FPTAS（完全多项式时间近似方案）**——$n$ 和 $1/\varepsilon$ 都是多项式。

### 伪多项式 vs 多项式

0/1 背包的精确 DP 是 $O(n\cdot\text{cap})$（在**容量维**上填表）。这看着像多项式，但 $\text{cap}$ 是**数值**（容量 1 亿就要 10 亿次运算）——这叫**伪多项式（pseudo-polynomial）**，不是真的多项式。

FPTAS 换一个维度：**在「价值维」上做 DP，但先把价值粗粒度化**。

$$K=\frac{\varepsilon\, v_{\max}}{n},\qquad v_i'=\Big\lfloor \frac{v_i}{K}\Big\rfloor$$

然后在「缩小的价值域」上求精确 DP（`minW[t]` = 达到缩放价值 $t$ 的**最小重量**）：

```cpp
const double K = eps * vmax / n;
vp[i] = (int)std::floor(v[i] / K);                 // 缩放价值
// minW[t]: 达到缩放价值 t 的最小重量
for (i) for (t = sumVp; t >= vp[i]; --t)
    minW[t] = std::min(minW[t], minW[t - vp[i]] + w[i]);
```

**为什么是多项式？** 缩放价值域 $\sum v_i' \le \dfrac{n\,v_{\max}}{K} = \dfrac{n}{\varepsilon}\cdot\dfrac{v_{\max}}{v_{\max}}\cdot n = \dfrac{n^2}{\varepsilon}$，DP 复杂度 $O(n\cdot\sum v')=O\!\left(\dfrac{n^3}{\varepsilon}\right)$——**只与 $n,\varepsilon$ 有关，与容量无关**。

**为什么精度有保证？** 舍入最多让每个价值损失 $K$，总共损失 $\le nK=\varepsilon v_{\max}\le\varepsilon\cdot\text{OPT}$，故 $A\ge(1-\varepsilon)\text{OPT}$。

实测（$n=100$，精确最优 $\text{OPT}=42379$）：

```
===== 2) FPTAS：eps 越小越准（但越慢） =====
  eps	近似值		比值 A/OPT	保证下界(1-eps)	达标?	耗时(ms)	缩放价值域
  0.5	42255.0		0.9971		0.5000		yes	0.51		10809
  0.20	42342.0		0.9991		0.8000		yes	1.32		27084
  0.10	42379.0		1.0000		0.9000		yes	2.63		54224
  0.05	42379.0		1.0000		0.9500		yes	4.16		108448
  0.02	42379.0		1.0000		0.9800		yes	8.87		271120
  0.01	42379.0		1.0000		0.9900		yes	16.53		542240
```

**两条规律**：

- **$\varepsilon$ 减半 $\Rightarrow$ 价值域翻倍 $\Rightarrow$ 耗时约翻倍**（$0.51\to1.32\to2.63\to4.16\to8.87\to16.53$ ms）——正是 $O(n^3/\varepsilon)$ 的实测；
- **实测比值远优于 $(1-\varepsilon)$ 下界**（$\varepsilon=0.5$ 时仍有 $0.9971$）：因为真实误差 $\approx\varepsilon v_{\max}/\text{OPT}$，而这里 $v_{\max}$ 比 $\text{OPT}$ 小得多。**理论保证是「最坏情况的界」，实际通常好得多。**

**与精确 DP 的本质区别**：

```
  精确 DP 复杂度 O(n*cap) = 100000（与容量成正比，容量大就崩）
  若容量 = 1 亿：精确 DP 需 O(n*cap) = 10000000000（~10.00 G 次）——不可行；
  而 FPTAS 只看价值域，与容量无关（只要容量够大容得下），仍可在 ms 级求解。
```

**容量 1 亿时精确 DP 崩（100 亿次），而 FPTAS 仍毫秒级**——这就是「伪多项式」与「多项式」的差距。

---

## 15.7 复杂度的全景图

把本章几个概念串成一张图，作为你以后看到新问题时的判断依据：

```
                    所有问题
        ┌───────────────┴───────────────┐
        │                               │
      P（易）                      NP 难（难）
        │                               │
   ┌────┴────┐                 ┌────────┴─────────┐
   │         │                 │                  │
排序/最短路   │            NP 完全（NP 里最难）   NP 难但不在 NP
   …         │            SAT/3-SAT/团/VC/      （如 TSP 优化版、
             │            TSP 判定/子集和…       某些博弈问题）
             │                 │
        （P ⊆ NP）             │
                        近似谱系（对付它们的手段）
        ┌────────────────┼──────────────────┬───────────────┐
   ρ-近似（常数比）   PTAS（(1+ε)，poly n）  FPTAS（poly n, 1/ε）  不可近似（APX 难）
   VC: 2-近似, 装…    Euclid-TSP 的 PTAS     背包 FPTAS           一般 TSP: 无常数比近似（除非 P=NP）
```

几个值得记住的结论（**不用记证明，记「存在性」**）：

- **P $\subseteq$ NP**；**P = NP?** 未解，公认 **P $\ne$ NP**；
- **有 NP 完全问题有多项式算法 $\iff$ P = NP**（因为可由 SAT 归约）；
- **不是所有 NP 难都能近似**：一般 TSP（不满足三角不等式）**没有**常数比近似（除非 P=NP）；而满足三角不等式的度量 TSP 有 $1.5$-近似（Christofides）；
- **顶点覆盖有 2-近似，且（在 P$\ne$NP 下）没有 $1.36$-近似**；背包有 FPTAS；
- **近似难度本身也是理论**：APX 难、PCP 定理（近似比的下界与「可证明性」的联系）。

---

## 常见错误与陷阱

1. **把「NP 难」当成「无法求解」**：NP 难只是**最坏情况**没有多项式算法；具体实例常能被分支限界 / 启发式快速拿下。**别一看到 NP 难就放弃**。
2. **混淆 P、NP、NP 完全、NP 难**：$NP\ne$「非多项式」！NP 是「非确定性多项式时间（可快速验证）」；P $\subseteq$ NP。**NP 完全 ⊆ NP，NP 难 ⊇ NP 完全**。
3. **把 NP 完全问题当成「已证明不能多项式求解」**：只是**没找到**，且相信没有——但**这不是定理**（除非证明了 P$\ne$NP）。
4. **近似比方向搞反**：最小化问题是 $A\le\rho\,\text{OPT}$（$\rho\ge1$）；最大化问题是 $A\ge\text{OPT}/\rho$ 或 $A\ge(1-\varepsilon)\text{OPT}$。**别把「$\rho<1$」当更好。**
5. **以为「贪心 = 近似算法」**：贪心常有**好平均表现**，但**没有比值保证**（15.4 的按度贪心就是）。要保证就得**证明**。
6. **近似比的证明漏掉「对任意实例」**：必须证明**所有**实例都满足 $A\le\rho\,\text{OPT}$，而不是举几个例子。**一个反例就能毁掉近似比。**
7. **模拟退火的温度尺度不对**：$T_0$ 与典型 $\Delta$ 差几个数量级时，退火要么「冻住」（纯爬山）要么「乱走」（不收）。**要按 $\Delta$ 的实际尺度设 $T_0$。**
8. **降温太快 / 迭代太少**：$T$ 掉太快或每个温度迭代太少，搜不到好解。**降温要慢（$\alpha\sim0.99$ 以上）、每个温度要有足够迭代。**
9. **SA 只看「当前最优」忘了「当前解」**：SA 靠「当前解 + 接受准则」游走，**必须保存并持续更新当前解**，而不是只留最优；否则就退化成随机采样。
10. **FPTAS 的 $K$ 设错**：$K=\varepsilon v_{\max}/n$（不是 $\varepsilon\,\text{OPT}$，也不是 $\varepsilon v_{\min}$）。$K$ 太小 $\Rightarrow$ 价值域爆炸（慢）；$K$ 太大 $\Rightarrow$ 精度差。
11. **混淆「伪多项式」与「多项式」**：$O(n\cdot\text{cap})$ 里的 $\text{cap}$ 是**数值**不是**位数**，容量一大就崩（$O(n\cdot 2^{\text{bits}})$）。**这才是背包判定是 NP 完全的本质。**
12. **以为「所有 NP 难问题都能任意精度近似」**：错。一般 TSP、最大团等**不可**常数比近似（除非 P=NP）。**近似难度本身是 NP 难问题的属性。**

---

## 与 AI / 世界模型的连接

「难题 + 近似」这套语言，是**理解现代 AI 方法论的底层框架**：

- **为什么 AI 里到处都是「近似 / 采样 / 启发式」？** 因为**精确推断在一般图模型上是 NP 难的**（MAP 推断 = 求最大概率配置）。于是有了：
  - **变分推断 / 平均场**——把难优化变成可优化的下界（近似）；
  - **MCMC / 吉布斯采样**——用随机游走近似后验（就是模拟退火的「同族」）；
  - **维特比 / 前向**（第 14 章）——**链状图**上的精确推断是多项式，一旦图有环就 NP 难。
  **「能精确的只有树 / 链」，是概率图模型最深刻的教训之一。**
- **组合优化与神经网络**：TSP 的**神经组合优化**（Pointer Network、GNN + 强化学习）用「学习到的启发式」替代人工设计；**注意力模型**本身就是「软化的最近邻 / 软化的 2-opt」。**它们对标的就是本章的 SA / 近似算法。**
- **可满足性与求解器**：SAT / SMT 求解器（CDCL）用**回溯 + 学习子句 + 剪枝**（第 14 章的回溯）在**工业实例**上惊人地快——**NP 难的「一般性」与「结构化实例的可解性」在此分野**。程序合成、验证、神经符号 AI 都靠它们。
- **规划与决策 = 近似搜索**：世界模型的**规划**是在一棵巨大的未来树上搜索（分支限界 / MCTS / beam search）。**「无法精确求解 $\Rightarrow$ 用有指导的近似搜索」**——这正是 MCTS（AlphaZero / MuZero）的灵魂，与模拟退火、束搜索同源。
- **「难度」决定了方法的形态**：
  - 能多项式精确 $\Rightarrow$ 直接算（DP、图算法）；
  - NP 难但可近似 $\Rightarrow$ 近似 / 启发式（SA、LP 松弛、半正定松弛 Goemans–Williamson）；
  - 不可近似或规模巨大 $\Rightarrow$ **学一个模型来近似**（神经网络成为「函数近似的通用工具」）。
  **大模型的成功，本质上就是「用海量数据学一个近似器」来绕过某些不可解/太复杂的推断与搜索。**
- **近似比思维在 AI 评估里**：近似的「保证」与「实践表现」的分离（15.4 的匹配近似 vs 按度贪心），对应着模型「理论界」与「实测指标」的分离——**别只看均值，要看最坏情况**。

**一句话**：NP 难与近似算法给了你一个**「问题难度」的坐标系**。世界模型要做的规划、推断、搜索，绝大多数落在「NP 难」区域；于是**「精确 + 近似 + 学习」三者的分工**，就成了现代 AI 系统设计的核心命题。

---

## 本章小结

- **判定问题 / P / NP / NP 完全 / NP 难**：P = 多项式可解；NP = 多项式可**验证**；NP 完全 = NP 里最难；NP 难 = 至少那么难。**P $\subseteq$ NP，P = NP? 未解**（公认 $\ne$）；
- **归约** $A\le_p B$ 是「比难度」的标尺；**Cook–Levin**：SAT 是第一个 NP 完全问题，成为归约树根；由此得到 SAT / 3-SAT / 团 / 顶点覆盖 / 独立集 / 哈密顿回路 / TSP / 子集和 / 背包 / 图着色的**图谱**；
- **四条出路**：近似算法（有保证）、启发式（实践好）、精确指数（分支限界 / FPT）、特殊情形多项式；
- **近似比 $\rho$**：最小化 $A\le\rho\,\text{OPT}$。**顶点覆盖极大匹配 2-近似**（$\text{OPT}\ge|M|$，$A=2|M|\le2\,\text{OPT}$），且 **2 是紧的**（不相邻边集）；按度贪心平均更好但**无保证**；
- **模拟退火**：状态 + 邻域（2-opt）+ **接受坏解 $\exp(-\Delta/T)$** + 几何降温。**温度要与典型 $\Delta$ 同量级**（太冷冻住、太热乱走）；$n=9$ 达精确最优、$n=40$ 比最近邻好 ~20%；
- **FPTAS**（背包）：$K=\varepsilon v_{\max}/n$ 缩放价值 + 缩小价值域上精确 DP，$O(n^3/\varepsilon)$ **多项式**、保证 $(1-\varepsilon)\text{OPT}$；精确 DP 是**伪多项式** $O(n\cdot\text{cap})$，容量大就崩；
- **不是所有 NP 难都能近似**（一般 TSP 无常数比近似）；**能精确的往往只有「有结构」的实例**（链、树、小树宽）。

---

## 练习

### 基础

1. 判断下列问题属于 P / NP / NP 完全 / NP 难，并说明理由：(a) 排序；(b) 判断图中是否有三角形；(c) 判断图是否 3-可着色；(d) 求最短路径；(e) 求最小顶点覆盖。
2. 写出「顶点覆盖判定（$\le k$）」到「独立集判定（$\ge k$）」的多项式归约，说明为什么「$S$ 是顶点覆盖 $\iff$ $V\setminus S$ 是独立集」。
3. 举一个**属于 NP 但不（已知）属于 P** 的具体问题，并说清它的「证书」是什么、怎么在多项式时间验证。
4. 证明：若 $A\le_p B$ 且 $B\in P$，则 $A\in P$（用「多项式次调用」的定义）。
5. 对 15.4 的按度贪心，构造一个具体小图，使它的解**严格大于**最优解，说明「无保证」。

### 进阶

6. **实现 Christofides 1.5-近似**（度量 TSP）：求最小生成树 → 奇度点最小权匹配 → 欧拉回路 → 抄近路（用三角不等式）。在满足三角不等式的实例上与最优解比较比值。
7. **实现模拟退火的 2-opt 邻域 + Or-opt**（移动一小段），与只用 2-opt 比较收敛速度；再实现**多次重启**（multi-start）SA，比较与单次长跑的差异。
8. **给顶点覆盖实现 LP 松弛**：解「$\min\sum x_v$ s.t. $x_u+x_v\ge1,\ x\in[0,1]$」，把 $x\ge1/2$ 的顶点选入覆盖，验证是否也是 2-近似（并思考为什么）。
9. **实现背包的 PTAS 与 FPTAS 并用实验对比**：固定 $\varepsilon$，看两者在小 $n$ 上的精度差异；说明 PTAS 与 FPTAS 的定义差别（对 $1/\varepsilon$ 是否为多项式）。
10. **实现「最大团」的精确分支限界**（带着色上界），在 $n=50$ 的随机图上测量耗时随边概率的变化。
11. **验证「伪多项式」的本质**：固定 $n=50$，把容量从 $10^3$ 涨到 $10^9$，测量精确 DP 耗时（应线性增长），与 FPTAS（几乎不变）对比。

### 挑战

12. **实现 SAT 求解器（DPLL + 单元传播 + 学习子句 CDCL）**：在随机 3-SAT 上测「相变点」（子句/变量比约 4.26）附近的行为，对比基本回溯。
13. **实现图着色（3-着色）的回溯求解器 + 分支限界**，在相位过渡附近测耗时；说明为什么「相变点附近最难」。
14. **实现神经组合优化**：用 Pointer Network / GNN + 强化学习学一个 TSP 启发式，与最近邻、SA 在相同实例上比较（解质量 + 推理速度）。
15. **实现度量 TSP 的 PTAS 骨架**（Arora / Mitchell 的 $1+\varepsilon$ 方案思想）：用「网格扰动 + 结构化 DP」，说明它为什么是 PTAS（poly $n$，但 $1/\varepsilon$ 出现在指数上）而非 FPTAS。
16. **做一个「难度侦探」**：给你一个没见过的优化问题（如「社团检测」「装箱」），完成「判定版 → 找已知 NP 完全问题归约 → 选择策略」的完整分析报告。

<details>
<summary>参考答案（要点）</summary>

**1.** (a) P；(b) $O(n^3)$ 枚举三顶点 $\Rightarrow$ P；(c) 3-着色 NP 完全（$k\ge3$ 时；$k=2$ 是二部图判定，属 P）；(d) P（Dijkstra/Floyd）；(e) NP 难（优化版），判定版 NP 完全。

**2.** 归约：对每个顶点 $v$，令 $x_v$ 表示「$v$ 是否在覆盖里」。$S$ 是顶点覆盖 $\iff$ 每条边至少一端在 $S$ $\iff$ 没有任何一条边两端都在 $V\setminus S$ $\iff$ $V\setminus S$ 内没有边 $\iff$ $V\setminus S$ 是独立集。故「$\le k$ 的覆盖」$\iff$「$\ge n-k$ 的独立集」。

**3.** 例：**子集和判定**（有无子集和恰为 $S$）或**哈密顿回路**。证书 = 那个子集 / 那条回路（长度多项式）；验证 = 求和 / 沿回路检查（多项式）。故在 NP；未见多项式算法，是 NP 完全。

**4.** 若 $B$ 有多项式算法，调用 $A\le_p B$ 的归约最多多项式次 $B$，每次多项式时间，总时间 poly·poly = poly，故 $A\in P$。

**5.** 例：两条「长路径」中间连着一条边。按度贪心先选中中间的高度顶点（覆盖 2 条边），之后两端各需…，结果比最优多 1。画出即可说明贪心无保证。

**6.** Christofides：MST（$O(m\log n)$）→ 取奇度顶点集（偶数个）做最小权完美匹配 → 合并得欧拉图 → 找欧拉回路 → 按「首次访问」抄近路。三角不等式保证抄近路不增代价，比值 $\le3/2$。

**7.** Or-opt：把长度 $\le3$ 的一段「搬家」到别处；与 2-opt 组合邻域更大，收敛更快。多起点重启避免单一初始解拖累。

**8.** LP 松弛最优 $z^*\le\text{OPT}$（松弛放宽了整数约束）。取 $x_v\ge1/2$：每条边 $x_u+x_v\ge1$ 至少一端 $\ge1/2$，故是覆盖；规模 $\le\sum 2x_v\le2z^*\le2\text{OPT}$。故 2-近似（且 $z^*$ 给下界）。

**9.** PTAS：对每个固定 $\varepsilon$ 有 poly$(n)$ 算法，但复杂度可含 $n^{1/\varepsilon}$（对 $\varepsilon$ 非多项式）；FPTAS：复杂度对 $n$ 和 $1/\varepsilon$ **都**是多项式（背包就是）。

**10.** 分支限界 + **着色上界**（把当前团延拓的贪心上界），$n=50$ 稠密图常秒级；边概率 $\to1$ 时团大但易剪。

**11.** 精确 DP 耗时 $\propto$ cap（线性），cap 涨 $10^6$ 倍耗时也涨 $\sim10^6$ 倍；FPTAS 与 cap 无关，几乎不变。**这就是「伪多项式」的实证。**

**12.** DPLL：单元传播 + 纯文字 + 回溯；CDCL 加「冲突分析 + 学习子句 + 非时序回跳」。3-SAT 在 $\alpha\approx4.26$ 附近最难（相变）。

**13.** 回溯：按顶点顺序染色 + 约束传播；分支限界：用「已用颜色数下界 + 贪心着色上界」剪枝。相变点（边密度在某阈值附近）最难。

**14.** Pointer Network 输出城市排列，用「巡回长度」的负值做 RL 奖励（REINFORCE / 基线）；GNN 学边特征 + 贪心/beam 构造解。目标：推理比 SA 快、质量接近。

**15.** 度量 TSP 的 $(1+\varepsilon)$-PTAS：把平面切成格子、扰动使最优解少穿越边界、对每个边界穿越模式做 DP。复杂度含 $n$ 的高次（$\sim n^{O(1/\varepsilon)}$），故是 PTAS 非 FPTAS（平面 TSP 没有 FPTAS，除非 P=NP）。

**16.** 框架：写出判定版 → 找一个经典 NP 完全问题（如 SAT / 顶点覆盖 / 3-分区）做**双向归约** → 确定在 NP 完全/NP 难 → 按「有结构否 / 实例大小 / 是否要保证」选近似 / 启发式 / 精确指数。

</details>

---

## 延伸阅读

- **Sahni《数据结构、算法与应用（C++ 版）》第 2 版 第 20 章**「NP-Hard, NP-Complete, and NP-Equivalent Problems」与相关近似算法讨论；
- **《算法导论》第 34 章（NP 完全性）、第 35 章（近似算法）**——归约、Cook–Levin、顶点覆盖 2-近似、TSP 近似、集合覆盖的 $O(\ln n)$；
- **Cook, S. (1971)** *The complexity of theorem-proving procedures* —— NP 完全性与 Cook–Levin 定理；
- **Karp, R. (1972)** *Reducibility among combinatorial problems* —— 21 个 NP 完全问题的经典清单；
- **Garey & Johnson (1979)** *Computers and Intractability: A Guide to the Theory of NP-Completeness* —— NP 完全性的「圣经」；
- **Kirkpatrick, Gelatt & Vecchi (1983)** *Optimization by Simulated Annealing* —— 模拟退火的奠基；
- **Christofides, N. (1976)** *Worst-case analysis of a new heuristic for the TSP* —— 度量 TSP 的 $3/2$-近似；
- **Vazirani, V. (2001)** *Approximation Algorithms* —— 近似算法的系统教材（含顶点覆盖、集合覆盖、LP 松弛、不可近似性）；
- **Arora, S. (1998)** *Polynomial time approximation schemes for Euclidean TSP…* —— 欧氏 TSP 的 PTAS；
- **Vinyals et al. (2015)** *Pointer Networks* / **Bello et al. (2016)** *Neural Combinatorial Optimization with RL* —— 用神经网络学组合优化启发式；
- **Karpathy / Sutton 等关于规划与搜索的综述**（MCTS、AlphaZero、MuZero）—— 世界模型里「近似搜索」的工程形态；
- 配套代码：`code/15_vertex_cover_approx.cpp`、`code/15_simulated_annealing_tsp.cpp`、`code/15_knapsack_fptas.cpp`。

---

**下一章**：第 16 章《面向世界模型研究的进阶路线》—— 这门课到达终点，世界模型的研究刚刚起步。这一章把前面 15 章的「数据结构 + 算法」重新编织成一张**面向研究的路线图**：从序列模型（HMM / RNN / 注意力）到状态空间模型与扩散，从搜索与规划（MCTS / MPC / 分支限界）到表示学习与检索（KD 树 / LSH / 向量库），从自微分（第 05 章的栈 → 计算图）到高效训练（矩阵乘 / 缓存 / 近似）。并给出一套**可跑的基准套件**与**读论文 / 复现的路线**，让你能真正把基础课的能力接到前沿研究上。
