# 第 02 章 线性表与数组

> 数据结构课的第一个"真家伙"。线性表（linear list）是所有容器的原型：一串元素排成一条线，每个元素有唯一的前驱和后继。本章要回答两个问题：**怎么把一条线放进入内存**（数组表示），以及**为了放进去要付什么代价**（扩容、搬移、迭代器失效）。最后一个问题特别重要——它就是"为什么 PyTorch 张量的 `transpose` 有时会让后续操作变慢"的答案。

---

## 本章目标

1. 用 ADT 的方式描述线性表：先定接口，再谈实现；
2. 分清**"第 k 个元素"**（1-based 语义）与**数组下标**（0-based）的区别，避免经典 off-by-one；
3. 实现一个动态数组类 `arrayList<T>`，包括扩容、插入、删除、迭代器；
4. 推导**倍增扩容的摊还复杂度为 $O(1)$**，并解释为什么不是"加 1"；
5. 说清 `size` 与 `capacity` 的区别，以及 `reserve`/`resize`/`shrink_to_fit` 各自做什么；
6. 掌握容器**失效规则**（什么时候迭代器/指针/引用会失效）；
7. 在 `std::vector`、`std::array`、原生数组之间做出正确选择。

## 前置知识

- 第 00 章：类、模板、RAII、迭代器概念、异常；
- 第 01 章：摊还分析、$O/\Theta$ 记号。

---

## 2.1 ADT：先定接口，再谈实现

**抽象数据类型（ADT, Abstract Data Type）** = 数据 + 操作 + **语义约定**，不涉及具体存储。

用 ADT 的视角看线性表，我们要说清三件事：

| 要素 | 线性表的内容 |
|---|---|
| 数据 | 类型为 `T` 的元素序列，允许重复，**有序**（位置有意义） |
| 操作 | `size`、`empty`、`get`、`set`、`insert`、`erase`、`indexOf`、`output`、`clear` |
| 语义 | 元素之间有线性次序；`insert(i,x)` 后 `x` 位于下标 `i`；… |

**为什么先定接口**：因为后面（第 03 章）我们要用**完全相同的接口**换一套"链表"实现。接口稳定，使用者代码就不用改。这是本书反复强调的方法论。

---

## 2.2 线性表 ADT 的接口设计

```cpp
template <typename T>
class LinearList {
public:
    virtual ~LinearList() = default;
    virtual bool empty() const = 0;
    virtual std::size_t size() const = 0;
    virtual T&       get(std::size_t i) = 0;
    virtual const T& get(std::size_t i) const = 0;
    virtual void set(std::size_t i, const T& x) = 0;
    virtual void insert(std::size_t i, const T& x) = 0;
    virtual void erase(std::size_t i) = 0;
    virtual std::size_t indexOf(const T& x) const = 0;
    virtual void clear() = 0;
};
```

> 用纯虚基类定义接口（运行期多态）在教学上很清晰；但**在真正追求性能的代码里**，我们更常用模板（编译期多态）——这正是 `std::vector` 的做法。第 00 章 0.4.3 比较过这两种多态。

### 关键约定：`get(i)` 里的 `i` 是什么？

这是初学者最容易混淆的地方。**Sahni 教材用 1-based 的"第 k 个元素"**，而 **C++ 数组与 `std::vector` 用 0-based 下标**。两者差 1！

| 说法 | 含义 | 对应下标 |
|---|---|---|
| "第 1 个元素" | 第一个 | 0 |
| "第 k 个元素" | 第 k 个 | k−1 |
| `get(0)` | 下标为 0 | 0 |
| "插入到第 k 个位置" | 插在原来第 k 个元素**之前** | 下标 k−1 |

**实用建议**：
- 教材的复杂度结论（比如"插入是 $O(n)$"）与用哪种记法**无关**，别纠结；
- 写代码时**统一用 0-based 下标**，并在函数注释里明确写清；
- 每个涉及位置的函数，都测三个边界：**头、尾、越界**。

```cpp
// 越界必须抛异常，而不是"未定义行为"
T& get(std::size_t i) {
    if (i >= size_) throw std::out_of_range("arrayList::get 下标越界");
    return data_[i];
}
```

---

## 2.3 数组表示：从静态数组到动态数组

**数组表示的核心思想**：用**一整块连续内存**存元素，第 $i$ 个元素存放在 `base + i * sizeof(T)`。

```
下标:    0     1     2     3     4     5     ...
内存:  [ a ] [ b ] [ c ] [ d ] [   ] [   ]  ...
        ↑
      base
```

优点：
- **随机访问 $O(1)$**：知道下标就直达，不需要走指针；
- **内存紧凑**：没有指针开销，一个 `int` 就是 4 字节；
- **缓存友好**：连续访问能打满内存带宽（第 00 章 0.9 节实测过）。

缺点：
- **插入/删除 $O(n)$**：中间插一个元素，后面全体后移；
- **容量固定**：`T a[100];` 满了就完蛋。

于是我们需要**动态数组**：容量不够时自动申请更大的空间。

### 2.3.1 一个最小可用的动态数组

```cpp
template <typename T>
class ArrayList {
public:
    ArrayList() = default;
    explicit ArrayList(std::size_t cap) { reserve(cap); }

    ~ArrayList() { clear(); ::operator delete(data_); }

    // 禁止默认拷贝（教学版先不做深拷贝，见练习）
    ArrayList(const ArrayList&) = delete;
    ArrayList& operator=(const ArrayList&) = delete;

    std::size_t size() const     { return size_; }
    std::size_t capacity() const { return cap_; }
    bool empty() const           { return size_ == 0; }

    T&       operator[](std::size_t i)       { return data_[i]; }
    const T& operator[](std::size_t i) const { return data_[i]; }

    void push_back(const T& x) {
        if (size_ == cap_) grow();          // ① 容量不够先扩容
        new (data_ + size_) T(x);           // ② 在未初始化槽位上构造
        ++size_;
    }

    void pop_back() {
        if (empty()) throw std::out_of_range("pop_back on empty");
        data_[--size_].~T();
    }

    void reserve(std::size_t newCap) {
        if (newCap <= cap_) return;
        reallocate(newCap);
    }

    void clear() {
        for (std::size_t i = 0; i < size_; ++i) data_[i].~T();
        size_ = 0;
    }

private:
    void grow() {
        const std::size_t newCap = (cap_ == 0) ? 1 : cap_ * 2;   // 倍增
        reallocate(newCap);
    }

    void reallocate(std::size_t newCap) {
        T* nd = static_cast<T*>(::operator new(newCap * sizeof(T)));
        for (std::size_t i = 0; i < size_; ++i) {
            new (nd + i) T(std::move(data_[i]));   // 移动而非拷贝
            data_[i].~T();
        }
        ::operator delete(data_);
        data_ = nd;
        cap_  = newCap;
    }

    T*          data_ = nullptr;
    std::size_t size_ = 0;
    std::size_t cap_  = 0;
};
```

**为什么用 `placement new` + 手工析构，而不是 `std::vector` 内部那样的复杂技巧**：

- `::operator new(bytes)` 只分配**原始内存**，不构造任何对象；
- `new (ptr) T(...)` 在这块内存上**构造**对象；
- `ptr->~T()` **手工析构**，但不释放内存（内存归 `::operator delete` 管）。

这三件事分开做，才能实现"容量 10 但只构造了 3 个元素"的状态——这正是 `size` 与 `capacity` 能分开的前提。

> **注意**：`delete[] p` 在数组长度未知时也要求编译器记住长度；而 `::operator new` / `::operator delete` 是"裸内存"接口，配合 placement new 使用，**不要混用** `new[]` 与 `::operator delete`。

### 2.3.2 插入与删除：$O(n)$ 的来源

```cpp
void insert(std::size_t i, const T& x) {
    if (i > size_) throw std::out_of_range("insert 位置非法");
    if (size_ == cap_) grow();
    for (std::size_t k = size_; k > i; --k)          // 从后往前搬，避免覆盖
        new (data_ + k) T(std::move(data_[k - 1]));
    data_[i].~T();                                   // 清掉旧对象（下面重建）
    new (data_ + i) T(x);
    ++size_;
}
```

**搬移次数**：插在尾部是 $0$ 次（$O(1)$），插在头部是 $n$ 次（$O(n)$），平均 $\frac{n}{2}$ 次 → 仍是 $O(n)$。

> **一个真实的坑**：如果写成 `for (k = i; k < size_; ++k) data_[k+1] = data_[k];`，会**从前向后**搬，导致同一个值被反复向右复制，把数组"刷"成一样的东西。**必须从后往前搬**。

删除同理，只是方向反过来（从前往后搬）。

---

## 2.4 扩容策略与摊还 $O(1)$

**问题**：容量满了，新容量取多少？

| 策略 | 新容量 | $n$ 次 `push_back` 的总搬移 | 单次摊还 |
|---|---|---|---|
| 加 1 | `cap + 1` | $0+1+\cdots+(n-1) = \Theta(n^2)$ | $\Theta(n)$ ❌ |
| 倍增 | `cap * 2` | $1+2+4+\cdots+2^{k} < 2n$ | $O(1)$ ✅ |
| 1.5 倍 | `cap * 1.5` | $\approx 2n$ | $O(1)$ ✅（常数更大） |

**倍增的摊还证明**（第 01 章三种方法都适用，这里用聚合法）：$n$ 次插入中，扩容发生在第 $1,2,4,8,\dots$ 次，搬移总数为

$$
1+2+4+\cdots+2^{\lfloor \log_2 n\rfloor} = 2^{\lfloor\log_2 n\rfloor+1}-1 < 2n
$$

加上 $n$ 次插入本身，总代价 $< 3n$，故**单次摊还 $O(1)$**。

**为什么工程上 `std::vector` 常用 1.5 倍（MSVC）或 2 倍（libstdc++/libc++）？** 2 倍的摊还常数最优，但释放的旧块尺寸"跳变"大，容易无法被分配器复用（进而触发 `mmap`/`munmap` 系统调用）；1.5 倍更容易复用刚释放的块。这是**理论复杂度相同、实测常数不同**的典型例子（第 01 章练习 13）。

**亲手验证**：`code/01_amortized_growth.cpp`（第 01 章）与本章的 `code/02_amortized_growth.cpp` 都会实测总搬移次数，前者断言倍增时为 $n-1$ 量级、后者对比 1.5 倍。

---

## 2.5 `size` vs `capacity`：动态数组的两个"长度"

```
capacity = 8
+----+----+----+----+----+----+----+----+
| 10 | 20 | 30 |    |    |    |    |    |
+----+----+----+----+----+----+----+----+
  ↑                  ↑
  data_             size_ = 3
```

- **`size()`**：**有效元素**个数（已构造的对象数）；
- **`capacity()`**：**已分配内存**能容纳的元素数（未构造）；
- **不变量**：$0 \le \text{size} \le \text{capacity}$。

三者常被混淆，务必分清：

| 操作 | 效果 | 复杂度 |
|---|---|---|
| `resize(n)` | 把 size 改成 n（少了析构、多了默认构造） | $O(\|n-\text{size}\|)$ |
| `reserve(n)` | 把 capacity 提到 ≥ n，**不改 size** | $O(\text{size})$（需搬移） |
| `shrink_to_fit()` | 请求把 capacity 降到 size（**非强制**） | $O(\text{size})$ |
| `clear()` | size 置 0，**capacity 不变** | $O(\text{size})$ |

```cpp
std::vector<int> v;
v.reserve(1000);            // 一次分配好，避免中间 10 次扩容
for (int i = 0; i < 1000; ++i) v.push_back(i);   // 全程零搬移
std::cout << v.size() << ' ' << v.capacity();    // 1000 1000
v.clear();
std::cout << v.size() << ' ' << v.capacity();    // 0 1000（capacity 保留！）
```

**性能建议**：如果你**预先知道**元素数量，`reserve` 是最便宜、最有效的优化之一——它把"摊还 $O(1)$"变成"真 $O(1)$"。

---

## 2.6 迭代器：为什么它决定了能用哪些算法

迭代器是"指针的抽象"，至少提供 `*it`（取值）、`++it`（前进）、`it != end`（比较）。

```cpp
template <typename T>
class ArrayList {
public:
    using iterator = T*;                        // 数组表示：迭代器就是指针
    iterator begin() { return data_; }
    iterator end()   { return data_ + size_; }
    // const 版本略
};
```

因为 `T*` 是**随机访问迭代器**，所以下面这些都能用：

```cpp
std::sort(v.begin(), v.end());                  // 需要随机访问
auto it = std::lower_bound(v.begin(), v.end(), x);  // 二分，需要随机访问
```

而链表的迭代器只能 `++/--`（双向），因此 `std::sort` **不能**用于 `std::list`（要用成员 `list::sort`）。**这不是算法不好，是迭代器能力不够。** 这条结论在第 03、09、12 章反复出现。

### 失效规则（invalidations）——最容易踩的坑

| 容器 | 什么操作让迭代器/指针/引用失效 |
|---|---|
| `std::vector` | **扩容后全部失效**；`insert`/`erase` 之后的元素位置也会移动 |
| `std::array` | 不涉及动态分配，生命周期内一直有效 |
| `std::list` | 只有**被删除元素**的迭代器失效，其它不受影响 |
| `std::deque` | 首尾插入可能让迭代器失效，但**引用**通常仍有效 |

```cpp
std::vector<int> v{1, 2, 3};
int& r = v[0];
v.push_back(4);              // 可能触发扩容
// r 现在可能悬垂（dangling）！使用它是未定义行为
```

**心法**：**不要跨"可能扩容"的操作持有 `vector` 元素的指针或引用。** 常见的正确写法是先 `reserve`，或者用**下标**而不是引用。

---

## 2.7 `std::vector`、`std::array` 与原生数组

| | 原生数组 `T a[N]` | `std::array<T,N>` | `std::vector<T>` |
|---|---|---|---|
| 大小 | 编译期固定 | 编译期固定 | 运行期可变 |
| 存哪 | 栈（或全局） | 栈 | 堆 |
| `.size()` | 无（要自己记） | 有 | 有 |
| 拷贝语义 | 退化成指针，危险 | 值拷贝，安全 | 值拷贝，安全 |
| 越界检查 | 无 | `at()` 有 | `at()` 有 |
| 适用 | 与 C API 交互 | 固定大小、零开销 | 绝大多数场景 |

**实践建议**：
- **默认用 `std::vector`**：灵活、安全、性能足够；
- 大小**编译期已知且很小**（如 3×3 矩阵、词表大小固定的查找表）用 `std::array`，避免堆分配；
- 只在**必须**传给 C 接口（CUDA kernel、BLAS）或需要极致控制时才用原生数组 + `data()`。

```cpp
std::vector<int> v(10, 7);       // 10 个 7
std::array<double, 3> x{1.0, 2.0, 3.0};
int raw[4] = {1, 2, 3, 4};
auto* p = raw;                   // 退化：sizeof(p) == 8，不是 16！
```

---

## 常见错误与陷阱

1. **0-based / 1-based 混用**：`get(i)` 到底是"第 i 个"还是"下标 i"？写代码时统一 0-based。
2. **插入时从前向后搬元素**：必须**从后往前**，否则数据被覆盖。
3. **扩容后继续用旧迭代器/引用**：悬垂 → 未定义行为。
4. **`reserve` 与 `resize` 搞混**：前者改容量，后者改元素个数。
5. **`new[]` 配 `delete`（或反之）**：未定义行为。
6. **忘记析构元素**：`::operator delete(data_)` 只释放内存，不调用 `~T()`。若 `T` 持有资源（如 `std::string`），就泄漏了。
7. **`push_back` 里传引用自身体内的元素**：`v.push_back(v[0])` 在扩容时会出问题（自引用）。正确做法是先拷贝一份（多数标准库实现已处理，但别依赖）。
8. **在循环里 `v.size()` 作为条件且循环体修改 `v`**：`size()` 是无符号数，`for (int i = 0; i <= v.size()-1; ++i)` 在 `size()==0` 时会因下溢变成天文数字。
9. **把 `O(n)` 的 `erase` 放进循环里**：`erase` 是 $O(n)$，循环里删多个元素要小心变成 $O(n^2)$；正确做法是"双指针原地删除"（remove-erase 惯用法）。
10. **用 `vector` 做"频繁中间插入"**：应该换 `std::list` 或 `std::deque`（第 03、06 章）。

---

## 与 AI / 世界模型的连接

### 1. 张量就是"加了形状的动态数组"

`torch::Tensor` 的底层是**一整块连续内存 + 形状 + 步长（stride）**：

```
一个 shape = (2, 3) 的 FP32 张量：
内存:  [x00][x01][x02][x10][x11][x12]      ← 连续，共 24 字节
stride = (3, 1)                            ← 第二个维度的 stride 是 1（最快变化）
```

- **连续内存** = 数组表示 → 随机访问 $O(1)$、缓存友好；
- **batch 的每一行就是线性表的一个元素**；
- 特征拼接（`torch.cat`）本质就是 `push_back` 一批元素 + 搬移，**代价是 $O(\text{总元素数})$**；
- **扩容**：`torch.cat` 在一个循环里反复拼接（`emb = torch.cat([emb, new])`）是经典的性能反模式——每次都会**重新分配 + 搬移全部数据**，复杂度 $O(n^2)$。正确做法是**先收集到 list 再一次性 cat**，或用 `reserve` 思想的预分配缓冲区。

### 2. `size` vs `capacity` 在 PyTorch 里的对应物

PyTorch 的 **caching allocator** 就有类似 `capacity` 的概念：释放的显存块不会立刻还给驱动，而是留在池里等复用。这让"反复申请同样大小的张量"变得便宜，代价是**显存占用看起来比实际需要多**（这也是为什么 `nvidia-smi` 显示的占用常常高于你的理论计算值）。

对应地，`torch.empty(n)` 只分配**不初始化**（等价于 `::operator new`），`torch.zeros(n)` 分配并写 0（等价于构造）。**训练代码里能用 `empty` 就别用 `zeros`**——省一次全量写显存。

### 3. 内存布局决定了算子性能

第 00 章 0.9 节我们实测了"顺序 vs 跨步访问"的带宽差异。在张量里，这表现为：

```python
x = torch.randn(64, 3, 224, 224)          # NCHW
y = x.permute(0, 2, 3, 1)                 # 变成 NHWC 的"视图"——但没有搬数据！
z = y.contiguous()                        # 这才真的搬一次数据（O(n)）
```

- `permute` 只改 **stride**，$O(1)$，但结果是**非连续**张量；
- 后续算子如果要求连续内存，就会触发一次隐式的 `contiguous()`（$O(n)$ 搬运）；
- 这就是"为什么 `channels_last` 内存格式能让某些卷积快 30%"——它让数据在**内存里就是算子想要的样子**，省掉搬运。

**结论**：第 02 章的"数组表示 + 连续内存"，直接决定了张量的 stride 语义、`view` vs `reshape` 的区别（`view` 要求连续，`reshape` 允许必要时拷贝）、以及大量算子的实际性能。

### 4. `std::vector` 在数据管道中的角色

- **DataLoader 的 batch 拼接**：把 $B$ 个样本堆成一个张量，本质是 $B$ 次 `push_back` + 一次连续化；
- **可变长序列的 padding**：先按长度排序/分桶，再拼接（第 12 章会讲 bucketing），目的是**减少 pad 造成的浪费**；
- **KV cache**：早期实现用 `torch.cat` 逐 token 追加（$O(n^2)$），现代实现用**预分配的大缓冲区 + 写入下标**（真 $O(1)$ 追加）——这正是 `reserve` 思想的胜利。

---

## 本章小结

- **ADT 先定接口**；线性表的接口是 `size/get/set/insert/erase/indexOf`，两种实现（数组、链表）共享它。
- **0-based 下标 vs "第 k 个"** 差 1；写代码统一 0-based，边界测头/尾/越界。
- **数组表示**：随机访问 $O(1)$、内存紧凑、缓存友好；插入删除 $O(n)$、容量固定。
- **动态数组**：`::operator new` 分配裸内存 + placement new 构造 + 手工析构，才能把 `size` 与 `capacity` 分开。
- **扩容必须倍增**（或 1.5 倍）：摊还 $O(1)$；"每次 +1"是 $\Theta(n^2)$，灾难。
- **`reserve`/`resize`/`shrink_to_fit`** 三者语义不同，别混。
- **迭代器能力决定可用算法**；`vector` 扩容会使全部迭代器/引用失效。
- 在 AI 里：张量 = 形状化的连续动态数组；`stride`/`contiguous`/`view` 的语义都源自本章；`torch.cat` 的循环拼接是 $O(n^2)$ 反模式；现代 KV cache 用预分配缓冲区避免它。

---

## 练习

### 基础

1. 写出 `insert(i, x)` 与 `erase(i)` 的搬移方向，并解释为什么方向不能反。
2. `std::vector<int> v; v.reserve(100); v.push_back(1);` 之后 `v.size()` 与 `v.capacity()` 各是多少？
3. `get(i)` 若 `i` 越界，抛异常和"直接返回垃圾值"各有什么后果？为什么标准库的 `[]` 不做检查而 `at()` 做？
4. 为什么数组表示不支持 $O(1)$ 的"在中间插入"？用搬移次数说明。
5. 判断：`std::vector` 的 `push_back` 是 $O(1)$。（提示：说清楚是哪种 $O$。）

### 进阶

6. 实现 `arrayList<T>` 的**迭代器**（含 `const_iterator`），使 `std::sort(v.begin(), v.end())` 可用。
7. 实现 `indexOf(x)`，并说明它的复杂度。若数组**已排序**，能否做到 $O(\log n)$？需要什么前提？（衔接第 12 章二分查找。）
8. 实现"原地删除所有等于 `x` 的元素"，要求 $O(n)$ 时间、$O(1)$ 额外空间，并返回新的逻辑长度（这是 C++ `std::remove` 的语义）。
9. 用 `code/02_amortized_growth.cpp` 实测 1.5 倍与 2 倍扩容的总搬移次数，解释为什么 1.5 倍的常数更大。
10. `std::vector<int> v{1,2,3}; auto it = v.begin(); v.push_back(4); std::cout << *it;` 可能有什么问题？怎么改才对？

### 挑战

11. 给 `arrayList<T>` 实现**强异常安全**的 `insert`：要么成功，要么容器完全不变。说明你如何处理"搬移过程中 `T` 的移动构造抛异常"。
12. 设计一个 `SmallVector<T, N>`：元素数 ≤ N 时用**栈上的内联缓冲区**，超过才用堆。分析它在"元素数都很小"的 AI 元数据场景下为什么更快（提示：省一次 `malloc` 与一次 cache miss）。
13. 证明：若扩容因子为常数 $b>1$，$n$ 次 `push_back` 的总搬移次数为 $\Theta(n)$，且常数为 $\frac{1}{b-1}$ 量级。
14. 在 100 万次 `push_back` 的场景下，比较三种写法：(a) 不 `reserve`；(b) `reserve(n)`；(c) 先 `resize(n)` 再用下标赋值。说明各自的实际耗时与适用场景。

<details><summary>参考答案与提示</summary>

**1.** `insert(i,x)` 从**后往前**搬（`k = size` 递减到 `i+1`，把 `data_[k-1]` 移到 `data_[k]`）；`erase(i)` 从**前往后**搬（把 `data_[k+1]` 移到 `data_[k]`）。方向反了会覆盖尚未搬移的数据。

**2.** `size()==1`，`capacity()==100`。`reserve` 只改容量。

**3.** 抛异常能被立即发现；返回垃圾值是未定义行为，可能几天后才在别处崩溃，极难调试。`[]` 不检查是为了**零开销**（热点循环里每次检查是负担），`at()` 提供安全的备选。**这是 C++"你不为你不用的东西付费"哲学的体现。**

**4.** 因为元素在内存里必须连续。在下标 `i` 处插入，原来下标 $\ge i$ 的元素全部要后移一格，共 $n-i$ 次搬移，最坏 $n$ 次 → $O(n)$。

**5.** **摊还** $O(1)$（最坏单次是 $O(n)$）。必须加"摊还"二字，否则不准确。

**6.** 用 `T*` 作迭代器即可（含 `using iterator = T*; using const_iterator = const T*;`，以及 `begin/end/cbegin/cend`）。`T*` 天然满足随机访问迭代器的全部要求。

**7.** 顺序 `indexOf` 是 $O(n)$。**若数组已排序**，可用二分查找 $O(\log n)$（第 12 章、第 09 章）。前提是"有序"，而维护有序性本身有代价（插入变成 $O(n)$）。

**8.**
```cpp
template <typename T>
std::size_t removeAll(ArrayList<T>& a, const T& x) {
    std::size_t w = 0;
    for (std::size_t r = 0; r < a.size(); ++r)
        if (!(a[r] == x)) a[w++] = a[r];      // 双指针：读指针 r，写指针 w
    // 若需真正减少 size，还要析构 [w, size) 区间
    return w;
}
```
（"写指针"是经典的 two-pointer 原地删除法。）

**9.** 总搬移 $\approx \frac{n}{b-1}$：$b=2$ 时 $\approx n$，$b=1.5$ 时 $\approx 2n$。物理原因：1.5 倍时**扩容更频繁**（次数是 2 倍的约 1.7 倍），每次搬移的量虽小，但累计更大。

**10.** `push_back(4)` 可能触发扩容，`it` 指向的旧内存已被 `::operator delete` 释放 → 悬垂，`*it` 是未定义行为。改法：(a) 先 `v.reserve(4)`；(b) 用下标 `v[0]`；(c) 在 `push_back` 之后重新取 `v.begin()`。

**11.** 要点：分两步——**先分配新空间并构造，全部成功后再交换/释放旧空间**（copy-and-swap）。若中途 `T` 的移动构造抛异常：
- 用 `std::move_if_noexcept`：若 `T` 的移动构造**不** `noexcept`，就退化为拷贝构造（拷贝失败时原数据完好）；
- 或者对元素做"就地搬移两次"（先搬到新空间，失败则原空间不变）。
简洁做法：`insert` 时先用 `T tmp(x)` 构造副本，再在**容量已足够**的前提下搬移；容量不足时先 `reserve`（这一步失败则容器不变）。

**12.** 要点：`SmallVector` 用 `alignas(T) unsigned char buf[N*sizeof(T)]` 做内联存储；`size <= N` 时完全不分配堆内存。收益：省一次 `malloc/free`（约几百 ns）+ 一次 cache miss（元数据随对象一起被加载）。TensorFlow 的 `InlinedVector`、LLVM 的 `SmallVector`、folly 的 `small_vector` 都是这个思路。

**13.** 第 $k$ 次扩容的容量 $\approx b^k$，总搬移 $= \sum_k b^k \approx \frac{b^{K+1}-1}{b-1}$，其中 $b^K \approx n$，故总量 $\approx \frac{bn}{b-1} = \Theta(n)$，常数 $\frac{b}{b-1}$。

**14.** (a) 多次扩容，总搬移 $\approx n$，最慢；(b) 一次分配，零搬移，最快；(c) `resize(n)` 会**默认构造 $n$ 个元素**（多一次全量写），然后你又赋值一次——对 `int` 差异小，对"构造昂贵/需要写零"的类型（如 `torch.zeros`）就明显浪费。**结论：默认用 `reserve` + `push_back`；只有需要"按位置随机写入"时才 `resize`。**

</details>

---

## 延伸阅读

- Sahni，《数据结构、算法与应用（C++版）》**第 2 版 第 5 章 Linear Lists—Array Representation**（含 ADT 定义、0-based/1-based 的讨论、`arrayList` 模板实现）。*章节号以第 2 版为准，不同版次可能略有差异。*
- 《Effective STL》（Scott Meyers）第 14 条（`reserve`）、第 22 条（`vector` 的失效规则）。
- PyTorch 文档 *Tensor Views*：`view` / `reshape` / `permute` / `contiguous` 的语义与代价。
- 配套代码：`code/02_array_list.cpp`（含迭代器与自测的完整 `arrayList<T>`）、`code/02_amortized_growth.cpp`（倍增 vs 1.5 倍的实测对比）。
