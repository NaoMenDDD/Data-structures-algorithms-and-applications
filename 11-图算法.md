# 第 11 章 图算法

> 上一章我们学会了「**怎么在图里走**」——DFS / BFS 是所有图算法的地基。这一章开始**盖楼**：在遍历的骨架上，长出图论里最有用的几棵大树。
>
> 你会看到一条极漂亮的**统一线索**贯穿全章：**贪心 + 松弛 + 优先队列**。最小生成树（MST）是在「给连通性问题做贪心」，Dijkstra 是在「给最短路问题做贪心」，A\* 是「给 Dijkstra 装上方向感」，而它们**共用同一套骨架**——优先队列出队、松弛邻居。并查集则是「连通性」这个问题的**近 O(1) 答案**，Kruskal 靠它判环。最后一节把镜头拉到**匹配（matching）**：任务分配、样本-原型指派，都是同一个「增广路」的故事。
>
> 这些算法不是纸上谈兵——**它们就是 AI 里「规划」「推理」「分配」的算法内核**。世界模型做规划（在想象的未来里找路）= A\*/树搜索；因果推断与消息传递 = 图上的 DP；最优传输与数据关联 = 二分图匹配。学完这一章，你就拿到了「**在图上做最优决策**」的全套工具。

---

## 本章目标

读完并跑完本章代码，你应该能够：

1. 说清**最小生成树（MST）**的定义，实现 **Kruskal**（边排序 + 并查集）与 **Prim**（优先队列长树），并讲清二者正确的共同基础——**切分性质（cut property）**；
2. 实现**并查集（Union-Find / DSU）**的**按秩合并 + 路径压缩**，说明为什么均摊代价是 $O(\alpha(n))$（$\alpha$ = 反阿克曼，实际 $\le 4$，即「近 $O(1)$」）；
3. 实现 **Dijkstra**（优先队列「惰性删除」版 + 朴素 $O(V^2)$ 版），**证明式地理解它为什么要求「非负权」**，并会用 `parent[]` 还原整条最短路；
4. 实现 **Bellman-Ford**（$O(VE)$），用它处理**负权边**，并**检测负环**（负环存在则最短路无定义）；
5. 实现 **Floyd–Warshall**（$O(V^3)$ 全源最短路），说清**为什么 $k$ 必须是最外层循环**，并用它求**传递闭包（可达性矩阵）**；
6. 实现 **A\*** 搜索（$f=g+h$），说清**可采纳（admissible）**与**一致（consistent）**两条性质的作用，并解释「开阔图省不了、有障碍才省」；
7. 实现**二分图最大匹配**（Kuhn 增广路），讲清 **Berge 定理**（无增广路 $\Leftrightarrow$ 最大）与 **König 定理**（最大匹配 = 最小点覆盖），并实现**指派问题的匈牙利算法**；
8. 建立一张**复杂度对照表**，能按「图稠密 / 稀疏、有无负权、单源 / 全源」快速选算法。

---

## 前置知识

- 第 10 章（图基础与遍历）—— 邻接表 / 邻接矩阵、DFS/BFS、判环、二分图二染色；本章**默认你已掌握**；
- 第 08 章（优先级队列与堆）—— Dijkstra / Prim / A\* 的引擎；**「惰性删除」**这一招在堆那章埋过伏笔；
- 第 01 章（算法分析）—— $O(E\log V)$、$O(VE)$、$O(V^3)$ 的来历；
- 一点点**贪心的直觉**：每一步都选「当前看起来最好」的，再证明这样不亏（切分性质就是那个证明）。

---

## 11.1 最小生成树（MST）：用最少的线连通所有点

**问题**：给一张**连通无向带权图**，找一棵**生成树**（连通所有 $V$ 个点、恰好 $V-1$ 条边、无环），使**边权总和最小**。原型：铺电网 / 修公路 / 布网线，用最少的钱把所有城市连起来。

**两个经典的贪心算法，策略不同但都正确：**

| | 策略 | 复杂度 | 适合 |
|---|---|---|---|
| **Kruskal**（加边） | 边按权**升序**，能加就加（用并查集判环） | $O(E\log E)$ | **稀疏图** / 只给边表 |
| **Prim**（长树） | 从一点出发，每次加「连接已选集合与外部的最小边」 | $O(E\log V)$ | **稠密图** |

### 为什么贪心对？——切分性质

$$ \textbf{对任意把点分成两半的「切」，横跨这个切的最小边一定在某个 MST 里。} $$

- **Prim** 每次加的，正是「当前已选集合 vs 外部」这个切的最小横跨边；
- **Kruskal** 每次加的，是「不产生环的全局最小边」，它等价于「某个切的最小横跨边」。

两个算法都只是**在实现这条性质的不同侧面**。这是「局部最优 = 全局最优」能成立的**唯一理由**，也是贪心算法正确的典型证明套路（「交换论证」：把任何 MST 逐步换成本算法的解，总权不变）。

### 代码骨架

```cpp
// Kruskal：排序 + 并查集
std::sort(edges.begin(), edges.end(), [](auto& a, auto& b){ return a.w < b.w; });
for (const Edge& e : edges)
    if (dsu.unite(e.u, e.v)) {            // 不成环才加
        total += e.w; chosen.push_back(e);
        if ((int)chosen.size() == n - 1) break;
    }

// Prim：优先队列维护「到已选集合的最小边」，惰性删除过期边
while (!pq.empty()) {
    auto [w, v, u] = pq.top(); pq.pop();
    if (inMST[v]) continue;               // 过期边，跳过
    inMST[v] = 1; total += w; chosen.push_back({u, v, w});
    for (auto [x, wx] : adj[v]) if (!inMST[x]) pq.push({wx, x, v});
}
```

跑 `code/11_mst.cpp`：

```
=== 1. 经典例子（6 个点）===
  Kruskal 选的边（按权升序）：
     1 - 2  权 2
     3 - 5  权 3
     0 - 1  权 4
     1 - 4  权 5
     1 - 3  权 6
  Kruskal 总权 = 20
  Prim    总权 = 20（应与 Kruskal 相同）
```

**两算法给出的总权一定相同（MST 的总权唯一），但选出的边可能不同**（等权边可互换，MST 一般不唯一）。代码用 **500 个随机连通图**对拍，两算法总权**完全一致**、边数都是 $V-1$，并用并查集独立复查「结果确实是一棵树（无环 + 连通）」：

```
=== 2. ★ 随机连通图：Prim 总权 == Kruskal 总权 ===
  500 个随机连通图：两算法总权完全一致、边数都是 V-1 ✓

=== 3. ★ 校验结果确实是一棵「树」===
  选出的 5 条边：无环 = 1，连通分量 = 1（都应为真 / 1）
```

> **MST 的 AI 落点**：聚类（单链 / 割边聚类）、图像分割（把像素当点、相似度当权）、网络设计、以及**稀疏化**——用 MST 骨架近似保持一张图的全局结构，降低后续图神经网络 / 消息传递的开销。

---

## 11.2 并查集（Union-Find）：近 $O(1)$ 的动态连通性

**问题**：维护一堆「等价类」。两个操作——`find(x)`（$x$ 属于哪个集合）、`unite(x, y)`（合并两个集合）。应用：判两点是否连通（Kruskal 判环）、数连通分量、社交网络「朋友圈」、图像连通区域。

**关键：两种优化，一次拥有：**

1. **按秩合并（union by rank）**：合并时把**矮树挂到高树**上，避免树越来越高；
2. **路径压缩（path compression）**：`find` 时把**一路上所有结点直接挂到根**，让树越用越扁。

> **两者叠加后，$m$ 次操作的**总**代价是 $O(m\cdot\alpha(n))$，其中 $\alpha$ 是**反阿克曼函数**——它增长极慢，对任何能想象的 $n$（$<$ 宇宙原子数）都有 $\alpha(n)\le 4$。所以说「近 $O(1)$」。（这个紧界由 **Tarjan, 1975** 证明。）

代码把两个优化做成开关，实测**四种组合**在 $n=10^5$、$m=2\times10^5$ 下的**总 find 步数**：

```
=== 3. ★ 四种组合的总 find 步数（n = 100,000，m = 200,000）===
     配置                       总 find 步数
     无优化（裸挂）              2312134733
     只用按秩合并                415552
     只用路径压缩                368678
     两者都用（推荐）            204394
```

**23 亿 → 20 万**，差了四个数量级！这就是「数据结构选对了，算法快一万倍」的活教材。代码还用 **200 个随机图**与 BFS 对拍：并查集数出的分量数、连通性抽查，全部正确。

> **注意**：并查集**只支持合并，不支持分裂 / 撤销**（要支持得用别的结构，如可持久化 DSU、link-cut tree）。这个限制正是它快的原因——**允许未来永久遗忘内部的树形**。

---

## 11.3 单源最短路 I：Dijkstra——「均匀扩散」的贪心

**问题**：给定**非负权**图和一个起点 $s$，求 $s$ 到所有点的最短路。

**Dijkstra 的心跳**：维护一个**已确定最短路**的集合。每次从「未确定」的点里挑**当前距离最小**的那个 $u$，把它「确定」下来，再用它**松弛（relax）**所有邻居：若 $dist[u]+w(u,v) < dist[v]$，就更新 $dist[v]$。用一个**优先队列**高效地找出「当前距离最小的未确定点」。

$$ \text{松弛：}\quad dist[v] \leftarrow \min\big(dist[v],\ dist[u] + w(u,v)\big) $$

```cpp
// 堆版（惰性删除）：允许同一点多次入堆，弹出时按 dist 判过期
gScore[start] = 0;
pq.push({0, start});
while (!pq.empty()) {
    auto [d, u] = pq.top(); pq.pop();
    if (done[u]) continue;                  // 过期项（该点已有更短距离）
    done[u] = 1;
    for (auto [v, w] : adj[u])
        if (gScore[u] + w < gScore[v]) {
            gScore[v] = gScore[u] + w;
            parent[v] = u;
            pq.push({gScore[v], v});
        }
}
```

跑 `code/11_dijkstra.cpp`：

```
=== 1. 一个小路网（有向、非负权）===
  从 0 出发到各点最短路：
     到 0 : 0   路径 0
     到 1 : 7   路径 0 -> 1
     到 2 : 9   路径 0 -> 2
     到 3 : 20   路径 0 -> 2 -> 3
     到 4 : 20   路径 0 -> 2 -> 5 -> 4
     到 5 : 11   路径 0 -> 2 -> 5
```

**注意到 4 的最短路是 $0\to2\to5\to4=9+2+9=20$**，而不是「直达 4」的那条边——Dijkstra 自动找到了这条绕路但更省的路。代码用 **300 个随机非负权图**对拍：堆版 == 朴素版 == Bellman-Ford。

### ★ 为什么 Dijkstra 要求「非负权」？

Dijkstra 的正确性建立在一个贪心断言上：**「第一个被确定（出队）的点，其距离已经是最终答案，永不改变。」** 这依赖「边权 $\ge 0$」——否则一条**后发现的负边**可能让已经确定的点**事后反悔**。代码给了具体反例：

```
=== 3. ★ 为什么 Dijkstra 要求「非负权」===
  朴素 Dijkstra  到 3 的距离 = 2（错！1 被过早「确定」，没再松弛 3）
  Bellman-Ford   到 3 的距离 = 1（对：0->2->1->3 = 0+1 = 1）
```

到 3 的真正最短路是 $0\to2\to1\to3$（用了一条 $-2$ 的负边）。但 Dijkstra 会**先把点 1「确定」成距离 2**，之后看到 $1\to3$ 这条负边使得到 3 变成 1 时，已经没有机会再回头松弛——**贪心失效**。**有负权必须换 Bellman-Ford。**

### 堆版 vs 朴素版：谁是赢家？

代码实测两版在稀疏 / 稠密图上的耗时：

```
=== 4. 复杂度实测：堆版 vs 朴素版 ===
     V       稀疏(deg=3)堆ms  稀疏朴素ms   稠密(deg=V/4)堆ms  稠密朴素ms
     20000         2.3546            889.893          77.9371          386.736
     50000         6.76382            5730.29          436.551          3886.64
```

- **堆版** $O((V+E)\log V)$：稀疏图只碰 $O(E)$ 条边，几乎线性；稠密图里虽 $E\approx V^2$，但 $\log V$ 增长了「平方」的痛；
- **朴素版** $O(V^2)$：稠密图常数小、无堆开销；但 $V$ 翻倍代价 **4 倍**（$V=5$ 万 → 25 亿次操作），很快被堆版甩开。

> **结论**：**稀疏图默认堆版**；朴素版只在 $V$ 很小、或配邻接矩阵做 $O(1)$ 判边时有优势。（口诀：$E\ll V^2$ 用堆，稠密且 $V$ 小用朴素。）

---

## 11.4 单源最短路 II：Bellman-Ford——容忍负权，还能捉负环

**Bellman-Ford 的心跳**：**重复 $V-1$ 轮**，每一轮「遍历所有边做一次松弛」。

$$ dist[v] \leftarrow \min\big(dist[v],\ dist[u]+w(u,v)\big)\quad \text{对所有边 } (u,v) $$

**为什么是 $V-1$ 轮？** 一条最短路上最多有 $V-1$ 条边。第 $i$ 轮结束时，所有「边数 $\le i$ 的最短路」都已被求出。所以 $V-1$ 轮后全部就绪。

**更强的本事——负环检测**：如果**再来第 $V$ 轮还能松弛**，说明存在**负环**（可以无限绕圈把距离越绕越小），此时「最短路」无定义。

```cpp
for (int iter = 0; iter < V - 1; ++iter)
    for (const Edge& e : edges)
        if (dist[e.u] != INF && dist[e.u] + e.w < dist[e.v])
            dist[e.v] = dist[e.u] + e.w;
// 第 V 轮还能松弛 -> 有负环
for (const Edge& e : edges)
    if (dist[e.u] != INF && dist[e.u] + e.w < dist[e.v]) return false;
return true;                                  // true = 无负环
```

跑 `code/11_bellman_ford_floyd.cpp`：

```
=== 1. Bellman-Ford：允许负权边 ===
  从 0 出发（含负权边）： d(0)=0 d(1)=2 d(2)=7 d(3)=4 d(4)=-2

=== 2. 负环检测（存在负环 -> 最短路无定义）===
  环 1->2->3->1 总权 = -1+3-3 = -1 < 0：
     Bellman-Ford 返回 0（false = 检测到负环）✓
  去掉回边 3->1 后变 DAG：无负环，返回 true ✓
```

> **复杂度 $O(VE)$**，比 Dijkstra 慢，但能处理负权、还能捉负环——这是它的不可替代性。它的队列优化版 **SPFA** 平均更快，但**最坏仍是 $O(VE)$**（勤务员可能会想「SPFA 更快吧」——是的，平均更快，但被特意构造的数据能把它打回原形）。

---

## 11.5 全源最短路：Floyd–Warshall——三行搞定一切

**问题**：求**任意两点间**的最短路（全源）。用**邻接矩阵** `dist[i][j]`，初始化为边权（无边为 $\infty$、自己到自己是 $0$），然后：

$$ \text{for } k:\quad \text{for } i:\quad \text{for } j:\quad dist[i][j] \leftarrow \min\big(dist[i][j],\ dist[i][k] + dist[k][j]\big) $$

**含义**：`dist[i][j]` 表示「$i\to j$ 的当前最短距离」。逐个把点 $k$ 当作**中转站**，看「$i\to k\to j$」是不是比已知的「$i\to j$」更短。$k$ 遍历完所有点后，答案就都对了。

> **★ 必须记住的坑：$k$ 必须在最外层！** 因为用的是「**逐步放开中转站集合**」的 DP：第 $k$ 轮结束时，`dist[i][j]` 表示「只允许用 $\{0,\dots,k\}$ 做中转」的最短距离。如果把 $k$ 放在内层，就会在「中转集合还没放开」时错误地更新，得到错误答案。**这是 Floyd 最常见的错**。

跑代码：

```
=== 3. Floyd-Warshall：一张表的「所有点对最短路」===
       到列:   0    1    2    3
    从 0 :   0    5    8    9   
    从 1 :   inf   0    3    4   
    从 2 :   inf   inf   0    1   
    从 3 :   inf   inf   inf   0   
  0->3 最短路 = 0->1->2->3 = 5+3+1 = 9 < 直达 10 ✓
```

代码用 **176 个无负环随机图**对拍：Floyd 的每一行 == 从该点出发的 Bellman-Ford。

### 副产品：传递闭包（可达性矩阵）

把 Floyd 里的 $\min$ 和 $+$ 换成 **「或」和「与」**，就是「**谁可达谁**」的闭包：

$$ reach[i][j] \leftarrow reach[i][j]\ \lor\ \big(reach[i][k]\ \land\ reach[k][j]\big) $$

```
=== 5. 副产品：传递闭包（可达性矩阵）===
       可达?    0  1  2  3
    从 0 :     1  1  1  0  
    从 1 :     0  1  1  0  
    从 2 :     0  0  1  0  
    从 3 :     0  0  1  1  
```

> **AI 落点**：传递闭包就是「**可达性 / 依赖可达**」，用于数据库查询优化、编译器（指针分析）、以及知识图谱推理里的**关系闭合**。Floyd 的「$k$ 做中转」思路，也是**图神经网络里「信息沿路径传播」**的离散原型。

---

## 11.6 A\*：给 Dijkstra 装上「方向感」

Dijkstra **没有眼睛**——明明终点在东边，它也要把西边探一遍。**A\* 给每个结点加一个启发式估计 $h(n)$**（「离终点还有多远」的猜测），按

$$ f(n) = g(n) + h(n) $$

出队：$g$ 是**已知**的起点到 $n$ 的代价，$h$ 是到终点的**估计**。于是搜索被「拽向」终点方向。

```
=== 1. 三种搜索对比（同一张地图）===
    算法                路径长   扩展结点数
    Dijkstra (h=0)       12       26
    A* (曼哈顿)          12       22
    加权 A* (w=3)        12       13
```

**两条关键性质**：

- **可采纳（admissible）**：$h(n)\le$ **真实**剩余代价。保证 A\* **一定最优**（$h$ 从不「高估」，就不会因为乐观而放弃最优路径）；
- **一致（consistent / monotone）**：$h(n)\le w(n,n')+h(n')$。保证每个点**只扩展一次**（像 Dijkstra 一样干净）。

网格上的**曼哈顿距离**同时满足①②，所以是标准选择。

> **若 $h=0$，A\* 就退化成 Dijkstra**；$h$ 越准，探的结点越少；$h$ 完全精确时只沿最短路走。**加权 A\*（$h$ 乘系数 $>1$）不可采纳——更快，但可能拿到次优解**（代码里 $w=3$ 时扩展数从 22 掉到 13，代价是最优性不保证）。

### ★ 什么时候 A\* 真能省？（一个诚实的观察）

代码用 30 个 $40\times40$ 随机迷宫对拍，A\* 与 Dijkstra **路径长完全一致**、平均扩展结点数只有 Dijkstra 的 **0.50**。但**不是所有地图都省**：

```
=== 3. ★ 开阔地图：A* 省不了；有障碍才省 ===
  无障碍 3x6：Dijkstra 扩展 18 个，A* 扩展 18 个（相同）
     -> 开阔图上每格都在某条最短路上，f=g+h 恒等，启发式无从剪枝。
  有障碍 6x9：Dijkstra 扩展 38 个，A* 扩展 11 个（路径长都 = 10，A* 只探了 28.9474%）
```

**为什么开阔图省不了？** 在一张没有障碍的网格上，**每个格子都在某条最短路上**，于是对格子 $n$ 有 $g(n)+h(n)=\text{const}$（到终点的真实距离），优先队列里所有点的 $f$ 一样大，A\* 无法区分「对方向」和「错方向」，只能和 Dijkstra 一样铺开。**只有障碍（绕路）才让「方向」产生价值**：墙右边那一大片格子「离起点近、却离终点远」，$h$ 大，被 A\* 挡住，而 Dijkstra 没方向感，只能按 $g$ 一层层铺过去。

> **这是 A\* 最重要的一课**：**启发式的价值来自「让搜索少走弯路」；开阔直线场景本就无弯路可省。** 这也是为什么 A\* 在**有结构的**问题（迷宫、拼图、机器人有障碍的导航）里大放异彩，而在开阔场地里和 Dijkstra 无异。

---

## 11.7 二分图匹配：增广路、König 定理、指派问题

**二分图**（第 10 章）顶点分成 $L, R$ 两半，所有边跨两半。**匹配（matching）**是一组互不共端点的边；**最大匹配**就是边数最多的匹配。原型：任务分给工人、论文分给审稿人、样本指派给原型。

### 核心：增广路（augmenting path）

**从「未匹配」的左点出发**，交替走「非匹配边 / 匹配边 / 非匹配边 ……」，**终于一个「未匹配」的右点**。把这条路上的匹配 / 非匹配**翻转**，匹配数 $+1$。

$$ \textbf{Berge 定理：匹配最大} \iff \textbf{不存在增广路。} $$

于是算法就一句话：**不断找增广路、翻转，直到找不到为止**（Kuhn / 匈牙利增广路算法，$O(V\cdot E)$）。

跑 `code/11_bipartite_matching.cpp`：

```
=== 1. 最大匹配：任务分给工人 ===
  任务 T0{W0,W1}  T1{W1}  T2{W1,W2}  T3{W2,W3}
  最大匹配（能同时派出的活）= 4（此图 4 个任务全能派出去）
  一个最优指派：T0->W0 T1->W1 T2->W2 T3->W3 
  另一例：T0,T1,T2 都只能找 W0，仅 T2 还能找 W1 -> 最大匹配 = 2
```

代码用 **3000 个随机小二分图**对拍「Kuhn 增广路 == 暴力 DP」，全部一致。

### ★ König 定理：最大匹配 = 最小点覆盖

**点覆盖**：一组顶点，使每条边**至少有一端被选中**。**König 定理**说，二分图里

$$ \textbf{最大匹配} = \textbf{最小点覆盖},\qquad \textbf{最大独立集} = V - \textbf{最小点覆盖} = V - \textbf{最大匹配}. $$

**怎么构造最小点覆盖？** 从所有**未匹配的左点**出发做「交替可达性搜索」（非匹配边向左→右、匹配边向右→左），得到集合 $Z$。则：

$$ \text{最小点覆盖} = (L\setminus Z)\ \cup\ (R\cap Z). $$

代码对 **2000 个随机图**验证：最小点覆盖大小 == 最大匹配，**且确实覆盖每条边**：

```
=== 3. ★ König 定理：最大匹配 = 最小点覆盖 ===
  2000 个随机图：最小点覆盖大小 == 最大匹配，且确实覆盖每条边 ✓
```

> 「匹配 $\leftrightarrow$ 覆盖」这层对偶是组合优化里最漂亮的结果之一，也是 **max-flow / min-cut** 的影子（二分图最大匹配可归约成最大流，König 定理是 max-flow-min-cut 的特例）。

### 指派问题（assignment）：最小代价完美匹配

**n 个工人干 n 项活**，代价 $c[i][j]$，求「一人一活」的**最小总代价**。用**匈牙利算法（Kuhn–Munkres）**在 $O(n^3)$ 内解决——核心是维护每行 / 每列的**势** $u_i, v_j$，让**约化代价** $c[i][j]-u_i-v_j\ge0$，再找「**零代价**的增广路」；不成立时用最小差值 $\delta$ 调整势。

```
=== 4. ★ 指派问题（最小代价完美匹配）===
  代价矩阵：
     4 1 3 
     2 0 5 
     3 2 2 
  最小总代价 = 5（= 1 + 2 + 2：0 号工人干 1 号活、1 号干 0 号、2 号干 2 号）
```

代码用 **400 个随机代价矩阵**（$n\le7$）对拍：匈牙利算法 == **枚举所有排列**的最优。

> **AI 落点（重点）**：**最优传输（optimal transport）**的离散版就是「最小代价匹配」——**Wasserstein 距离**、**地球移动距离**都建立在它之上。多目标跟踪里的**数据关联**（哪条轨迹对上哪个检测框）、自监督 / 聚类里的**样本-原型指派**、图的**二部匹配**，也全是这套增广路的故事。可以说，**「匹配」是 AI 里「分配」问题的通用骨架**。

---

## 11.8 复杂度对照

| 算法 | 问题 | 复杂度 | 数据结构 | 限制 / 备注 |
|---|---|---|---|---|
| Kruskal | MST | $O(E\log E)$ | 排序 + 并查集 | 稀疏图 / 边表 |
| Prim | MST | $O(E\log V)$ | 堆 | 稠密图 |
| 并查集 find/unite | 动态连通性 | $O(\alpha(n))$ 均摊 | 数组 + 按秩 + 压缩 | 只合并，不分裂 |
| Dijkstra（堆） | 单源最短路 | $O((V+E)\log V)$ | 堆 | **非负权** |
| Dijkstra（朴素） | 单源最短路 | $O(V^2)$ | 邻接矩阵 | 稠密、$V$ 小 |
| Bellman-Ford | 单源最短路 | $O(VE)$ | 边表 | **可负权 / 可捉负环** |
| Floyd–Warshall | **全源**最短路 | $O(V^3)$ | 邻接矩阵 | $k$ 必须最外层 |
| A\* | 单源最短路 | 取决于 $h$ | 堆 + 启发式 | $h$ 可采纳则最优 |
| Kuhn 匹配 | 二分图最大匹配 | $O(V\cdot E)$ | 邻接表 + 匹配数组 | 稠密可用 Hopcroft–Karp $O(E\sqrt V)$ |
| 匈牙利 / KM | 指派（最小代价完美匹配） | $O(n^3)$ | 势 + 增广路 | 最小费用流的特例 |

> **选择口诀**：**稀疏** Dijkstra 堆版 / **稠密** 朴素版；**有负权** Bellman-Ford / **全源** Floyd；**连通性** 并查集 / **分配** 二分图匹配。

---

## 常见错误与陷阱

1. **Dijkstra 用在有负权的图上。** 贪心「已确定点不变」的前提是「边权 $\ge 0$」；负权会让已确定的点被事后反悔。**有负权换 Bellman-Ford**。

2. **Floyd 把 $k$ 放在了内层。** 这是 Floyd 最经典的错。$k$ 是「逐步放开的中转站集合」，**必须在最外层**，否则中转集合还没放开就更新，答案错误。

3. **Bellman-Ford 忘了乘 / 加 $INF$ 溢出。** 松弛前要判 $dist[u]\ne\infty$，否则 $\infty+w$ 可能溢出成负数，把「不可达」误判成「可达且很小」。

4. **并查集只做路径压缩、不做按秩合并（或反过来）。** 单用一个也能大幅改善，但**两者叠加**才能在最坏输入上稳到 $O(\alpha(n))$；只用「裸挂」会退化成 $O(n)$ 长链（代码实测差四个数量级）。

5. **Kruskal 忘了 `unite` 返回 false 表示成环。** `unite` 返回 **false** 才说明「两端已连通、加这条边会成环」，应当**跳过**；写反了会得到带环的「假生成树」。

6. **A\* 的 $h$ 高估了却以为还最优。** 只有**可采纳**（$h\le$ 真实剩余）才保证最优；加权 A\*（$h$ 乘 $>1$）会**更快但可能次优**——这是「速度 vs 最优」的旋钮，要清楚自己在哪一档。

7. **开阔图上期待 A\* 省结点。** 无障碍网格里每格都在最短路上，$f$ 恒等，A\* 省不了（见 11.6 实测）。省不省**取决于地图有没有「弯路」**。

8. **指派问题当成「贪心每行取最小」做。** 逐行取最小会冲突，**不是最优**。必须用匈牙利 / KM（或最小费用流）做全局的增广。

9. **König 构造最小点覆盖时方向弄反。** 交替搜索里「**非匹配边左→右、匹配边右→左**」；取 $(L\setminus Z)\cup(R\cap Z)$。方向或符号弄反，得到的集合就不覆盖了。

10. **A\* / Dijkstra 的惰性删除忘了判过期。** 同一个点可能被多次入堆；出队时要 `if (done[u]) continue;`，否则会重复扩展、甚至答案错乱。

11. **堆里存「结点」却用「旧距离」出队。** 入堆时应把**当前最新的** $f$ / $dist$ 一起存进去（`pair{dist, node}`），比较的是这个值；别指望堆能感知结点距离后来变了。

12. **Floyd 的 $\infty$ 用 `INT_MAX`。** $dist[i][k]+dist[k][j]$ 会整数溢出。用「足够大但不会溢出」的值（如 `INT_MAX/4`），或改用 `long long`。

---

## 与 AI / 世界模型的连接

图算法是 AI 里「**在关系上做最优决策**」的骨架。这一章每个算法，都能在世界模型 / 强化学习 / 图神经网络里找到血肉。

### 1. ★ A\* 与规划：世界模型「在想象的未来里找路」

**世界模型做规划**，本质是「在当前状态出发，向前推演若干步，找一条通向高回报状态的路径」。这正是**图搜索**：

- **A\* 是规划的主力**——启发式 $h$（如「离目标状态还有多远」）用来**剪枝**，把搜索拽向有希望的方向；
- **MPC（模型预测控制）**、**MCTS（蒙特卡洛树搜索）**、**AlphaZero 的树搜索**，都是「图 / 树上的启发式搜索」的亲戚——**A\* 是它们的共同祖先**；
- **15-puzzle 求解、机器人导航、游戏寻路**是 A\* 的传统战场（曼哈顿 / 打乱距离启发式同时可采纳且一致）。

**一句话**：A\* 教你的「$f=g+h$、用启发式把搜索导向目标」，就是「世界模型如何高效地在想象的未来里做决策」的第一课。

### 2. ★ Dijkstra / Bellman-Ford 与「代价敏感」的决策

**最短路 = 最小代价路径**。在强化学习里：

- **值迭代 / 策略迭代**里「更新状态价值」的式子，和 **Bellman 方程**同源——**Bellman-Ford 的「反复松弛」正是动态规划的思想**（下一章第 14 章会正式展开 DP）；
- **图中的最短路**对应「**最省代价 / 最省时间的行动序列**」：把「动作」当边、「代价」当权，规划就是最短路；
- **负权 / 负环**在 RL 里有对应：「**负回报循环**」（绕圈越绕越差）在建模时要小心，否则值函数不收敛。

**因果推断里的置信传播、CRF / HMM 的维特比解码（第 14 章）**，都是「在图上做动态规划」，与 Bellman-Ford / Floyd 的「松弛 + DP」一脉相承。

### 3. ★ 并查集与 MST：连通性、聚类、稀疏化

- **连通分量 / 动态连通性**是图数据的**预处理**（先把互不连通的块切出来，再各自处理）；
- **MST 是聚类算法**（单链聚类 = 去掉最大权边）、**图像分割**（把像素当点、相似度当权）的经典内核；
- **MST 骨架做图稀疏化**：用 $V-1$ 条边保留全局结构，降低图神经网络 / 消息传递在超大图上的开销——「**先用 MST 抽骨架，再在骨架上传播**」是常见工程手法。

### 4. ★ 二分图匹配与最优传输

- **最优传输（optimal transport）**：把「一个分布搬成另一个分布」的最小代价，其离散版就是**最小代价匹配**；**Wasserstein 距离**、**地球移动距离**都建立在它之上——它们是**生成模型（WGAN）、域适应、度量学习**的常用工具；
- **数据关联**：多目标跟踪里「这条轨迹对上哪个检测框」= 二分图匹配；**匈牙利算法是 MOT（多目标跟踪）的标准模块**；
- **Assignment / Sinkhorn**：可微的最优传输（Sinkhorn 算法）用**熵正则化**把匹配变成可微运算，接进神经网络端到端训练——这是「**匹配**」走向「**可微匹配**」的现代形态。

### 5. 图算法的「统一口径」

| 图算法 | 世界的抽象 | AI 里的名字 |
|---|---|---|
| Dijkstra / Bellman-Ford | 最小代价路径 | 规划、值迭代、最短路 |
| A\* | 有方向的搜索 | 树搜索、MPC、AlphaZero |
| MST / 并查集 | 连通 / 聚类 / 骨架 | 聚类、图稀疏化 |
| Floyd / 传递闭包 | 全局可达 / 传播 | 知识图谱推理、GNN 传播 |
| 二分图匹配 | 分配 / 对偶 | 最优传输、数据关联、可微匹配 |

**一句话**：从「**找一条最省的路**」（最短路）到「**怎么连最省**」（MST），从「**谁该配给谁**」（匹配）到「**信息怎么在关系上传播**」（闭包 / DP）——**世界模型的每一次「有目的的推理」，几乎都是在本章的某个图算法上跑一遍**。

---

## 本章小结

- **MST**：Kruskal（排序 + 并查集，$O(E\log E)$）+ Prim（堆，$O(E\log V)$）；共同的正确性来自**切分性质**（横跨任一切的最小边必在某 MST 里）；总权唯一、边集可能不唯一；
- **并查集**：**按秩合并 + 路径压缩** ⇒ 均摊 $O(\alpha(n))$（反阿克曼，$\le4$，即近 $O(1)$）；只合并、不分裂；Kruskal 靠 `unite` 返回 false 判环；
- **Dijkstra**：非负权、贪心「已确定点不变」；堆版 $O((V+E)\log V)$、朴素版 $O(V^2)$；负权会让贪心失效；
- **Bellman-Ford**：$O(VE)$，可负权，**可捉负环**（第 $V$ 轮还能松弛 ⇒ 有负环）；
- **Floyd–Warshall**：$O(V^3)$ 全源，**$k$ 必须最外层**（逐步放开中转站）；换成「或 / 与」得**传递闭包**；
- **A\***：$f=g+h$；**可采纳**（$h\le$ 真实）⇒ 最优，**一致** ⇒ 每点只扩展一次；**开阔图省不了、有障碍才省**（$h=0$ 即 Dijkstra）；
- **二分图匹配**：**增广路** + **Berge 定理**（无增广路 $\Leftrightarrow$ 最大）；**König 定理**（最大匹配 = 最小点覆盖 = $V-$ 最大独立集）；**指派问题**用匈牙利 / KM $O(n^3)$；
- ★ **AI 连接**：A\* → 规划 / 树搜索 / MPC；最短路 / DP → 值迭代 / Viterbi / 置信传播；并查集 / MST → 聚类 / 图稀疏化；二分图匹配 → 最优传输 / 数据关联 / 可微匹配。

---

## 练习

### 基础

1. 对 `code/11_mst.cpp` 的例子，手工跑一遍 Prim（从点 0 出发），写出每次加入的边和当前树，验证总权 = 20。
2. 用 `code/11_union_find.cpp` 的 `DSU` 实现：给一组无向边，**动态**回答「两点是否连通」，并统计「加第几条边时图从 3 个分量变成 2 个」。
3. 对一张有负权但无负环的图，手算 Bellman-Ford 的 $V-1=4$ 轮松弛，画出每轮结束后的 `dist[]`。
4. 写出 $4\times4$ 矩阵的 Floyd 迭代过程（$k=0,1,2,3$），并解释为什么 $k=0$ 那一轮只允许用「点 0」做中转。
5. 用一个 $3\times3$ 网格（$S$ 在左上、$E$ 在右下、无障碍），手算 A\*（曼哈顿）与 Dijkstra 的扩展顺序，验证二者扩展数相同。

### 进阶

6. **实现邻接矩阵版的 Dijkstra（朴素 $O(V^2)$）**，并与堆版在稠密随机图上跑基准，重现 11.3 表格里「稠密图常数小」的结论。
7. **用 MST 做单链聚类**：给定 $n$ 个点和两两距离，建 MST，删掉最长的 $k-1$ 条边得到 $k$ 个簇。在一维 / 二维随机点上可视化簇的划分。
8. **实现 Hopcroft–Karp**（二分图最大匹配的 $O(E\sqrt V)$ 版本），与 Kuhn 在稠密二分图上对拍并比较耗时。
9. **把二分图最大匹配归约成最大流**（源点 $\to L$、$L\to R$、$R\to$ 汇点，容量 1），用最大流求出匹配，并说明 König 定理与 **max-flow-min-cut** 的关系。
10. **实现传递闭包的 Floyd 版与「对每个点跑 DFS」版**，对比复杂度（$O(V^3)$ vs $O(V(V+E))$），说清何时该用哪个。

### 挑战

11. **实现 A\* 求 15-puzzle 的最优解**：启发式用「曼哈顿距离之和」（可采纳但不够准）与「打乱距离（linear conflict，更准且仍可采纳）」，比较两者扩展的结点数。
12. **实现最小费用流（Successive Shortest Paths）**：用 Bellman-Ford / 势 + Dijkstra 反复沿最短路增广，并用它求指派问题，验证与匈牙利算法结果一致。这就是「**匹配 = 最小费用流的特例**」的实证。
13. **把 Bellman-Ford 的「反复松弛 = DP」翻译成「强化学习里的值迭代」**：定义一个小 MDP（状态 = 图的点、动作 = 边、奖励 = $-w$），用值迭代求最优值函数，验证它等于「取负的最短路」。
14. **软 / 可微版匹配**：用 Sinkhorn 算法（熵正则化的最优传输）近似指派问题，观察正则强度 $\varepsilon$ 从大到小时，软匹配如何逐步逼近硬匹配（匈牙利）的解。

<details>
<summary>参考答案（要点）</summary>

**1.** 从 0 起：切「{0} vs 其余」最小横跨边 $0{-}1$（权 4）；切「{0,1} vs 其余」最小横跨边 $1{-}2$（权 2）；再 $1{-}3$（权 6）…… 依据切分性质，每一步选的都必在某 MST 里。总权 20（=2+3+4+5+6）。

**2.** 逐个 `unite`，用 `countRoots()` 或维护一个计数器：每次 `unite` 成功（返回 true）计数 $-1$；当计数从 3 变 2 时记下当前边的编号。

**3.** 初始化 `dist[0]=0`、其余 $\infty$。每轮遍历所有边松弛一次；$i$ 轮后所有「边数 $\le i$ 的最短路」就位，$V-1$ 轮后全部就位。画表可见 `dist[]` 单调下降、逐轮收敛。

**4.** $k=0$ 轮只允许「用点 0 做中转」，即只能更新 $dist[i][j]=\min(dist[i][j],dist[i][0]+dist[0][j])$。因为这一轮里所有中转路径都形如 $i\to0\to j$，不含更靠后的点。

**5.** 无障碍网格里每格 $g+h$ 相等（都等于到 $E$ 的曼哈顿距离），A\* 的优先队列里 $f$ 全同，出队顺序与 Dijkstra 一致 ⇒ 扩展数相同。

**6.** 朴素版每个点扫一整行找最小 $O(V)$，共 $O(V^2)$；稠密图上无堆开销、内存连续，常数比堆版小，但 $V$ 大时 $O(V^2)$ 主导。

**7.** MST 的 $V-1$ 条边按权排序，删最大的 $k-1$ 条 ⇒ 剩下 $k$ 个连通块 = $k$ 个簇。这就是单链 / 层次聚类。

**8.** Hopcroft–Karp 用 BFS 分层 + 一次 DFS 找多条**最短**增广路，把每轮增广的复杂度从 $O(E)$ 降到整体 $O(E\sqrt V)$。稠密图上明显更快。

**9.** 源→每个左点容量 1、左→右（有边则）容量 1、右→汇容量 1。最大流的整数解就是最大匹配。König 定理 = max-flow-min-cut 在「单位容量二分网络」上的特例。

**10.** Floyd 版 $O(V^3)$ 简单、稠密图划算；「每点一次 DFS」$O(V(V+E))$，稀疏图（$E\ll V^2$）更省。判「全源可达」时按稀疏度选。

**11.** 曼哈顿和是可采纳但不一致/不很准；linear conflict 额外加上「同一行/列两个方块互相阻挡」的代价，仍可采纳但更紧，扩展结点数明显少。用 IDA\* 显存更省。

**12.** 每次沿「残量图上的最短路」增广单位流，用势（potential）把负权边转成非负以便用 Dijkstra。得到的流 = 最小费用完美匹配 = 指派问题最优。与匈牙利对拍应一致。

**13.** MDP：状态 = 图顶点，动作 = 出边，转移确定、奖励 $=-w$，折扣 $\gamma=1$。值迭代 $V_{k+1}(s)=\max_a(-w+R\,V_k(s'))$ 收敛到「$-1\times$ 最长路」；求最短路则用 $\min$。可见 **Bellman-Ford 的 $V-1$ 轮 = 值迭代的 $V-1$ 次扫描**。

**14.** Sinkhorn 对代价矩阵做「行归一化、列归一化」的交替缩放，得双随机矩阵的软指派；$\varepsilon\to0$ 时退化为硬匹配。$\varepsilon$ 大 ⇒ 软（可微、可反传），小 ⇒ 接近匈牙利解。

</details>

---

## 延伸阅读

- **Sahni《数据结构、算法与应用（C++ 版）》第 2 版 第 14 章**「Graphs」：最小生成树（Prim / Kruskal）、单源最短路（Dijkstra / Bellman-Ford）、全源最短路（Floyd）的 ADT 与实现；
- **Sahni 第 2 版 第 5 章 / 第 9 章**关于并查集（union-find）的讨论（等价类与 union-find 结构）；
- **《算法导论》第 21 章**「用于不相交集合的数据结构」——按秩合并 + 路径压缩的均摊分析（势能法）；
- **《算法导论》第 22–25 章**「单源 / 全源最短路」——Dijkstra、Bellman-Ford、Floyd–Warshall 的严格证明；
- **Kruskal, J. B. (1956)** *On the shortest spanning subtree of a graph* —— Kruskal 原始论文；
- **Dijkstra, E. W. (1959)** *A note on two problems in connexion with graphs* —— 3 页的经典，Dijkstra 原始论文；
- **Hart, Nilsson & Raphael (1968)** *A Formal Basis for the Heuristic Determination of Minimum Cost Paths* —— A\* 的奠基论文（可采纳性 / 一致性）；
- **Tarjan, R. E. (1975)** *Efficiency of a Good But Not Linear Set Union Algorithm* —— $O(\alpha(n))$ 紧界；
- **Kuhn, H. W. (1955)** *The Hungarian method for the assignment problem* —— 匈牙利算法原始论文；
- **Cuturi, C. (2013)** *Sinkhorn Distances* —— 可微最优传输（把匹配接进深度学习）；
- 配套代码：`code/11_mst.cpp`、`code/11_union_find.cpp`、`code/11_dijkstra.cpp`、`code/11_bellman_ford_floyd.cpp`、`code/11_astar.cpp`、`code/11_bipartite_matching.cpp`。

---

**下一章**：第 12 章《排序》—— 从 $O(n^2)$ 的冒泡 / 插入 / 选择，到 $O(n\log n)$ 的归并 / 快排 / 堆排，再到「不比较」的计数 / 基数排序；讲清**比较排序的 $\Omega(n\log n)$ 下界**，以及快排的划分、随机化、三路划分与工程优化。
