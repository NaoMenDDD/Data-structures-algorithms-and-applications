# 第 01 章 算法分析基础

> 这一章教你**用数学判断"这段代码跑得动吗"**。它是整门课的地基：后面每一章说"插入是 $O(n)$、查找是 $O(\log n)$"时，你必须知道这句话是什么意思、怎么推出来的、以及在真实机器上意味着什么。学不好这一章，后面的复杂度表就只是"背下来的咒语"。

---

## 本章目标

1. 用**操作计数法（operation count）**分析一段代码的代价；
2. 区分**最好 / 最差 / 平均**情形，并完整推导一个平均情形；
3. 掌握 $O,\Omega,\Theta,o,\omega$ 的严格定义，识破常见误用；
4. 建立复杂度增长的**数量级直觉**（哪些规模可行、哪些不可行）；
5. 解递推式：代入法、递归树法、**Master 定理**（三种情形）；
6. 用**摊还分析**（聚合、记账、势能）证明动态数组 `push_back` 是 $O(1)$；
7. 分析**空间复杂度**，理解递归栈的代价；
8. 知道性能测量的三个陷阱（第 00 章已见，这里补上理论依据）。

## 前置知识

- 第 00 章的 C++ 基础（会写循环、函数、`std::vector`）；
- 高中数学的对数与数列：$\log_2 n$、等比数列求和 $\sum_{i=0}^{k} r^i = \frac{r^{k+1}-1}{r-1}$。

---

## 1.1 为什么要分析算法

写程序时你面临的核心问题不是"能不能跑通"，而是：

> **当输入规模从 1000 变成 10 亿时，这个程序还能跑吗？**

看一个例子。两个求和的程序：

```cpp
// A：两重循环
long long sumA(int n) {
    long long s = 0;
    for (int i = 0; i < n; ++i)
        for (int j = 0; j < n; ++j)
            s += 1;
    return s;
}

// B：等差数列公式
long long sumB(int n) {
    return 1LL * n * n;
}
```

`n = 1000` 时两者都是"瞬间"。`n = 10^9` 时：

- A 需要 $10^{18}$ 次加法。按每秒 $10^9$ 次算，需要 **$10^9$ 秒 ≈ 31.7 年**；
- B 需要 1 次乘法。

**这就是算法分析的全部意义**：它让你在写下代码之前就知道答案是"能跑"还是"跑不完"。而且注意——A 和 B 在 `n` 小时**没有可观察的差别**。性能问题总是"在小规模上隐形、在大规模上致命"，这也是为什么**凭感觉调优往往没用**。

---

## 1.2 操作计数法

**思路**：把程序中最"贵"的那类操作（比较、加法、赋值、数组访问）数出来，得到一个关于输入规模 $n$ 的函数。

看顺序查找：

```cpp
int sequentialSearch(const std::vector<int>& a, int x) {
    for (int i = 0; i < (int)a.size(); ++i)   // (a) 循环条件判断
        if (a[i] == x) return i;              // (b) 元素比较
    return -1;
}
```

数两类操作：

| 操作 | 次数 |
|---|---|
| 循环条件判断 `i < n` | 找到时 $i+1$ 次；未找到时 $n+1$ 次 |
| 元素比较 `a[i] == x` | 找到时 $i+1$ 次；未找到时 $n$ 次 |

定义**计数函数** $C(n)$ 为元素比较次数，则：

- 找到（在下标 $i$ 处）：$C(n) = i+1$
- 未找到：$C(n) = n$

**关键心法**：不要数"所有指令"，只数**与 $n$ 同步增长的那部分**。常数倍的差别（比如"3 次赋值 vs 5 次赋值"）在 $n$ 很大时无关紧要——这就是渐进分析的正当性来源。

> **练习式提问**：`sumA` 的操作计数是多少？（答：$n^2$ 次加法 + $n(n+1)$ 次循环判断，主导项是 $n^2$。）

---

## 1.3 最好、最差、平均情形

对顺序查找：

- **最好情形（best case）**：第一个元素就是 `x`，$C(n)=1$；
- **最差情形（worst case）**：`x` 不在表里，$C(n)=n$；
- **平均情形（average case）**：需要知道"输入的概率分布"。

### 完整推导平均情形

**假设**：`x` 在表中，且出现在每个位置的概率相等（均为 $1/n$）。

$$
C_{\text{avg}}(n) = \sum_{i=1}^{n} \frac{1}{n} \cdot i = \frac{1}{n}\cdot\frac{n(n+1)}{2} = \frac{n+1}{2}
$$

**如果 `x` 可能不在表中呢？** 设"在表中"的概率为 $p$，"不在表中"的概率为 $1-p$。若在表中则位置等概率（各 $p/n$），则：

$$
C_{\text{avg}}(n) = \sum_{i=1}^{n}\frac{p}{n}\cdot i \;+\; (1-p)\cdot n
= \frac{p(n+1)}{2} + (1-p)n
$$

检查边界：$p=1$ 时得 $\frac{n+1}{2}$ ✓；$p=0$ 时得 $n$ ✓。

> **为什么平均情形要小心**：它的结论**完全依赖假设**。如果"要查找的元素总在表头附近"（缓存热点），平均代价远低于 $(n+1)/2$。所以在工程中，**最差情形往往更值得关注**——它决定了你的服务会不会超时、显存会不会 OOM。

---

## 1.4 渐进记号：$O$、$\Omega$、$\Theta$、$o$、$\omega$

### 1.4.1 严格定义

设 $f(n), g(n)$ 是定义在正整数上的非负函数。

**$O$（大 O，上界）**：

$$
f(n) = O(g(n)) \iff \exists c>0,\ n_0>0,\ \forall n \ge n_0:\ f(n) \le c\cdot g(n)
$$

**$\Omega$（下界）**：

$$
f(n) = \Omega(g(n)) \iff \exists c>0,\ n_0>0,\ \forall n \ge n_0:\ f(n) \ge c\cdot g(n)
$$

**$\Theta$（紧确界）**：

$$
f(n) = \Theta(g(n)) \iff f(n)=O(g(n)) \ \text{且}\ f(n)=\Omega(g(n))
$$

**$o$（严格小于）**：$\lim_{n\to\infty} f(n)/g(n) = 0$，如 $n = o(n^2)$。

**$\omega$（严格大于）**：$\lim_{n\to\infty} f(n)/g(n) = \infty$，如 $n^2 = \omega(n)$。

### 1.4.2 三个必须澄清的误区

**误区 1："$f = O(g)$ 说明 $f$ 大概和 $g$ 差不多大"。**
错。$O$ 只是**上界**。$n = O(n^2)$ 是对的（取 $c=1, n_0=1$ 即可），但 $n^2 = O(n)$ 是错的。想说"差不多大"，要用 $\Theta$。

**误区 2："$O(1)$ 意味着只有一条指令"。**
错。$O(1)$ 意味着"不随 $n$ 增长"。一段 10000 次加法但和 $n$ 无关的代码仍是 $O(1)$。**它的常数可能很大**——这正是"渐进更优的算法在现实小规模下不一定更快"的原因。

**误区 3："渐进分析没意义，因为常数被忽略了"。**
错。渐进分析回答的是"**能不能 scale**"，这是定性的、不依赖具体机器的结论。常数决定"在可行规模下谁快"，两者互补：
- 先看渐进：如果一个是 $O(n^2)$、另一个是 $O(n\log n)$，$n>10^4$ 后前者必输；
- 再看常数：若两者渐进相同（都是 $O(n\log n)$），比常数与缓存行为。

### 1.4.3 运算性质（速查）

- 加法：$O(f) + O(g) = O(\max(f,g))$（**主导项**）；
- 乘法：$O(f)\cdot O(g) = O(f\cdot g)$；
- 传递：$f=O(g),\ g=O(h) \Rightarrow f=O(h)$；
- 忽略常数与低阶项：$3n^2 + 100n + 7 = O(n^2)$。

---

## 1.5 常见复杂度与规模直觉

| 复杂度 | 名称 | $n=10$ | $n=10^3$ | $n=10^6$ | $n=10^9$ | 典型来源 |
|---|---|---|---|---|---|---|
| $O(1)$ | 常数 | 1 | 1 | 1 | 1 | 哈希查找（期望）、数组下标 |
| $O(\log n)$ | 对数 | ~3 | ~10 | ~20 | ~30 | 二分查找、平衡树 |
| $O(n)$ | 线性 | 10 | $10^3$ | $10^6$ | $10^9$ | 遍历、计数排序 |
| $O(n\log n)$ | 线性对数 | ~33 | ~10⁴ | 2×10⁷ | 3×10¹⁰ | 比较排序、FFT |
| $O(n^2)$ | 平方 | 100 | $10^6$ | $10^{12}$ | $10^{18}$ | 朴素双重循环、朴素矩阵乘 |
| $O(n^3)$ | 立方 | 1000 | $10^9$ | $10^{18}$ | — | 朴素矩阵乘、Floyd |
| $O(2^n)$ | 指数 | 1024 | — | — | — | 子集枚举、朴素背包递归 |
| $O(n!)$ | 阶乘 | 3.6×10⁶ | — | — | — | 全排列枚举、朴素 TSP |

**"一秒能做什么"的经验值**（现代 CPU/CUDA，纯数值运算）：

| 每秒操作数 | 量级 | 说明 |
|---|---|---|
| $10^8 \sim 10^9$ | CPU 单核 | 带分支的代码取低端，纯算术取高端 |
| $10^{11} \sim 10^{12}$ | CPU 多核（24 核） | 需并行 |
| $10^{13} \sim 10^{14}$ | GPU（TF32/FP16） | 大型矩阵乘可达 |
| $10^{15}$ | 高端 GPU + 低精度 | 稀疏/低精度专用 |

**读表方法**：把 $n=10^6$ 代入，若超过 $10^8$（1 秒 CPU），你就得想想"是不是要换算法，或者上 GPU"。这张表是你以后判断"这个方案要跑几天还是几分钟"的**主要工具**。

---

## 1.6 解递推式

递归算法的代价自然是递推式，例如归并排序：

$$
T(n) = 2T(n/2) + \Theta(n),\qquad T(1)=\Theta(1)
$$

### 1.6.1 代入法（substitution）

先猜答案，再数学归纳验证。猜 $T(n) = O(n\log n)$，即 $T(n) \le c\,n\log n$：

$$
T(n) \le 2c\frac{n}{2}\log\frac{n}{2} + dn
= cn\log n - cn + dn \le cn\log n \quad(\text{当 } c \ge d)
$$

✓。代入法的难点在"猜"，靠经验与递归树。

### 1.6.2 递归树法（recursion tree）

把递推画成树，逐层求和：

```
层次 0:                    n                    ← 代价 n
层次 1:            n/2           n/2            ← 代价 n
层次 2:        n/4    n/4     n/4    n/4        ← 代价 n
  ...                                            ← 共 log n 层
层次 log n:   1 1 1 ... 1 1 (n 个)               ← 代价 n
```

每层代价都是 $n$，共 $\log_2 n$ 层 → $T(n) = \Theta(n\log n)$。

**递归树法的价值**：它把"看不出来的递推"变成"求和"，是最实用的工具。

### 1.6.3 Master 定理

**适用形式**：

$$
T(n) = a\,T(n/b) + f(n),\qquad a \ge 1,\ b > 1
$$

设临界指数 $c^* = \log_b a$。比较 $f(n)$ 与 $n^{c^*}$：

| 情形 | 条件 | 结论 | 直觉 |
|---|---|---|---|
| **情形 1** | $f(n) = O(n^{c^*-\varepsilon})$（叶子更贵） | $T(n) = \Theta(n^{c^*})$ | 代价集中在叶子 |
| **情形 2** | $f(n) = \Theta(n^{c^*})$（各层一样贵） | $T(n) = \Theta(n^{c^*}\log n)$ | 每层代价相同，共 $\log_b n$ 层 |
| **情形 3** | $f(n) = \Omega(n^{c^*+\varepsilon})$（根更贵） | $T(n) = \Theta(f(n))$ | 代价集中在根 |

**情形 1 例子**：$T(n) = 8T(n/2) + n^2$。$c^* = \log_2 8 = 3$，$f(n)=n^2 = O(n^{3-1})$ → 情形 1 → $\Theta(n^3)$。

**情形 2 例子**：$T(n) = 2T(n/2) + n$（归并排序）。$c^*=\log_2 2=1$，$f(n)=n=\Theta(n^1)$ → 情形 2 → $\Theta(n\log n)$。

**情形 3 例子**：$T(n) = 2T(n/2) + n^2$。$c^*=1$，$f(n)=n^2=\Omega(n^{1+1})$ → 情形 3 → $\Theta(n^2)$。

**扩展情形 2**：若 $f(n) = \Theta(n^{c^*}\log^k n)$，则 $T(n)=\Theta(n^{c^*}\log^{k+1}n)$。例如 $T(n)=2T(n/2)+n\log n \Rightarrow \Theta(n\log^2 n)$。

> **什么时候不能用 Master 定理**：$T(n) = 2T(n/2) + n\log n$ 的 $f$ 不满足任何情形的严格条件（既非 $O(n^{1-\varepsilon})$ 也非 $\Theta(n)$）；更一般地，**若 $f$ 与 $n^{c^*}$ 之间存在"多项式级别的间隙"才能用**。此时改用递归树或 Akra–Bazzi 方法。

**练习式提问**：$T(n) = 9T(n/3) + n$ 是什么？（$c^*=\log_3 9=2$，$n = O(n^{2-1})$ → 情形 1 → $\Theta(n^2)$。）

---

## 1.7 摊还分析：把"偶尔很贵"摊平

**动机**：某个操作**偶尔**很贵，但**很少**发生。摊还分析给出的是"**一长串操作的平均代价**"——注意，这是**最坏情形的平均**，不含任何概率假设。

### 经典例子：动态数组的 `push_back`

策略：容量满时，分配 2 倍新空间，搬移全部元素。

- 大多数 `push_back`：$O(1)$（直接放）；
- 少数 `push_back`：$O(n)$（扩容并搬移）。

**问题**：整体代价是多少？如果只看最坏，会说"$O(n)$ 一次"；但实际上……

### 方法一：聚合法（aggregate）

从空数组连续做 $n$ 次 `push_back`。扩容发生在第 $1,2,4,8,\dots$ 次，搬移次数为：

$$
1+2+4+\cdots+2^{\lfloor\log_2 n\rfloor} < 2n
$$

加上 $n$ 次插入本身，总代价 $< 3n$，故**单次摊还代价**为 $3n/n = O(1)$。

### 方法二：记账法（accounting）

每次 `push_back` 收 3 块钱：1 块付"放入自己"，2 块存在自己的位置上。扩容时，把老元素搬过来的费用就从它自己存的 2 块里出——**永不透支**。故摊还代价 3 = $O(1)$。

### 方法三：势能法（potential）

定义势函数 $\Phi(D) = 2\cdot \text{size} - \text{capacity}$（$D$ 为当前状态）。摊还代价：

$$
\hat{c}_i = c_i + \Phi_i - \Phi_{i-1}
$$

- **不扩容**时：$c_i=1$，size 加 1、capacity 不变 → $\Phi$ 增加 2 → $\hat{c}_i = 1+2 = 3$；
- **扩容**时：设扩容前 size = capacity = $s$。$c_i = 1 + s$（插入 1 + 搬移 $s$）。$\Phi_{i-1} = 2s - s = s$；$\Phi_i = 2(s+1) - 2s = 2$。故 $\hat{c}_i = (1+s) + 2 - s = 3$。

**两种情况摊还代价都是 3**，故 $n$ 次操作总代价 $= 3n = O(n)$。✓

> 这个"恰好是 3"的结论可以用本章的 `code/01_amortized_growth.cpp` **实测验证**（程序里用势函数算出每次的 $\hat c_i$ 并断言最大值为 3）。

### 摊还 vs 平均

| | 摊还分析 | 平均情形分析 |
|---|---|---|
| 是否用概率 | **不用** | 用（需要输入分布假设） |
| 保证的是什么 | 一长串操作的**最坏**总代价 | 随机输入下的期望代价 |
| 典型用途 | 动态数组、并查集（第 11 章）、哈希表（第 13 章） | 快排的平均复杂度 |

---

## 1.8 空间复杂度

**空间复杂度** = 算法运行所需的**额外**内存（不含输入本身），通常也写成关于 $n$ 的函数。

```cpp
// O(1) 额外空间：原地迭代
long long sum(const std::vector<int>& a) {
    long long s = 0;
    for (int x : a) s += x;
    return s;
}

// O(n) 额外空间：递归深度 n，每层一个栈帧
long long fact(int n) { return n <= 1 ? 1 : n * fact(n - 1); }
```

**递归的隐形开销**：每次递归调用都要压栈（保存返回地址、局部变量、参数）。深度为 $n$ 的递归，空间是 $O(n)$——**即使代码看起来"没有数组"**。这就是"快速排序递归深度 $O(\log n)$（随机化后）"和"链表递归反转会爆栈"的原因。

**与 AI 的连接**：训练时的显存主要由三块构成：

1. **参数**：$P$ 个参数 × 每参数字节数（FP32 为 4、FP16 为 2）；
2. **激活值（activations）**：正比于 batch × 序列长度 × 层数 × 隐藏维度 × 层数，常是真正的瓶颈；
3. **优化器状态**：Adam 需要一阶、二阶动量，通常是参数量的 2 倍（FP32 下 8 字节/参数）。

粗略地，Adam + FP16 混合精度训练，显存 ≈ $P \times (2 + 2 + 4 + 4 + 4)$ 字节 ≈ $16P$ 字节。一个 7B 参数的模型，光模型状态就要 **112 GB**——这就解释了为什么需要 ZeRO（分片优化器状态）、梯度检查点（用计算换激活显存）等技术。**"空间复杂度"在这里不再是理论练习，而是能不能训得动的硬门槛。**

---

## 常见错误与陷阱

1. **把 $O$ 当 $\Theta$ 用**："哈希查找是 $O(1)$"应当说"**期望** $O(1)$"；最坏是 $O(n)$。
2. **忽略隐含开销**：`std::vector` 的 `erase` 是 $O(n)$（要搬移后面所有元素），不是 $O(1)$；`std::list` 的 `size()` 在 C++11 后是 $O(1)$（早期实现是 $O(n)$）。
3. **递归忘算栈空间**：递归深度大的算法（朴素快排最坏 $O(n)$ 层）可能栈溢出。
4. **Master 定理用错**：$f(n)$ 与 $n^{\log_b a}$ 之间必须有"多项式级别"的差距；差距只有 $\log$ 因子时要用扩展情形 2。
5. **把摊还当平均**：摊还**不需要**概率假设，别混。
6. **测量时用 `-O0`**：测出的是"编译器多笨"，不是"算法多快"。
7. **小规模实验外推**：$n=100$ 时 $O(n^2)$ 和 $O(n\log n)$ 跑得一样快，别据此下结论。
8. **忘记"常数很大"的可能**：$O(n\log n)$ 的算法常数若是 100，$n<2^{100}$ 时都打不过 $O(n^2)$ 的算法（理论上），所以**小规模下要实测**。

---

## 与 AI / 世界模型的连接

### 1. 自注意力为什么是 $O(n^2 d)$，以及 FlashAttention 改了什么

标准自注意力：$Q,K,V \in \mathbb{R}^{n\times d}$（$n$ 是序列长度，$d$ 是头维度）。

$$
\text{Attention}(Q,K,V) = \text{softmax}\!\left(\frac{QK^\top}{\sqrt d}\right)V
$$

- $S = QK^\top$：$n\times n$ 矩阵 → 计算量 $O(n^2 d)$，**显存 $O(n^2)$**；
- softmax → $O(n^2)$；
- $SV$ → $O(n^2 d)$。

所以序列长度翻倍，计算 ×4、显存 ×4。**这是长上下文的核心瓶颈。**

**FlashAttention** 的关键洞察：注意力是**显存带宽受限（memory-bound）**的，而不是算力受限。它通过**分块（tiling）**把 $Q,K,V$ 切成小块放进片上 SRAM，用 online-softmax 逐块累积，**从不把完整的 $n\times n$ 矩阵写回显存**：

- 计算量仍是 $O(n^2 d)$；
- **显存从 $O(n^2)$ 降到 $O(n)$**；
- 实测速度提升主要来自**减少显存读写**，而非减少浮点运算。

**结论**：这里的"复杂度"分两层——**时间复杂度**没变，**空间复杂度/IO 复杂度**变了，而后者才决定实际速度。这印证了第 00 章的结论：**瓶颈常常是数据搬运，不是计算。**

### 2. 参数量与 FLOPs 的量级估算

**Transformer 参数量**（每层，忽略偏置）：

$$
\underbrace{4d^2}_{\text{attention } W_Q,W_K,W_V,W_O} + \underbrace{8d^2}_{\text{FFN（隐藏层 }4d\text{）}} = 12d^2 \quad\text{（每层）}
$$

总参数 $P \approx 12\,d^2\,L + V\cdot d$（$L$ 层，词表 $V$）。

**例**：$d=4096,\ L=32$，词表 $V=50000$：

- $12 \times 4096^2 \times 32 \approx 6.4\times10^9$（6.4B）；
- 嵌入 $50000\times4096 \approx 2.0\times10^8$；
- 合计约 **6.6B 参数**。

**训练 FLOPs** 经验公式：$C \approx 6\,P\,D$（$D$ 为训练 token 数，6 = 前向 2 + 反向 4）。**推理 FLOPs** ≈ $2P$（每 token，只前向）。

**例**：$P=6.6\times10^9$，$D=3\times10^{11}$ tokens →
$C \approx 6\times6.6\times10^9\times3\times10^{11}\approx 1.2\times10^{22}$ FLOPs。

按 A100 约 $3\times10^{14}$ FLOP/s（有效利用率取 40%），需 $\approx 1.2\times10^{22}/(1.2\times10^{14}) = 10^8$ 秒 ≈ **3.2 年**（单卡）。这就是为什么要几百上千张卡。

**这项估算能力非常实用**：以后你读论文看到"我们在 X 张卡上训了 Y 天"，能立刻反推它的 $P$ 和 $D$ 是否自洽。

### 3. 数据加载与吞吐

训练吞吐（tokens/s）由**瓶颈环节**决定：

$$
\text{吞吐} = \min(\text{GPU 计算吞吐},\ \text{数据供给吞吐})
$$

若数据预处理是 $O(n)$ 的 Python 循环，它很容易成为瓶颈：GPU 利用率掉到 20% 不是因为模型慢，而是因为**喂不饱**。对策是第 06 章要讲的**多进程预取队列**。

### 4. 精确 kNN vs 近似检索（衔接第 13 章）

- **精确 kNN**：$N$ 个库向量、$d$ 维、查询 $Q$ 次，代价 $O(NdQ)$。$N=10^9,\ d=768$ 时不可行；
- **近似最近邻（ANN）**：用 LSH（第 13 章）或 HNSW 图结构，把查询降到**亚线性**，代价是接受少量召回损失。

这是"用算法复杂度换可行性"的教科书案例，也是向量数据库（FAISS、Milvus）存在的理由。

### 5. NP 难与近似（衔接第 15 章）

并非所有问题都有多项式算法。世界模型中的**规划**（在动作序列空间里找最优）通常就是 NP 难的，因此实际系统用的是**采样、近似、或学习式搜索**（MCTS、beam search），而不是精确求解。

---

## 本章小结

- **操作计数**只数主导项；常数倍差异在渐进意义下被忽略。
- **最好/最差/平均**三者要分清；平均情形完全依赖分布假设。
- $O$ 是上界，$\Omega$ 是下界，$\Theta$ 才是"同阶"；$O(1)$ 不等于"很快"。
- 记住**增长量级表**与"每秒能做什么"的经验值，用来判断方案可行性。
- 递推式三件套：**代入法、递归树、Master 定理**（三情形 + 扩展情形 2）。
- **摊还分析**（聚合/记账/势能）给出"一串操作的最坏平均代价"，不需要概率假设；动态数组 `push_back` 摊还 $O(1)$。
- **空间复杂度**要算上递归栈；在深度学习中，激活值与优化器状态常是显存瓶颈。
- 深度学习的核心复杂度结论：自注意力 $O(n^2d)$；FlashAttention 降的是 **IO/空间**而非时间；训练 FLOPs $\approx 6PD$，推理 $\approx 2P$。

---

## 练习

### 基础

1. 求下列各式的渐进阶（用 $\Theta$）：(a) $3n^2+5n+7$；(b) $n\log n + n$；(c) $2^{n+3}$；(d) $\sum_{i=1}^n i$。
2. 判断真假并说明理由：(a) $n^2 = O(n^3)$；(b) $n^3 = O(n^2)$；(c) $2^{n+1} = O(2^n)$；(d) $2^{2n} = O(2^n)$。
3. 顺序查找在"$x$ 一定在表中且位置均匀分布"时平均比较次数是多少？若"$x$ 在表中的概率为 $1/2$"呢？
4. 写一个 $O(1)$ 额外空间、$O(n)$ 时间的数组逆序函数。
5. `std::vector::erase(begin())` 的复杂度是多少？为什么？

### 进阶

6. 用 Master 定理解：(a) $T(n)=4T(n/2)+n$；(b) $T(n)=4T(n/2)+n^2$；(c) $T(n)=4T(n/2)+n^3$；(d) $T(n)=2T(n/4)+\sqrt n$。
7. 用递归树推导 $T(n)=3T(n/4)+\Theta(n^2)$，并指出主导项来自哪一层。
8. 证明：若动态数组的扩容因子是 $1.5$（取整后至少 +1），$n$ 次 `push_back` 的总搬移次数仍是 $O(n)$，但常数比因子 2 时大。给出两者的总搬移次数表达式。
9. 一个算法先做 $O(n\log n)$ 的排序，再做 $O(n)$ 的扫描。总复杂度是多少？如果我改成"先 $O(n)$ 扫描再 $O(n\log n)$ 排序"会更快吗？为什么？
10. 用势函数 $\Phi = 2\cdot\text{size}-\text{capacity}$ 验证：扩容因子 $b=2$ 时最大摊还代价为 3；若改为"每次只 +1"（不倍增），最大摊还代价是多少？

### 挑战

11. 证明比较排序的下界是 $\Omega(n\log n)$（提示：决策树有 $n!$ 个叶子，高度至少 $\log_2(n!)$，再用 Stirling 近似）。（第 12 章会再遇到它。）
12. 设计一个数据结构支持：`push_back(x)` 摊还 $O(1)$，且 `undo()`（撤销上一次未撤销的 push）也 $O(1)$。给出势函数或记账方案。
13. 分析"两层嵌套的 `for`，但内层从 `i*i` 开始直到 `n`"的总操作数：$\sum_{i=1}^{n}(n-i^2)^+$ 的量级是多少？
14. 某模型 $P=70\times10^9$（70B），用 $D=2\times10^{12}$ tokens 训练。用 $6PD$ 估算所需 FLOPs，并估算在 2048 张 A100（有效算力 $1.5\times10^{14}$ FLOP/s/卡）上需要多少天。

<details><summary>参考答案与提示</summary>

**1.** (a) $\Theta(n^2)$；(b) $\Theta(n\log n)$；(c) $\Theta(2^n)$；(d) $\Theta(n^2)$。

**2.** (a) 真（$c=1$ 即可）；(b) 假（$n^3/(n^2)=n\to\infty$，不可能是上界）；(c) 真（$2^{n+1}=2\cdot2^n$，取 $c=2$）；(d) **假**（$2^{2n}=4^n$，比值 $2^n\to\infty$）。区分"底数不同"与"常数倍"是关键。

**3.** 均匀分布时 $\frac{n+1}{2}$。概率 $1/2$ 时用公式 $\frac{p(n+1)}{2}+(1-p)n$，代入 $p=1/2$ 得 $\frac{n+1}{4}+\frac{n}{2}=\frac{3n+1}{4}$。

**4.** 双指针从两端向中间交换：
```cpp
void reverseInPlace(std::vector<int>& a) {
    for (std::size_t i = 0, j = a.size(); i + 1 < j; ++i, --j)
        std::swap(a[i], a[j - 1]);
}
```

**5.** $O(n)$。删除首元素后，**后面所有元素都要前移一格**。这是 `vector` 与 `list` 的本质差异（第 02/03 章）。

**6.** (a) $c^*=\log_2 4=2$，$f=n=O(n^{2-1})$ → 情形 1 → $\Theta(n^2)$；(b) $f=n^2=\Theta(n^2)$ → 情形 2 → $\Theta(n^2\log n)$；(c) $f=n^3=\Omega(n^{2+1})$ → 情形 3 → $\Theta(n^3)$；(d) $c^*=\log_4 2=1/2$，$f=\sqrt n=n^{1/2}=\Theta(n^{c^*})$ → 情形 2 → $\Theta(\sqrt n\log n)$。

**7.** 每层代价是 $3^i\cdot(n/4^i)^2=n^2(3/16)^i$，等比数列公比 $3/16<1$，求和收敛到 $O(n^2)$。**主导项来自根那一层**（因为 $f$ 成长得比递归产生的子问题总代价快）。

**8.** 因子 $b$ 时，总搬移次数 $= \sum_{\text{扩容点}} \text{cap} \approx \frac{n}{b-1}$。$b=2$ 时约 $n$；$b=1.5$ 时约 $2n$。两者都是 $O(n)$，但常数差约 2 倍。（还可提"1.5 倍更容易复用内存块"的工程理由。）

**9.** 总复杂度 $\Theta(n\log n)$（加法取主导项）。改顺序**不会**更快——两个 $\Theta$ 相加仍是 $\Theta(n\log n)$；但在工程上先排序可能让后续扫描的缓存行为更好（数据有序），从而常数更小。**渐进相同，常数可以差很多。**

**10.** 倍增时已在上文证明恒为 3。"每次 +1"时：第 $i$ 次 push 需搬移 $i-1$ 个元素，代价 $c_i = i$，$\Phi_i = 2i - i = i$。$\hat c_i = i + i - (i-1) = i+1$，**最大摊还代价 $\Theta(n)$** → 总代价 $\Theta(n^2)$。这就是为什么必须"倍增"而不是"每次加一"。

**11.** 比较排序的决策树：每个叶对应一种排列，共 $n!$ 个叶；二叉树高度 $h$ 至多 $2^h$ 个叶，故 $h \ge \log_2(n!)$。由 Stirling 公式 $n! \approx \sqrt{2\pi n}(n/e)^n$，得 $\log_2(n!) = \Theta(n\log n)$。

**12.** 要点：维护一个 `std::vector` 存 `(值, 是否被撤销)`，或单独维护一个"已撤销"的下标栈。`undo()` 弹出栈顶并把该位置标记为无效；`push_back` 时若栈非空且栈顶是"无效位置"则复用它，否则正常追加。每次操作 $O(1)$，总空间 $O(n)$。（这正是撤销栈 + 空闲链表的思想，第 03 章会讲"自由表"。）

**13.** 只有当 $i^2 < n$ 即 $i<\sqrt n$ 时内层才非空，贡献 $\sum_{i<\sqrt n}(n-i^2)\approx n\sqrt n$，量级 $\Theta(n^{3/2})$。

**14.** $C\approx 6PD = 6\times7\times10^{10}\times2\times10^{12}=8.4\times10^{23}$ FLOPs。总算力 $2048\times1.5\times10^{14}=3.07\times10^{17}$ FLOP/s。时间 $=8.4\times10^{23}/3.07\times10^{17}\approx 2.7\times10^6$ 秒 ≈ **31.6 天**。（真实情况还要乘上并行效率 <1，所以实际更久。）

</details>

---

## 延伸阅读

- Sahni，《数据结构、算法与应用（C++版）》**第 2 版 第 2 章 Performance Analysis**、**第 3 章 Asymptotic Notation**。*章节号以第 2 版为准，不同版次可能略有差异。*
- CLRS《算法导论》第 3 章（渐进记号）、第 4 章（分治策略与 Master 定理）——推导最严谨。
- Sedgewick《算法（第 4 版）》第 1.4 节——分析风格更工程化，例子丰富。
- Dao et al., *FlashAttention: Fast and Memory-Efficient Exact Attention with IO-Awareness*（2022）——"IO 复杂度比时间复杂度更重要"的经典论文。
- Kaplan et al., *Scaling Laws for Neural Language Models*（2020）——$C\approx 6PD$ 的出处与缩放律。
- 配套代码：`code/01_amortized_growth.cpp`（势函数与实测搬移次数）、`code/01_master_theorem_demo.cpp`（递归树求和 vs Master 定理预测）。
