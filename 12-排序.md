# 第 12 章 排序

> 排序是算法世界的「**万能前置步骤**」：二分查找要先有序、去重先有序、找中位数先有序、很多贪心（区间调度、Kruskal 的边排序）也先有序。它简单到「谁都会写」，却深到「有严格的 $\Omega(n\log n)$ 下界、有随机化的优雅、有工程调参的讲究」——**它是检验你是否真懂算法分析的试金石**。
>
> 这一章我们把排序讲透三层：
> **第一层**是最朴素的三兄弟（插入 / 选择 / 冒泡，$O(n^2)$），重点不是会写，而是理解「**为什么插入排序对近乎有序的数据这么快**」；
> **第二层**是三大将（归并 / 快排 / 堆排，$O(n\log n)$），核心是**分治**、**原地划分**、**最坏情况**；
> **第三层**是「**不比较**」的排序（计数 / 基数 / 桶），它们能突破 $O(n\log n)$ 下界，代价是要求「键是整数且范围可控」。
>
> 一路上贯穿三条主线：**时间复杂度 / 空间开销 / 稳定性**。搞清这三者，你就能对任何排序问题做出正确的选型。而排序的「分治 + 划分 + 随机化」思想，正是**快速选择、Top-k、乃至深度学习里的采样与分桶**的共同基因。

---

## 本章目标

读完并跑完本章代码，你应该能够：

1. 手写**插入 / 选择 / 冒泡**三种 $O(n^2)$ 排序，说清各自的**最好 / 最坏 / 平均**代价，并解释**插入排序对近乎有序数据退化成 $O(n)$** 的原因；
2. 手写**归并排序**（$O(n\log n)$、稳定、$O(n)$ 额外空间）与**堆排序**（$O(n\log n)$、原地、不稳定），讲清分治与建堆 + 取堆顶的过程；
3. 手写**快速排序**，说清划分（partition）的作用；用**随机化 pivot** 化解「已有序 / 逆序」的最坏输入，用**三路划分**处理大量重复键，并解释**为什么快排最坏是 $O(n^2)$**；
4. 用**实测**验证七种排序的正确性（对拍 `std::sort`）与**稳定性**（相等键的相对次序是否保持）；
5. 实现**快速选择（Quickselect，期望 $O(n)$）**与 **BFPRT（中位数的中位数，最坏 $O(n)$）**，并用它们求**中位数 / 第 k 大 / Top-k 阈值**，理解「只排序需要的那部分」；
6. 实现**计数排序**（$O(n+k)$、稳定）、**基数排序**（$O(d(n+b))$、稳定）、**桶排序**（均匀分布期望 $O(n)$），并说清它们的**适用边界**（键是整数、范围可控 / 分布均匀）；
7. 用**决策树**论证比较排序的 $\Omega(n\log n)$ **下界**，理解「想更快只能不比较」；
8. 建立一张按「时间 / 空间 / 稳定性 / 数据特征」选排序的对照表。

---

## 前置知识

- 第 01 章（算法分析）—— 最好 / 最坏 / 平均、$O(n\log n)$、主定理；**递归式 $T(n)=2T(n/2)+O(n)$**；
- 第 08 章（优先级队列与堆）—— 堆排序的引擎、`siftDown`；
- 第 11 章（图算法）—— Kruskal 用到的「边排序 + 并查集」；
- 一点点**概率**：随机化 pivot 的期望分析、桶排序「均匀分布下每桶期望 $O(1)$」。

---

## 12.1 排序的三个评价维度

选排序，先问三个问题：

| 维度 | 含义 | 典型取值 |
|---|---|---|
| **时间复杂度** | 最好 / 最坏 / 平均 | $O(n^2)$ vs $O(n\log n)$ vs $O(n+k)$ |
| **空间复杂度** | 额外空间 | 原地 $O(1)$ / $O(\log n)$（递归栈） / $O(n)$ |
| **稳定性** | 相等键的**相对次序**是否保持 | 稳定 / 不稳定 |

**稳定性**最容易忽略，却极其重要：**多级排序**（先按次关键字排、再按主关键字排）**必须用稳定排序**，否则第二次排序会打乱第一次的结果。例如「先按姓名排、再按班级排」，若第二次排序不稳定，同班同学的姓名次序就乱了。

> **稳定排序**：插入、冒泡、**归并**、计数、基数。**不稳定**：选择、快排（经典版）、堆排、希尔。（代码里全部实测验证。）

---

## 12.2 $O(n^2)$ 三兄弟：插入 / 选择 / 冒泡

| 算法 | 思想 | 比较次数（最坏） | 最好 | 稳定 |
|---|---|---|---|---|
| **插入排序** | 像理扑克牌，把第 $i$ 张插进左边有序部分 | $n(n-1)/2$ | **$O(n)$**（已有序） | ✅ |
| **选择排序** | 每轮选出剩下的最小值放到末尾 | 恒 $n(n-1)/2$ | $O(n^2)$ | ❌ |
| **冒泡排序** | 相邻两两比较，大数一路冒到右边 | $n(n-1)/2$ | **$O(n)$**（带提前退出） | ✅ |

```cpp
// 插入排序：稳定；对「近乎有序」数据极快
for (std::size_t i = 1; i < a.size(); ++i) {
    T key = std::move(a[i]);
    std::size_t j = i;
    while (j > 0 && key < a[j - 1]) { a[j] = std::move(a[j - 1]); --j; }  // 一路右移让位
    a[j] = std::move(key);
}
```

**插入排序的关键洞察**：内层 `while` 的比较次数 = 当前元素「要往回移动多少位」。**若数组已有序，每个元素只比较 1 次就停**，总比较 $O(n)$；**越乱，移动越多，越接近 $O(n^2)$**。实测 $n=5000$：

```
=== 4. 插入排序的「最好情况」：近乎有序极快 ===
  n = 5000 的插入排序比较次数：
     随机    ： 6269893
     已升序  ： 4999（每轮只比较 1 次 -> O(n)）
     近乎有序： 32581（略高于已升序，远低于随机）
```

**626 万 vs 4999，差了一千多倍**——同样一个算法，输入不同差出三个数量级。这正是「**最好 / 最坏 / 平均要分开看**」的活教材，也是工程里「**小数组 / 近乎有序用插入排序**」（快排 / 归并的常见收尾优化）的底气。

选择排序的比较次数**恒**为 $n(n-1)/2$（无论输入如何），但它的**交换次数最少**（$\le n$ 次），在「交换昂贵」的场景有优势。冒泡排序带「本趟无交换即已有序」的提前退出，最好能到 $O(n)$，但**教学价值大于实用价值**。

---

## 12.3 $O(n\log n)$ 三巨头：归并 / 快排 / 堆排

### 归并排序：稳扎稳打的分治

**分治三步**：分（把数组切两半）、治（递归排两半）、合（**合并两个有序数组**）。递归式 $T(n)=2T(n/2)+O(n)$，由主定理得 $O(n\log n)$。

```cpp
while (i < mid && j < hi) buf[k++] = (a[j] < a[i]) ? a[j++] : a[i++];   // 相等取左 -> 稳定
```

**关键在合并时的这行 `(a[j] < a[i]) ? ... : ...`**：当左右相等时**取左边**，就保证了稳定。归并**恒定 $O(n\log n)$**（不受输入影响）、**稳定**，代价是 $O(n)$ 额外空间。**外排序、链表排序**首选它。

### 快速排序：原地划分的舞蹈

**分治**：选一个 pivot，**划分（partition）**成「左边都 $\le$ pivot、右边都 $>$ pivot」，再递归两半。平均 $O(n\log n)$，**原地**、常数小，是实践中最快的比较排序——但**最坏 $O(n^2)$**。

### 堆排序：用堆「选出最大值」$n$ 次

先 $O(n)$ 建大顶堆，再重复 $n-1$ 次「把堆顶（最大值）换到末尾、堆缩小、下沉调整」。恒定 $O(n\log n)$、**原地**、**最坏也有保证**，代价是**不稳定**且缓存不友好。

跑 `code/12_sorts.cpp` 的实测（$n=200000$）：

```
=== 5. ★ 耗时基准（随机 int）===
  n = 5000  插入排序   2.2291 ms
  n = 5000  选择排序   18.5886 ms
  n = 5000  冒泡排序   9.85959 ms
  n = 200000 归并排序   12.4449 ms
  n = 200000 快排(随机) 14.1697 ms
  n = 200000 堆排序     9.71802 ms
  n = 200000 std::sort  8.96147 ms（内省排序，工程最强）
```

**$O(n^2)$ 的只能撑到几千，$O(n\log n)$ 的到 20 万仍毫秒级**——量级差距一目了然。（顺便：`std::sort` 用的是**内省排序（introsort）**：快排为主 + 递归过深时转堆排 + 小区间用插入排序，集各家之长，所以最快。）

---

## 12.4 快排的工程化：随机化 + 三路划分

快排的**最坏情况**是真问题：**若 pivot 总取到极值**（比如「固定取首元素」遇到「已升序数组」），划分会一路切出空区间，递归深度 $O(n)$、比较 $O(n^2)$。代码给了血的实测（$n=2000$ 已升序）：

```
=== 3. ★ 已升序输入上，快排的两种 pivot 策略 ===
  已升序 n = 2000：
     固定首元 pivot ：比较次数 = 1999000（≈ n^2/2 = 1999000，退化！）
     随机   pivot ：比较次数 = 26200（≈ n·log2 n = 22000，正常）
```

**199.9 万 vs 2.62 万**——就差一个「pivot 怎么选」！两个标准解法：

1. **随机化 pivot**：随机选一个元素当 pivot。这样「最坏输入」变成了「概率极小的坏运气」，**期望 $O(n\log n)$**，且**对手无法构造**让你退化的数据；
2. **三数取中（median-of-3）**：取首 / 中 / 尾的中位数当 pivot，简单且有效。

还有第三个坑——**大量重复键**。经典两路划分会把「都等于 pivot」的元素全塞到一边，遇到「全数组一个值」时退化成 $O(n^2)$。解法是**三路划分（Dutch national flag）**：切成「$<$ pivot / $=$ pivot / $>$ pivot」三段，「等于段」不再递归：

```cpp
while (i < gt) {
    if (a[i] < pivot)      std::swap(a[lt++], a[i++]);   // 归入「< pivot」
    else if (pivot < a[i]) std::swap(a[i], a[--gt]);     // 归入「> pivot」
    else                   ++i;                          // 与 pivot 相等，留在中间
}
if (lt - lo < hi - gt) { quickSort3(a, lo, lt, rng, cmp); lo = gt; }   // 只递归小侧
else                   { quickSort3(a, gt, hi, rng, cmp); hi = lt; }
```

**代码里还藏了一手**：每轮只**递归较小的那半**，较大的那半改成 `while` 循环——这样**递归栈深保证 $O(\log n)$**，避免最坏输入把栈撑爆。

---

## 12.5 选择问题：不必全排序

**问题**：找**第 $k$ 小**的元素（中位数是 $k=n/2$ 的特例）。朴素做法是先排序再取，$O(n\log n)$——但我们**只要一个元素**，却把 $n$ 个全排好了，太浪费。

### 快选（Quickselect）：划分一次，只递归一边

借用快排的划分，但**只往有 $k$ 的那一边递归**：

$$ T(n) = n + \frac{n}{2} + \frac{n}{4} + \cdots = 2n = O(n)\ (\text{期望}) $$

跑 `code/12_quickselect_bfprt.cpp` 的基准（$n=200000$，取中位数）：

```
=== 3. ★ 基准：n = 200,000，取中位数 k = n/2 ===
  方法                平均耗时(ms)
  全排序后取中位数    8.68436
  快选 Quickselect    1.56066（期望 O(n)，比全排序快几倍）
  BFPRT 中位数的中位数 4.53288（最坏 O(n)，但常数大）
```

**快选比全排序快 5 倍多**——因为它丢掉了「不需要排序的那一半」。但它和快排一样，**最坏是 $O(n^2)$**（pivot 总取极值）。

### BFPRT：中位数的中位数，最坏也线性

**BFPRT**（Blum–Floyd–Pratt–Rivest–Tarjan, 1973）用「**中位数的中位数**」当 pivot：把数组每 5 个一组，取各组中位数，再对这些中位数递归求中位数当 pivot。这样能保证**每次都至少切掉 $3n/10$**，于是**最坏也是 $O(n)$**。

代价是**常数大**（分组、反复排序小段、递归），实践中常比随机快选慢——但它的卖点是**最坏也能线性**，在对抗性输入下不会退化。这是「**选择问题线性时间可解**」的理论保证。

> **工程建议**：默认用随机快选（快、常数小）。只需 **Top-k** 且 $k$ 很小时，用**大小为 $k$ 的堆** $O(n\log k)$ 更省（见第 08 章）。BFPRT 主要作为「最坏保证」的备胎。

---

## 12.6 不比较的排序：计数 / 基数 / 桶

### 计数排序：数一数每个值出现几次

```cpp
for (int x : a) ++cnt[x];                                    // 数数
for (v = 1; ...) cnt[v] += cnt[v-1];                         // 前缀和 -> 每个值的「结束位置」
for (i = n; i-- > 0; ) out[--cnt[a[i]]] = a[i];              // 逆序回填 -> 稳定
```

时间 $O(n+k)$、空间 $O(k)$（$k$ = 键的取值范围）、**稳定**。**但只在 $k=O(n)$ 时才划算**——键范围一大（如 $0..10^9$），光计数数组就要几 GB：

```
=== 5. ★ 计数排序的适用边界（它需要 O(k) 空间）===
  * n=200000、键在 0..999    ：k=1000，几乎无额外空间，极快（见上）；
  * n=200000、键在 0..10^9   ：k=10 亿，光计数数组就要 ~4GB —— 直接崩。
```

### 基数排序：按「位」逐轮稳定排序

把键按位（十进制 / 256 进制）拆开，**从低位到高位**各做一轮**稳定**的计数排序。因为每轮稳定，低位的次序不会被打乱：

```
=== 3. 基数排序「逐位」过程（base 10）===
  原数组： 170 45 75 90 2 802 24 66 5 
  排序后： 2 5 24 45 66 75 90 170 802 
```

时间 $O(d(n+b))$（$d$ = 位数，$b$ = 进制）、**稳定**、空间 $O(n+b)$。**它不要求键范围小**（只需位数固定），所以是「整数 / 定长字符串排序」的利器。

### 桶排序：分桶 + 桶内排序

把 $[0,1)$ 的值分到若干桶，各桶内部排序，再拼接。**均匀分布下每桶期望 $O(1)$ 个元素，总期望 $O(n)$**。但桶数是把「双刃剑」：

```
  键范围 0..999   std::sort 6.05327 ms  |  计数排序 0.502478 ms
  32 位整数       std::sort 8.98569 ms  |  基数排序(base256) 3.81117 ms
  [0,1) 均匀浮点  std::sort 9.60273 ms
                 桶排序(桶数=12500) 6.17382 ms
                 桶排序(桶数=50000) 8.72083 ms
                 桶排序(桶数=200000) 9.94765 ms
```

**桶数取 $n$（二十万个桶）反而比取 $n/16$ 慢**——因为「创建二十万个桶对象」的分配 / 碎片开销压过了 $O(n)$ 的收益。这是桶排序调参的**经验点**：**少量大桶**往往比「一元素一桶」更快。

> **共同前提**：这三种排序都要求**键是整数（或可离散化）且「结构可控」**。一旦键是任意可比较对象（如自定义结构体），就只能回到比较排序。

---

## 12.7 比较排序的下界：为什么 $O(n\log n)$ 是极限？

**决策树论证**：任何**基于比较**的排序，其执行过程可以画成一棵二叉**决策树**——每个内部结点是一次「$a_i$ vs $a_j$」的比较，每个叶子是一个可能的输出排列。$n$ 个元素有 $n!$ 种排列，所以决策树**至少有 $n!$ 个叶子**。

一棵高度为 $h$ 的二叉树最多有 $2^h$ 个叶子，于是：

$$ 2^h \ge n! \ \Rightarrow\ h \ge \log_2(n!) \overset{\text{Stirling}}{\approx} n\log_2 n - 1.44n = \Omega(n\log n). $$

**结论**：任何比较排序**最坏情况**至少要做 $\Omega(n\log n)$ 次比较。归并 / 堆排**达到了这个下界**（$O(n\log n)$），所以它们在最坏情况下**已是最优**。

> **想更快怎么办？** 只能**不比较**——利用键的整数结构（计数 / 基数 / 桶排序）。这就是 12.6 节那三种排序存在的意义：**它们绕过了下界，因为「下界」只对比较排序成立**。

---

## 12.8 复杂度对照与选择

| 算法 | 平均 | 最坏 | 空间 | 稳定 | 适用 |
|---|---|---|---|---|---|
| 插入排序 | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ✅ | **小数组 / 近乎有序** |
| 选择排序 | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ❌ | 交换昂贵时（交换 $\le n$） |
| 冒泡排序 | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ✅ | 教学 |
| **归并排序** | $O(n\log n)$ | $O(n\log n)$ | $O(n)$ | ✅ | **稳定 + 最坏保证** / 外排序 / 链表 |
| **快速排序** | $O(n\log n)$ | $O(n^2)$ | $O(\log n)$ | ❌ | **通用最快**（随机化 + 三路） |
| **堆排序** | $O(n\log n)$ | $O(n\log n)$ | $O(1)$ | ❌ | 原地 + 最坏保证 |
| 计数排序 | $O(n+k)$ | $O(n+k)$ | $O(k)$ | ✅ | 键是小范围整数 |
| 基数排序 | $O(d(n+b))$ | $O(d(n+b))$ | $O(n+b)$ | ✅ | 定长整数 / 字符串 |
| 桶排序 | $O(n)$ 期望 | $O(n^2)$ | $O(n)$ | 取决于桶内 | 均匀分布 |
| 快选 / BFPRT | $O(n)$ / $O(n)$ | $O(n^2)$ / $O(n)$ | $O(1)$ | — | 只求第 $k$ 小 |

> **选择口诀**：**通用**用 `std::sort`（内省排序）；**要稳定**用归并；**要原地 + 最坏保证**用堆排；**小数组 / 近乎有序**用插入；**键是整数小范围**用计数 / 基数；**只要第 k 个**用快选。

---

## 常见错误与陷阱

1. **快排固定取首元素当 pivot。** 遇到「已有序 / 逆序」输入会退化成 $O(n^2)$（实测 2000 个数比较 199.9 万次）。**必须随机化 pivot 或三数取中**。

2. **快排用两路划分却遇到大量重复键。** 「全数组一个值」会让两路划分退化 $O(n^2)$。**用三路划分**把「等于 pivot」的元素单独放中间、不递归。

3. **快排递归深度爆栈。** 即使平均 $O(n\log n)$，最坏递归深 $O(n)$ 会爆栈。**每轮只递归较小的一侧、大的一侧改循环**，保证栈深 $O(\log n)$。

4. **归并排序的「相等取左边」写反。** 合并时若「相等取右边」，就**破坏了稳定性**。稳定归并必须 `(a[j] < a[i]) ? a[j] : a[i]`——**相等时取左**。

5. **计数排序用在键范围极大的数据上。** $O(k)$ 空间里 $k$ 是**键的取值范围**，不是元素个数。键在 $0..10^9$ 时计数数组要几 GB。**范围大改用基数排序**。

6. **基数排序没保证「每轮稳定」。** 基数排序的**正确性完全依赖每轮稳定**——如果某一轮用了不稳定的排序，「低位已排好的次序」就被打乱了。**必须用稳定计数排序**。

7. **桶排序桶数取成 $n$。** 二十万个桶对象的分配开销会压过 $O(n)$ 收益（实测比取 $n/16$ 慢）。**桶数要调**，少量大桶常更优。

8. **排序不稳定却拿去做多级排序。** 「先按班级排、再按姓名排」若第二次排序不稳定，同班同学的姓名次序就乱了。**多级排序必须用稳定排序**，或把多个关键字打包成一个复合键一次排完。

9. **以为 $O(n\log n)$ 是「排序的绝对下界」。** 下界 $\Omega(n\log n)$ **只对比较排序成立**。计数 / 基数 / 桶排序能突破它——前提是「键是整数、结构可控」。

10. **快选返回「值」却当成「下标」。** 快选返回的是**第 $k$ 小的值**（不是它排好后的下标）。三路划分后若 $k$ 落在「等于 pivot」段，直接返回 `pivot`，别去索引元素。

11. **堆排序的 `siftDown` 边界写错。** 下沉时子结点下标 `2i+1`、`2i+2` 必须 `< n`（当前堆大小，不是数组总长）。堆在「取出」过程中不断缩小，边界要用**当前堆大小**。

12. **比较函数不是「严格弱序」。** 用 `<=` 当比较器（而非 `<`）会破坏严格弱序，`std::sort` 可能**越界 / 崩溃 / 给出错误结果**。比较器必须满足「$a<b$、$b<c$ $\Rightarrow$ $a<c$」且**自身不相等**。

---

## 与 AI / 世界模型的连接

排序看似「古典」，但它的思想（**分治、划分、随机化、采样分桶**）已经渗透进现代机器学习的每个角落。

### 1. ★ 分治与「递归地解决一半」

**归并 / 快排 / 快选的分治骨架**，和深度学习里的**分治式模型**同源：

- **二分查找 / 分治递归**是「用 $O(\log n)$ 深度换 $O(n)$ 广度」的通用杠杆；
- **快速选择**（只递归有答案的一侧）是「**不需要完整计算，只算需要的那部分**」的典范——这类「**按需计算（lazy / conditional computation）**」思想，正对应**条件计算网络、早退（early-exit）、稀疏注意力**：不重要的部分干脆不算。

### 2. ★ 随机化与「打破对手的攻击」

**随机化快排**用「随机 pivot」把最坏输入变成「概率极小的坏运气」。这套「**用随机性消除最坏情况**」的思路，在 AI 里随处可见：

- **随机初始化、随机失活（Dropout）、随机数据增强、SGD 的随机采样**——都是用随机性换「**没有可被利用的最坏结构**」；
- **随机投影 / Johnson–Lindenstrauss**：用随机矩阵降维，**期望**保持距离——和「随机快排期望 $O(n\log n)$」是同一种「期望正确」的哲学。

### 3. ★ Top-k / 分位数：采样与优先经验回放

**快选 / BFPRT / 大小为 $k$ 的堆**解决的是「**只要前 $k$ 个 / 某个分位数**」，这在 AI 里是高频操作：

- **优先经验回放（Prioritized Experience Replay, PER）**：按 **TD 误差**排序抽取「最重要的 $k$ 个经验」——底层的「找 Top-k」就是堆或快选；
- **分位数回归 / 分位数神经网络（QR-DQN）**：直接建模回报的**分位数**，而分位数 = 第 $k$ 小问题；
- **鲁棒统计**：中位数（$k=n/2$）比均值**抗异常值**，是「稳健损失 / 稳健聚合」的基础——**联邦学习里的 Krum / 中位数聚合**就是「对梯度找中位数」。

### 4. ★ 桶化 / 分位化：连续量的离散化

**桶排序**的思想是「把连续值映射到离散的桶」（**分桶 / 量化**）。这在 AI 里是核心操作：

- **向量量化（VQ-VAE）**、**乘积量化（PQ，用于十亿级向量检索）**：把连续向量分到「码本」里的离散桶；
- **直方图 / 分位数分桶（feature binning）**：梯度提升树（XGBoost / LightGBM）**按特征分桶**来加速分裂点搜索；
- **注意力 / Hash 分桶**：用局部敏感哈希（LSH）把相似向量分到同一桶，把 $O(n^2)$ 的注意力 / 检索降到近线性——**这正是第 13 章要讲的哈希与 LSH**。

### 5. 排序作为「可微 / 不可微」的分水岭

一个值得记住的观察：**排序本身不可微**（输出是离散的排列），但它有大量**可微松弛**：

- **SoftSort / NeuralSort**：用「可微的排序运算符」把排序接进神经网络；
- **Sinkhorn / 可微最优传输**（第 11 章）也用「软排序」实现可微的指派；
- **可微排序**让「Top-k、分位数」也能端到端反传梯度——是「**离散结构 × 深度学习**」的活跃前沿。

**一句话**：排序教你的「**分治、划分、随机化、分桶**」四个招式，就是机器学习里「**递归计算、条件计算、随机化鲁棒、连续量离散化**」的算法原型。**看懂排序，你看懂了一半的工程优化思路。**

---

## 本章小结

- **三个评价维度**：时间 / 空间 / **稳定性**；多级排序必须用稳定排序；
- **$O(n^2)$ 三兄弟**：插入（**近乎有序退化成 $O(n)$**，稳定）、选择（恒定 $O(n^2)$、交换少、不稳定）、冒泡（可提前退出、仅教学）；
- **$O(n\log n)$ 三巨头**：归并（稳定、$O(n)$ 空间）、快排（原地、通用最快、最坏 $O(n^2)$）、堆排（原地、最坏也有保证、不稳定）；
- **快排工程化**：**随机化 pivot**（破除有序最坏输入，实测 199.9 万 → 2.62 万次比较）+ **三路划分**（处理重复键）+ **只递归小侧**（栈深 $O(\log n)$）；
- **选择问题**：快选期望 $O(n)$（比全排序快 5 倍）、BFPRT 最坏 $O(n)$（常数大）；
- **不比较的排序**：计数 $O(n+k)$、基数 $O(d(n+b))$、桶期望 $O(n)$；都要求**键是整数 / 分布可控**；
- **下界**：比较排序 $\Omega(n\log n)$（决策树 $2^h\ge n!$），归并 / 堆排已最优；想更快只能不比较；
- ★ **AI 连接**：分治 / 条件计算、随机化鲁棒、Top-k / 分位数（PER / QR-DQN / 鲁棒聚合）、桶化 / 量化（VQ / PQ / LSH）、可微排序（SoftSort / Sinkhorn）。

---

## 练习

### 基础

1. 对 `code/12_sorts.cpp` 的 `insertionSort`，构造三组输入（随机 / 已升序 / 已降序），分别统计比较次数，解释为什么降序输入会产生最多的移动。
2. 手写一个**稳定的**选择排序（提示：不用交换，改用「插入到正确位置」或「移动元素」），并验证它与不稳定版的输出差异。
3. 用归并排序的骨架实现「**计数逆序对**」：在合并时，每当取右边的元素，就说明左边的剩余元素都比它大，逆序对数增加 `(mid - i)`。
4. 用 `n = 8` 手画归并排序的递归树，标出每一层的合并，验证总代价 $\sum_i 2^i \cdot (n/2^i) = n\log n$。
5. 解释为什么「**已升序数组 + 固定首元 pivot**」会让快排退化成 $O(n^2)$，并用 $n=4$ 手动模拟一次划分。

### 进阶

6. **实现自底向上（迭代版）归并排序**，避免递归；用它与自顶向下版对拍并比较常数。
7. **实现三数取中 pivot 的快排**，在「已升序」「已降序」「全相等」三种输入上与随机 pivot 版比较比较次数。
8. **实现「快速排序 + 插入排序收尾」**（小区间 $< 16$ 时改用插入排序），实测在随机数据上的加速。
9. **实现「查找第 k 大」的堆版本**（维护大小为 $k$ 的最小堆，扫一遍数据），与快选比较；说明何时该用哪个。
10. **实现「按多个关键字稳定排序」**：给一组学生（姓名 + 班级 + 分数），先按分数、再按班级、再按姓名排序，用稳定排序逐级排，并验证结果等价于「按复合键一次排完」。
11. **验证计数排序空间爆炸的临界点**：固定 $n=10^6$，让键范围 $k$ 从 $10^3$ 涨到 $10^8$，测量计数排序的耗时与内存（用 `/usr/bin/time -v`），找出它「打不过 `std::sort`」的 $k$。

### 挑战

12. **实现可微排序（SoftSort / NeuralSort）**：用可微的「软排列」近似快排 / 快选的输出，接到一个小网络里，验证梯度能反传（对比数值梯度）。
13. **实现「外部排序」**：数据大到内存放不下时，分块读入、块内排序、写回临时文件，再多路归并。用 `mmap` 或文件流实现，处理一个比内存大的文件。
14. **实现「线性时间求中位数 + 用它做鲁棒聚合」**：用 BFPRT 求一批梯度向量的逐坐标中位数，与均值聚合在「含少量异常值」的场景下比较稳健性（这是联邦学习抗投毒聚合的简化版）。
15. **实现「分位数摘要（t-digest / KLL sketch）」**：在**一趟流式扫描**、$O(\text{polylog})$ 空间的约束下，近似估计任意分位数。这是流式 ML / 监控系统里的真实工具，与计数排序「精确但费空间」形成鲜明对比。

<details>
<summary>参考答案（要点）</summary>

**1.** 已升序：每轮内层比较 1 次就停，总比较 $O(n)$。已降序：每个元素都要一路移到最左，第 $i$ 个元素移动 $i$ 次，总移动 $\approx n^2/2$，最大。随机：期望移动 $\approx n^2/4$。

**2.** 不稳定版用 `std::swap`，会把后面的相等元素「跳过」它；稳定版把「最小值」通过**右移**插入，保持相等元素的相对次序。可用带 tag 的元素验证。

**3.** 合并时：`if (a[j] < a[i]) { cnt += (mid - i); buf[k++] = a[j++]; }`——右边元素 `a[j]` 更小时，左边尚未取出的 `mid-i` 个都比它大，正是逆序对。复杂度归并的 $O(n\log n)$。

**4.** 第 $i$ 层有 $2^i$ 个子问题、每个规模 $n/2^i$，合并总代价 $2^i \cdot O(n/2^i)=O(n)$；共 $\log n$ 层，总 $O(n\log n)$。

**5.** 已升序时首元是**最小值**，划分后 `i` 停在 `lo`，递归变成 `[lo,lo)` 和 `[lo+1,hi)`——每次只减少 1 个元素，深度 $n$。$n=4$：`[4,1,2,3]` 划分后 pivot 4 在首，递归 3 次，比较 $3+2+1=6=n(n-1)/2$。

**6.** 自底向上：先两两合并（段长 1）、再四四合并（段长 2）、…、直到段长 $\ge n$。无递归、循环写起来更「直」，常数略小。

**7.** 三数取中在三种输入上都接近 $n\log n$；随机 pivot 在这些输入上也接近 $n\log n$（随机性已破结构），但如果运气差（概率小）仍可能退化。三数取中**确定性**地避开首尾极值。

**8.** 小区间（$<16$）改插入排序省去递归开销，实测通常快 10%~20%（因为递归 + 划分的常数在小数组上不划算）。

**9.** 堆版 O(n log k)：适合 $k$ 远小于 $n$ 或**流式**数据（无法回看全部）。快选 O(n) 但需要**随机访问 + 可打乱**整个数组；$k$ 大时快选更优。

**10.** 逐级稳定排序（分数 → 班级 → 姓名），最内层是主关键字。因为每级稳定，最终次序 = 按 (姓名, 班级, 分数) 的字典序。等价于一次复合键排序。

**11.** $k$ 超过 $\sim 10^7$（计数数组 ~40MB）后，缓存miss + 初始化开销使计数排序开始输给 `std::sort`；$k$ 接近 $n$ 的若干倍是临界带。

**12.** SoftSort：对 \(-\text{dist}/T\) 做 `softmax` 得到可微的「软排序矩阵」，乘上 value 得软 Top-k。$T\to0$ 趋近硬排序。梯度用反向传播算，与数值梯度比对验证。

**13.** 分块：每块读入内存排序后写临时文件；然后**多路归并**（用大小为「路数」的堆维护各文件当前最小），边读边写。复杂度 $O(n\log n)$ + $O(n)$ I/O。

**14.** 逐坐标取中位数（BFPRT/快选），对「少量坐标被恶意放大」的异常梯度不敏感；均值会被拉偏。比较两种聚合下模型的准确率下降幅度。

**15.** t-digest：把数据分到「簇」，每簇大小按分位数位置加权（尾部分得细、中部分得粗），保证分位数误差有界；KLL sketch 用随机化的压缩子采样。都是「**一趟扫描、近似分位数**」，与精确计数排序权衡空间。

</details>

---

## 延伸阅读

- **Sahni《数据结构、算法与应用（C++ 版）》第 2 版 第 15 章**「Sorting」：插入 / 冒泡 / 选择 / 快速 / 归并 / 堆排序的实现与分析，以及「外部排序」与「排序下界」的讨论；
- **《算法导论》第 2、7、8 章**——插入 / 归并 / 快排 / 线性时间排序（计数、基数、桶），以及**比较排序下界**的决策树证明；
- **Hoare, C. A. R. (1961/1962)** *Quicksort* —— 快排原始论文；
- **Blum, Floyd, Pratt, Rivest & Tarjan (1973)** *Time bounds for selection* —— BFPRT，选择问题最坏线性；
- **Musser, D. R. (1997)** *Introspective Sorting and Selection Algorithms* —— 内省排序（`std::sort` 的算法）；
- **Cuturi (2013) / Grover et al. (2019) SoftSort / Mena et al. (2018) NeuralSort** —— 可微排序，把排序接进深度学习；
- 配套代码：`code/12_sorts.cpp`、`code/12_quickselect_bfprt.cpp`、`code/12_radix_counting.cpp`。

---

**下一章**：第 13 章《哈希与散列》—— 从「用数组下标直接寻址」（散列表 / 哈希函数 / 冲突处理 / 开放定址 / 链地址法）到「用哈希做近似」（布隆过滤器、局部敏感哈希 LSH），讲清为什么哈希能让查找平均 $O(1)$，以及它如何支撑起十亿级向量检索与推荐系统。
